# 🗂️ HANDOFF — 로한 ↔ 로버트 인수인계 로그

> **이 파일이 규칙이다.** 새 방(대화)을 열면 로한이 "showmethemoney 폴더에서 **가장 최신 handoff_MMDD.md** 랑 index.html 읽어"라고 말한다.
> 로버트는 압축된 기억에 의존하지 말고 **이 파일을 진실의 원천(source of truth)** 으로 삼는다.

---

## 📜 프로토콜 (로버트가 지킬 규칙)

1. **새 방 시작 시 읽을 것 (순서대로):**
   - `handoff_MMDD.md` ← **이 파일. 가장 최신 날짜 하나만.** (누적식이라 옛 날짜 파일은 안 읽어도 됨)
   - `index.html` ← 앱 전체 코드 (단일 파일 SPA)
   - 필요 시: `cashflow-design.md`(현금흐름 설계), `요구사항정의서.md`(초기 스펙)
2. **데이터가 필요하면** → Supabase MCP로 조회 (아래 접속정보). 앱 화면 추측 금지, 실제 조회.
3. **날짜가 바뀌면** → 이 파일을 `handoff_(새날짜).md`로 복사하고, 맨 위 로그에 새 항목만 얹는다. 옛 파일은 남겨둔다(히스토리).
4. **응답 원칙**: 로한 선호 = 결론→왜→다음. 아부·보험멘트 금지. 불확실하면 "불확실" 명시. 로버트는 재정 조언 시 "면허 있는 자문가 아님" 밝힘.
5. **로버트 = 나(Claude)의 이름.**
6. **작업 완료 시 항상 커밋 메시지를 써줄 것.** GitHub Desktop 기준 **Summary**(한 줄, `v버전 — 무엇을`) + **Description**(변경 항목 불릿 + 검증 결과 + 주의사항). 로한이 복붙만 하면 되게.

---

## 🧭 시스템 지도

**앱**: "Rohan Book" (구 가계부). 단일 `index.html` SPA. 재정+건강+명리(사주)+일지 통합. 현재 **v5.0**.
**배포**: GitHub Pages, 공개 repo `rohanchung/gagyebu`. (넷리파이에서 이관함 — 무료티어 배포 막힘 이슈)
- 코드 변경 → **commit + push** 필요 (GitHub Desktop).
- 데이터 변경 → Supabase 직접 쓰기, **커밋 불필요.**

**Supabase**: project_id `ytkbrdgbklnijbwkvino`. 단일 `app_state.data` jsonb 행에 전 상태 저장. `migrateDB()`로 스키마 진화.

**스킬 3개 (모든 방에서 발동)**:
- `load-my-finance-health` — 재정·건강 현황 스냅샷
- `evaluate-my-saju` — 사주 세운/월운/일운 평가 (고정 원국)
- `check-account-security` — 계정 로그인 이력 점검

---

## 👤 로한 프로필 (고정 사실)

- IT기획자 출신. 기획·사업·마케팅·창작 병행.
- 명리 앱은 **결정론적 만세력 엔진** 자체 구현(API 없음): Julian Day 일진 + Meeus 태양황경 근사 절기/월주 + 60갑자.
- **건강**: 키 **163cm** (BMI 역산 확정). 체중 하락 추세(약 78→68kg대). 8/5 기준 68.2kg. 피검사 21회 × 77지표 임포트됨.

---

## 💰 재정 로드맵 (2026-08 확정, 은행 실제숫자로 갱신 예정)

- **풍무점 이전 = 2026년 10월, 확정·강제.** 월세 아끼려는 게 아니라 출근지 이동. → 보증금 **500~1000만** 필요, 그중 **현금 500 확보가 관건.**
- **부채 금리 우선순위**: 약관대출 **5.5%** > 정책자금(소상공인대환) **3.11%** > 세이프박스 ~2%.
  - → **약관대출 먼저 상환.** 정책자금은 일정대로만 납부.
- **로드맵**: 2026 = 보증금용 저축(현실적 상한 ~500만, 1천만은 비현실) · 2027 = 약관대출 상환 + 신용 구축 · 2028 = 전세 점핑(1억 오피스텔은 현금 1~2천 필요, 지금은 불가).
- 신용점수 900+ 안정. 약관·정책 대출은 신용 영향 미미.
- ✅ **앱 부채 계산 교정 완료 (8/14)**. 소상공인대환 실제 방식 = **원금 고정 416,000원**(1회차 448,000, 단수 조정) + **이자 = 잔액 × 3.11% × 경과일수 / 365**(일할). 납입일 매월 19일, 주말이면 익영업일. 총 48회 · **총이자 1,269,938 · 총상환 21,269,938 · 월평균 443,124**. 앱 `bankSchedule()`이 은행앱 6회차와 원 단위 일치.
- 약관대출 잔액 **1,990만 @5.5%** (SEED도 실제값으로 동기화).

---

## 🧾 날짜별 로그 (최신이 위)

### 2026-08-14
- **🎯 목표·루틴 탭 신설 (v5.0). 로한북의 마지막 축 — 돈·건강·명리에 이어 실행.**
  - 데이터: `goals`(level: year|month, period, metric, progress, status) · `routines`(freq daily|weekly, days, goalId, active) · `checks`(**날짜키 맵** `{'2026-08-14':{routineId:1}}` — 스트릭·달성률 O(1) 조회용).
  - 화면: 오늘 루틴 **원터치 체크** / 🔥연속·⭐완전달성 스트릭 / 🌱12주 잔디 히트맵 / 월간 목표 진행률 / 연간 방향(표시등).
  - **연간 목표는 의도적으로 체크 대상에서 제외.** 월간이 실질 단위, 일간 루틴이 실제 엔진이라는 판단.
  - 월간 목표 측정 4종: 완료/미완 · **체크일수(자동집계)** · 횟수 · 금액. autodays는 그 달에 루틴을 하나라도 체크한 날 수를 자동으로 센다.
  - **사주 × 루틴 교차**: 히트맵 셀 우상단 빨간점 = 그날 충·형 등 '주의' 플래그 **2개 이상**. (1개 이상 기준은 365일 중 40%로 노이즈 → 2개 이상 27%로 상향)
  - 가계부 캘린더 날짜 옆에 루틴 달성 점(회색/부분/완전) 추가.
  - **첫 화면을 현황판 → 목표·루틴 탭으로 변경.** 매일 여는 화면이 체크리스트여야 지속된다는 판단. 되돌리려면 `enter()`의 `renderGoal()`을 `renderDash()`로, 사이드바 `class="m on"` 위치를 dash로.
  - 검증: Playwright로 렌더·클릭 토글·모달·전체 렌더러 12개 무예외 확인. 스트릭/요일필터/자동집계 단위 테스트 통과.
- **부채 상환스케줄 엔진 교체 (v4.5).** 기존 `computeMonthly()`의 원금균등 근사(월 416,667 고정)를 버리고 `bankSchedule()` 신설 — 은행 실제 방식 그대로 48회 생성.
  - 파라미터는 `debt.schedule.bank`에 저장: `{payDay:19, prevDate:'2026-08-19', firstYM:'2026-09', principal:416000, firstPrincipal:448000, basis:365}`.
  - `bankNextPay()` → 계좌·부채 탭에 다음 회차 실금액 표시. `bankSchedTable()` → 스케줄 모달에 48회 전체표(지난 회차는 흐리게).
  - **검증**: 2026-09-21 504,235 / 10-19 462,646 / 11-19 466,545 / 12-21 467,041 / 2027-01-19 461,228 / 02-19 463,248 — 은행앱 캡처와 **원 단위 전부 일치**. 원금합 = 정확히 2,000만, 최종잔액 0.
  - ⚠️ **공휴일 미반영** (주말만 익영업일 처리). 설·추석 등 낀 회차는 하루이틀 차이 가능.
- 푸터 버전 하드코딩 `v1.1` → `APP_VERSION` 동적 참조로 수정.
- SEED 부채값을 실제와 동기화(3.03→3.11%, 5.05→5.5%, 1,850만→1,990만). SEED는 폴백용이라 실사용 영향은 없었음.
- 사이드바 라벨 `🩹 체중·사고` → `🩹 체성분·사고`.
- **체중 페이지 → 체성분 페이지로 전면 개편 (v4.4).**
  - 그래프를 맨 위로, 8지표 탭(체중/BMI/체지방률/체지방량/근육량/골격근/내장지방/기초대사) + 중세풍 리디자인(양피지 배경·골드선).
  - 입력은 **8개만, 체중계 화면 순서대로**: 체중 → 체수분 → 체지방 → 골무기질 → 단백질량 → 근육량 → 골격근량 → 기초대사율.
  - 비율·BMI·제지방량·근육비율 등은 **자동 계산**(입력 안 함). 기록 리스트 각 행에 인라인으로 전 수치 표시("·계"=자동계산).
  - 데이터 모델: weight 항목에 flat 필드(kg, waterKg, fatKg, boneKg, protein, muscle, skMuscle, bmr, memo).
  - 계산 검증 완료: 키 163 → BMI 25.7, 체지방률 23.2, 근육비율 72.9 등 샤오미 화면값과 일치.
- **이 handoff 규칙 파일 신설** — 방 갈아탈 때 압축 토큰 낭비 방지용.

### 2026-08-13
- 대시보드 재구성: 총자산/총부채/순자산 한 테이블 + "로버트의 정리" 게시글 보드(클릭→모달). 로버트 이름 확정.
- 일지(📖 성찰) 탭 신설 — 노션 "한번씩 기록" 미러링. 15편 임포트. 마스머리 3단 카드(전체내용 표시). v4.3.

### 2026-08-12
- 현금흐름 시스템(P1~P5) 구현: 계좌 기초잔액, 거래-계좌 연결, 이체 타입, 실시간 대조, 🌊 현금흐름 탭(Sankey). 상세는 `cashflow-design.md`.
- 명리 🔮 사주 탭: 총운/대운/연도별세운/월별/특이일 드릴다운 + 저장 해석 버블 + 길흉평 배지.

### 2026-08-11
- 프로젝트 시작. Supabase 스키마 + 시드, 피검사 엑셀 임포트(21회×77지표), 네비 재정/건강/기타 구획화.
- 초기 스펙: `요구사항정의서.md`.

---

## 🗺 목표·루틴 로드맵 (2026-08-14 결정)

**구글 캘린더 연동은 Phase 2로 미룸.** 판단 근거:
- 기술적으로는 가능. 브라우저 전용 정적 사이트에서 `gapi` + Google Identity Services로 백엔드 없이 호출 가능. 필요: Google Cloud 프로젝트 → OAuth 클라이언트 ID(웹) + API 키 → 승인된 JS 원본에 `https://rohanchung.github.io` 등록 → 동의화면.
- ⚠️ **불확실**: 브라우저 토큰은 1시간 만료에 refresh token이 없다. 조용한 재발급이 되는지 구글 문서에 명시가 없어, 최악의 경우 접속마다 구글 로그인 팝업.
- ❌ **로버트 경유 미러링 불가**: Google Calendar MCP 커넥터에 노출된 도구가 `search_events` 하나뿐. 기간 조회·이벤트 생성 없음. 배치로 긁어와 Supabase에 밀어넣는 우회로는 현재 도구로 막혀 있음.
- ❌ **양방향은 반대.** 루틴 체크 월 150건이 구글 캘린더를 도배하고, 충돌 해결 로직은 개인 프로젝트가 가장 많이 좌초하는 지점.
- → 붙인다면 **읽기 전용 오버레이**로만. 2주 써보고 실제로 아쉬울 때 판단.

**다음 단계 후보**
- 데이터 2~3개월 쌓이면 **사주 주의일 vs 평상일 루틴 달성률 상관 분석**. 지금은 표시만, 분석은 표본이 모인 뒤.
- 주간 리뷰 화면(주별 달성률 추이).

## ⏳ 미해결 / 대기

- [x] ~~은행 실제 상환액 받아서 앱 부채 계산 교정~~ → **8/14 완료.**
- [ ] "About 로한" 공유용 다이제스트 (타 AI/URL 공유로 로한 학습용) — 로한이 "천천히"라 함, 보류.
- [ ] **아침 브리핑 스케줄 태스크** — 로한북엔 알림이 없어 앱을 열어야만 보인다. 매일 아침 로버트가 Supabase 읽고 어제 달성·연속일·오늘 루틴·재정 알림 전송. 이게 없으면 루틴 시스템은 2주 안에 죽는다.
- [ ] 9/1 0시 계좌 기초잔액 입력 → 현금흐름 100% 정합 가동 (cashflow-design.md 참조).
