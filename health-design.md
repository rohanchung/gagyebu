# 🩺 EMR — 로한북 건강 정본 위의 Dual AI 문제중심 상담 · 구현 명세 (v1)

> 작성: 로버트(Claude, 제작방) · 2026-09-18
> 원 기획: `handoffs` id=11 「로한북 건강파트 — Dual AI EMR UI 기획 핸드오프」(수연 작성, 2026-09-18)
> 구현: **코덱스**. 이 문서가 구현의 정본이다. 구조 판단이 다시 필요하면 로버트에게 되돌린다.
> 🚫 이 문서는 진단 도구의 명세가 아니다. AI 답변은 **기록 해석·경과 정리·진료 질문 준비·독립 의견** 층이다.

---

## 0. 확정된 결정 (로한, 2026-09-18)

| 항목 | 결정 |
|---|---|
| 페이지 이름 | **EMR** — 사이드 메뉴 **건강 묶음의 맨 첫 항목** (`🩹 체성분·사고` 앞) |
| 페이지의 전제 | 🔒 **로한북이 건강 정보의 정본이다.** 식이·체성분·피검사·병원 방문·사고가 이미 로한북 안에 있다. AI 에게 로한이 따로 설명하지 않고 **로한북이 사실을 공급한다** |
| 구현 순서 | 🔌 **모의 모드로 전부 먼저 만들고, API 키는 맨 마지막에 꽂는다**(§2.3 · §14) |
| 월 API 예산 | **3만원 이하** — 초과 시 입력을 막는다(§6.4) |
| 모델 등급 | **중간급 둘** — Claude `claude-sonnet-5` + OpenAI 중간급(모델명은 로한이 정함, §10) |
| 초기 문제 목록 | `handoffs` 의 문제별 방 **이관** — id 5 순환기 · id 4 오십견 재활 · id 6 칼륨 식단(§8) |
| 커밋 | 로한이 GitHub Desktop 으로 한다. 🚫 코덱스도 이 폴더에서 `git` 명령을 실행하지 않는다 |

---

## 1. 한 줄 요약

**사실은 하나, 해석은 둘.** 로한북이 만든 **사실 묶음 하나**를 두 AI 가 똑같이 받는다.
로한이 한 번 입력하면 로버트(Claude)와 수연(ChatGPT)이 **서로의 답을 보지 않고** 각자 답하고,
각자 **자기와 로한이 나눈 대화만** 기억한다.

```
Claude ↔ 로한 ↔ ChatGPT        (O)
Claude ↔ ChatGPT                (X — 절대 안 된다)
```

---

## 2. 🔴 아키텍처

### 2.1 왜 서버 함수가 필요한가
로한북은 GitHub Pages 의 정적 `index.html` 하나다. 브라우저에서 Anthropic/OpenAI 를 직접 부르면
**API 키가 페이지 소스에 그대로 노출된다** — 로그인 여부와 무관하게 누구나 읽는다.
→ **Supabase Edge Function** 하나를 둔다. 키는 Edge Function 비밀값(secrets)에만 있다.
⚠️ 실측(2026-09-18): 이 프로젝트엔 **Edge Function 이 0개**다. 이건 기능 추가가 아니라 **새 인프라 층**이다.

### 2.2 🔒 사실 묶음은 브라우저가 만든다 — 서버는 `app_state` 를 읽지 않는다
```
[index.html · EMR 페이지]
   emrFacts(문제) ── 로한북 함수로 사실 묶음을 만든다 (§4)
        │
        │  { problem_id, client_turn_id, question, facts }   (로그인 세션 JWT)
        ▼
[Edge Function: emr-chat]
   ① JWT 로 user 확인   ② 예산 검사
   ③ provider 별 스레드 조회 (격리, §3)
   ④ 같은 facts 를 두 AI 의 system 에 똑같이 넣는다
        ├──▶ Anthropic Messages API (claude-sonnet-5)
        └──▶ OpenAI API (중간급)            ← Promise.allSettled 병렬
   ⑤ 결과 저장 → 두 답변 + 이번 달 누적 비용 반환
```

**왜 서버가 직접 읽지 않나** — 이게 이 명세에서 가장 중요한 구조 결정이다.
- "그날 뭘 먹었나"·"최근 병원 언제 갔나" 같은 계산은 **로한북(`index.html`)에 이미 있거나 거기 있어야 할 함수**다.
  서버가 `app_state` JSON 을 따로 파싱해 같은 계산을 하면 **같은 질문에 답하는 함수가 둘**이 된다.
  v2.11(미결제 134만 차이)·v3.8(K패스 후불 누락)이 **정확히 이 구조로 두 번** 터졌다(원칙 2).
- 로한이 **AI 가 본 것과 똑같은 사실**을 EMR 화면에서 볼 수 있다(§7.3 「AI 에게 보낸 사실」).
- Edge Function 이 단순해진다 — 격리·예산·호출만 한다.
- 🔒 **격리와 예산은 서버가 강제한다.** 클라이언트는 사실 묶음만 만든다. 클라이언트 코드에 결함이 있어도 두 AI 가 섞이거나 예산이 뚫리지 않는다.

- SDK: Claude 쪽은 **공식 Anthropic TypeScript SDK**(Deno 에서 npm 지정자). 호출 형태는 추측하지 말고 공식 문서를 보고 쓴다. OpenAI 쪽도 공식 SDK.
- 🚫 스트리밍은 v1 에 넣지 않는다(§11).

### 2.3 🔌 모의 모드 — 키는 맨 마지막에 꽂는다 (로한 결정 2026-09-18)
**구현은 전부 모의 응답으로 먼저 끝낸다. 실제 API 키는 마지막 단계에서만 연결한다.**

Edge Function 안에서 AI 호출은 **어댑터 함수 하나**를 거친다:
```
callProvider(provider, { system, messages, max_tokens })
   → { status: 'ok'|'error'|'refused', content, model, usage:{in,out,cache_read}, truncated }
```
- 구현이 둘이다: **`mock`**(가짜 AI) · **`live`**(실제 SDK). 나머지 코드(격리·저장·예산·화면)는 **어느 쪽인지 모른다**
- 모드 결정: provider 별로 **그 provider 의 키가 secrets 에 있으면 `live`, 없으면 `mock`**. 설정 스위치를 따로 두지 않는다 —
  키를 넣는 행위 자체가 전환이다(스위치를 두면 "키는 넣었는데 모드는 mock" 같은 어긋남이 생긴다)
- 🔒 **mock 응답은 자기가 받은 입력을 드러낸다** — 이게 mock 의 존재 이유다. 예:
  ```
  🧪 [모의 응답 · 로버트]
  받은 사실 묶음: 2,431자 (sha256 앞 8자리 3fa9c1d2)
  받은 대화: 로한 3턴 · 내 답변 2개 · 상대 답변 0개
  이번 질문: "아까 네가 뭐라고 했지?"
  ```
  → 로한이 **격리(상대 답변 0개)와 동일 사실(해시가 두 칸에서 같다)을 화면에서 바로 확인**한다. 키 없이 핵심 검증이 끝난다
- mock 은 테스트용 트리거도 받는다: 질문에 `#fail:claude` → 그쪽 `error`, `#refuse:openai` → `refused`, `#slow` → 타임아웃, `#long` → `truncated`. 🔒 **live 에서는 이 트리거를 무시한다**
- 🔒 **mock 은 화면에 모의라고 보여야 한다.** 칸 제목에 `🧪 모의`, 페이지 상단에 「AI 미연결 — 모의 응답」 띠. 가짜 답이 진짜 AI 의견으로 읽히면 안 된다
- mock 행의 `cost_krw` 는 **0** 이다. `model` 은 `mock` 으로 저장한다 — 나중에 실제 대화와 섞여도 가를 수 있게
  - 🔒 예산 누적은 `model <> 'mock'` 행만 센다. 차단 화면은 `health_settings.monthly_cap_krw` 를 **0** 으로 바꾸면 키 없이 바로 볼 수 있다
- 🔒 **live 어댑터는 공식 문서의 응답 형태로만 쓴다.** mock 의 모양에 live 를 맞추지 않는다 — 반대다. mock 이 live 의 모양을 흉내낸다

---

## 3. 데이터 모델 — `app_state` 밖에 둔다

### 3.1 왜 `app_state` 에 넣지 않나
`app_state` 는 **128KB 단일 JSON + 낙관적 잠금**이다. 대화 한 턴마다 전체를 저장하면 충돌이 폭증하고,
v3.6 의 **단어 96건 유실**과 같은 구조가 된다. 대화는 **행 단위로 쌓이는 별도 테이블**이 맞다.
- ✅ 새 테이블은 `app_state` 밖이라 `SYNC_COLS` 등록 대상이 **아니다**.
  ⚠️ 반대로 **`DB` 안에 새 배열을 추가하게 되면** 그건 `SYNC_COLS` 등록까지가 한 작업이다(v3.6).

### 3.2 테이블 (전부 `user_id = auth.uid()` RLS)

**`health_problems`** — 문제 목록
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid pk | |
| `user_id` | uuid | RLS 기준 |
| `title` | text | 예: 「순환기 — 박성미 교수 추적」 |
| `status` | text | `active`(현재) / `watch`(경과관찰) / `closed`(종료) |
| `summary` | text | 로한이 쓰는 **경과 요약**. 두 AI 가 똑같이 받는다 |
| `sources` | jsonb | 이 문제에 보낼 기록의 범위(§4.2). 없으면 기본값 |
| `source_handoff_id` | bigint null | 이관 원본(`handoffs.id`) |
| `created_at` / `updated_at` | timestamptz | |

**`health_messages`** — 대화 (🔒 **격리 규칙이 이 테이블 구조 자체에 박혀 있다**)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid pk | |
| `user_id` | uuid | |
| `problem_id` | uuid fk | |
| `turn_no` | int | 1부터. 같은 턴 = 사용자 행 1 + 답변 최대 2 |
| `role` | text | `user` / `assistant` |
| `provider` | text null | `user` 행은 **null**(양쪽 공유). `assistant` 행은 `claude` / `openai` |
| `content` | text | |
| `facts` | text null | **`user` 행에만.** 그 턴에 두 AI 에게 보낸 사실 묶음 원문. "그때 AI 가 뭘 알고 있었나"의 증거이자, 두 AI 가 **같은 사실**을 받았다는 검증 수단 |
| `status` | text | `ok` / `error` / `refused` / `pending` |
| `error` | text null | 실패 사유(사용자에게 보인다) |
| `model` | text null | 실제 응답 모델 id |
| `in_tokens` / `out_tokens` / `cache_read_tokens` | int null | API 가 돌려준 `usage` 그대로 |
| `cost_krw` | numeric null | §6.4 계산식 결과 |
| `client_turn_id` | text | 중복 전송 방지 키(클라이언트 생성). `(problem_id, client_turn_id, role, provider)` unique |
| `created_at` | timestamptz | |

> 🔒 **provider P 의 스레드 = (`role='user'` 행 전부) + (`role='assistant' AND provider=P AND status='ok'` 행).**
> 상대 provider 의 행은 **조회 조건에서부터 빠진다.** 코드 어디서도 "상대 답변을 빼는" 필터를 따로 쓰지 않는다 —
> 빼먹는 자리가 반드시 생긴다(원칙 2). 이 조회는 서버의 **함수 하나**다.

**`health_agents`** — 두 AI 의 설정 (코드에 박지 않는다)
| 컬럼 | 설명 |
|---|---|
| `provider` pk | `claude` / `openai` |
| `display_name` | 「로버트」 / 「수연」 |
| `model` | `claude-sonnet-5` / (로한이 정함) |
| `max_tokens` | 기본 3000 |
| `in_usd_per_mtok` / `out_usd_per_mtok` / `cache_read_usd_per_mtok` | 단가. Claude 는 §6.4 값, OpenAI 는 로한이 공식 가격표에서 채움 |
| `enabled` | 끄면 그쪽 호출을 안 한다 |

**`health_settings`** — 단일 행: `monthly_cap_krw`(30000), `usd_krw`(환율), `history_turns`(12)

---

## 4. 사실 묶음 — "사실은 하나"의 실체

### 4.1 로한북 안의 건강 기록 (실측 2026-09-18)

| 영역 | 저장 위치 | 규모 | 메모 |
|---|---|---|---|
| **식이 — 직접 기록** | `meals[날짜][b·l·d·s] = {p, a}` | 16일 (9/2~) | `b`아침 `l`점심 `d`저녁 `s`간식. 🔒 **`a` = 실제로 먹은 것만 보낸다** |
| **식이 — 구매** | `transactions[]` 의 `food`·`slot` | 37건 (8/22~9/16) | 식당·배달·편의점. `slot` 없는 음식 거래도 있다(장보기 등, v2.14) |
| **체성분** | `health.weights[]` | 88건 | `kg·fatKg·muscle·skMuscle·waterKg·boneKg·protein·bmr·memo` |
| **피검사** | `health.labDates[i]` × `health.labValues[지표명][i]` | 21회차 · 83지표 | 참고범위 `metrics[].low/high/range`. ⚠️ **단위 필드 없음** — 지어내 붙이지 않는다 |
| **복용약** | `health.labMeds[i]` (아침/저녁) | 회차별 | 최신 회차를 현재 약으로 본다 |
| **병원 방문** | `transactions[]` 중 `cat='의료'` 의 `date·memo` + `health.labVisits[i]`(진료일) | 의료 26건 · 진료일 21 | 메모 예: 「풍무모두튼튼_도수3차」「순환기_3개월치」. 🚫 `cat='건강보험'` 은 보험료 납부라 방문이 아니다 |
| **사고·증상** | `health.events[]` | 1건 | `date·name·symptom·treatment·status·memo` |

### 4.2 문제별 범위 — `health_problems.sources`
```json
{ "labs": ["BUN", "크레아티닌", "칼륨"],   // 보낼 피검사 지표명. [] 이면 피검사 안 보냄
  "body": true,                            // 체성분 최근 8건
  "diet_days": 14,                         // 식이 최근 N일. 0 이면 안 보냄
  "visits_days": 90,                       // 병원 방문 최근 N일. 0 이면 안 보냄
  "events": true }                         // 사고·증상
```
- 신규 문제 기본값: `labs:[]` · `body:true` · `diet_days:14` · `visits_days:90` · `events:true`
- 피검사는 최근 **6회차**, 복용약은 `labs` 가 비어 있지 않으면 같이 보낸다
- ⚠️ 지표엔 별도 id 가 없어 **이름이 곧 키**다(`labValues[m.name]`). 앱에서 지표 이름을 바꾸면 `labs` 연결이 끊긴다 →
  사실 묶음에 **「연결 끊김: ○○」**으로 드러낸다(조용히 빼지 않는다)

### 4.3 🔒 새 함수 — `index.html` 에 **한 번만** 만든다
| 함수 | 답하는 질문 | 규칙 |
|---|---|---|
| `dietOn(ds)` | 그날 뭘 먹었나 | `meals[ds][slot].a` + 그날 `food` 가 있는 거래를 끼니별로 합친다. 각 항목에 출처(`기록`/`구매`)를 붙인다. 같은 끼니에 **글자가 완전히 같으면** 한 줄로 합친다(그 외 추정 합치기 🚫). 🚫 **`p`(생활방 제안)는 넣지 않는다** — 먹기로 한 것과 먹은 것은 다르다(v2.51 원칙) |
| `medVisits(from,to)` | 언제 병원에 갔나 | `cat='의료'` 거래(날짜·메모)와 `labVisits` 진료일을 날짜순으로 합친다. 🚫 금액은 넣지 않는다(재정 데이터) |
| `emrFacts(problem)` | 이 문제에 대해 AI 가 알아야 할 사실 | §4.2 범위로 위 함수들 + 체성분·피검사·약·사고를 **평문 한 덩어리**로 만든다 |

- 🔒 `dietOn` 은 **EMR 전용이 아니다.** 앞으로 데일리·캘린더가 "그날 먹은 것"을 보여줄 때도 이 함수를 쓴다. 두 번째 구현을 만들지 않는다
- 🔒 **기록이 없는 날은 「기록 없음」이지 「안 먹음」이 아니다.** 사실 묶음 머리에 **기록 시작일**을 적는다
  (`식이 기록: 직접 2026-09-02~ · 구매 2026-08-22~`). 안 쓰면 AI 가 빈 날을 금식으로 읽는다
- 🔒 **순서와 형식을 고정한다** — 날짜 오름차순, 항목 순서 고정, 매번 같은 머리글. 같은 데이터면 같은 글자가 나와야 캐시가 걸린다(§5)
- 🚫 넣지 않는 것: 재정·세무·사주·일지·학습 등 **건강 외 데이터 전부**, 다른 문제의 summary, 금액

### 4.4 사실 묶음 모양 (예시 — 형식만 참고)
```
[문제] 순환기 — 박성미 교수 추적 (현재)
[경과 요약]
…summary…
[기록 범위] 식이: 직접 2026-09-02~ · 구매 2026-08-22~ / 기록 없는 날은 '기록 없음'이다
[병원 방문 — 최근 90일]
2026-09-03 의료 · 순환기_3개월치
2026-09-03 진료일(피검사)
…
[피검사 — 최근 6회]
{지표명} (참고 {low}–{high}): {날짜} {값} · … · {날짜} {값}
[현재 복용약] 아침: … / 저녁: …
[체성분 — 최근 8건]
{날짜} 체중 … · 체지방 … · 골격근 …
[식이 — 최근 14일]
{날짜} 저녁 현미밥+치킨3조각(가슴살) [기록]
{날짜} 간식 공차 저당 초콜렛, 꿀빵 1개 [기록]
{날짜} 점심 (기록 없음)
…
```
⚠️ 위는 **형식만** 보여준다. 수치·날짜는 자리표시다 — 구현 검증은 실데이터로 한다(§12.2).

---

## 5. 컨텍스트 조립 — provider P 에게 보내는 것

```
system  = [A] 공통 운영원칙           ← 두 AI 동일. 고정문
          [B] 사실 묶음 (facts)        ← 클라이언트가 보낸 그대로. 두 AI 동일. 캐시 경계는 여기 끝
messages = P 의 스레드 최근 history_turns 턴 (§3.2 규칙)
          + 이번 사용자 입력  (맨 앞에 "오늘: 2026-09-18 (KST)" 한 줄)
```

- 🔒 **[A] 는 두 AI 가 글자 하나까지 같다.** 차이는 `display_name` 한 단어뿐이다.
  **의견 차이가 모델 차이에서 나와야 한다** — 페르소나 문구가 다르면 차이가 모델 때문인지 프롬프트 때문인지 못 가린다.
- 🔒 **[B] 는 두 AI 가 바이트 단위로 같다.** 서버가 클라이언트의 `facts` 를 받아 **그대로 두 번** 쓴다. provider 별로 가공하지 않는다.
- 🔒 **날짜·시각은 system 에 넣지 않는다.** 사용자 입력 쪽에 붙인다(캐시 보호).
- 사실 묶음은 새 기록이 생기면 바뀐다(하루에 한 번쯤) → 그때 캐시가 한 번 깨진다. 정상이다.
- ⚠️ 캐시는 접두부가 일정 길이 이상이어야 걸리고 짧으면 **조용히 안 걸린다.** `cache_read_tokens` 가 계속 0 이어도 억지로 늘리지 않는다.
- **히스토리 창**: 최근 `history_turns`(12) 턴. 오래된 건 보내지 않는다.
  🚫 AI 자동 요약 금지 — 누가 요약하느냐에 따라 **한쪽 해석이 공통 사실로 둔갑한다.** 오래된 맥락은 로한이 `summary` 에 옮긴다.
- 실패한 턴: 턴 2 에서 Claude 가 실패하고 로한이 턴 3 으로 넘어가면 Claude 스레드엔 Q2·Q3 가 연속된다.
  → 🔒 **연속된 사용자 메시지는 한 메시지로 합쳐 보낸다**(`[이전 질문 — 네 답변이 실패해 전달되지 않았다]\nQ2\n\n[이번 질문]\nQ3`).

### 5.1 공통 운영원칙 [A] 초안 (구현 시 그대로 쓰되 로한이 다듬는다)
```
너는 {display_name}이고, 로한의 건강 기록을 함께 읽는 상담 파트너다.
- 너는 의사가 아니다. 진단·처방을 하지 않는다. 기록 해석, 경과 정리, 진료 때 물을 질문 준비를 돕는다.
- 아래 [사실 묶음]은 로한의 개인 기록 앱에서 뽑은 것이고 사실의 정본이다. 거기 없는 수치를 지어내지 않는다. 모르면 모른다고 한다.
- '기록 없음'은 '하지 않음'이 아니다. 기록이 빈 날을 금식·운동 안 함 등으로 해석하지 않는다.
- 응급 징후(흉통, 호흡곤란, 의식 변화 등)가 언급되면 다른 말보다 먼저 즉시 진료·119를 권한다.
- 너와 같은 질문을 다른 AI 도 독립적으로 받는다. 너는 그 답을 볼 수 없다. 네 판단을 말하라.
- 한국어로, 짧고 구체적으로. 필요하면 "진료 때 물어볼 것"을 따로 정리한다.
```

---

## 6. 턴 흐름과 예외

### 6.1 정상 흐름
1. 로한이 입력 → 클라이언트가 `emrFacts(문제)` 로 사실 묶음을 만들고 `client_turn_id` 생성, 입력창 잠금, 두 칸에 「생각 중…」
2. Edge Function: JWT 확인 → 예산 검사(§6.4) → `user` 행 저장(`facts` 포함) → 두 provider 병렬 호출
3. 각 결과를 `assistant` 행으로 저장(`ok` / `error` / `refused`) → 두 답변 + 이번 달 누적 비용 반환
4. 클라이언트가 두 칸을 채우고 입력창 잠금 해제

### 6.2 예외 표
| 상황 | 처리 |
|---|---|
| 한쪽만 실패 | 성공한 쪽은 그대로. 실패한 칸에 사유 + **「이쪽만 다시」**. 재시도는 **같은 턴·같은 `facts`** 로 그 provider 만 다시 부른다(새 사용자 행 안 만듦) |
| 둘 다 실패 | 사용자 행은 남긴다(로한이 쓴 글이 사라지면 안 된다). 두 칸 모두 재시도 버튼 |
| 재시도 안 하고 다음 턴 | §5 의 "연속 사용자 메시지 합치기" |
| `refused`(안전상 거절) | 실패와 구분해 「이 질문엔 답하지 않았다」. 재시도 버튼은 준다 |
| 타임아웃 | provider 별 60초. 넘으면 `error: timeout` |
| 중복 전송(더블탭·재전송) | `client_turn_id` unique 로 서버가 거른다. 이미 있으면 저장된 결과를 돌려준다 |
| 응답 도중 화면을 닫음 | ⚠️ **불확실** — 연결이 끊겨도 Edge Function 이 끝까지 도는지 코덱스가 실측할 것. 안 돈다면 **2분 넘은 `pending`** 을 `error: 중단됨` 으로 보이고 재시도 버튼을 준다 |
| `max_tokens` 로 잘림 | 내용은 저장하고 「답변이 길이 제한으로 잘렸다」 한 줄 |
| provider `enabled=false` | 그 칸은 「꺼져 있음」. 호출·비용 없음 |
| `labs` 에 없는 지표명 | 사실 묶음에 「연결 끊김: ○○」(§4.2) |

### 6.3 🔒 조용히 버리지 않는다
실패·거절·잘림·연결 끊김은 **전부 화면에 사유가 보여야 한다.** 칸이 그냥 비어 있으면 로한은 AI 가 할 말이 없었다고 읽는다(v2.14 교훈).

### 6.4 예산
- **이번 달** = KST 기준 1일 00:00 부터. 🔒 서버 `now()` 를 날것으로 쓰지 않는다 — `kst_now()` 기준(STATUS §5-1).
- 누적 = 이번 달 `health_messages.cost_krw` 합.
- 비용 = `(in_tokens × in단가 + out_tokens × out단가 + cache_read_tokens × cache단가) / 1,000,000 × usd_krw`
  - ⚠️ `in_tokens` 에 캐시 읽기분이 포함되는지 provider 마다 다르다 — 코덱스가 각 API 의 `usage` 정의를 공식 문서로 확인하고 **이중 계산이 없게** 할 것
- Claude(`claude-sonnet-5`) 단가: **입력 $2 · 출력 $10 / 100만 토큰** (Anthropic 공식, 2026-06 기준). 캐시 읽기 단가는 공식 가격표에서 확인해 채운다
- 환율 `usd_krw` 는 설정값(초기 1,400 — ⚠️ 가정값. 로한이 조정)
- **80%**: 입력창 위 「이번 달 24,000원 / 30,000원」 경고색
- **100%**: 서버가 호출 **전에** 거부. 입력창 잠금 + 사유. 🚫 한쪽만 부르는 우회 없음
- 대략적 감(가정 기반): 사실 묶음이 약 4천 토큰이면 턴당 Claude 약 20~30원, OpenAI 가 비슷하면 턴당 약 50~60원 → 월 3만원에 **약 500턴**. 히스토리가 길수록 오르므로 상한은 **실측 누적**으로 판단한다

---

## 7. 화면 — EMR 페이지

### 7.1 진입
- 사이드 메뉴 **건강 묶음의 맨 첫 항목**: `🩺 EMR` → 그 다음 `🩹 체성분·사고` · `🧪 피검사` · `🛡️ 보험`
- `VIEW_RENDER` 에 `emr: renderEmr` 등록. 뷰 id `v-emr`
- ⚠️ 모바일 가로 스크롤 네비에 항목이 하나 늘어난다 — t14(브레이크포인트) 통과 필수

### 7.2 PC (≥900px)
```
┌─ 문제 목록 ─────┐┌─ 순환기 — 박성미 교수 추적 ──── [현재 ▾] [요약 편집] [기록 범위] ─┐
│ ● 현재           ││ 경과 요약 (앞 3줄 · 펼치기)     [📋 AI 에게 보낸 사실 보기]      │
│   순환기          │├──────────────────────────────────────────────────────────────────┤
│   오십견 재활      ││ 턴 1  로한: …                                                    │
│ ◐ 경과관찰        ││   ┌ 🤖 로버트 (claude-sonnet-5) ─┐┌ 💬 수연 (gpt-…) ──────────┐ │
│   칼륨 식단        ││   │ …                            ││ …                          │ │
│ ○ 종료 (접힘)     ││   └──────── AI 의견 · 진단 아님 ─┘└─────── AI 의견 · 진단 아님 ┘ │
│                  ││ 턴 2 …                                                           │
│ [+ 신규 문제]     │├──────────────────────────────────────────────────────────────────┤
└──────────────────┘│ [ 입력 …                                        ] [보내기] 이번 달 12,400원/30,000원 │
                    └──────────────────────────────────────────────────────────────────┘
```

### 7.3 📋 AI 에게 보낸 사실 보기 — EMR 의 기록 면
- 현재 문제의 `emrFacts()` 결과를 **그대로** 펼쳐 보인다(AI 가 받는 글자와 동일)
- 🔒 이게 "사실은 하나"를 로한이 **눈으로 확인하는 곳**이다. 빠진 기록·잘못 합쳐진 식이·끊긴 지표가 여기서 보인다
- 턴별로도 볼 수 있다 — 각 턴의 로한 질문 옆 작은 「그때 사실」 → 저장된 `facts` 를 보여준다
- **「기록 범위」** 버튼: §4.2 `sources` 를 편집(지표 체크리스트 · 체성분 · 식이 N일 · 병원 N일 · 사고)

### 7.4 모바일 (<900px) — 🔒 로한의 주 사용처다. 이걸 먼저 맞춘다
- 1단계: 문제 목록(상태별 묶음) → 누르면 2단계
- 2단계: 상단 문제 제목 + 「← 목록」, 요약·사실 보기는 접힘
- 턴은 **세로로 쌓는다**: 로한 질문 → 로버트 카드 → 수연 카드. 두 칸 나란히 🚫(400px 에서 못 읽는다)
- 각 AI 카드는 접을 수 있다
- 입력창은 하단 고정. ⚠️ 가상 키보드가 올라올 때 입력창이 가려지지 않는지 실기기 확인
- 🔒 **공용 클래스를 재사용하면 그 모바일 규칙까지 물려받는다**(v3.7 `.calwrap` 사고). 새 클래스는 base 규칙부터 쓴다

### 7.5 공통
- 모든 AI 답변 하단에 작게 **「AI 의견 · 진단 아님」**
- 두 칸 순서 **고정**(로버트 왼쪽/위, 수연 오른쪽/아래)
- 답변은 텍스트로 렌더(`mdEsc`) — 🚫 AI 출력을 `innerHTML` 로 그대로 넣지 않는다(XSS)

---

## 8. 문제 생애주기와 이관

- **신규**: 「+ 신규 문제」 → 제목 + 첫 질문 → `health_problems` 생성(기본 `sources`) 후 턴 1
- **기존**: 목록에서 선택 → 메시지 로드 → 이어서 입력
- **상태 변경**: 현재 ↔ 경과관찰 ↔ 종료. 종료된 문제도 열람·재개 가능
- **summary·sources 편집**: 로한만 한다. 다음 턴부터 두 AI 가 똑같이 새 사실을 받는다
- 🚫 문제 삭제는 v1 에 없다(대화 기록 유실 방지)

### 8.1 초기 이관 (1회성)
| handoffs | → 문제 | status | 비고 |
|---|---|---|---|
| id 5 「2026-09-03 박성미 진료 결과 및 12월 계획」(순환기) | 순환기 | `active` | `labs` 는 로한과 확인 후 채움 |
| id 4 「오른쪽 어깨 오십견 재활 경과」(재활의학과) | 오십견 재활 | `active` | 도수치료 방문이 `visits` 에 자동으로 잡힌다 |
| id 6 「식단 설계 제약조건 — 칼륨 제한」(순환기방 발행) | 칼륨 식단 | `watch` | `diet_days` 넉넉히. ⚠️ 순환기에서 파생된 제약 — 합칠지는 로한이 정한다 |
- `summary` = 해당 handoff 의 `body` 원문. `source_handoff_id` 기록
- 🔒 `handoffs` 원본은 **건드리지 않는다**(다른 방들이 계속 읽는다). 복사만 한다

---

## 9. 개인정보·보안
- 새 테이블 전부 RLS(`user_id = auth.uid()`). Edge Function 은 **로한의 JWT 로** DB 에 접근 — service role 키를 쓰지 않는 것을 기본으로 한다
- API 키는 Edge Function secrets 에만. 🚫 `index.html`·레포·`app_state` 어디에도 두지 않는다
- 외부로 나가는 건 **문제별 `sources` 범위의 건강 기록뿐**이다(§4). 건강 외 데이터·금액은 나가지 않는다
- 두 회사의 API 데이터 보관 정책은 로한이 각 사 문서로 확인한다(이 문서는 판단하지 않는다)

---

## 10. 로한이 해야 하는 것 (코덱스·로버트가 대신 못 한다)

### 10.1 1단계 전에 — Supabase 배포 수단
⚠️ AI 키는 마지막이어도 되지만, **1단계부터는 누군가 Supabase 에 테이블을 만들고 Edge Function 을 배포해야 한다.**
이건 AI 키가 아니라 **Supabase 권한**이다. 둘 중 하나를 정한다(§15):
- **(가) 코덱스에게 배포 권한** — 코덱스 환경에 Supabase CLI 로그인. 코덱스가 구현·배포를 다 한다
- **(나) 배포만 로버트** — 로버트(클로드 코드)는 이미 Supabase 에 연결돼 있다(테이블 생성·함수 배포 가능).
  코덱스는 코드를 레포에 쓰고, 로버트가 그걸 읽어 배포한다
- 🔒 어느 쪽이든 **Supabase 액세스 토큰은 채팅에 붙여넣지 않는다**

### 10.2 5단계(맨 마지막)에 — AI 키
🔌 0~4단계는 키 없이 모의 모드로 끝난다(§2.3). 미리 발급해 둘 필요도 없다 — 5단계 직전에 하면 된다.
1. **Anthropic API 키 발급 + 결제 등록** (Anthropic 콘솔)
2. **OpenAI API 키 발급 + 결제 등록** · **중간급 모델 하나 지정** + 공식 가격표 단가를 `health_agents` 에 기입
3. 두 키를 **Supabase 대시보드 → Edge Functions → Secrets** 에 직접 입력 (🚫 채팅에 붙여넣지 않는다)
4. 각 사 콘솔에서 **월 사용 한도**도 걸어 둔다 — 앱 상한(§6.4)의 두 번째 안전망
5. 이관 때 문제별 `sources`(특히 순환기 `labs`) 확인

---

## 11. 🚫 v1 에서 안 만드는 것 (스코프 방어)
- 스트리밍 · AI 자동 요약 · 검사지 사진/PDF 첨부 · 음성
- **두 답변 비교·요약 화면** — 수렴시키지 않는 게 설계 의도다. 비교는 로한의 눈이 한다
- 두 AI 가 서로의 답을 읽는 기능 — 영구히 안 만든다
- 기존 건강 기록의 테이블 이전 — 정본은 로한북 그대로다(§4)
- 문제 삭제

---

## 12. 완료 조건 (코덱스 → 로한 인계 전 전부 ✓)

### 12.1 격리·동일 사실 (가장 중요 — 하나라도 실패하면 인계 불가)
- [ ] Claude 로 나가는 요청 본문에 OpenAI 답변 텍스트가 **한 글자도 없다** (반대도 동일). 목 provider 로 요청 본문을 잡아 검사
- [ ] 같은 턴에 두 provider 가 받은 [B] 사실 묶음이 **바이트 단위로 같고**, 저장된 `user.facts` 와도 같다
- [ ] provider 별 스레드 조회가 §3.2 규칙 **한 함수**로만 이뤄진다
- [ ] 한쪽 실패 후 다음 턴에서, 실패한 쪽 요청에 이전 질문이 합쳐져 들어간다(§5)

### 12.2 사실 묶음
- [ ] `dietOn` — `a` 는 들어가고 **`p` 는 안 들어간다**. 같은 끼니 같은 글자는 한 줄. 출처 표시
- [ ] `medVisits` — `의료` 는 들어가고 **`건강보험` 은 안 들어간다**. 금액 없음
- [ ] 기록 없는 날은 「기록 없음」, 머리에 기록 시작일
- [ ] 없는 지표명은 「연결 끊김」
- [ ] 같은 데이터로 두 번 만들면 **글자가 완전히 같다**(캐시 전제)
- [ ] 🔒 **실데이터로 확인** — 로한 계정의 실제 기록으로 사실 묶음을 뽑아 로한이 읽고 맞다고 한다(픽스처 통과는 증명이 아니다, v2.6)

### 12.3 흐름·예외
- [ ] 정상 1턴 / 3턴 연속 / 한쪽 실패 / 둘 다 실패 / 「이쪽만 다시」 / refused / 중복 전송 / 예산 80%·100%
- [ ] 100% 초과 시 **API 호출이 0회**(목 provider 호출 횟수로 검증)
- [ ] 시스템 프롬프트에 날짜·시각이 없다

### 12.4 화면
- [ ] `🩺 EMR` 이 건강 묶음 **맨 앞**이다
- [ ] PC 1440 / 모바일 400 스크린샷 — 목록·턴·사실 보기·실패 칸·예산 경고
- [ ] AI 출력에 `<script>` 를 넣어도 실행되지 않는다(XSS)
- [ ] t14(브레이크포인트) 통과

### 12.5 회귀
- [ ] `bash tests/run.sh all` **25개 + 신규 스위트 전부 통과** (실행법 STATUS §21)
- [ ] 신규 Playwright 스위트 `t43.js` — Edge Function 호출은 **목으로 대체**(기존 스위트의 supabase 목과 같은 방식)
- [ ] Edge Function 단위 테스트(Deno) — 격리·동일 사실·비용 계산·예산 거부
- [ ] mock 트리거(`#fail` 등)가 **live 에서 무시**된다
- [ ] 키가 없는 provider 는 mock, 있는 provider 는 live — **한쪽만 키를 넣은 상태**도 동작한다

### 12.6 🔑 실연결 확인 (5단계 — 키를 꽂은 뒤, 로한과 함께)
0~4단계의 ✓ 는 모의 응답 기준이다. 실제 API 는 모양이 다르므로 이것만은 **실제 키로** 확인한다.
- [ ] 두 칸에 `🧪 모의` 표시가 **사라지고** 실제 답이 온다. 상단 「AI 미연결」 띠가 사라진다
- [ ] 저장된 `model` 이 실제 모델 id 다(`mock` 아님)
- [ ] `in_tokens`·`out_tokens`·`cache_read_tokens` 가 **두 콘솔의 사용량 화면 숫자와 맞는다** — 이중 계산 없음(§6.4)
- [ ] `cost_krw` 가 두 콘솔의 청구 금액과 **자릿수까지 대략 맞는다**(환율 차이 정도만)
- [ ] 같은 문제로 2턴 연속 → 두 번째 턴에 `cache_read_tokens` 가 0 보다 크다. 0 이면 원인을 적는다(접두부가 짧을 수도 있다 — 결함 아닐 수 있음)
- [ ] 격리 실전 확인: 턴 1 에서 로버트·수연이 다른 말을 하게 하고, 턴 2 에 "상대 AI 가 뭐라고 했는지 아냐"고 묻는다 → **둘 다 모른다고 한다**
- [ ] 한쪽 키를 일부러 틀리게 넣어 실제 인증 오류가 「이쪽만 다시」로 뜬다(모의 `#fail` 과 같은 화면인지)
- [ ] 실제로 1주일 써본 뒤 이번 달 누적이 예산 추정(§6.4)과 크게 어긋나지 않는다. 어긋나면 `history_turns`·`max_tokens` 를 조정한다

---

## 13. 코덱스 작업 규칙
- 🔒 **추측 금지** — 모르면 「불확실」로 표시하고 로한에게 묻는다
- 🔒 **같은 질문에 답하는 함수는 하나** — `dietOn`·`medVisits` 를 만들었으면 서버에도, 다른 화면에도 두 번째 구현을 두지 않는다
- 🔒 **실데이터에서 확인한다.** 픽스처 통과는 증명이 아니다(v2.6)
- 🔒 **빈 서랍 금지** — 없으면 키를 만들지 않는다(`undefined` 대신 `delete`)
- `index.html` 은 80만 바이트 단일 파일이다. EMR 코드는 **한 구역**(`/* ══════ 🩺 EMR ══════ */`)에 모은다. 단 `dietOn`·`medVisits` 는 공용이라 식사 기록 구역(`🍚 식사 기록`) 옆에 둔다
- 버전·CHANGELOG·STATUS 갱신은 레포 관례를 따른다. 버전 번호는 로한이 정한다
- 인계할 때 GitHub Desktop 에 붙여넣을 **커밋 Summary / Description** 을 같이 낸다

---

## 14. 🪜 진행 순서 — 한 번에 다 만들지 않는다

로한도 코덱스도 **AI 두 개를 붙이는 건 처음**이다. 한 번에 전부 만들면 안 될 때 어디서 막혔는지 모른다.
**각 단계가 로한 눈으로 확인된 뒤에 다음으로 간다.**

🔌 **0~4단계는 전부 키 없이 모의 모드(§2.3)로 한다. 비용 0원. 키는 5단계에서만 꽂는다.**

| 단계 | 만드는 것 | 로한이 확인하는 것 | 여기서 배우는 것 |
|---|---|---|---|
| **0. 사실 묶음** | `dietOn`·`medVisits`·`emrFacts` + EMR 페이지 뼈대(메뉴 맨 앞) + **「AI 에게 보낸 사실 보기」**. 문제는 임시로 「전체」 하나. 서버 없음 | 내 실제 기록이 빠짐없이, 틀림없이 모였나 — **로한이 직접 읽고 판정** | AI 가 무엇을 알게 되는지는 **우리가 보내는 것으로 결정된다** |
| **1. 서버 + 모의 AI** | Edge Function `emr-chat` 배포 + `callProvider` 어댑터(mock). 질문 → 🧪 모의 답 둘. DB 저장 없음 | 버튼 → 모의 답 두 개가 오고, **두 칸의 사실 해시가 같다** | 서버 함수·배포·JWT. 키가 없어도 서버는 돈다 |
| **2. 기억** | 테이블 + 격리 조회. 문제 하나로 3턴 | 모의 답의 「상대 답변 **0개**」 · 「내 답변 n개」가 턴마다 맞게 늘어난다 | API 는 스스로 기억하지 않는다 — 기억은 앱이 만든다 |
| **3. 화면** | §7 PC·모바일 + 문제 목록 + 이관 3건 + 기록 범위 편집 | 폰에서 실제로 쓴다(답은 모의) | |
| **4. 안전망** | §6 예외 전부 + §6.4 예산 + 완료조건 §12 전체 | `#fail:claude` 로 「이쪽만 다시」 · 상한 0 으로 차단 화면 | 실패가 정상 상황이라는 것 |
| **5. 🔑 연결 (맨 마지막)** | 로한이 키 두 개를 secrets 에 넣는다. **코드 변경 없음**(§2.3 — 키가 있으면 live) | §12.6 실연결 확인 전부 | 실제 AI 가 모의와 어떻게 다른지 |

- ⚠️ **0단계와 2단계 확인이 이 설계의 핵심 검증이다.** 사실이 틀리면 두 AI 가 같이 틀리고, 격리가 깨지면 두 AI 를 쓰는 이유가 없다. 둘 다 **키 없이** 끝난다
- 🔒 **5단계에서 코드를 고치게 되면 그건 mock 이 live 의 모양을 잘못 흉내냈다는 뜻이다.** 고친 뒤 mock 도 같이 고친다 — 안 고치면 테스트가 실제와 다른 걸 검증한다
- 단계마다 코덱스가 커밋 Summary/Description 을 내고, 로한이 커밋한다

---

## 15. 열린 질문 (구현 중 로한 확인)
1. OpenAI 쪽 모델·단가 (§10)
2. 칼륨 식단을 순환기에 합칠지 (§8.1)
3. 순환기 문제의 `labs` 지표 (§8.1)
4. 환율 초기값 1,400 이 맞는지 (§6.4)
5. 식이 기본 14일 · 병원 90일이 적당한지 (§4.2)
6. **Supabase 배포를 누가 하나** — 코덱스(가) / 로버트(나) (§10.1) · 1단계 전까지 정한다
