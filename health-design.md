# 🩺 EMR — 로한북 건강 정본 위의 Dual AI 문제중심 상담 · 구현 명세 (v2 · 중계 모드)

> 작성: 로버트(Claude, 제작방) · 2026-09-18
> 원 기획: `handoffs` id=11 「로한북 건강파트 — Dual AI EMR UI 기획 핸드오프」(수연 작성, 2026-09-18)
> 구현: **코덱스**. 이 문서가 구현의 정본이다. 구조 판단이 다시 필요하면 로버트에게 되돌린다.
> 🚫 이 문서는 진단 도구의 명세가 아니다. AI 답변은 **기록 해석·경과 정리·진료 질문 준비·독립 의견** 층이다.
>
> 📌 **v1 → v2 변경 (2026-09-18)**: API 과금을 아직 쓰지 않기로 했다. AI 는 API 가 아니라 **로한이 이미 쓰는 두 앱의 방**
> (Claude 앱의 로버트 방 · ChatGPT 앱의 수연)이 답한다 — **중계(relay) 모드**. API 연결은 나중의 선택지로 미뤘다(§13).
> ✅ **0단계(사실 묶음 + EMR 뼈대) 범위는 바뀌지 않았다.** 진행 중인 작업을 그대로 이어가면 된다.

---

## 0. 확정된 결정 (로한, 2026-09-18)

| 항목 | 결정 |
|---|---|
| 페이지 이름 | **EMR** — 사이드 메뉴 **건강 묶음의 맨 첫 항목** (`🩹 체성분·사고` 앞) |
| 페이지의 전제 | 🔒 **로한북이 건강 정보의 정본이다.** 식이·체성분·피검사·병원 방문·사고가 이미 로한북 안에 있다. **로한북이 사실을 공급한다** |
| AI 가 답하는 곳 | 🔒 **중계 모드** — 로한이 이미 쓰는 앱의 방이 답한다. 추가 비용 0원 |
| 질문하는 곳 | 🔒 **EMR 페이지에만 쓴다.** 앱의 채팅창에 질문을 쓰지 않는다 — 두 AI 가 **글자 하나 다르지 않은 같은 질문**을 받아야 한다 |
| 초기 문제 목록 | `handoffs` 의 문제별 방 **이관** — id 5 순환기 · id 4 오십견 재활 · id 6 칼륨 식단(§8) |
| API 연결 | 보류. 나중에 예산이 생기면 붙인다(§13). 붙일 때의 기준은 월 3만원 · 중간급 모델 둘 |
| 커밋 | 로한이 GitHub Desktop 으로 한다. 🚫 코덱스도 이 폴더에서 `git` 명령을 실행하지 않는다 |

---

## 1. 한 줄 요약

**사실은 하나, 해석은 둘.** 로한이 EMR 에 질문을 한 번 쓰면, 로버트와 수연이 **각자 자기 방에서 자기 몫만 읽고** 답을 EMR 에 남긴다.
두 AI 는 같은 질문·같은 사실 묶음을 받고, **상대의 답은 보지 않는다.** 로한이 가운데서 둘을 비교한다.

```
Claude ↔ 로한 ↔ ChatGPT        (O)
Claude ↔ ChatGPT                (X — 절대 안 된다)
```

---

## 2. 🔴 구조

### 2.1 한 턴의 흐름 (중계 모드)
```
① 로한 ─ EMR 페이지에 질문 (턴 N)
         └ 브라우저가 emrFacts() 로 사실 묶음을 만들어 질문과 함께 저장

② 로한 ─ Claude 앱의 해당 방(예: 로버트(순환기))에 붙여넣기: 「EMR 답할 거 있나 봐」
         └ 로버트: emr_pending('claude') → emr_thread(문제,'claude') 로 자기 몫만 읽고
                   → emr_reply(문제, N, 'claude', 답) 로 EMR 에 기록

③ 로한 ─ ChatGPT 앱의 수연에게 같은 한 줄
         └ 수연: 같은 함수로 'openai' 몫만 읽고 답을 기록 (수연은 Supabase 에 직접 쓸 수 있다 — 로한 확인 2026-09-18)

④ EMR 에 두 답이 나란히 → 로한이 판단 → 턴 N+1
```
- 로한이 앱에 치는 말은 **매번 같은 한 줄**이다. EMR 이 복사 버튼으로 준다(§7.3). 질문 내용을 다시 치지 않는다
- ②와 ③의 순서는 상관없다. 한쪽만 답한 상태로 다음 턴에 가도 된다(§6)

### 2.2 🔒 사실 묶음은 브라우저가 만든다
- 「그날 뭘 먹었나」「최근 병원 언제 갔나」 같은 계산은 **로한북(`index.html`)에 한 번만** 있다(§5.3).
  AI 방이 `app_state` JSON 을 직접 뒤져 같은 계산을 하게 두면 **같은 질문에 답하는 곳이 셋**(로한북·로버트·수연)이 된다.
  v2.11·v3.8 이 두 곳짜리로도 터졌다(원칙 2)
- 사실 묶음은 **질문을 저장할 때 같이 저장**된다(`user` 행의 `facts`). 로버트가 먼저 읽든 수연이 나중에 읽든 **같은 글자**를 받는다
- 사실 묶음은 메인 화면에 띄우지 않는다 — 저장돼 있고, 정확성은 로버트가 검수한다(§7.4)

### 2.3 🔒 격리는 함수가 한다 — 그리고 규칙이 지킨다
- AI 는 `health_messages` 테이블을 **직접 읽지 않는다.** `emr_thread(문제, 자기 provider)` 만 쓴다(§4)
- 이 함수는 **상대 provider 의 행을 돌려주지 않는다.** 걸러주는 게 아니라 조회 조건에 애초에 없다
- ⚠️ **솔직한 한계**: 두 앱의 AI 는 DB 전체를 읽을 권한이 있다. 그래서 중계 모드의 격리는 **구조적 강제가 아니라 규칙**이다.
  각 방의 지침(§9)에 "EMR 은 세 함수로만"을 박아 지킨다. API 모드(§13)에서는 서버가 강제하므로 이 한계가 사라진다
- ⚠️ **배경 지식 비대칭**: 순환기방에는 로버트와의 긴 추적 이력이 있고, 수연에게는 ChatGPT 쪽 기억이 있다.
  두 의견의 차이는 모델 차이 + 각자 아는 것의 차이다. 로한이 "추적해온 맥락을 쓰겠다"고 택한 대가로 받아들인다

---

## 3. 데이터 모델 — `app_state` 밖에 둔다

### 3.1 왜 `app_state` 에 넣지 않나
`app_state` 는 **128KB 단일 JSON + 낙관적 잠금**이다. 대화를 거기 넣으면 AI 방이 답을 쓸 때마다 앱과 충돌하고,
v3.6 의 **단어 96건 유실**과 같은 구조가 된다(그때도 로버트가 MCP 로 쓰고 앱이 덮어썼다). 대화는 **별도 테이블**이다.
- ✅ 새 테이블은 `app_state` 밖이라 `SYNC_COLS` 등록 대상이 **아니다**
- ⚠️ 반대로 `DB` 안에 새 배열을 추가하게 되면 그건 `SYNC_COLS` 등록까지가 한 작업이다(v3.6)

### 3.2 테이블 (전부 `user_id = auth.uid()` RLS)

**`health_problems`** — 문제 목록
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid pk | |
| `user_id` | uuid | RLS 기준 |
| `slug` | text unique | 방에서 부르는 짧은 이름. 예: `순환기` · `오십견` · `칼륨식단` |
| `title` | text | 예: 「순환기 — 박성미 교수 추적」 |
| `status` | text | `active`(현재) / `watch`(경과관찰) / `closed`(종료) |
| `summary` | text | 로한이 쓰는 **경과 요약**. 사실 묶음 맨 앞에 들어간다 |
| `sources` | jsonb | 이 문제에 보낼 기록의 범위(§5.2). 없으면 기본값 |
| `rooms` | jsonb | 어느 방이 답하나 — 예: `{"claude":"로버트(순환기)","openai":"수연"}`. 화면 안내용(§7.3) |
| `source_handoff_id` | bigint null | 이관 원본(`handoffs.id`) |
| `created_at` / `updated_at` | timestamptz | |

**`health_messages`** — 대화 (🔒 **격리 규칙이 테이블 구조에 박혀 있다**)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid pk | |
| `user_id` | uuid | |
| `problem_id` | uuid fk | |
| `turn_no` | int | 1부터. 같은 턴 = 질문 1 + 답 최대 2 |
| `role` | text | `user` / `assistant` |
| `provider` | text null | 질문 행은 **null**(양쪽 공유). 답 행은 `claude` / `openai` |
| `content` | text | |
| `facts` | text null | **질문 행에만.** 그 턴의 사실 묶음 원문 — 두 AI 가 받는 것 |
| `mode` | text null | 답 행만: `relay`(앱의 방이 기록) · `mock`(테스트) · `live`(API, §13) |
| `room` | text null | 답한 방 이름(예: `로버트(순환기)`) — 답 행만 |
| `status` | text | `ok` / `error` / `refused` / `pending` |
| `error` | text null | 실패 사유 |
| `model` / `in_tokens` / `out_tokens` / `cache_read_tokens` / `cost_krw` | | **`live` 전용**(§13). 중계 모드에선 비운다(빈 서랍 금지 — 키를 만들지 않는다가 아니라 컬럼이 null) |
| `created_at` | timestamptz | |
- unique: `(problem_id, turn_no, role, provider)` — **한 턴에 AI 별 답은 하나**다

> 🔒 **provider P 가 볼 수 있는 것 = (질문 행 전부) + (`provider=P AND status='ok'` 답 행).**
> 상대 provider 의 행은 **조회 조건에서부터 빠진다.** 이 조회는 `emr_thread` **하나**다 — 화면·테스트·API 모드 모두 이걸 쓴다(원칙 2).

---

## 4. 🔌 중계 프로토콜 — AI 방이 쓰는 함수 셋

Postgres 함수로 만든다. AI 방은 **이 셋만** 부른다. 🚫 테이블을 직접 `select`/`insert` 하지 않는다.

### 4.1 `emr_pending(provider text)` — 답할 게 있나
- 반환: `[{slug, title, turn_no, asked_at}]` — 질문은 있는데 **그 provider 의 `ok` 답이 없는** 턴들, 오래된 순
- 방에서는 로한이 「EMR 답할 거 있나 봐」 한 마디만 하면 된다 — 문제·턴 번호를 로한이 외울 필요가 없다
- ⚠️ 한 방이 여러 문제를 맡으면 여러 건이 나온다. 방 지침에 따라 **자기 방이 맡은 문제(`rooms`)만** 답한다

### 4.2 `emr_thread(slug text, provider text)` — 내 몫만 읽기
- 반환 (JSON 하나):
  ```
  { problem: {slug, title, status},
    principles: "…§6.1 공통 운영원칙…",          ← 두 AI 동일
    facts: "…가장 최근 질문의 사실 묶음…",        ← 두 AI 동일
    turns: [ {turn_no, question, my_answer|null}, … ],   ← 내 답만. 상대 답 필드는 존재하지 않는다
    to_answer: [turn_no, …] }                      ← 내가 아직 답 안 한 턴
  ```
- 🔒 `turns` 에는 **상대 답이라는 필드 자체가 없다.** "상대 답: null" 같은 자리표시도 두지 않는다 — 있다는 걸 알면 찾으러 간다
- 🔒 `facts` 는 **가장 최근 질문의 것**이다. 과거 턴의 사실은 그 턴 당시 것이라 오래됐다 — 판단은 최신 사실로 한다

### 4.3 `emr_reply(slug text, turn_no int, provider text, content text, room text)` — 답 쓰기
- 그 턴의 질문이 없으면 거부 · 이미 `ok` 답이 있으면 거부(덮어쓰기 🚫 — 고치려면 로한이 EMR 에서 「다시 받기」, §6)
- `status='ok'`, `mode='relay'` 로 저장
- 반환: `{saved:true, turn_no}` — 방은 이걸 보고 로한에게 「EMR 턴 N 기록했다」고 알린다

### 4.4 두 방 모두 직접 쓴다
- ✅ 로버트(Claude 앱 방들)와 수연(ChatGPT) **둘 다 Supabase 에 직접 쓸 수 있다**(로한 확인 2026-09-18)
- 그래서 EMR 에 「답 붙여넣기」 칸은 **만들지 않는다.** 모든 답은 `emr_reply` 한 경로로만 들어온다 — 경로가 둘이면 검사도 둘이 돼야 한다
- 한쪽 앱의 DB 연결이 끊기는 날이 생기면 그때 붙여넣기를 붙인다(지금은 스코프 밖)

---

## 5. 사실 묶음 — "사실은 하나"의 실체

### 5.1 로한북 안의 건강 기록 (실측 2026-09-18)

| 영역 | 저장 위치 | 규모 | 메모 |
|---|---|---|---|
| **식이 — 직접 기록** | `meals[날짜][b·l·d·s] = {p, a}` | 16일 (9/2~) | `b`아침 `l`점심 `d`저녁 `s`간식. 🔒 **`a` = 실제로 먹은 것만 보낸다** |
| **식이 — 구매** | `transactions[]` 의 `food`·`slot` | 37건 (8/22~9/16) | 식당·배달·편의점. `slot` 없는 음식 거래도 있다(v2.14) |
| **체성분** | `health.weights[]` | 88건 | `kg·fatKg·muscle·skMuscle·waterKg·boneKg·protein·bmr·memo` |
| **피검사** | `health.labDates[i]` × `health.labValues[지표명][i]` | 21회차 · 83지표 | 참고범위 `metrics[].low/high/range`. 🔒 **단위·의미는 `metrics[].func` 에 있다**(예: `노력성 폐활량(L) · 예측치 대비 %로 판정한다`) — 별도 단위 필드는 없지만 `func` 를 **반드시 같이 보낸다**. 단위를 코드에서 지어내 붙이지 않는다 |
| **복용약** | `health.labMeds[i]` (아침/저녁) | 회차별 | 최신 회차를 현재 약으로 본다 |
| **의료 기록** | `transactions[]` 중 `cat='의료'` 의 `date·memo` + `health.labVisits[i]`(진료일) | 의료 26건 · 진료일 21 | ⚠️ 의료 **지출**이지 방문이 아니다(약국·물품 포함, 메모 없는 건 多). 🚫 `cat='건강보험'` 은 보험료 납부라 넣지 않는다 |
| **사고·증상** | `health.events[]` | 1건 | `date·name·symptom·treatment·status·memo` |

### 5.2 문제별 범위 — `health_problems.sources`
```json
{ "labs": ["지표명", "…"],   // 보낼 피검사 지표명. [] 이면 피검사 안 보냄
  "body": true,              // 체성분 최근 8건
  "diet_days": 14,           // 식이 최근 N일. 0 이면 안 보냄
  "visits_days": 90,         // 의료 기록 최근 N일. 0 이면 안 보냄
  "events": true }           // 사고·증상
```
- 신규 문제 기본값: `labs:[]` · `body:true` · `diet_days:14` · `visits_days:90` · `events:true`
- 피검사는 최근 **6회차**, 복용약은 `labs` 가 비어 있지 않으면 같이 보낸다
- 🔒 **6회차 안에 값이 하나도 없는 지표는 줄을 만들지 않고**, 끝에 `최근 6회 값 없는 지표 N개 생략: 이름, 이름, …` 한 줄로 알린다.
  실측(「전체」 83지표, 2026-09-18): **34개가 6회 내내 비어 있었다.** 나머지도 「기록 없음」이 대부분이라 사실 묶음 1만 4천 자의 상당 부분이 빈칸이었다. 숨기는 게 아니라 **개수와 이름을 남기고 줄인다**
- ⚠️ 지표엔 별도 id 가 없어 **이름이 곧 키**다(`labValues[m.name]`). 이름을 바꾸면 연결이 끊긴다 → 사실 묶음에 **「연결 끊김: ○○」**

### 5.3 🔒 새 함수 — `index.html` 에 **한 번만** 만든다
| 함수 | 답하는 질문 | 규칙 |
|---|---|---|
| `dietOn(ds)` | 그날 뭘 먹었나 | 🔒 **(날짜, 끼니)당 한 줄.** 직접 기록(`meals[ds][slot].a`)이 있으면 그게 본문이고, 같은 끼니의 음식 구매는 **근거로 붙인다**: `간식 아이스크림, 과자 2봉 [기록 · 구매: 과자 2봉, 아이스크림]`. 기록이 없고 구매만 있으면 `[구매만 · 섭취 미확인]`. 끼니 없는 구매는 `끼니 미지정`. 🚫 **`p`(생활방 제안)는 넣지 않는다** — 먹기로 한 것 ≠ 먹은 것(v2.51) |
| `medVisits(from,to)` | 의료 관련해 무엇이 있었나 | `cat='의료'` 거래(날짜·메모) + `labVisits` 진료일을 날짜순으로. 🚫 금액 없음. ⚠️ **의료 지출은 방문이 아니다** — 약국·물품(예: 여드름패치)도 `의료`다. 항목을 「의료 지출」로 적고 「방문」이라 부르지 않는다 |

> 🔴 **[로버트 오류 정정 2026-09-18]** v2 초안은 `dietOn` 을 "같은 끼니에 **글자가 완전히 같을 때만** 한 줄"로 정했다. 실데이터로 뽑아 보니
> 로한은 한 끼 구매 두 건(과자 2봉 · 아이스크림)을 기록 한 줄(「아이스크림, 과자 2봉」)로 적는다. 글자가 달라 **세 줄**이 나왔고,
> AI 는 **과자 4봉·아이스크림 2개**로 읽는다 — 사실이 부풀었다. 규칙을 **끼니당 한 줄**로 바꾼다. 코덱스는 명세대로 만들었다.
| `emrFacts(problem)` | 이 문제에 대해 AI 가 알아야 할 사실 | §5.2 범위로 위 함수들 + 체성분·피검사·약·사고를 **평문 한 덩어리**로 |

- 🔒 `dietOn` 은 **EMR 전용이 아니다.** 앞으로 데일리·캘린더도 이걸 쓴다. 두 번째 구현을 만들지 않는다
- 🔒 **기록이 없는 날은 「기록 없음」이지 「안 먹음」이 아니다.** 머리에 **기록 시작일**을 적는다
- 🔒 **순서와 형식을 고정한다** — 날짜 오름차순, 항목 순서 고정. 같은 데이터면 같은 글자
- 🚫 넣지 않는 것: 재정·세무·사주·일지·학습 등 **건강 외 데이터 전부**, 다른 문제, 금액

### 5.4 사실 묶음 모양 (형식만)
```
[문제] 순환기 — 박성미 교수 추적 (현재)
[경과 요약]
…summary…
[기록 범위] 식이: 직접 2026-09-02~ · 구매 2026-08-22~ / 기록 없는 날은 '기록 없음'이다
[의료 기록 — 최근 90일] (의료 지출은 방문을 뜻하지 않는다 · 약국·물품 포함)
{날짜} 의료 지출 · {메모|내용 미기록}
{날짜} 진료일(피검사)
[피검사 — 최근 6회]
{지표명} — {func} (참고 {range}): {날짜} {값} · … · {날짜} {값}
최근 6회 값 없는 지표 N개 생략: {이름}, {이름}, …
[현재 복용약] 아침: … / 저녁: …
[체성분 — 최근 8건]
{날짜} 체중 … · 체지방 … · 골격근 …
[식이 — 최근 14일]
{날짜} 저녁 … [기록]
{날짜} 간식 {기록 본문} [기록 · 구매: {구매1}, {구매2}]
{날짜} 저녁 {구매} [구매만 · 섭취 미확인]
{날짜} 점심 (기록 없음)
```
⚠️ **형식만** 보여준다. 수치·날짜는 자리표시다 — 검증은 실데이터로 한다(§12.2).

---

## 6. 턴 흐름과 예외

### 6.1 공통 운영원칙 — `emr_thread.principles` 로 두 AI 에게 똑같이 준다
```
이 글은 로한북 EMR 에서 온 것이다. 너는 로한의 건강 기록을 함께 읽는 상담 파트너다.
- 너는 의사가 아니다. 진단·처방을 하지 않는다. 기록 해석, 경과 정리, 진료 때 물을 질문 준비를 돕는다.
- [사실 묶음]은 로한북에서 뽑은 것이고 사실의 정본이다. 거기 없는 수치를 지어내지 않는다. 모르면 모른다고 한다.
- '기록 없음'은 '하지 않음'이 아니다. 기록이 빈 날을 금식·운동 안 함 등으로 해석하지 않는다.
- 응급 징후(흉통, 호흡곤란, 의식 변화 등)가 언급되면 다른 말보다 먼저 즉시 진료·119를 권한다.
- 같은 질문을 다른 AI 도 독립적으로 받는다. 너는 그 답을 볼 수 없고, 찾아서도 안 된다. 네 판단을 말하라.
- 한국어로, 짧고 구체적으로. 필요하면 "진료 때 물어볼 것"을 따로 정리한다.
```
- 🔒 **두 AI 가 글자 하나까지 같은 원칙을 받는다.** 방마다 원래 성격은 있지만, EMR 답에 적용되는 원칙은 하나다
- 🔒 원칙 문구는 DB(설정 행)에 한 벌만 둔다. 방 지침에 복사해 두지 않는다 — 복사본은 반드시 어긋난다

### 6.2 예외 표 (중계 모드)
| 상황 | 처리 |
|---|---|
| 한쪽만 답함 | 답한 쪽은 보이고, 다른 칸은 **「답 대기 — 수연에게 한 줄 보내기」**(복사 버튼). 기다리지 않고 다음 턴에 가도 된다 |
| 답 없이 다음 턴으로 | 그 AI 의 `emr_thread` 에 두 턴이 `to_answer` 로 뜬다. 방은 **밀린 턴을 한 번에 읽고 최신 턴에 답하되, 밀린 질문도 반영한다**(방 지침 §9). 밀린 턴에 빈 답을 억지로 채우지 않는다 |
| 답이 마음에 안 듦 / 잘못 기록 | EMR 에서 그 칸 **「다시 받기」** → 그 답 행을 `status='error', error='로한이 다시 요청'` 으로 바꾼다(지우지 않는다) → 다시 `pending` 에 뜬다 |
| 방이 엉뚱한 문제·턴에 씀 | `emr_reply` 가 질문 없는 턴은 거부한다. 다른 문제에 쓴 건 「다시 받기」로 되돌린다 |
| AI 가 답을 거절 | 방이 거절 사유를 그대로 `content` 에 쓰고 `status='refused'` — 「이 질문엔 답하지 않았다」로 표시 |
| 새 답 반영 | EMR 이 열려 있고 대기 중인 칸이 있으면 **20초마다** 새 답을 확인한다. 「새로고침」 버튼도 둔다 |
| 사실 묶음 갱신 | 새 질문마다 그 시점의 `emrFacts()` 로 새로 만든다. 과거 턴의 `facts` 는 고치지 않는다(그때 AI 가 본 것의 기록) |

### 6.3 🔒 조용히 버리지 않는다
대기·거절·다시 받기·연결 끊김은 **전부 화면에 보여야 한다.** 칸이 그냥 비어 있으면 로한은 AI 가 할 말이 없었다고 읽는다(v2.14 교훈).

---

## 7. 화면 — EMR 페이지

### 7.1 진입
- 사이드 메뉴 **건강 묶음의 맨 첫 항목**: `🩺 EMR` → `🩹 체성분·사고` · `🧪 피검사` · `🛡️ 보험`
- `VIEW_RENDER` 에 `emr: renderEmr` 등록. 뷰 id `v-emr`
- ⚠️ 모바일 가로 스크롤 네비 항목이 하나 는다 — t14 통과 필수

### 7.2 PC (≥900px)
```
┌─ 문제 목록 ─────┐┌─ 순환기 — 박성미 교수 추적 ──── [현재 ▾] [요약 편집] [기록 범위] ─┐
│ ● 현재           ││ 경과 요약 (앞 3줄 · 펼치기)     [📋 AI 에게 보낸 사실 보기]      │
│   순환기  ⏳1     │├──────────────────────────────────────────────────────────────────┤
│   오십견 재활      ││ 턴 1  로한: …                                                    │
│ ◐ 경과관찰        ││   ┌ 🤖 로버트 · 로버트(순환기) ──┐┌ 💬 수연 ────────────────────┐ │
│   칼륨 식단        ││   │ …                            ││ ⏳ 답 대기                  │ │
│ ○ 종료 (접힘)     ││   │                              ││ [📋 수연에게 보낼 한 줄]     │ │
│                  ││   └──────── AI 의견 · 진단 아님 ─┘└─────────────────────────────┘ │
│ [+ 신규 문제]     │├──────────────────────────────────────────────────────────────────┤
└──────────────────┘│ [ 질문 …                                              ] [남기기] │
                    └──────────────────────────────────────────────────────────────────┘
```
- 문제 목록의 `⏳1` = 답 대기 중인 칸 수

### 7.3 📋 방에 보낼 한 줄 — 귀찮음을 줄이는 장치
- 질문을 남기면 두 칸에 **복사 버튼**이 뜬다. 누르면 클립보드에:
  `EMR 답할 거 있나 봐` (+ 방이 여러 문제를 맡으면 `— 순환기 턴 3`)
- 칸 제목에 **어느 방으로 가야 하는지**(`rooms`)를 적는다 — 「→ 로버트(순환기)」
- 로한이 하는 일은 **질문 쓰기 → 복사 → 앱 열어 붙여넣기 → 돌아오기**가 전부다. 질문을 다시 치지 않는다

### 7.4 🚫 「AI 에게 보낸 사실 보기」는 메인 화면에 두지 않는다 (로한 결정 2026-09-18)
- 로한: *"어차피 수파베이스에 기록돼 있는데 그걸 메인 페이지에 보여줄 필요가 있나."* — 맞다
- 사실 묶음은 질문마다 `health_messages.facts` 에 **저장된다.** 필요하면 방에 「그때 사실 묶음 보여줘」라고 물으면 된다
- 사실 묶음의 **정확성 검증은 화면이 아니라 로버트가 한다** — 실데이터로 뽑은 결과를 로버트가 원본 기록과 대조한다(§15 0단계)
- EMR 메인 = **문제 목록 + 대화.** 그 외 기록 원문은 기존 메뉴(체성분·피검사)가 이미 보여준다
- 「기록 범위」(§5.2 `sources` 편집)는 문제 헤더의 작은 버튼으로 남긴다 — 무엇을 보낼지는 로한이 정해야 한다
- ⚠️ 0단계에 코덱스가 만든 사실 미리보기 화면은 **임시 검수용**이다. 2단계에서 대화 화면으로 바뀔 때 걷어낸다

### 7.5 모바일 (<900px) — 🔒 로한의 주 사용처다. 이걸 먼저 맞춘다
- 1단계: 문제 목록 → 2단계: 문제 화면(「← 목록」, 요약·사실 보기는 접힘)
- 턴은 **세로로 쌓는다**: 로한 질문 → 로버트 카드 → 수연 카드. 🚫 두 칸 나란히
- 입력창은 하단 고정. ⚠️ 가상 키보드가 올라올 때 가려지지 않는지 실기기 확인
- 🔒 **복사 버튼이 폰에서 동작해야 한다** — 모바일 크롬 클립보드. 안 되면 글을 선택 상태로 띄운다
- 🔒 **공용 클래스를 재사용하면 그 모바일 규칙까지 물려받는다**(v3.7 `.calwrap` 사고). 새 클래스는 base 규칙부터

### 7.6 공통
- 모든 AI 답변 하단에 작게 **「AI 의견 · 진단 아님」** + 답한 방 이름
- 두 칸 순서 **고정**(로버트 왼쪽/위, 수연 오른쪽/아래)
- 답변은 텍스트로 렌더(`mdEsc`) — 🚫 AI 가 쓴 글을 `innerHTML` 로 그대로 넣지 않는다(XSS). **방이 DB 에 쓰는 글도 외부 입력이다**

---

## 8. 문제 생애주기와 이관

- **신규**: 「+ 신규 문제」 → 제목·slug·답할 방 + 첫 질문 → 생성(기본 `sources`) 후 턴 1
- **기존**: 목록에서 선택 → 대화 로드 → 이어서 질문
- **상태 변경**: 현재 ↔ 경과관찰 ↔ 종료. 종료된 문제는 `emr_pending` 에 안 뜬다. 재개 가능
- **summary·sources 편집**: 로한만 한다. 다음 질문부터 두 AI 가 똑같이 새 사실을 받는다
- 🚫 문제 삭제는 v2 에 없다

### 8.1 초기 이관 (1회성)
| handoffs | → 문제(slug) | status | 답할 방 | 비고 |
|---|---|---|---|---|
| id 5 「2026-09-03 박성미 진료 결과 및 12월 계획」 | `순환기` | `active` | 로버트(순환기) · 수연 | `labs` 는 로한과 확인 |
| id 4 「오른쪽 어깨 오십견 재활 경과」 | `오십견` | `active` | 로버트(재활) · 수연 | ⚠️ Claude 쪽 방 이름은 로한 확인 |
| id 6 「식단 설계 제약조건 — 칼륨 제한」 | `칼륨식단` | `watch` | 로버트(생활) · 수연 | ⚠️ 순환기에서 파생 — 합칠지는 로한이 정한다 |
- `summary` = 해당 handoff 의 `body` 원문. `source_handoff_id` 기록
- 🔒 `handoffs` 원본은 **건드리지 않는다**. 복사만 한다

---

## 9. 각 방에 붙일 지침 (로한이 붙여넣는다)

Claude 앱의 해당 방 지침과 ChatGPT(수연)의 지침에 **아래 한 덩어리만** 추가한다. 원칙 본문은 여기 복사하지 않는다(§6.1 — DB 에서 읽는다).
```
[EMR 중계 규칙]
로한이 "EMR 답할 거 있나 봐"라고 하면:
1. emr_pending('{내 provider}') 로 답할 턴을 찾는다. 내 방이 맡은 문제만 답한다.
2. emr_thread('{slug}','{내 provider}') 로 읽는다. 여기 담긴 principles 를 따른다.
3. emr_reply 로 답을 기록하고, 로한에게 "EMR {문제} 턴 N 기록했다" 한 줄만 말한다.
🚫 health_messages 테이블을 직접 조회하지 않는다. 다른 AI 의 답을 찾거나 읽지 않는다.
🚫 EMR 질문에 대한 답을 채팅창에만 쓰고 끝내지 않는다 — 반드시 emr_reply 로 기록한다.
밀린 턴이 여럿이면 전부 읽고, 최신 턴에 한 번 답하되 밀린 질문도 반영한다.
```
- `{내 provider}`: Claude 방은 `claude`, 수연은 `openai`

---

## 10. 개인정보·보안
- 새 테이블 전부 RLS(`user_id = auth.uid()`). EMR 페이지는 로한의 JWT 로 읽고 쓴다
- 🔒 세 함수는 **문제의 `user_id` 로 범위를 잡는다** — AI 방은 MCP 로 로그인 세션 없이 부르기 때문에 `auth.uid()` 에 기대지 않는다
- 외부로 나가는 건 **문제별 `sources` 범위의 건강 기록뿐**이다. 건강 외 데이터·금액은 사실 묶음에 없다
- ⚠️ 중계 모드의 격리는 규칙이다(§2.3). 방 지침(§9)이 그 규칙의 전부다

---

## 11. 로한이 해야 하는 것
1. **§9 지침을 각 방에 붙여넣기** — Claude 앱의 해당 방들, ChatGPT 수연 (1단계 확인 전에)
2. 이관 때 문제별 답할 방·`sources`(특히 순환기 `labs`) 확인
- 🚫 API 키·결제는 **지금 필요 없다**(§13)

---

## 12. 완료 조건 (코덱스 → 로한 인계 전 전부 ✓)

### 12.1 격리·동일 사실 (가장 중요 — 하나라도 실패하면 인계 불가)
- [ ] `emr_thread(slug,'claude')` 결과에 `openai` 답의 글자가 **한 글자도 없다**(반대도 동일) — SQL 테스트
- [ ] `turns` 에 상대 답 **필드 자체가 없다**
- [ ] 같은 턴에 두 provider 가 받는 `facts` 가 **바이트 단위로 같다**
- [ ] 화면·테스트가 스레드를 **`emr_thread` 로만** 조회한다(두 번째 조회 로직 없음)

### 12.2 사실 묶음
- [ ] `dietOn` — `a` 는 들어가고 **`p` 는 안 들어간다**. 같은 끼니 같은 글자는 한 줄. 출처 표시
- [ ] `medVisits` — `의료` 는 들어가고 **`건강보험` 은 안 들어간다**. 금액 없음
- [ ] 기록 없는 날은 「기록 없음」, 머리에 기록 시작일
- [ ] 없는 지표명은 「연결 끊김」
- [ ] 같은 데이터로 두 번 만들면 **글자가 완전히 같다**
- [ ] 🔒 **실데이터로 확인** — 로한 계정의 실제 기록으로 사실 묶음을 뽑아 로한이 읽고 맞다고 한다(v2.6)

### 12.3 중계 흐름·예외
- [ ] `emr_pending` — 답 없는 턴만, 종료된 문제 제외, 오래된 순
- [ ] `emr_reply` — 질문 없는 턴 거부 · 이미 `ok` 인 턴 거부
- [ ] 한쪽만 답 / 답 없이 다음 턴 / 「다시 받기」(행을 지우지 않는다) / 거절 / 20초 확인
- [ ] 🔒 **실제 방으로 한 번 돌려본다** — 로한이 순환기에 질문 → 순환기방이 기록 → 수연이 기록 → 두 칸 채워짐

### 12.4 화면
- [ ] `🩺 EMR` 이 건강 묶음 **맨 앞**
- [ ] PC 1440 / 모바일 400 스크린샷 — 목록·턴·대기 칸·복사 버튼·사실 보기
- [ ] 모바일 크롬에서 복사 버튼 동작
- [ ] AI 답에 `<script>` 를 넣어도 실행되지 않는다(XSS)
- [ ] t14 통과

### 12.5 회귀
- [ ] `bash tests/run.sh all` **25개 + 신규 스위트 전부 통과**(실행법 STATUS §21)
- [ ] 신규 `t43.js` — Supabase 는 기존 스위트처럼 목. 답 도착은 목 데이터로 흉내낸다(`mode='mock'`)

---

## 13. ⏸ 나중에 — API 모드 (예산이 생기면)

중계 모드의 ②③(방에 한 줄 붙여넣기)을 **서버가 자동으로** 하는 것이 API 모드다. **테이블·함수·화면은 그대로 쓴다.**
- Supabase Edge Function 이 질문 저장 직후 `emr_thread` 로 각 provider 몫을 읽어 두 API 를 병렬 호출 → `emr_reply` 와 같은 경로로 기록(`mode='live'`)
- 🔒 이때는 서버가 상대 답을 **아예 보내지 않으므로** 격리가 규칙이 아니라 구조가 된다(§2.3 한계 해소)
- 🔒 API 키는 Edge Function secrets 에만 — 정적 `index.html` 에 두면 누구나 읽는다
- 붙일 때 기준(로한 결정 2026-09-18): **월 3만원 상한** · **중간급 모델 둘**(Claude `claude-sonnet-5` = 입력 $2·출력 $10 / 100만 토큰, 2026-06 공식가) · 80% 경고 · 100% 호출 전 차단 · 비용은 실측 `usage` 누적(`mode='live'` 행만)
- ⚠️ API 쪽 AI 는 **방의 기억이 없다.** 순환기방의 추적 이력 대신 `summary` 가 그 역할을 해야 한다 — 전환 전에 `summary` 를 충실히 채워 둔다
- 이 절은 **설계만** 남긴다. 구현은 로한이 결정할 때 로버트가 별도 명세로 낸다

---

## 14. 코덱스 작업 규칙
- 🔒 **추측 금지** — 모르면 「불확실」로 표시하고 로한에게 묻는다
- 🔒 **같은 질문에 답하는 함수는 하나** — `dietOn`·`medVisits`·`emr_thread` 의 두 번째 구현을 두지 않는다
- 🔒 **실데이터에서 확인한다.** 픽스처 통과는 증명이 아니다(v2.6)
- 🔒 **빈 서랍 금지** — 없으면 키를 만들지 않는다(`undefined` 대신 `delete`)
- `index.html` 은 80만 바이트 단일 파일이다. EMR 코드는 **한 구역**(`/* ══════ 🩺 EMR ══════ */`)에 모은다. 단 `dietOn`·`medVisits` 는 공용이라 `🍚 식사 기록` 구역 옆에 둔다
- DB 변경(테이블·함수)은 **마이그레이션 SQL 파일**로 레포에 남긴다 — 누가 적용하든(§16) 무엇을 적용했는지 남아야 한다
- 버전·CHANGELOG·STATUS 갱신은 레포 관례. 버전 번호는 로한이 정한다
- 인계할 때 **커밋 Summary / Description** 을 같이 낸다

---

## 15. 🪜 진행 순서 — 한 번에 다 만들지 않는다
**각 단계가 로한 눈으로 확인된 뒤에 다음으로 간다.** 전부 추가 비용 0원이다.

| 단계 | 만드는 것 | 로한이 확인하는 것 |
|---|---|---|
| **0. 사실 묶음** *(1차 완료 · 수정 4건 대기 — §15.1)* | `dietOn`·`medVisits`·`emrFacts` + EMR 뼈대(메뉴 맨 앞) + 임시 검수 화면. 문제는 임시 「전체」 하나 | 🔒 **검증은 로버트가 한다** — 실데이터 결과(`tests/.out/emr-real-facts.txt`)를 원본과 대조. 로한은 로버트가 올린 의문점만 답한다 |
| **1. 테이블 + 중계 함수** | §3 테이블 · §4 함수 셋 · 질문 남기기 · 두 칸 표시(대기/답) | 순환기에 질문 → **순환기방·수연이 각자 기록** → 두 칸 채워짐. 방에서 「상대 답이 보이냐」 물어 **안 보인다** |
| **2. 화면** | §7 PC·모바일 · 문제 목록 · 이관 3건 · 복사 버튼 · 기록 범위 편집 | 폰에서 실제로 한 문제를 3턴 돌린다 |
| **3. 예외** | §6.2 전부 + 완료조건 §12 전체 | 「다시 받기」 · 한쪽만 답하고 다음 턴 |

- ⚠️ **0단계와 1단계 확인이 이 설계의 핵심 검증이다.** 사실이 틀리면 두 AI 가 같이 틀리고, 격리가 깨지면 두 AI 를 쓰는 이유가 없다
- 1단계 전에 §16-1(DB 적용 주체)을 정한다

---

## 15.1 0단계 1차 검수 결과 (로버트, 2026-09-18 · 실데이터 `emr-real-facts.txt` 184줄)

✅ **잘 된 것**: 메뉴 위치·`VIEW_RENDER` 등록 · `p` 제외 · 금액 제외 · `건강보험` 제외 · 기록 시작일 명시 ·
출력 결정성(정렬 고정) · `textContent` 렌더(XSS 안전) · 진료일 빈 값 = 검사일 규칙 반영 · 구매는 섭취 확정이 아님을 명시(명세보다 한 걸음 더 나갔다 — 좋다) ·
러너가 도구 누락 시 코드 2로 끝나게 한 것(미실행을 통과로 보고하지 않는다)

🔧 **코덱스가 고칠 것**
1. **식이 이중 계산** — `dietOn` 을 §5.3 새 규칙(끼니당 한 줄, 구매는 근거)으로. 실례: 9/11·9/12·9/13·9/14 간식이 2~3줄로 갈라져
   과자·아이스크림이 두 배로 읽힌다. ⚠️ **원인은 로버트의 명세 규칙**이었다
2. **「병원 방문」 → 「의료 기록」** — 의료 지출 17건 중 **9건이 메모가 없고** 「여드름패치」 같은 물품도 있다. 방문이라 부르면 AI 가
   9/16 에 병원에 갔다고 읽는다. 머리말·항목 이름을 §5.4 대로
3. **빈 지표 생략** — 83개 중 **34개가 6회 내내 비었다.** §5.2 대로 줄이고 이름·개수는 남긴다
4. **피검사 줄에 `func` 를 넣는다**(§5.4 형식). 실례: `FEV1 (참고 예측치 대비 %): … 1.91` 은 AI 가 「예측치의 1.91%」로 읽는다.
   `func` 에 이미 `(L) · 예측치 대비 %로 판정한다` 가 적혀 있다 — **데이터는 맞고 사실 묶음이 그걸 빼먹었다.** ⚠️ 원인은 로버트 명세(§5.1 이 `func` 를 언급하지 않았다)
5. (사소) 날짜 검사 `emrDateOK` 가 기존 `isDateStr` 와 겹친다. `emrDateOK` 가 더 엄격하다(없는 날짜 거부) —
   `isDateStr` 를 그 수준으로 올리고 하나만 쓰는 게 원칙 2 에 맞다. 다른 화면에 영향이 있을 수 있으니 **이번엔 보류**하고 기록만 남긴다
6. 임시 검수 화면은 §7.4 결정에 따라 2단계에서 걷어낸다(지금은 그대로 둔다)

✅ **로한 확인 완료 — 폐기능 단위**: 검사지(기계 출력) 그대로 적은 값이고 표준(FVC·FEV1 = L, PEF·FEF25-75% = L/s)과 맞는다.
**데이터 수정 없음.** 단위는 이미 `func` 에 있었고, 사실 묶음이 `func` 를 안 보낸 게 문제였다 → 위 4번

---

## 16. 열린 질문
1. **DB 변경(테이블·함수)을 누가 적용하나** — 코덱스가 Supabase CLI 로 / 로버트가 레포의 마이그레이션 SQL 을 읽어 적용 · 1단계 전까지
2. 오십견·칼륨식단의 Claude 쪽 답할 방 이름 (§8.1)
3. 칼륨식단을 순환기에 합칠지 (§8.1)
4. 순환기 문제의 `labs` 지표 (§8.1)
5. 식이 기본 14일 · 의료 기록 90일이 적당한지 (§5.2)

### 해결됨
- ~~수연이 Supabase 에 직접 쓸 수 있나~~ → **쓸 수 있다**(로한 2026-09-18). 붙여넣기 기능은 만들지 않는다(§4.4)
