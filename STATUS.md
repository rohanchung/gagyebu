# 로한북 현재 상태 — 2026-09-18

## 🗝️ v4.17 — 예산 현실화 (로버트, 9/23)

로한: *"예산 영역을 현실화한다. 그 방은 로버트(생활)이 담당하고 그도 조정 권한을 갖고 있다.
상단에 월을 바꿀 수 있게, 로버트가 한마디 남길 공간을, 한 줄로 쫑 말고 2·3열 컴팩트하게."*

### 🔎 실측이 문제를 바꿔놓았다
지출 분류 **32개** 중 예산이 잡힌 건 **11개뿐**이다. 9월 기준:

| | 금액 |
|---|---|
| 예산 합계 | 1,373,000 |
| 예산 **안** 지출 | 1,377,854 |
| 예산 **밖** 지출 | **1,465,905** (예산 합계의 107%) |

이자비용 502,944 · 의료 308,400 · 비즈니스 212,890 · 국민연금 140,220 · 건강보험 122,020 — 매달 나가는데 예산이 없다.
→ **예산을 안 지트 게 아니라 예산이 현실을 안 덩고 있었다.**

### 🔴 옛 화면은 그걸 볼 수 없게 독해했다
‘예산 합계 − **총지출**’ 을 ‘예산 잔여’ 라고 불렀다. 예산 밖 지출까지 뻔 값이라
**예산을 전부 지켜도 ‘초과’로 뜨다.** 숫자는 맞는데 이름이 거짓이었다 → 안/밖을 갈랏다(t50-E4).

### 만든 것
- 상단 ◀ ▶ **월 전환** + 통계 6칸. 월은 재정 탭 전체가 공유한다(`ui.month`)
- 🔒 **예산값은 기준값 하나**다. 월을 바꾸면 '지금 예산을 그 달 실적에 대본 것' —
  새 예산안을 지난 달로 검산하라고 그렇게 둔다. 화면에 명시한다
  ⚠️ 달마다 다른 예산이 필요해지면 `budgetsM[YYYY-MM]` 을 덧붙이면 된다 — 지금은 필요가 없다
- 💬 **로버트(생활) 한마디** — `budgetNote["YYYY-MM"]`, 앱은 읽기만 한다
- 분류를 **예산 안 / 예산 밖 / 조용한 것**(접기) 으로 갈랏다. 예산 밖 칸이 현실화의 작업대다
- 정렬은 **초과가 큰 순** · 예산칸에 0 을 넣으면 키를 지운다(빈 서랍 금지)
- ⚠️ **[결함]** 모바일 CSS 규칙을 기본 정의 **앞**에 둔 탓에 400px 에서 3열이 나와 입력칸이 잘렸다.
  미디어쿼리는 구체성을 안 올린다 — 같으면 뒤가 이긴다. 기본 정의 바로 뒤로 옮겼고 t50-C2·C3 으로 잠갔다
- t50 신설 **34건** — 실데이터 32분류를 그대로 넣고 돌린다

## 📅 v4.16 — 캘린더 칸에서 한 달치 수행여부를 읽는다 (로버트, 9/21)

로한: *"아주 작은 글씨라도 상관없으니 수행여부를 다 확인할 수 있었으면. 과거에 뭔 했는지를 일일이 클릭해서 봐야 하는 게 불편해."*
- 칸마다 그날 과제를 한 줄씩 — **✓ 했다 / ✗ 지난 날인데 안 했다 / · 앞으로 할 것**
- 🔒 **✗ 는 지난 날에만.** 오늘 오전의 미완을 실패로 칠하면 화면이 거짓말을 한다(처음엔 '완료 아니면 ✗' 로 짰다가 고쳤다)
- 🔒 칸이 넘치면 **+N 건 더** — 잘라내면 거짓말이다(v3.7 원칙)
- 미완이 위 — 오른쪽 패널과 **같은 정렬**(두 곳이 다르면 같은 날이 다르게 보인다)
- ⚠️ 모바일은 표시만(칸 폭 ~50px) — 잘린 제목보다 ✓✗ 가 낛다
- 오른쪽 패널은 손대지 않았다 — 거기는 체크·뽀모 자리, 칸은 훑는 자리다
- t42 검사 6건 추가(**344건**)

⚠️ 곁다리 — 9/22 에 `[아침] 예상어휘 p166` 이 **두 번** 들어 있다(u041·u043, id 는 다르다). 학습방 확인 필요.

## 🔴 v4.15 — 보기에 정답이 없는 문항 · 겹친 번호(id) (로버트, 9/21)

로한 신고: *"보기가 답이 없는게 2개나 나오더라."* 「くるま」 → 특히·언젠가·특별히·잘하다.
- 🔎 **원인**: `words` 에 id 가 겹친 레코드 **17쌍**(w0508~w0524) · `units` 2쌍(u042·u043).
  부사 묶음과 v20 신규단어 묶음이 같은 번호대를 썼다. 문항은 `とくべつに` 로 만들고 화면은 `くるま` 를 그렸다
- 🔴 **증상보다 저장이 더 위험했다.** v4.13 병합·흔수는 id 로 묶는다 → 겹치면 맵에 마지막 것만 남고,
  앞엣것에 쌓은 채점이 "안 바꿈 것"으로 판정돼 **서버 것으로 되돌린다**(t49-C1 에서 재현)
- 🔒 **로드·병합·흔수 세 길목**에서 정규화한다. 앞엣것이 번호를 지키고 뒤엣것은 `w0524#2`
- 🔒 새 번호는 **결정적**이어야 한다. 처음 `uid()` 를 썼다가 기준본·서버본·내 것이 각각 다른 번호를 받아
  병합이 복사본을 늘리는 걸 테스트에서 보고 바꿈(t49-C4)
- 🚫 레코드를 지우지 않는다 — 번호가 겹치는 건 번호가 틀렸다는 뜻이지 내용이 중복이라는 뜻이 아니다
- `stWordById` 가 forEach 로 돌아 **마지막 것**을 돌려주던 것도 첫 번째로 바꿈
- 몇 건을 갈랐는지 화면·활동로그에 내보낸다 — 조용히 고치면 올리는 쪽이 계속 겹쳐 쓴다
- t49 신설 19건

## 🔤 v4.14 — 동사 활용 드릴 · 예문 다목적 재사용 (로버트, 9/21)

학습방 v19·v20 처리. **v19의 미구현 목록은 낡았다** — `gadv`·`gpart` 는 v4.13 에서 이미 끍났다(배포 전이라 학습방이 몰랐다).

### 🔤 동사 활용 드릴 `vconj` (v19)
로한: *"て형은 안다. 아는지 모르는지 어떻게 아느냐는 거지. 드릴로 만들던가 해야지."*
- 🔒 **활용형을 저장하지 않는다.** `words[].vgroup`(1·2·3) 하나만 받고 앱이 규칙으로 만든다 — 107개 × 4형 = 428개를 손으로 넣으면 오타가 난다
- 🔒 **오답은 틀린 그룹 규칙을 적용한 형태**다(買う → かいて·かんで·かして). 랜덤 오답이면 난도가 가짜다
- 불규칙 2종을 박았다: 경어 5동사는 ます형만 불규칙(いらっしゃいます) · 行く 는 いって(데이터에 아직 없지만 들어오는 날 조용히 틀리면 안 된다)
- 채점 후 **네 형을 다 보여준다** — 하나만 보여주면 규칙이 아니라 답을 외운다. `vnote` 는 경고로 뜨고, 그룹은 **문제에서 안 보여준다**(그룹 판별이 절반이다)
- ⚠️ **검산에서 3건이 걸렸다** — 訳(わけ)·忘れ物·割合 에 `vgroup:1` 이 달려 있다. 명사다. 그대로 두면 「わけって」가 출제된다
  → 사전형 어미로 **검산**해 빼고(1그룹=う단·2그룹=る·3그룹=する), 조용히 빼지 않고 **화면에 이름을 낸다**. 학습방이 고쳐야 한다
- 현재 출제 가능 **104개**(태깅 107 − 명사 3)

### 📖 `sents` 는 공용 문항 풀이다 (v20)
로한: *"조사 부분에 나온 문장이라고 거기서만 쓰지 말고 접속사나 여러 문법 부분에서도 쓸 수 있게 만들어놓으라고."*
- 출처(`src`)로 거르지 않는다 — 문장이 **그 종류의 슬롯을 가졌는가**만 본다. 조사 예문 안의 접속사도 접속사 드릴에 나온다
  - ⚠️ 이건 v4.13 부터 이미 그러였다(풀 필터가 슬롯 `kind` 만 봤다). 바뀜 것은 아래다
- 🔒 **슬롯을 전부 센다** — 이전에는 한 문장에서 **첫 슬롯 하나만** 출제돼 나머지가 죽어 있었다. 이제 슬롯 3개면 문항 3개다
- 🔒 **라운드로빈** — 슬롯이 많은 문장이 세트 앞을 다 먹지 않는다. 한 바퀴 돌고 두 번째 슬롯으로 간다
- 화면 숫자가 **문장 수가 아니라 문항 수**로 바뀜다 — "61문항"이라 적어놓고 20문항만 나오면 화면이 거짓말을 한 것이다

### 추석 연휴(9/24~9/26) 대비
학습방 v20 이 미구현 3종 때문에 연휴 드릴이 단어 4모드로 줌어든다고 우선순위 상향을 요청했다.
**3종 전부 끝났다** — 조사·부사(v4.13) + 활용(v4.14). 배포만 하면 연휴에 돌릴 수 있다.

- t48 신설 **28건**(활용 규칙 14동사 × 4형 · 검산 · 오답 생성 · v20 슬롯 확장)

## 🔴 v4.13 — 학습 데이터 유실 P0 · 조사·부사 드릴 (로버트, 9/19)

**학습방 v18: 통산 4회 유실**(words 96·96, sents 19·10). 로한: *"학습방에서 올린 데이터 지우지 마라."*
- 🔎 **원인(실측 · pg_stat_statements)**: 방들이 `jsonb_set` 으로 쓰면서 **updated_at 을 안 바꿨다** → 앱의 잠금이 통과 → 열린 앱이 옛 데이터로 통째로 덮음. 흡수 장치는 충돌이 감지돼야 도는데 감지가 안 됐다
  - ⚠️ 학습방 추정("words 에만 적용")은 틀렸다 — 앱은 둘을 똑같이 다뤘다. words 는 운이었다
- 🔒 ① **DB 트리거 `app_state_bump`** — data 가 바뀌면 DB 가 updated_at 을 올린다(`db/20260919_app_state_bump.sql`)
- 🔒 ② **3자 병합** — 기준본 대비 필드마다 '내가 바꿨으면 내 것, 아니면 서버 것'. 지운 쪽을 따르고 새로 생긴 건 살린다. **앱 데이터 전체**에 적용(생활방 meals.p 도 같은 구조였다)
  - 병합 후 저장도 잠금 · 확인창 없어짐(병합이 깨질 때만) · 같은 칸 충돌 시 서버본 스냅샷
  - v18 요구 3("studyDB 는 있는 배열을 건드리지 마라")은 **원래 지켜지고 있었다**(`S.x=S.x\|\|[]`) — t46-I1 로 잠갔다
- ⚠️ **r011~r020 은 어느 스냅샷에도 없다 → 학습방 재업로드 필요.** 이제 트리거가 있어 올리면 남는다
- 📖 **조사 채우기(gpart)**: 수동 슬롯 우선 + 보수 규칙 자동 후보(실측 오탐 23종 차단) · は·が·も, に·へ 는 서로 오답 제외
- 📖 **부사 채우기(gadv)**: 부사는 죽어 있던 게 아니라 접속사에 **섞여** 나가고 있었다 → 분리
- ⏱ **학습방 v18 뽀모 요구와 다르게 간 것**: "블록을 쪼개지 말고 기존 블록에 tag2 만" → 🚫 **안 따랐다.** 9/18 로 치면 10시간 work 블록 전체가 work+study 가 되어
  학습 시간이 **600분**으로 잡힌다(v4.12 의 칸 단위 계산에서도 뽀모 없는 칸 20개가 30분씩). 걸친 칸만 쪼개는 게 맞다 — 제목·코드는 보존된다
- ⏱ "뽀모 plan:25 고정" → 사실 아님. v3.7 부터 프리셋+직접입력. 로한 현재 설정 **10/3**(9/18 한 번은 25로 돌렸다)
- t46(병합 34) · t47(문법 23 · 실제 예문 39개) 신설 · t20 B·C 갱신

## ⏱ v4.12 — 뽀모가 타임로그에 남는다 · EMR 턴 접기 (로버트, 9/19)

- 로한: *"미리 로그를 채워놓으면 반영이 안 된다. 서브 활동으로 넣어주면 된다."* 실측: 9/18 10~20시 work 블록 안의 19:17 뽀모 25분이 로그에 없었다
- 🔒 **걸친 칸만** 바꾼다: 채워진 칸 → `tag2:study` · 빈 칸 → study 블록 · 이미 서브가 있는 칸 → 그대로
- 🔴 **학습 시간 세는 법 변경**(`stStudyMins` 한 곳): 30분 칸마다 **뽀모 있으면 뽀모 분, 없으면 timelog study 30분**. 이전의 '그냥 더하기'면 뽀모가 timelog 에 찍는 순간 25분이 85분이 된다. 표시와 세기가 분리돼 칸을 고쳐도 두 번 안 센다
- 옛 뽀모 1건 접속 시 소급(`rec.tl` 로 한 번만)
- EMR: 턴마다 접힘(기본) · 제목줄에 질문 앞부분 + 두 AI 상태 · **방금 남긴 턴만 펼침**(복사해야 하므로) · 펼침은 다시 그려도 유지
- t45 신설 26건(KST 고정) · t44 54건 · t42 W7·X22 갱신

### 🔎 학습방 핸드오프(id=8 v17) 검토 — 조사 자동 슬롯화
- 요청: 예문 속 조사를 전부 자동 출제. **실측(예문 39개)**: 단순 규칙("어절이 조사로 끝나면")은 후보 120개 중 **39개가 틀린 문항** — この·その·いつも·とても·でも·それから·だから·では·やっと·姉妹かも…
- 보수 규칙(조사 앞 말에 한자·가타카나가 있거나 알려진 명사/대명사일 때만, か·の·で 로 끝나는 앞말 제외) → 81개, 오탐 1개(そこで — 목록 보정으로 해결)
- ⚠️ **정답이 둘인 문항**: は↔が↔も, に↔へ 는 서로 바꿔도 맞는 경우가 많다 → 오답 풀에서 서로 빼야 한다(v3.4 동음이의어와 같은 구조)
- 조사는 어절 **끝**에 붙어 있어 현재 가리기(어절 시작)와 다른 분기가 필요하다
- r001~r010 에 이미 수동 `particle` 슬롯이 있는데 **지금 앱은 conj·adv 만 출제한다** — 수동분도 아직 안 나간다
- D1(문장 만들기 4어절·★)은 v3.9 에서 이미 가동 중. D2 는 D1 이후
- → **로한 승인 대기**: 보수 규칙으로 구현할지

## ⚠️ v4.11 — 방이 쓴 답이 안 보였다 (로버트, 9/18)

- 로한 신고: "로버트가 EMR 에 기록했다는데 표시가 안 된다"
- 실측: 재활의학과 방이 **`emr_reply` 를 안 쓰고 테이블에 직접 insert** — 질문 턴 1 인데 답을 **턴 2**, `mode` 없이. 화면은 짝 없는 답을 조용히 버렸다
- 🔒 **규칙은 어겨진다 → 테이블이 막는다.** 트리거 `emr_guard`: 질문 없는 턴 · `mode` 없는 답 · 문제 주인과 다른 `user_id` 거부(롤백 검증 7건)
- 🔒 화면은 짝 없는 답을 **경고로 드러낸다**. 데이터 보정: 그 답 → 턴 1
- ⚠️ 방이 §9 지침대로 움직이지 않았다는 뜻이다 — 지침을 **방 지침(프로젝트 지침)** 에 붙였는지, 대화창에만 붙였는지 로한 확인 필요

## 🩺 EMR 중계 상담 가동 — v4.1 (로버트, 9/18)

로한: *"싹다 너가 해."* (코덱스 토큰 소진) → 1~3단계를 로버트가 구현·적용했다. 정본 `health-design.md`.
- **DB**: `health_settings`·`health_problems`·`health_messages` + 함수 `emr_pending`·`emr_thread`·`emr_reply`. 파일 `db/20260918_emr_relay.sql`
  - 적용 전 **실제 DB 에서 16개 시나리오를 돌리고 전부 롤백**해 검증(자기 답만 보인다·상대 답 필드 없음·중복 답 거부·질문 없는 턴 거부·다시 받기 후 재대기·거절은 답함·종료 제외)
  - 보안 점검(advisor): 새 객체 경고 **0건**
  - 초기 이관 3건: 순환기(방 `순환기`) · 오십견(`재활의학과`) · 칼륨식단(`로버트(생활)`, 경과관찰). 방 이름은 `handoffs.room` 그대로
- **화면**: 문제 목록(현재·경과관찰·종료) · 턴별 로한 질문 + 로버트/수연 칸 · 📋 방에 보낼 한 줄 · 다시 받기 · 요약 편집 · 기록 범위 · 신규 문제 · 모바일 2단계
- 🔧 피검사 기본 범위 = **기록 있는 지표 전부**(기본이 '없음'이면 순환기 첫 질문에 검사값이 안 간다)
- t44 신설 **46건**(메모리 Supabase 목 — 실제로 행을 저장해 흐름을 본다)
- **남은 것 = 로한**: `health-design.md` §9 지침을 각 방(순환기·재활의학과·로버트(생활)·수연)에 붙이고, 한 문제로 **한 턴 왕복**해 보기
- ⚠️ 이번과 무관하게 보안 점검에 원래 있던 경고 3건: `v_now`·`v_snapshot` 뷰가 SECURITY DEFINER · `kst_now`·`kst_today` search_path 미고정 · 유출 비밀번호 차단 꺼짐

## ✅ EMR 0단계 완료 — v4.01 (로버트 검수, 9/18)

- 코덱스 §15.1 수정 4건을 **실데이터로 재검수 — 전부 의도대로.** 사실 묶음 14,390 → 11,068자
- 로한 결정: **사실 묶음은 메인 화면에 띄우지 않는다.** 저장은 질문마다(`facts`), 정확성 검증은 로버트가 실데이터로. 임시 검수 화면 제거
- 생략 지표 구분자 `, ` → ` / ` (지표 이름에 쉼표가 있다). t43 화면 검사 교체 → **36건 통과**
- 코덱스가 남긴 "최종 전체 회귀 결과는 완료 후 기록" → 로버트가 `run.sh all` 로 확인(아래 버전 행)
- **다음 = 1단계(중계 모드)**: 테이블 2 + 함수 3(`emr_pending`·`emr_thread`·`emr_reply`). 그 전에 정할 것: DB 적용 담당 · 문제별 답할 방

## 인계 기록 — EMR 0단계 (Codex, 9/18)

- 구현 정본: `health-design.md`. 건강 첫 메뉴 `EMR` + 읽기 전용 사실 미리보기.
- `dietOn`·`medVisits`는 식사 기록 옆 공용 함수, `emrFacts`·화면은 EMR 구역. 서버·AI 호출·새 DB 필드 없음.
- 임시 문제 「전체」는 등록 지표 83개 중 최근 검사 6회 값 있는 지표만 상세 출력(빈 지표 34개는 이름·개수로 축약), 체성분 8건·식이 14일·의료 기록 90일·사고를 포함. 진료일 빈 값은 검사일과 같다는 기존 규칙 반영.
- 실제 기록으로 원본 불변성·결정성·화면 원문 일치 확인. 실제 사실 묶음과 화면은 `tests/.out/emr-real-*`에만 저장(Git 제외).
- **로버트 재검수 대기**(v2 §15.1). 끼니당 한 줄 + 구매 근거, 의료 지출/진료일 구분, 빈 지표 축약, `func` 원문 포함을 반영했다. 사실 묶음 14,390 → 11,068자. 로한은 로버트의 의문점만 확인한다. 확인 전 1단계로 넘어가지 않는다. 로한 지정 버전 **v4.0**, `APP_VERSION`·CHANGELOG 반영.
- 신규 t43 **35건 통과**. 최종 전체 회귀 결과는 완료 후 기록한다. 배포 전 수동 스냅샷 **#107** 생성(원본 app_state 변경 없음).
- 날짜 검사 `emrDateOK`/`isDateStr` 통합은 §15.1에 따라 보류. 임시 검수 화면은 2단계에서 걷어낸다.
- 환경 차이: §21의 전용 Chromium이 현재 경로에 없어 설치된 Chrome 사용. `CHROME=C:\Program Files\Google\Chrome\Application\chrome.exe`. Bash PATH에 `/usr/bin:/bin`도 필요.
- 커밋·푸시는 로한 담당. 다음은 **중계 모드**의 테이블·함수 3개, AI API/키/과금 구현은 보류. DB 적용 담당·문제별 방과 기록 범위는 1단계 전에 결정한다.

---

> 🔒 **이 파일 하나만 읽으면 새 창에서 바로 시작할 수 있다.**
> 상세 이력은 `handoff_0825.md`(8/25~9/5, 마감), 그 이전은 `handoff_08XX.md`.
> 업데이트할 때마다 이 파일을 갱신한다. **비대해지면 안 된다 — 한 장을 넘기지 마라.**

---

## 1. 앱

| 항목 | 값 |
|---|---|
| 버전 | **v4.17** |
| 배포 | `index.html` 단일 파일 → GitHub Pages (`rohanchung/gagyebu`) |
| 저장소 | Supabase `ytkbrdgbklnijbwkvino` · 테이블 `app_state` / `app_state_snap` / `handoffs` |
| 테스트 | `tests/run.sh all` — **25개 전부 통과**(360초 · t42 337건 · t40 73건). 🟢 **이제 로한 PC 에서 직접 돈다**(아래 §21) · 🟢 푸시하면 GitHub Actions 도 같은 걸 돌린다 |
| 최근 스냅샷 | `app_state_snap` #104 (v3.9 배포 직전, manual) · 단어 **409건** · 문법 예문 **19건** |

**커밋·푸시는 로한이 GitHub Desktop 으로 한다.** 🚫 이 폴더에서 `git` 명령을 실행하지 마라.

---

## 2. 재정 현황 (2026-09-05 확정)

### 자산 7,348,511

| 그룹 | 금액 | 방식 |
|---|---:|---|
| 현금성 | 3,199,823 | **파생** (기초잔액 + 가계부) |
| 저축 | 2,400,139 | 노랑우산만 **파생** |
| 외환 | 1,459,059 | 수기 |
| 투자 | 289,490 | 수기 |

**현금성 내역**
| 계좌 | 잔액 | 기초(9/1) |
|---|---:|---:|
| 카카오뱅크 세이프박스 | 1,482,988 | 1,482,988 |
| 우리 SUPER주거래 | 1,065,803 | 299,258 |
| 우리 우리동네사장님 | 462,873 | 227 |
| 카카오뱅크 개인사업자 | 180,639 | 13,245 |
| 네이버페이 | 7,515 | 5,939 |
| 카카오뱅크-입출금 | 5 | 5 |

### 부채 40,656,750

| 항목 | 금액 |
|---|---:|
| 소상공인대환 (3.11%) | 20,000,000 |
| 약관대출 교보 종신 (5.5%) | 19,900,000 |
| 카드 미결제 (**할부 잔여 기준**) | 756,750 |

**카드 미결제 내역**
| 건 | 잔여 | 상태 |
|---|---:|---|
| 브리즘 안경 (3회 중 1회 완료) | 290,066 | 10·11월 |
| LGU+ 단말기 (24회 중 20회) | 207,600 | 10~1월, 월 51,900 |
| 소원약국 (2회 중 1회) | 74,700 | 10월 |
| 고려대의료원 (3회 중 1회) | 74,266 | 10·11월 |
| LGU+ 통신 07월분 | 72,870 | **9/25 청구 예정** |
| 네이버파이낸셜 (3회 중 2회) | 25,298 | 10월 |
| K패스 후불교통 3건 | 11,950 | 9/25 청구 |

### 순자산 **−33,308,239**

---

## 3. 실측 대조 결과 ✅

| 계좌 | 앱 | 실측 | 오차 |
|---|---:|---:|---:|
| 우리 SUPER주거래 | 1,065,803 | 1,067,603 | **−1,800** ⚠️ |
| 네이버페이 | 7,515 | 7,515 | 0 |
| 삼성카드 9/4 선결제 | 711,795 | 711,795 | 0 |
| 삼성 할부 잔액 | 671,700 | 671,700 | 0 |

⚠️ **우리SUPER 1,800 차이** — 9/4 카페 1,800(K패스)을 즉시출금으로 되돌린 결과.
은행 원장은 9/5 14:40까지라 그 이후 반영됐을 수 있다. **다음 은행 조회 때 확인.**

### 데이터 무결성
- 고아 결제수단 **0**
- 기초잔액(9/1) 이후 계좌 미지정 **0건**
- 이체 from/to 누락 0 · 금액 0/음수 0 · 날짜 이상 0
- 총 거래 943건 (2025년 1건 = 단말기 할부 원거래, 의도됨)
- 9/1 이전 계좌 미지정 27건 10,656,863원 → **현재 잔액에 영향 없음**

---

## 4. 입력 규칙 (지키지 않으면 또 어긋난다)

### 💳 카드 — 결제 성격 토글
결제수단이 카드면 **[즉시출금 | 후불(카드값)]** 이 뜬다. 기본값은 카드 설정.
- 우리(K패스) **교통비는 후불** → 25일에 나간다. 나머지는 즉시.
- 기본값과 같으면 저장되지 않는다(빈 서랍 금지).

### 💚 네이버페이 — **모달이 알아서 한다 (v2.13)**
결제수단에서 네이버페이를 고르고 금액을 치면 **잔액·충전액·결제 후 잔액**이 바로 계산된다.
저장하면 **이체 + 지출 두 건**으로 나뉜다. 손으로 두 줄 넣을 필요 없다.
- 충전은 **부족분을 10,000 단위 올림**. 잔액이 충분하면 충전하지 않는다.
- 지출을 지우면 짝인 충전 이체도 같이 지운다.
- 🔒 충전 계좌는 **우리 SUPER 고정** (`fundedBy`).
- 🌊 현금흐름 Sankey 에서는 지갑을 **자금원으로 접는다** — 그림뿐이고 잔액은 지갑에 남는다.

### 💰 파생 vs 수기 (v2.12)
기준은 **그룹이 아니라 기초잔액**이다. 기초를 넣은 계좌만 파생한다.
- 파생: 현금성 6개 + **노랑우산**(납입액이 쌓이는 계좌)
- 수기: 주식·외환 (평가액이라 거래로 파생 불가)

### 💳 카드 선결제
⚠️ **승인액 ≠ 청구액.** 할인·포인트가 붙는다.
→ 선결제는 **삼성카드 앱의 청구명세 금액**으로 넣는다. 승인내역으로 계산하면 틀린다.

### 카드 설정 (마감 12일 · 결제 25일)
| 카드 | 종류 | 연결계좌 |
|---|---|---|
| 삼성 탭탭오 | 신용 | 우리 SUPER |
| 삼성 ID MOVE | 신용 | 우리 SUPER |
| 우리(K패스) | 체크 (교통비만 후불) | 우리 SUPER |
| 카카오-사업자체크카드 | 체크 | 카카오 개인사업자 |
| 네이버페이(이체) | 체크 | **네이버페이** |

---

## 5. 미결 과제

- 🟡 **문법 유형 2개가 더 있다** — 조사(`particle`, もんだい1 의 62% · 오답 자동생성 가능)와 문형(`form`, `opts` 수동). 학습방이 올리면 바로 연다. 접속사 19건은 v3.9 에서 구현 완료
- ✅ CI 도입 완료(2026-09-16) — 푸시하면 25개 스위트가 깃허브에서 돈다. `.github/` 는 원격 도구가 못 쓰는 보호 경로라 **로한이 직접 넣었다**
- 🟡 **K패스 마감일 확인** — 기본값 10일 마감/25일 결제로 돌고 있다. 실제와 다르면 카드 수정에서 바꿔라

### 🔴 세무 (기한 있음)
- **채움영어 원천징수 미입력 8건** — 13건 중 5건만 `wht` 입력. 총 24,831,187원
- 2025 귀속 종소세 사업소득명세서 발급 → 2026 신고유형(단순경비율/기준경비율) 결정
- 세무사 질문: "2026년 귀속 종소세 신고유형"

### 🟠 재정 정합
- `기타`(사업) **23건 777,905원 정체 확인** — 계정과목 불명
- 노랑우산·국민연금·건강보험은 **경비가 아니라 소득공제** — 분류 재설계
- 2·4·5·6·7월 카드 매칭 검수 후 순자산 재확인
- 2025-01~08 부가세 공백 원인

### 🟠 앱 기능
- 🟠 **학습 3단계 잔여** — 일요일 게이트(학습페이지 차단 + 데일리 배너·닫기 없음) · `month` · goals 정리(C.8~C.10 archived, P1~P4 마일스톤 신설)
- 🟠 **캘린더 탭 + 뽀모 탭** (로한 요청 2026-09-16) — 학습 페이지 맨 왼쪽에 캘린더(날짜 클릭 → 우측에 그날 과제 + 학습방 멘트), 그 옆에 뽀모. '오늘' 탭을 대체한다. ⚠️ D-day 는 전 탭 공통 헤더로, 과락·오답·주간은 우측 하단으로 옮긴다. 뽀모 시간은 `study.pomos` 에 **분 단위**로 (timelog 한 칸이 30분이라 10분 뽀모를 찍으면 3배로 부푼다)
- 🟠 **문법 드릴(`sents`) 미착수** — 학습방 v11 기획 수신. ⚠️ `sents` 데이터가 0건이라 만들어도 빈 화면. p199~200 접속사 18예문 대기
- 🟠 **`sub` 드릴 필터 미구현** — 존경어·겸양어만 골라 돌리기. 학습방 v9 요청분 중 남은 것
- 🟢 **`r2m` 오답 21%가 아직 멀다** — 긴 단어는 읽기가 가까운 후보가 물리적으로 없다. 의미 근접성 데이터가 없으면 더는 못 줄인다
- 🟢 **가타카나 단어 0건** — `kata` 모드는 만들어 뒀고 데이터가 들어오면 저절로 켜진다. 학습방 소관
- 🟠 **`miss` 는 누적이라 안 깎인다** — 맞혀도 출제 우선순위가 영구히 굳는다. 기획서 v7 스펙대로 만들었으나 **학습방에 되물어야 한다**
- 카드 참조를 이름 → id 로 전환 (이름 바꾸면 과거 거래가 연결을 잃는다)
- B단계 기초-기말 표

### 🟢 사소
- `매일 유산균&비타민C` / `매일 칼마디&오메가3` 루틴의 days 가 평일뿐 (제목은 '매일')
- 8월 이전 네이버페이 결제수단이 뒤섞임 (잔액 영향 없음)

---

## 5-1. ⏰ 시각 — 서버는 UTC, 로한은 KST

**서버(Supabase·컨테이너)는 UTC 다.** KST = UTC+9 이므로
**한국시각 00:00~08:59 사이엔 `current_date` 가 하루 전이다.** 로한은 밤에도 작업한다.

🔒 **날짜·시각은 항상 헬퍼로 읽는다. `now()` / `current_date` 를 날것으로 쓰지 마라.**
```sql
select * from v_now;        -- 지금 시각(KST) + 앱·스냅샷·핸드오프 최신성
select kst_today();         -- 한국시각 오늘
select kst_now();           -- 한국시각 현재
```
- 앱(`todayStr()`)은 **브라우저 로컬 시각**이라 KST 로 정확하다 — 앱은 손댈 것이 없다.
- 로버트/수연은 **세션 시작 시 `select * from v_now;` 를 한 번 조회**한다.
  로한이 말해주는 시각은 대화가 길어지면 낡는다(오늘도 "11시"라고 시작해 12:23 에 이 작업을 했다).

## 6. 절대 어기지 않는 원칙

1. 🔒 **파생 vs 저장** — 파생 가능한 것은 저장하지 않는다. 계좌는 `card.acct` 에서 파생, 진도는 로그 누적에서 파생, 총점은 과목 점수에서 파생.
2. 🔒 **같은 질문에 답하는 함수는 하나여야 한다.** v2.11 결함이 이것이었다 — `txnOwed()` 가 답을 알고 있는데 현황판이 자기 식으로 다시 계산했다.
3. ⚠️ **추측 금지.** 모르면 '불확실'이라 쓰고 로한에게 묻는다. 시험일·할부 총액을 추측하지 않았다.
4. ⚠️ **플래그로 켜지는 기능은 실데이터에서 그 플래그를 확인한다.** 픽스처 통과는 증명이 아니다(v2.6 `isFood` 사고).
5. ⚠️ **빈 서랍을 남기지 않는다.** `undefined` 를 넣으면 키가 남는다 — `delete` 여야 한다.
6. 🔒 배포 전 `./run.sh all` 전체 통과 + 스냅샷 1건.
7. ⏰ **시각은 `v_now` / `kst_today()` 로 읽는다.** 서버는 UTC 라 심야에 날짜가 밀린다.
8. ⚠️ **id 는 건수가 아니다.** 세는 건 `count(*)` — 시퀀스는 삭제해도 줄지 않는다.

---
## 7. 오늘(9/5) 배포 이력
v2.9 과락 게이지·오답노트 → v2.10 거래별 후불 판정·은행 원장 정합 →
v2.11 미결제를 할부 잔여로 정정 → v2.12 파생 기준을 기초잔액으로(노랑우산) →
v2.13 선불 지갑(네이버페이) 경유지 구조

**로한이 숫자를 물어서 잡은 결함 3개**
1. "네이버페이가 충전만 되고 안 빠져나가네" → 계좌만 만들고 카드 연결을 안 바꾼 **반쪽 작업**
2. "총부채가 어떻게 4,200만이 되냐" → 미결제를 **할부 총액**으로 계산. 134만 부풀어 있었다
3. "24개월 핸드폰 할부 51,900" → 승인내역엔 없고 **청구명세에만** 있었다. 내가 "원인 아니다"라고 한 게 틀렸다

🔒 **숫자가 이상하면 계속 물어라. 테스트 통과는 정합의 증명이 아니다.**

---
## 8. 2026-09-08 추가

**v2.14 — 끼니 미선택 경고**
로한: "데일리에 식사는 들어가는데 간식이 안 들어간다."
→ 코드·화면·플래그 전부 정상이었다. 실측 9/7 간식 4,050 은 **음식명은 있고 slot 이 비어** 있었다.
🔒 **문제는 조용히 버린다는 점.** 음식명을 적었다는 건 '이건 끼니다'라는 신호인데 아무 말 없이 빠졌다.
→ 음식명이 있는데 끼니가 없으면 저장 시 확인한다. ⚠️ **막지는 않는다** — 장보기는 '해당없음'이 맞다.

**노랑우산 납입 5건 이체 입력 완료**
출금계좌 = **우리 우리동네사장님**(로한 확인). 4/30·5/31·6/30·7/28·8/31 각 100,000.
⚠️ 날짜는 hist 관측일이라 실제 이체일과 며칠 다를 수 있다. 전부 9/1 이전이라 **현재 잔액엔 영향 없다**(기록용).
🔒 9월부터는 납입할 때마다 **이체**로 넣으면 잔액이 따라온다.

**🤝 ChatGPT(수연) 합류**
`handoffs` id=9 / room `ChatGPT(수연)` / 2026-09-08 등록.
역할 분담: 수연은 **상담 인터페이스**, 로버트(Claude)는 **앱 개발 + 데이터 정합**.
원칙은 충돌하지 않는다(현재 DB 우선 · 추측 금지 · 충돌 시 드러내기).
⚠️ **[내 오류 정정]** 나는 수연 문서의 "스냅샷 36건"이 낡았다고 했다. **틀렸다.**
   `max(id)=58` 을 건수로 착각했다 — 실제 `count(*)` 는 **36건**이 맞다(22건은 보관기한으로 삭제됨).
   handoffs 8건도 수연 자신이 추가되기 전 기준이라 작성 시점엔 정확했다.
   🔒 **id 는 건수가 아니다.** 시퀀스는 삭제해도 줄지 않는다. 세는 건 언제나 `count(*)`.
🔒 **수연이 재정 상담을 하려면 이 STATUS.md 를 읽어야 한다** — handoffs 만 보면 9/5 정합 작업이 통째로 빠진다.

---
## 9. v3.0 — 학습 페이지 제로베이스 (2026-09-08)

로버트(학습) 기획서 v5 를 그대로 구현. **옛 `study` 는 지웠다** — books·tests·errors 전부 빈 배열,
plan 1건뿐이라 **버려도 손실 없음을 실조회로 확인**하고 진행했다.

### 왜 갈아엎었나
옛 마일스톤이 '일 4/6/8뽀모'였다. 실측 루틴 **due 11일 / done 0일**, timelog study **0시간**.
🔒 **뽀모 개수는 합격의 대리지표가 아니고 실행 판정도 못 한다.**

### 핵심 구조
- 🔒 **시간표가 아니라 장소로 과제를 고른다.** 채널 `move`(이동·폰) / `academy`(학원·교재1권) / `home`(집·교재2권+음원)
  교재를 분철해 두 곳에 나눠 뒀다. 장소를 무시하면 '집에 있는 책' 과제가 학원에서 계속 미완으로 뜬다.
- ⚠️ 다른 채널 과제는 **지우지 않고 흐리게** 남긴다 — 있다는 건 알아야 한다.
- ⚠️ **학습시간은 timelog 의 `tag` 또는 `tag2`.** 근무 중 학습을 `tag:'work'+tag2:'study'` 로 이중 기록한다.
  **tag 만 보면 0시간이 나온다.**
- 상단 고정: D-day + **입실시각** + phase(P1~P4). 2025년 시험장 앞에서 돌아온 이력 → **입실까지가 목표**.
- 과락 게이지는 데이터 없으면 **'미측정'** 으로 정직하게 비운다.
- 탭 5개 → **3개**(오늘·오답·기록).

### 결함 (스크린샷으로 잡음)
완료한 과제가 목록에서 **사라졌다**. 오늘 한 것이 안 보이면 성취도 안 보이고 잘못 누른 체크를 되돌릴 수도 없다.
→ **'오늘 끝낸 것'** 섹션 신설.

### 투입한 실데이터 (학습방이 쌓아둔 것)
units 7 · tests 2(9/7 8/10, 9/8 2/8) · errors 7 · phases 4 · books 3 · 시험일 12/06 입실 13:40

### 🚫 안 만든 것 (스코프 방어)
문항 본문 저장 · 채점 엔진 · 앱 내 풀이 UI. **출제·풀이·채점은 대화방에서 한다.**

---
## 10. v3.1 — 데일리 「학습」 탭 (2026-09-09)

로버트(학습) 기획서 v6. **v3.0 구현분은 그대로 두고 추가만 했다.**

### 역할 분리
- **학습 페이지** = 전략·판정 (D-day · 과락 게이지 · 오답노트 · 진도 전체)
- **데일리 학습 탭** = 실행 (오늘 것만 · 체크)

로한이 매일 여는 건 데일리다. 실행이 거기서 일어나야 하는데
학습 페이지에 이번 주 과제가 한꺼번에 떠서 **"오늘 뭘 하나"에 답을 못 했다.**

### 스키마 추가 — `units[]` 2개 필드
- 🔒 **`day`(실행 예정일)와 `due`(마감)를 분리한다.** 데일리는 `day` 로 거른다.
- `backlog:true` = 날짜 없이 ④ 섹션에만.
- ⚠️ 두 필드가 없는 옛 unit 도 기본값 주입으로 렌더된다.

### 🔒 미완은 자동 이월하지 않는다
그날 자리에 남고 **"밀린 것 n건"** 으로만 뜬다.
자동으로 밀어주면 실패가 안 보이고, 안 보이면 일요일에 판정이 안 된다. **이월은 일요일 판정에서만.**

### 3일 롤링 창
`day` 가 채워진 unit 은 **오늘+이틀치만** 존재한다. ⚠️ **생성은 로버트(학습)가 한다 — 앱은 만들지 않는다.**
90일치를 미리 깔면 첫 주에 속도가 틀렸을 때 89일치가 통째로 틀어진다.
3일만 보면 큰 그림이 사라지므로 **상단에 이번 주 총량**을 항상 띄운다.

### backlog 넣는 기준
✅ 청해 회독 · 오답 회전 · 지난 페이지 재인출
🚫 **단어 신규 진도** — 마감 역산 필요량이 하루 2p라 앞당길 근거가 없다.
   로한이 "명분 없는 분량 추가"를 명시적으로 거부했다. **배점·마감 근거가 있는 것만.**

### 투입한 3일 창 (9/9~9/11) — ⚠️ 초안이다, 학습방이 조정한다
9/9 예상어휘 p155-156(20분) + 회독JLPT(10분) / 9/10 p157-158 + 5장01 청해요령 / 9/11 p159-160 + 회독JLPT
backlog 3건 · 9/8 한자 규칙 재검증은 **밀린 것**으로 남아 있다

t42 에 I·J 블록 19건 추가 → **69건**. 전체 24개 통과.

---
## 11. v3.2 — 🈶 단어 드릴(k2r) + study 결함 2건 (2026-09-11)

로버트(학습) 기획서 v7. **제4차 제작방 첫 작업.**

### ⚠️ [결함] 존재 보장이 두 곳에 있었다
`studyDB()` 주석은 "존재 보장은 여기 한 곳에서만"인데 `migrateV6` 가 매 로드마다
`d.study.plan` · `d.study.cfg.target` 을 **되살리고 있었다.** studyDB 는 `v!==1` 일 때만 지우니까
**영원히 못 지웠다** — v3.0 에서 버린 옛 구조가 실 DB 에 `cfg:{target:{}}`·`plan:{}` 로 남아 있었다.
🔒 **같은 구조를 두 곳에서 보장하면 한쪽을 고쳐도 다른 쪽이 되돌린다.** 원칙 2·5 동시 위반.
→ migrateV6 의 study 블록을 없앴다. 존재 보장은 `studyDB()` 하나뿐이고, 옛 키 `delete` 는 **매번** 돈다.

### ⚠️ [결함] 주간 키가 월요일이었다
`week` 에 `"2026-09-07"`(월) 하나뿐인데 앱 `weekStart()` 는 **일요일 기준**이다.
→ 이 키는 **어느 주에도 안 걸렸다.** 주간 판정·이월이 통째로 빈 주를 보고 있었다.
`2026-09-06` 으로 이전. 🔒 **화면이 0/0 이라고 조용히 말하는 종류의 결함이다.**

`verdict.rate` 삭제 — 완료율은 `unitIds` 중 done 비율에서 **파생**한다. 저장하면 두 화면이 다른 숫자를 낸다.
자기평가는 `memo` 자유서술. **척도를 만들지 않았다** — 5점 척도는 매주 4점이 찍히고 정보가 0이 된다.

### 🈶 단어 드릴 — k2r 부터
v5 §9 의 "채점 엔진 금지"에 **단어만 예외**다. 데이터가 `{kana,kanji,ko}` 3필드고 채점이 문자열 비교로 끝난다.
문법·독해·청해 문항은 여전히 앱에서 만들지 않는다.
- 🚫 **랜덤 금지.** 출제 순서 = `miss` 많은 것 → 책에 체크한 것(`flag`) → 오래 안 본 것(미출제 먼저)
- 보기 **위치는 섞는다** — 정답이 항상 첫 칸이면 단어를 안 보고도 맞힌다.
  단, **시작할 때 한 번 만들어 세션에 박는다** — 채점마다 다시 섞으면 보기가 춤춘다
- ⌨ **4지선다 ↔ 직접입력** 토글. 기본은 4지선다 — 시험 もんだい1 이 4지선다고 **IME 없이도 돌아가야 한다**
- 🔒 **드릴 오답을 오답노트로 바로 밀지 않는다.** 하루 20문항이면 며칠 만에 오답노트가 무용지물이 된다.
  `miss>=3` 만 **로버트가** 승격한다
- 진입점을 **데일리 학습 탭에도** 뒀다(실행은 데일리에서). ⚠️ 단어가 0개면 버튼을 띄우지 않는다
- r2k·kata·w2m 은 회색 + `disabled`. **입구만 만들고 안을 비우지 않는다**
- 🔒 진행 중 드릴은 DB 에 저장하지 않는다. 끝나면 `drills` 1건만 남는다(중간에 그만둬도 **푼 만큼은** 남는다)

### 🔒 역할 경계 — 제작방은 그릇, 콘텐츠는 학습방 (로한 확정 2026-09-11)
**제작방(이 방) = 기능 구현.** 스키마·화면·채점·테스트까지가 내 몫이다.
**콘텐츠 = 학습방.** `words` 업로드, `units` 3일 창 생성, 진도 재배치, `errors` 승격은 **학습방 로버트가 한다.**
🚫 **제작방이 로한에게 콘텐츠를 요구하지 않는다.** `words` 가 0건이어도 그건 내 미결 과제가 아니다 —
그릇이 비어 있는 것과 그릇이 없는 것은 다르다. 빈 상태 렌더만 보장하면 내 일은 끝이다.
⚠️ v3.2 에서 내가 이 선을 넘어 "교재 사진을 보내라"고 로한에게 요구했다. 그건 학습방에 갈 말이었다.

### 검증
t42 에 K·L·M 블록 30건 추가 → **99건**. `run.sh all` **25개 전부 통과**(211초).
스냅샷 #75(착수 직전). 새 화면 스크린샷 PC·모바일 확인 — `Claude outputs/shot_word*.png` `shot_drill*.png`.
⚠️ t20 은 `/home/claude/state_rw.json` 을 하드코딩으로 읽는다. 새 컨테이너에선 `cp tests/state_rw.json /home/claude/` 먼저.

### 🚫 알림 기능은 만들지 않는다 (로한 확정 2026-09-11 · 폐기)
기획서 v7 ④ 의 "일요일 20:00 시각지정 알림 루틴 신설"은 **폐기한다.**
로한: **"네이티브 앱도 아닌데 무슨 알림이야."** 맞는 말이다 — 웹앱이라 브라우저를 열어둘 때만 뜨고,
그건 안 뚫리는 알림을 만드는 것과 같다. 예약 작업 푸시도 같이 폐기한다.
🔒 **압박은 화면으로 건다** — 일요일 게이트의 **닫기 없는 배너**가 그 역할이다. 알림은 그 대체물이 아니다.
⚠️ 다음 방은 v7 ④ 를 읽고 다시 만들려 하지 마라. 이 항목은 로한이 직접 잘랐다.

---
## 12. v3.3 — 🈶 드릴 4모드 완성 (2026-09-11)

학습방이 p150~156 단어 **284건**을 올렸다. 실데이터로 나머지 3모드를 만들었다.
| 모드 | pool | 시험 대응 |
|---|---:|---|
| `k2r` 한자→히라가나 | 270 | もんだい1 한자읽기 |
| `r2k` 히라가나→한자 | 270 | もんだい2 표기 |
| `kata` 가타카나→뜻 | **0** | 외래어 (데이터 대기) |
| `w2m` 단어→뜻 | 284 | 시험에 직접 안 나옴 |

### ⚠️ 실데이터가 잡아준 함정 3개 (픽스처로는 안 나왔다)
1. **동음이의어** — `は`→葉·歯, `ちゅうしゃ`→注射·駐車. r2k 에서 뜻이 없으면
   **정답이 하나로 안 정해지고 보기 두 개가 다 맞는 문항**이 된다.
   → r2k 문제에 **뜻을 같이 띄운다**(전용 칸 `.stdhint`, 15px). 시험은 문맥 문장이 그 역할을 한다.
   → 그리고 **같은 읽기의 다른 한자를 보기에 강제로 넣는다.** 🔒 초안은 이걸 우연에 맡겼고,
     실데이터로 찍어 보니 歯 가 보기에 없었다 — **동음이의어 문항이 그냥 쉬운 문항이 됐다.**
2. **`cat` 에 katakana 가 0건** — 업로드본은 sino/native 뿐이다. `cat` 을 믿었으면 kata 모드가
   **이유도 모르게 빈 화면**이었다. → 🔒 **가타카나는 `kana` 문자범위에서 파생 판정.**
   실제로 p150~156 에 외래어가 없는 것이 맞았다(분류 누락 아님). 데이터 들어오면 저절로 켜진다.
3. **복수 뜻 46건** — "사이, 동안" 형태. 전체 문자열 비교로 채점하면 **아는 단어를 틀렸다고 한다.**
   → 4지선다는 보기 전체 문자열, 직접입력은 쉼표로 쪼개 **하나만 맞으면 정답.**
   ⚠️ k2r·r2k 는 부분 일치를 쓰지 않는다 — 읽기·표기는 통째로 맞아야 한다.

### 그 외
- 한자 없는 순가나 14건(`あいさつ` 등)은 k2r·r2k 에서 자동 제외, w2m 에는 포함
- **모드 선택 → 문항 수(10·20·40)** 2단계 + **고른 모드를 기억한다.** 데일리 진입점도 그 모드로 시작
- 🔒 pool 0 인 모드는 `disabled` — 누르면 빈 화면이 되는 입구는 만들지 않는다
- `자주 틀리는 단어` 칩: `seen` 이 0이면 `5/0`(거짓말)이 아니라 `5틀림` 으로 쓴다. 칩에 읽기·뜻 tooltip

### 검증
t42 N·O 블록 35건 추가 → **134건**. `run.sh all` **25개 전부 통과**(225초). 스냅샷 #79.
스크린샷 PC·모바일 — `Claude outputs/s33_*.png`. **실데이터 분포를 재현한 284건으로 렌더**해서 눈으로 봤다.
⚠️ 앱에서 로한이 직접 돌린 기록이 남아 있다(`drills` k2r 9/10, 75초) — **실데이터에서 도는 것이 확인됐다.**

---
## 13. v3.4 — 🔊 음성 모드 · 모드 6개 · 출제 가중치 확정 (2026-09-11)

> ✅ **2026-09-13 검증 완료** — `run.sh all` **25개 전부 통과**(242초, t42 173건).
> 9/11 에는 클라우드 셸 승인이 막혀 못 돌렸고, 9/13 에 풀려서 돌렸다. **코드는 그대로**다.
> ⚠️ 이 일로 배운 것: 검증을 못 돌리는 상태면 **문서에 '미검증'이라고 쓰고 멈춘다.** 통과했다고 쓰지 않는다.

로버트(학습) v8·v9 반영.

### 🔴 `w2m` 폐기 — 모드를 6개로 재정의
v7 은 "뜻 맞추기는 시험에 직접 안 나오니 최하위"라고 했다. **문자·어휘만 보고 청해를 뺀 판단이었다.**
**청해 60점(과락 19)은 전부 소리다. 한자가 안 보인다.** 로한 자가보고: *"한자 보고 뜻은 쉽고, 히라가나 보고 뜻이 어렵다."*
→ 한국어 한자음 덕에 「한자→뜻」이 쉬운 것이고 그게 **안다는 착각**을 만든다.

| 순위 | 모드 | pool(313건 기준) |
|---|---|---:|
| 0 | `a2m` 음성→뜻 | 313 (일본어 음성 있는 기기에서만) |
| 1 | `r2m` 히라가나→뜻 | 313 · **기본 모드** |
| 2 | `k2r` 한자→히라가나 | 290 |
| 3 | `r2k` 히라가나→한자 | 290 |
| 4 | `kata` 가타카나→뜻 | 0 (데이터 대기) |
| 5 | `k2m` 한자→뜻 | 290 · 최하위 |

### 🔊 음성 = 브라우저 내장 `speechSynthesis`
외부 API·음원파일·키 없다. 🚫 알림과 다르다 — 알림은 폐기됐지만 이건 페이지 안에서 소리를 내는 것이다.
- 🔴 **읽히는 건 언제나 `kana`.** `開く` 를 넣으면 `あく` 가 아니라 `ひらく` 로 읽어 **틀린 발음을 외운다**(p157 에 실제로 있다)
- 🔒 일본어 음성이 없는 기기에서는 **카드 자체를 안 그리고** 왜 없는지·어떻게 추가하는지 한 줄로 알린다
- `getVoices()` 가 첫 호출에 빈 배열을 주는 문제는 `onvoiceschanged` 에서 다시 그려 해결
- 문항이 뜨면 **한 번 자동 재생**(문항당 1회) + 🔊 다시 듣기 버튼. 청해는 한 번에 안 들린다
- ⚠️ 고저 악센트는 신뢰하지 마라. **단어 발음 ≠ 청해** — 교재 2권 음원이 본체, 이건 보조

### 🔴 [결함] 소리로 뜻을 물으면 동음이의어는 정답이 둘이다
실데이터 313건에 5쌍 — `おる`(있다·겸양 / 접다), `おこる`(일어나다 / 화내다), `おりる`, `は`, `ちゅうしゃ`.
하나만 정답으로 치면 **맞는 답이 오답으로 기록되고 → `miss` 가 올라가고 → 출제 알고리즘이 그 문제를 계속 낸다.**
**틀릴 수밖에 없는 문제가 무한 반복된다.** 학습방이 `r2k` 에서 지적한 것과 같은 구조가 소리 모드에도 있었다.
- → **같은 읽기의 모든 뜻을 정답으로 인정**하고, **4지선다 보기에서는 같은 읽기 단어를 뺀다**(보기 둘이 다 정답이 되므로)
- → 정답 화면에 `같은 읽기 は — 歯 이, 치아 도 정답` 을 띄운다. 안 알려주면 채점이 틀린 줄 안다
- ⚠️ `k2r`·`r2k`·`k2m` 은 확장하지 않는다 — 한자가 정답을 하나로 고정한다(실데이터 kanji 중복 0건)
- ⚠️ **괄호는 설명이다** — `있다 (いる의 겸양어)` 에서 묻는 건 '있다' 하나다. 괄호까지 다 써야 정답이면 아는 단어를 계속 틀린다

### ⚖️ 출제 가중치 확정 (학습방 v9)
```
score = miss*10 + (flag ? 3 : 0) + min(안 본 날수, 7)   // lastSeen 이 null 이면 7
정렬: score 내림차순 → 동점이면 seen 오름차순
```
🔒 **저장하지 않는다. 매번 파생한다.**
⚠️ **`lastSeen` 결합이 핵심** — `miss` 만으로 정렬하면 `miss=0` 단어가 영영 안 나온다.
`miss` 가 내려갈수록 경과일이 정렬을 주도해 **자연히 순환**한다.

### ✅ `streak` — v3.3 의 "miss 가 안 깎인다" 해결
**연속 2회 정답이면 `miss` 를 1 깎는다**(하한 0). 오답이면 `miss+1`, `streak=0`.
리셋이 아니라 **점진 하향** — 3번 틀린 단어는 6번 연속 맞혀야 0이 된다. '어려웠던 단어' 이력이 남는다.
`errors.cleared`(2연속 정답) 와 같은 원리다.

### 🈶 `sub` 필드 (존경어·겸양어·자동사·타동사)
학습방이 채운다. 앱은 상단에 개수를 세고 정답 화면에 배지로 띄운다. 현재 14건.
⚠️ **모드 필터는 아직 없다** — 학습방이 "드릴 필터"로 쓰라고 했지만 다음 차례로 넘겼다.

---
## 14. v3.5 — 🎯 오답 선택지 전면 교체 · 4지선다 고정 · 「모르겠다」 (2026-09-15)

로버트(학습) v10 반영. `run.sh all` **25개 전부 통과**(239초 · t42 **203건**). 스냅샷 #92.

### 🔴 [결함] 오답의 74%가 무의미했다 — 측정으로 잡았다
v3.4 까지 오답은 **같은 글자수 우선 → 나머지 랜덤**이었다. 학습방이 "랜덤 오답은 눈감고 맞힌다"고
지적했고, **실제 앱 코드를 290문항 돌려 정답↔오답 편집거리를 쟀다.**

| | 평균 편집거리 | 거리 3 이상(=눈감고 구분) | 사전에 없는 변형 |
|---|---:|---:|---:|
| **v3.4 (배포본)** | **3.37** | **75.9%** | 0% |
| **v3.5** | **1.27** | **3.7%** | **57.1%** |
| v3.4 `r2m` | 3.68 | 91.5% | — |
| v3.5 `r2m` | 2.00 | 21.4% | — |

실제 문항: 「いがく(의학)」에 v3.4 는 `おちゃ·うりば·おっと` — **글자가 하나도 안 겹친다.**
v3.5 는 `いがぐ·いかく·かがく` — 이게 시험 문항이다.
🔒 **객관식의 난도는 오답이 100% 결정한다.** 눈감고 맞히면 `miss` 가 안 쌓이고,
그러면 출제 가중식(v9)·주간 판정·오답 승격이 **통째로 무력화된다.**

### 모드별 오답 규칙
- `k2r` ① 같은 한자의 다른 읽기 ② **청탁·촉음·장음 변형 생성** ③ 읽기 편집거리 1~2
- `r2k` ① 동음 레코드 ② 한자 편집거리 ③ 같은 `cat`
- `r2m`·`a2m` ① 읽기 1글자 차이 단어의 뜻 ② **자·타동사 짝의 반대쪽 뜻** ③ 같은 `sub`
- `k2m` 한자 편집거리 · 폴백은 **거리 우선**, 같은 거리끼리 `cat`·`lv`

### 만들면서 네 번 고쳤다 — 측정 없이는 다 놓쳤을 것들
1. **촉음을 아무 데나 넣어 「いぜっん」이 나왔다.** 일본어에 없는 표기는 **오히려 눈감고 배제된다** —
   쉬운 오답은 없느니만 못하다. → `っ` 는 **か·さ·た·ぱ행 앞**에서만
2. **한 티어가 3개를 다 먹어 변형만 셋**이 됐다(= '변형 찾기' 문제). → **티어당 2개**까지
3. **`cat` 을 거리보다 앞에 뒀더니** 「いっしゅうかん」에 같은 `cat` 이라는 이유로 「あし」가 붙었다.
   → 폴백은 **거리가 먼저**, 같은 거리끼리 `cat`·`lv`
4. **같은 `sub` 티어에 정렬이 없어** r2m 오답의 23%가 멀었다. → **전 티어 거리순 통일**
5. **1글자 단어(は·し·じ·ち·け·ゆ·ひ)는 변형을 아예 못 만들었다**(`length<2` 가드).
   청탁은 1글자에도 적용된다 → `は`→`ば`·`ぱ`. 실데이터에 8건 있다

### ⌨ 전 모드 4지선다 고정 · 「모르겠다」
- 직접입력 토글 삭제. ① JLPT 는 전 영역 4지선다 ② 주 사용처가 이동 중(대중교통·화면 봄)이라
  20문항 타이핑 불가 ③ IME 부하는 학습과 무관. 옛 `ui.stdIn` 은 빈 서랍이라 지운다
- ⚠️ **사실관계**: v10 은 "`r2k` 가 키보드 입력으로 구현됐다"고 했는데 아니다 —
  v3.2 부터 기본이 4지선다고 입력은 꺼진 토글이었다. 결론(토글 제거)은 받아들였다
- ❓ **「모르겠다」는 5번째 선택지가 아니라 별도 버튼**이고 처리는 오답과 완전히 같다.
  **목적은 찍기 제거** — 찍어서 맞히면 `miss` 가 안 쌓여 약점 탐지가 죽는다.
  결과 화면에 몇 개를 모른다고 했는지 따로 보여준다. `drills.dunno` 로 집계(0 이면 키 없음)
- 🔒 채점 기록은 `stDrillRecord()` **한 곳**에서만 — 「모르겠다」가 따로 세면 두 곳이 갈린다(원칙 2)
- **한 세트 20문항 기본**(문항당 5초 목표). 단어 **4개 미만이면 모드를 막는다**

### ⚠️ 테스트가 날짜에 의존해 깨졌다
9/11 에 `lastSeen:'2026-09-11'`(=당시 오늘)로 짠 Q 블록이 9/15 에 순서가 바뀌어 실패했다.
→ 🔒 **경과일이 정렬에 들어가는 함수는 오늘 기준 상대 날짜로 먹인다.** 날짜를 박아 쓰지 마라.

### 🔴 회독JLPT 는 아직 지우지 마라
v10 이 "드릴이 도는 걸 확인한 뒤 지운다"고 단서를 달았고 **그 확인이 아직 안 됐다.**
`drills` 는 9/11 k2r 1건뿐이고, 그것도 **오답이 무의미했던 로직**으로 푼 것이다.
→ v3.5 배포 후 실제 세트를 돌려 기록이 쌓이는 걸 보고 판단해라.

---
## 15. v3.6 — 🔴 단어 96건 유실 원인 규명·복구 + 출제 편향 수정 (2026-09-16)

`run.sh all` **25개 전부 통과**(276초 · t42 **226건** · t20 갱신). 스냅샷 #101. 복구 후 **409건**.

### 🔴 [결함·중대] 충돌창이 학습 데이터를 아예 비교하지 않았다
**사고**: 로버트가 `words` 를 409건으로 올렸는데 **두 번** 313건으로 되돌아갔다(p158·159 + p199·200 접속사 96건).

**원인 — 낙관적 잠금은 정상이었다.** 앱은 충돌을 감지해 확인창을 띄웠고 **로한이 「덮어쓰기」를 눌렀다.**
증거: `pre_overwrite` 스냅샷 4건(#97~#100, 9/15 23:26~23:56)이 **전부 409건**. 그 스냅샷은 덮어쓰기를 골랐을 때만 생긴다.

🔴 **그런데 그 확인창에 단어가 안 보였다.** `SYNC_COLS`(충돌 비교 목록)에 **`study` 가 한 건도 없었다.**
화면에 "저쪽에만 96건"이 뜨지 않았고, 로한은 **잃을 게 없다고 보고 눌렀다.** 판단이 아니라 화면이 틀렸다.
⚠️ **반복결함 #8(반쪽 작업)** — v2.7 에서 `study` 를 만들 때 이 목록에 등록하지 않았다.
🔒 **새 컬렉션을 만들면 `SYNC_COLS` 에 등록하는 것까지가 한 작업이다.**

### 수정 3단 (앞 단계가 뒷 단계를 불필요하게 만든다)
1. **등록** — `SYNC_COLS` 에 단어·문법예문·학습과제·오답·테스트·드릴기록 추가(중첩 경로 지원)
2. 🔒 **자동 흡수** — **묻기 전에** 서버에만 있는 레코드를 내 쪽으로 합친다.
   대상은 **로버트가 append 하고 앱이 생성·삭제하지 않는 배열**(`words`·`sents`·`units`)뿐이다.
   ⚠️ `errors` 는 앱이 지울 수 있어(`stErrDel`) 제외 — 흡수하면 삭제가 되살아난다
3. **흡수 후 손실 0이면 묻지 않고 저장.** 남으면 맨 위에 🔴 **"덮어쓰면 서버에만 있는 N건이 사라진다"**
   🔒 **잃을 게 없는데 확인창을 띄우면, 정작 잃을 게 있을 때도 같은 창이라 안 읽힌다.** 그래서 96건이 날아갔다

### 복구
`pre_overwrite` #100 에서 96건을 꺼내 **409건 복구**. 현재 카운터(`seen`·`miss`·`streak`)는 보존, id 중복 0.
→ 로한이 새로고침으로 409건 확인.

### ⚖️ [결함] 출제 콜드스타트 편향 — `flag` 가중치 제거
실측: 313건 중 출제된 47건의 **91%(43건)가 `flag:true`**. **266건이 한 번도 안 나왔다.**
초기엔 모든 `miss` 가 0이라 **`flag` 가 유일한 차별 요소**가 되어 체크한 단어가 상위에 영구히 박힌다.
`flag` 는 "책에서 어렵다고 체크한 것"이라는 **사전 추정**일 뿐이다 —
🔒 **실측 `miss` 가 쌓이기 전에 사전 추정으로 줄을 세우면 안 된다.**

```
score = (seen == 0 ? 100 : 0) + miss*10 + min(경과일, 7)
정렬: score 내림 → seen 오름 → 랜덤
```
- **미출제에 100점** → 전 단어 1회 순회가 끝난 뒤에야 `miss` 정렬이 작동한다
- 동점 랜덤 — 미출제가 수백 건이면 전부 동점이라 고정 순서면 **매번 같은 20개**만 나온다
- `flag` 필드는 유지(로버트 분석용, 출제엔 미사용)
- 단어 탭에 **커버리지 단계 / 약점 집중 단계**를 한 줄로 표시 — "왜 아는 단어가 계속 나오나"의 답

⚠️ **랜덤 타이브레이크가 테스트를 흔든다.** 순서를 검증하는 픽스처는 전부 `seen>0` + `lastSeen` 제각각으로
둬야 결정적이 된다(K·N·P·Q 블록 수정). 미출제 동점에 순서를 기대하면 안 된다.

### 학습방 진단 중 정정한 것
- **`units` 중복은 앱 결함이 아니다.** 앱에 `study.units.push` 가 없다 — 생성은 로버트뿐(`status` 토글만 한다)
- **`day` 미표시도 결함이 아닐 것.** 데일리는 `day` 만 보고 채널을 안 가린다.
  학습 **페이지**엔 채널 탭 필터가 있어 `move` 가 선택돼 있으면 이동 건만 보인다 — 증상이 정확히 일치한다
- **`sents` 는 실제로 0건이다.** 409건 시점 스냅샷에도 0. 접속사 18건은 `words` 로 들어갔다(313+38+40+18=409)
- **정답률 하락은 나쁜 신호가 아니다.** 9/11 k2r 90% → 9/15 55~60%.
  v3.4 오답은 74%가 눈감고 구분 가능했으니 그 로직이면 정답률이 **더 높게** 나온다.
  떨어진 건 **오답이 제 역할을 한 결과**로 읽힌다(드릴 시각 기록이 없어 단정은 불가)

---

## 16. v3.7 — 📅 학습 캘린더 탭 · ⏱ 뽀모도로 · '오늘' 탭 폐지 (2026-09-16)

**로한 지시**: "학습페이지 맨 왼쪽에 캘린더 탭을 만들어. 거긴 학습 전용이야. 날짜를 클릭하면
오른쪽에 그날 할 일이 뜨고, 거기에 **학습방이 멘트를 남길 수 있는 공간**을 만들어.
그리고 그건 현재 '오늘' 페이지를 대체해 — 캘린더랑 중복이야."
이어서: "뽀모를 캘린더 탭에 **버튼으로** 집어넣어서 새 페이지 떠서 나오게 처리하면 되지.
뽀모는 10분인지 아닌지 모른다. **보통의 뽀모도로는 25-5분, 50-10분 이런 식으로 자유롭게 설정**할 수 있어야 한다."

### 📅 캘린더 — 가치는 제로데이 시각화다
- 칸 색 농도 = 그날 학습 분. 🔒 기준을 **60분 고정**으로 둔다 — 그날 최대치를 100%로 잡으면 한 날 때문에 나머지가 창백해진다
- 🔴 **제로데이**: 지난 날인데 학습도 과제도 없는 칸은 **붉은 사선**. 미달은 전부 '안 한 날'에서 나왔고, **빈칸이 숫자보다 강하게 작동한다**
- 마커: 시험일 / 모의고사 4회(`ST_MOCKS` 10-25·11-15·11-22·11-29) / 일요일 판정
- 우측 패널 = 그날 과제 + 💬 **학습방 멘트**(`logs[날짜].coach`) + ⏱ 뽀모 버튼 + 과락 게이지·오답·이번 주
- 🔒 **계획 편집 도구로 만들지 않았다.** 먼 미래에 과제를 심으면 3일 롤링 창(v6)이 깨진다.
  미래는 오늘+2일까지만 보고 그 뒤는 흐리게. ⚠️ 단, **이미 올라온 과제는 흐린 날에도 숨기지 않는다** — 숨기면 거짓말이다
- 🗑 **'오늘' 탭 폐지.** D-day·입실시각·phase 는 **전 탭 공통 헤더**(`stHead`)로 올렸다
- 🔒 **탭은 4개 유지**(캘린더·오답·단어·기록) — 400px 에서 5개는 글자가 잘린다

### ⏱ 뽀모 — 목적은 시간 재기가 아니라 로그 자동화다
최대 데이터 결함이 `timelog` 누락이었다(9/10·9/11 학습이 안 찍혀 주간 판정이 실제보다 낮게 나왔다). 손으로 찍는 구조가 원인이다.
- **길이 자유**: 프리셋 25/5 · 50/10 · 10/3 · 15/5 · 45/15 + 직접입력(집중 1~180, 휴식 0~60).
  ⚠️ 학습방이 "10/3 고정"이라 단정했는데 **로한이 부정했다** — 제작방은 학습방 주장도 로한 확인 전엔 확정하지 않는다
- 🔒 **`setInterval` 로 카운트를 세지 않는다.** 시작 시각을 저장하고 `Date.now()` 차이로 계산한다 —
  탭을 벗어나거나 화면이 꺼지면 타이머가 throttle 돼 멈춘다. 경과 계산이면 돌아와도 정확하고, **새로고침해도 이어진다**
- 🔒 **기록은 `study.pomos` 에 분 단위.** timelog 는 안 건드린다 — **한 칸이 30분**이라 25·50분을 찍으면 학습시간이 부푼다
- 학습시간 파생은 `stStudyMins(ds) = stMinsOn(ds) + stPomoMinsOn(ds)` **한 곳**으로 합쳤다
- 중단해도 그때까지의 시간을 기록한다(1분 미만 미기록). 계획보다 짧으면 `cut:true`. 과제 없으면 `unitId` 키를 안 만든다

### ⚠️ [결함·모바일] 캘린더가 화면 맨 아래로 갔다
`.calwrap` 이 가계부 규칙(`column-reverse` — 가계부의 매일 동작은 '거래 입력'이라 조작부가 위로 가야 맞다)을
물려받아, 400px 에서 **격자가 우측 패널·게이지 전부 아래**에 붙었다. 캘린더 탭을 눌렀는데 캘린더를 보려면 스크롤을 해야 했다.
→ 학습 캘린더에 `.stw` 를 붙여 `flex-direction:column` 으로 되돌렸다.
🔒 **공용 클래스를 재사용할 때는 그 클래스에 걸린 모바일 규칙까지 같이 물려받는다.** 스크린샷 없이는 못 봤다(반복결함 #7).

### 학습방 주장 중 정정
- **"뽀모는 10/3 고정"** → 로한이 부정. 자유 설정으로 구현
- **알림(Notification)** → v3.2 에서 이미 폐기. 페이지 안에서 나는 소리(`AudioContext` 비프)는 다른 층이다

---

## 17. v3.8 — ⚠️ [결함·중대] 체크카드 후불이 '카드 미결제'에 안 떴다 (2026-09-16)

**로한 신고**: "우리K패스카드 중 **후불결제(신용카드) 부분은 현황판에 카드 미결제에 노출이 안 된다.**"

### 실측
`우리(K패스)` 후불 **10건 39,500원** (9/1~9/15, `credit:true` · `card:true`).
**총부채에는 들어가 있었고, 현황판 '💳 카드 미결제'와 선결제 화면에는 한 건도 없었다.**
→ **낼 방법이 없는 부채.** 선결제로 지울 수 없으니 영원히 쌓일 구조였다.

### 원인 — 같은 질문에 두 함수가 다르게 답했다
| 함수 | 후불 판정 | K패스 후불 |
|---|---|---|
| `cardPendingByCard()` | **거래** (`txnIsCredit`) | 39,500 셌다 |
| `cardCycles()` | **카드 종류** (`c.type!=='credit'`) | 버렸다 |

현황판 목록·선결제 화면은 후자를 쓴다.
⚠️ **v2.11 과 같은 자리에서 같은 실수**(그때는 현황판 2,102,213 vs 선결제 756,750, 134만 차이).
반복결함으로 올린다 → **#11 같은 질문에 답하는 함수가 둘이면 반드시 갈라진다.**

### 🔒 확정 규칙
**후불 여부는 카드 종류가 아니라 거래가 답한다** (`txnIsCredit(t)` 하나).
카드 종류는 **기본값**일 뿐이다. 체크카드에도 후불이 붙는다(K-패스 후불교통) — 한 장이 두 성격을 갖는다.

### 고친 곳 5 + 삭제 1
1. `cardCycles()` — 결제분 분해가 거래 기준으로
2. `monthCashPL()` — 후불은 거래월이 아니라 **결제월**에 나간다(체크카드 후불도)
3. 현황판 목록 — '신용카드인가' → **'낼 돈이 남았나'**. 체크카드 후불분엔 `체크·후불분` 칩
4. 선결제 화면 — 같은 기준. K패스도 선결제 대상
5. 카드 설정 줄 — 후불 거래가 있으면 '즉시결제'가 아니라 미결제 금액 + `후불분 있음`
6. 🗑 `creditCard(name)` 삭제 — 쓰는 곳은 없는데 **'카드 종류가 후불을 정한다'는 틀린 규칙이 거기 박혀** 코드 5곳에 복사됐다
- 후불 거래가 있는 체크카드는 **마감일·결제일을 고칠 수 있게** 했다(기본값 10/25 에 묶이면 실제 청구일과 어긋난다)

### ✅ 검증
`run.sh all` **25개 전부 통과**(264초). `t40` J블록 **18건 신설**(73건), `t42` W·X블록 **52건 신설**(283건).
스크린샷: 현황판 카드 미결제 · 카드 설정 · 선결제 모달 (PC 1440 / 모바일 400) — 눈으로 확인.
스냅샷 **#103** (manual, 배포 직전). `index.html` sha `79e1c625…` 로 커밋 대기.

⚠️ **불확실 — 로한이 확인할 것 한 가지.**
K패스는 `closeDay` 가 없어 **기본값 10일 마감 / 25일 결제**가 먹는다.
그래서 39,500 이 **9월 결제분 18,000(7건) + 10월 결제분 21,500(3건)** 으로 갈린다.
실제 K-패스 후불교통 청구 주기가 10일 마감인지는 **모른다.** 다르면 계좌·부채 → 카드 → 우리(K패스) 수정에서
마감일·결제일을 실제 값으로 바꿔라(v3.8 에서 체크카드도 이 칸이 열린다).

---

## 18. 🟢 검증을 세션 밖으로 — GitHub Actions CI (2026-09-16)

**로한 지시**: *"근데 저번에도 테스트 러너가 한번 막히던데 왜 자꾸 그런 거야? 시스템적으로 해결해버려."*

### 원인 — 테스트 러너가 막힌 게 아니다
에러 문구가 정확히 이것이었다:
```
claude-sonnet-5[1m] is temporarily unavailable (timed out),
so auto mode cannot determine the safety of Bash right now.
```
자동승인 모드에서는 **명령을 실행하기 전에 별도의 작은 모델이 안전성을 검사**한다.
그 검사기가 죽으면 **명령 실행 전부가 막힌다.** 테스트·컨테이너·플레이라이트·네트워크는 다 정상이었다.
증거: 그 시간에 파일 읽기·수정, `device_bash`(로한 PC 쪽 셸), Supabase 조회는 **전부 정상 동작했다.**
9/14 에도 같은 문구였다. **제작방이 고칠 수 있는 층이 아니다.**

### 🔒 해결 — 검증을 세션에 두지 않는다
`.github/workflows/test.yml` 신설. **푸시하면 깃허브가 25개 스위트를 돌린다.**
- 커밋 옆에 ✓/✗ 가 붙는다. GitHub Desktop 에서도 보인다
- 실패하면 `test-logs` 아티팩트에 스위트별 로그가 남는다
- 손으로도 돌린다: GitHub → Actions → tests → Run workflow
- 부수효과가 더 중요하다 — **로버트가 "통과했다"고 잘못 말해도 CI 가 걸러낸다**

⚠️ `.github/` 는 원격 도구가 **쓸 수 없는 보호 경로**다(CI 는 코드를 실행하니 당연하다).
**이 두 파일은 로한이 직접 넣어야 한다** — 대화창 파일 카드로 보냈다.

### 이를 위해 고친 것 (이식성)
- 테스트 25개 전부에 박혀 있던 `executablePath:'/opt/pw-browsers/chromium'` 을
  `process.env.CHROME` → `/opt/pw-browsers/chromium`(있으면) → playwright 번들 순으로 바꿨다.
  🔒 **테스트 파일에 절대경로를 박지 마라.** 박으면 그 기계 밖에서 안 돈다
- `run.sh` 가 `/opt/pw-browsers/chromium` 이 있을 때만 `CHROME` 을 export 한다
- `t20.js` 가 `/home/claude/state_rw.json` 을 하드코딩하고 있었다 → `__dirname` 기준으로
- ⚠️ **[결함·테스트] t42 V8 이 1/2048 확률로 거짓 실패했다.** 동점 2건의 순서를 12회 표본으로
  검사해서, 전부 같게 나오면 실패로 찍혔다. 오늘 실제로 한 번 떴다 → 표본 40회로(약 2e-12).
  🔒 **거짓 빨간불이 한 번 뜨면 그 다음부터 아무도 CI 를 안 본다.** 랜덤이 들어간 검사는 표본을 계산해라

### 그래도 막혔을 때의 절차 (오늘 이 순서로 처리했다)
1. 에러 문구에 `cannot determine the safety` 가 있으면 **검사기 장애다.** 코드를 의심하지 마라
2. 재시도는 **3회까지.** 그 이상은 낭비다
3. 읽기·`device_bash`·Supabase 는 살아 있다 → **문서·핸드오프·DB 작업을 먼저 끝낸다**
4. STATUS 와 핸드오프에 **'미검증'** 이라 박는다. 🚫 통과했다고 쓰지 않는다
5. `send_later` 로 20분 뒤 재시도를 예약한다 — 예약은 검사기와 무관하게 깨어난다
6. 로한에게는 **한 줄.** "내 쪽 도구 문제, 로한이 할 건 없다"

---

## 19. v3.9 — 📖 문법 드릴 신설 · 🔤 폰트 Pretendard 통일 (2026-09-16)

### 📖 문법 드릴 — v5 의 "문법은 앱이 못 만든다"를 뒤집었다
학습방이 접속사 예문 **19건**(`sents`, p199~200)을 올렸다. 데이터 검사 통과:
id 중복 0 · `ja`/`ko` 누락 0 · **`target` 이 `ja` 안에 실제로 있는 것 19/19** · `opts` 3개 19/19 · `why` 19/19.

근거: **N4 시험 문장에는 어절 띄어쓰기가 있다**(「雨が 降って いました。」).
띄어쓰기가 있으면 어순 문제가 문자열 split 으로 끝난다.
🚫 **독해·청해는 여전히 못 만든다** — 지문 길이와 음원이 필요하다.

모드 둘:
- **접속사 채우기**(もんだい3형) — `target` 을 가리고 `opts` 3개로 4지선다
- **문장 만들기**(もんだい2형) — 연속 4어절을 골라 섞고 **★ 자리**에 오는 것을 묻는다.
  ★는 2~3번째다 — **양 끝을 물으면 조사만 보고도 맞힌다**

🔒 **가리기는 어절 경계 우선이다.** 단순 첫 등장 치환이면 「では」를 물을 때 같은 문장의
「それでは」 **안쪽이 먼저 걸려 엉뚱한 자리가 가려진다.** 실데이터에 그 쌍이 둘 다 있다(s008·s017).
→ 어절 토큰이 `target` 으로 **시작**하는 자리만 가린다. t42-Y9/Y10 이 잠갔다.

🔒 **답하기 전에 한국어 번역을 보여주지 않는다.** 번역에 「그래서」가 박혀 있으면
**정답이 번역에 그대로 적혀 있는 셈이다.** 번역은 채점 후, 해설(`why`)은 **틀렸을 때만**.

🔒 **문항은 시작할 때 한 번 만들어 세션에 박는다.** 채점마다 다시 만들면 보기가 춤추고
어순 문제는 정답 자리까지 바뀐다(단어 드릴의 `opts` 와 같은 규칙).

🔒 **카운터를 단어와 같은 필드로 뒀다**(`seen`·`miss`·`streak`·`lastSeen`).
그래서 출제 가중식 `stWordScore()` 와 채점·기록 경로 `stDrillRecord()` 를 **그대로 공유한다.**
가중식을 두 벌 만들면 두 화면이 다른 순서를 낸다(원칙 2).

🔒 **탭을 새로 만들지 않았다** — 400px 에서 5개는 글자가 잘린다(v3.7 확정).
「🈶 단어」를 **「🈶 드릴」**로 바꾸고 단어 위·문법 아래로 한 탭에 담았다.

### 🔤 폰트 — "싹다 프린텐다드로. 볼드는 프리텐다드 볼드로"
- Google Fonts(Cinzel·고운바탕) 제거 → **Pretendard Variable 동적 서브셋**(jsDelivr)
- 🔒 **동적 서브셋**을 쓴다. 한글 static 은 웨이트마다 수 MB 라 전부 받으면 모바일 첫 화면이 늦는다
- 🔒 `font-synthesis:none` — 브라우저가 굵게 **'그려서' 흉내내지 못하게** 막았다.
  variable 이라 웨이트가 45~920 연속이고 **볼드가 진짜 Pretendard Bold** 로 나온다
- 🔒 글꼴은 **`--sans` · `--ja` · `--serif` 세 변수로만** 정한다.
  ⚠️ 이번에 **SVG 안에 폰트 이름이 직접 박힌 자리가 하나 있었다** — 변수를 안 쓰면 이렇게 빠진다.
  t42-Z8 이 'CSS 에 폰트 이름을 직접 박은 규칙 0개'를 잠갔다
- 🇯🇵 **일본어는 `--ja` 로 따로 잡았다.** 한국 폰트의 한자 자형으로 JLPT 한자를 외우면
  **시험지의 자형과 다른 걸 외운다**(直·骨·青처럼 한·일 자형이 갈리는 글자가 있다).
  체인: `Pretendard JP Variable` → OS 일본어 폰트(Yu Gothic·Hiragino·Noto Sans JP) → Pretendard
  ⚠️ **불확실**: Pretendard JP 의 가나 커버리지를 이 환경에서 실측하지 못했다(페이지의 CDN 요청이 막힌다).
  그래서 체인 뒤에 OS 폰트를 둔다 — 빠지는 글자는 거기서 채워진다
- ⚠️ **명리·일지 제목의 세리프가 사라졌다.** 의도된 분위기였지만 로한 지시("싹다")를 따랐다.
  되돌리려면 `--serif` **한 줄**이다(쓰는 곳 29군데는 그대로 둔다)

### 검증
`run.sh all` **25개 전부 통과**(286초). t42 **337건** — Y블록(문법) 43건 · Z블록(폰트) 11건 신설.
스크린샷: 문법 시작칸 · 접속사 문항 · 해설 화면 · 어순 문항 · 결과 (PC/모바일) — 눈으로 확인.
스냅샷 **#104**. 🟢 CI 도 같은 걸 돌린다(첫 실행 초록 확인됨 · 4분 31초).

### 남은 문법 유형 — 데이터가 오면 바로 열린다
- **조사**(`kind:"particle"`) — もんだい1 의 62%. **오답을 앱이 자동 생성할 수 있는 유일한 유형**
  (を·が·に·で·と·へ·から·まで·の·も·は 풀에서 뽑는다). 1순위
- **기능어·문형**(`kind:"form"`) — 오답 자동 생성 **불가**. `opts` 를 학습방이 손으로 채워야 한다

---

## 20. v3.91 — 폰트 CDN 이 앱 스크립트를 막던 잠재 결함 (2026-09-17)

> 🔴 **[내 오류 정정]** 나는 이걸 **로한이 신고한 "클릭 먹통"의 원인이라고 단정해서 이 문서에 박았다.
> 틀렸다.** 로한에게 되물어 보니 **"내용 다 보였고 아무것도 안 눌림"** 이었다.
> 화면이 그려졌다는 건 그 스크립트가 **이미 실행됐다는 뜻**이다 — 스크립트가 막혔다면 로그인
> 박스(정적 HTML)에서 멈췄어야 하고 학습 페이지는 그려지지도 않는다. **증상이 진단을 반증한다.**
> 🔒 재현에 성공한 것과 **신고된 사고를 재현한 것**은 다르다. 나는 전자를 후자로 착각했다.
> 신고 원인은 **아직 못 찾았다** — §22 에 미해결로 남긴다.

**로한 신고**: "모바일 크롬으로 로한북 하는데, 학습 페이지에서 클릭해도 반응이 없더라."
→ 조사 중에 **별개의 진짜 결함**을 하나 발견했다. 아래는 그것이다(신고 건과 무관).

### 발견한 잠재 결함 — head 의 stylesheet 가 본문 스크립트를 막는다
v3.9 가 넣은 Pretendard 폰트 두 개가 `<link rel="stylesheet" href="...jsdelivr...">` 로
head 에 들어갔다. **브라우저는 head 의 stylesheet 요청이 안 끝나면 그 뒤의 동기 스크립트를
실행하지 않는다**(스펙 동작). 이 앱은 클릭 핸들러·렌더러가 전부 그 스크립트 한 덩어리 안에 있어서,
CDN 이 느리면 **로그인 화면에서 멈춘 채 아무것도 안 눌리는** 구간이 생긴다.
로컬 정적 서버로 CSS 응답을 30초 지연시켜 재현했다 — 그 구간에 버튼을 누르면
`ReferenceError`(함수 미정의)만 나고 화면은 그대로였다.
⚠️ 단, 그 증상은 **로그인 화면**에서 나타난다. 로한이 본 것은 학습 페이지였다.

### 🔒 수정
두 폰트 `<link>` 를 `rel="preload" as="style" onload="this.rel='stylesheet'"` 로 바꿨다
(+ `<noscript>` 폴백). 리소스는 그대로 받지만 **본문 스크립트를 막지 않는다**.
로한이 원래 의도한 "CDN 막히면 조용히 폴백"이 **폰트 렌더링엔 지켜졌지만 스크립트 실행엔
안 지켜지고 있었다.** 같은 재현으로 CSS 30초 지연 상태에서도 클릭이 즉시 동작함을 확인했다.

### ✅ 검증
`run.sh all` **25개 전부 통과**(360초 · t42 **337건** · t40 73건) — 로한 PC 에서 직접 돌렸다(§21).
t42 Z블록(폰트)은 `--sans`·`--ja` **CSS 변수 값**을 보지 CDN 로딩 방식을 보지 않아 영향이 없다.

---

## 21. 🟢 테스트를 로한 PC 에서 직접 돌린다 (2026-09-17, 클로드 코드 전환)

**로한 지시**: *"설치가 필요한 게 있으면 요구해라. 머뭇거리지 마라. 이제 여기서 거의 모든 걸 해결해야 한다."*

그 전까지는 이 PC 에 Node 가 없어서 검증을 클라우드 셸이나 CI 에만 의존했다. 이제 로컬에서 돈다.

| 항목 | 값 |
|---|---|
| Node | **v24.19.0** (winget `OpenJS.NodeJS.LTS`) · npm 11.17.0 |
| Playwright | **1.56.0** — 🔒 **CI 와 같은 버전으로 못 박는다**(`.github/workflows/test.yml` 과 동일) |
| 설치 위치 | `C:\Users\bhj95\Documents\claude\_testenv` — 🔒 **레포 바깥이다** |
| 크로미움 | `%LOCALAPPDATA%\ms-playwright\chromium-1194` (전역, 영구) |

🔒 **레포에 `node_modules` 를 두지 않는다.** CI 워크플로가 명시한 원칙이고, 레포에 넣으면
`.gitignore` 에 없어서 GitHub Desktop 에 수만 개 파일이 뜬다. 그래서 형제 폴더 `_testenv` 에
설치하고 `NODE_PATH` 로 물린다.

### 돌리는 법 (bash)
```bash
export PATH="/c/Program Files/nodejs:$PATH"
export NODE_PATH="C:\\Users\\bhj95\\Documents\\claude\\_testenv\\node_modules"
export HTML="C:/Users/bhj95/Documents/claude/showmethemoney/index.html"
JOBS=4 bash tests/run.sh all      # 360초 · 25개
```
- ⚠️ `PATH` 를 매번 export 해야 한다 — 클로드 코드의 셸은 세션 시작 시점의 PATH 를 물고 있어서
  Node 설치 뒤에도 `node: command not found` 가 난다
- ⚠️ `HTML` 을 안 주면 `run.sh` 가 없는 `work.html` 을 찾는다. `bash tests/run.sh` 로 부른다
  (`./run.sh` 는 git index 의 실행권한 때문에 안 된다 — CI 주석과 같은 이유)
- 🔒 `CHROME` 은 **비워 둔다** — playwright 가 제 번들을 쓴다

---

## 22. 🔴 미해결 — 모바일 "다 보이는데 아무것도 안 눌린다" (2026-09-17 신고)

**아직 원인을 못 찾았다.** 추측으로 닫지 않는다.

### 확인된 사실
- 증상: 학습 페이지 **내용이 전부 보이는 상태**에서 탭·날짜·버튼이 **전부** 반응 없음 (모바일 크롬)
- 로한은 **새로고침을 안 해봤다** — 복구되는 성질인지 모른다
- 🚫 **폰트 CDN 블로킹 아님**(§20) — 그거면 로그인 화면에서 멈춘다
- 🚫 **뽀모 오버레이 아님** — 실측 `ui.pomoRun` 이 `null` 이다
- 🚫 **데이터 이상 아님** — 실측으로 `phases` 4건 정상, `units` 22건 `day` 형식 오류 0,
  `logs`·`week` 키 정상, 9/17 드릴 2건이 정상 기록됨(그 시점엔 앱이 멀쩡히 동작했다는 증거다)
- 마지막 저장 `updated_at` = **2026-09-17 19:43 KST**. 사고는 그 언저리다
- ⚠️ `ui.stCalSel` 이 **9/16 에 멈춰 있다** — 9/17 에 날짜를 눌렀는데 저장이 안 됐거나, 안 눌렀거나

### 남은 가설 (우선순위)
1. **메인 스레드 정지** — 그러면 그려진 화면 그대로 굳고 탭이 전부 죽는다. 증상과 가장 잘 맞는다.
   ⚠️ 가드 없는 `while` 이 둘 있다: `stMinsRange`(3319) · `weeksOfMonth`(5723).
   이 파일의 다른 루프는 전부 `guard++<N` 을 다는데 이 둘만 없다.
   지금 호출자 인자(전부 `todayStr()` 파생)로는 안 터지지만 **가드가 없는 건 사실이다**
2. **크롬의 탭 폐기(discard)** — 안드로이드 크롬이 백그라운드 탭의 렌더러를 죽이고 **옛 화면을
   그림처럼 남긴다.** 그 상태는 앱 버그가 아니고 새로고침으로만 풀린다. 그래서 새로고침 여부가 중요하다
3. **보이지 않는 오버레이** — `#ov`(z-index 20) 가 내용 없이 떠 있으면 탭을 전부 먹는다.
   단 바깥 탭에 닫히게 돼 있어 자가복구돼야 한다

### 🔒 다음에 또 나면 이것부터
1. **새로고침해서 되는지** 확인 (가설 2 판별 — 이거 하나로 절반이 갈린다)
2. 되돌아오기 전에 **화면 캡처**
3. 🚫 재현 못 한 채로 원인을 단정하지 마라. §20 에서 내가 그 실수를 했다
