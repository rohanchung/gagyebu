# 🗂️ HANDOFF — 로한 ↔ 로버트 인수인계 로그

> **이 파일이 규칙이다.** 새 방(대화)을 열면 로한이 "showmethemoney 폴더에서 **가장 최신 handoff_MMDD.md** 랑 index.html 읽어"라고 말한다.
> 로버트는 압축된 기억에 의존하지 말고 **이 파일을 진실의 원천(source of truth)** 으로 삼는다.
> **최종 갱신 2026-08-25 · 앱 v1.61** — 상세 로그는 8/25, 그 이전은 요약(원문은 `handoff_0824.md`).

---

## 📜 프로토콜 (로버트가 지킬 규칙)

1. **새 방 시작 시 — 다 읽지 마라. 이 표에서 필요한 것만.** (2026-08-21)
   - **`handoff_MMDD.md`(이 파일, 최신 날짜 하나)는 무조건.** 유효한 상태는 전부 여기 있다.
     옛 날짜 파일은 상세 로그 원문 보관용이라 안 읽어도 된다.
   - 나머지는 **건드릴 영역이 정해진 뒤에** 해당 줄만 편다.

   | 건드릴 영역 | 추가로 읽을 것 | 크기 |
   |---|---|---|
   | 목표·루틴·보상·데일리·버킷 | `goal-design.md` + `integration-design.md` | 46KB |
   | 가계부·카드·할부·미결제 | `card-design.md` | 9KB |
   | 현금흐름·계좌 기초잔액 | `cashflow-design.md` + `0901_기초잔액_정합계획.md` | 16KB |
   | 보상↔가계부, 명리 권장일 등 파트 교차 | `integration-design.md` | 20KB |
   | 초기 스펙이 궁금할 때만 | `요구사항정의서.md` | 15KB |
   | **코드를 고칠 때 (항상)** | `index.html` — **통째로 읽지 말고 `grep` 으로 함수 위치만** | 424KB |
   | **테스트를 돌릴 때 (항상)** | `tests/README.md` → `./run.sh <영역>` | 4KB |

   ⚠️ **`index.html` 을 통째로 읽는 것은 낭비다.** 4,774줄 · 440 함수다.
   `grep -n "function 이름\|클래스명"` 으로 위치를 잡고 그 구간만 `sed -n` 으로 본다.
   ⚠️ **왜 이렇게 바꿨나**: 예전 프로토콜은 *"순서대로 다 읽어라"* 라 매 방마다 85KB를 읽었다.
   0821 카드 작업에 `goal-design.md` 26KB 는 한 글자도 안 쓰였다.

2. **데이터가 필요하면** → Supabase MCP로 조회 (아래 접속정보). 앱 화면 추측 금지, 실제 조회.
3. **날짜가 바뀌면** → 이 파일을 `handoff_(새날짜).md`로 복사하고, 맨 위 로그에 새 항목만 얹는다. 옛 파일은 남겨둔다(히스토리).
   - **⚠️ 작업 시작 전에 오늘 날짜부터 확인한다.** 로한이 말해주기를 기다리지 않는다. (8/19 작업이 통째로 `handoff_0818.md`에 들어가 파일이 28KB로 불어난 사고가 있었다.)
   - **로그 상세는 최근 2일치만 유지.** 그 이전은 **한 줄 요약 + 옛 파일 포인터**로 압축한다. 압축해도 되는 이유 = 설계 근거는 `goal-design.md` / `integration-design.md` / `cashflow-design.md`에 남아 있고, handoff는 **지금 유효한 상태**를 전달하는 문서지 아카이브가 아니다.
   - 목표 크기 **30KB 이하.** 넘으면 압축을 안 한 것이다. (0820 = 약 20KB)
4. **⚠️ UI/UX 기본 원칙 — 구현 전에 이걸 먼저 통과시킨다.** (2026-08-19 로한 질책: *"구현은 잘하는데 디자인 기본원칙을 안 지킨다"*)
   - **클릭되는 것은 클릭처럼 보인다.** 경계 + `cursor:pointer` + **hover/active 반응** + 의도 라벨. 넷 중 하나라도 빠지면 사용자는 그게 버튼인 줄 모른다.
   - **버튼은 위, 내용은 아래.** 목록형 모달의 액션(추가·설정)은 헤더 바로 아래 액션 바에 두고 선으로 가른다. (폼 모달의 '저장'은 입력 흐름의 끝이라 하단이 맞다.)
   - **버튼 옆에 긴 설명을 인라인으로 붙이지 않는다.** 버튼이 문장 속에 묻힌다. 설명은 아래 줄로.
   - **손가락 크기.** 아이콘 액션의 히트 영역은 최소 26px. 11px 글자를 누르라고 하면 안 된다.
   - **스크롤되는 모달의 헤더는 sticky.** 닫기(×)를 잃으면 안 된다.
   - **포커스 링(`:focus-visible`)은 지운 적이 없어야 한다.**
   - **왼쪽 = 큰 범위, 오른쪽 = 조작.** 단 모바일에서는 '자주 쓰는 것 먼저'가 이긴다.
   - **진행바는 남는 가로를 쓰는 요소다.** 세로 한 줄을 따로 잡아먹게 두지 않는다.
   - **🔒 CSS base 규칙 누락 점검**: 클래스를 새로 쓰면 `.name{` base 규칙이 실제로 있는지 확인한다.
     `.name.modifier` 만 있고 base가 없으면 아무 스타일 없이 렌더된다 — v7.1 `.tcard`, v8.4 `.rwpick` 두 번 겪었다.
5. **응답 원칙**: 로한 선호 = 결론→왜→다음. 아부·보험멘트 금지. 불확실하면 "불확실" 명시. 로버트는 재정 조언 시 "면허 있는 자문가 아님" 밝힘.
6. **로버트 = 나(Claude)의 이름.**
7. **⚠️ 버전 체계 (2026-08-20 로한 확정).**
   - **`v1.0` = 정식.** 8/11~8/20 개발 이터레이션(v1.1~v12.2)은 여기서 마감했다.
   - **기능 추가 = 0.1 단위** (v1.0 → v1.1) · **세부 수정 = 0.01 단위** (v1.1 → v1.11 → v1.12).
   - `APP_VERSION` 과 `CHANGELOG` 를 **항상 같이** 올린다. 푸터는 `APP_VERSION` 을 동적 참조한다.
   - CHANGELOG 안에 `{v:'—'}` 구분 행이 있고 그 아래는 옛 이터레이션 로그다. 건드리지 않는다.
8. **결과물은 폴더에 넣는 것이 목적이다. 대화창에 파일을 띄우는 건 수단일 뿐.** (2026-08-19 로한 지시)
   - 로한은 받자마자 푸시해서 실사용한다. 파일 카드 자체는 아무 쓸모가 없다.
   - **작은 텍스트 파일(handoff·설계문서)** → `device_bash`로 디바이스에서 직접 작성/패치. 카드 안 뜬다.
   - **`index.html`처럼 큰 파일** → 클라우드에서 수정·검증 후 `SendUserFile` → `device_commit_files`.
     ⚠️ **이 경로는 대체 불가다.** 컨테이너와 디바이스는 파일시스템이 분리돼 있고,
     바이트를 옮기는 다리가 `file_uuid` 하나뿐이다. `device_bash`로 300KB를 넣으려면
     내용을 전부 내 출력으로 타이핑해야 해서 토큰이 수만 단위로 든다. **카드 1장이 훨씬 싸다.**
   - **반드시 양쪽 sha256을 대조한다.** 체크섬이 맞아야 "검증한 그 파일이 디스크에 놓였다"가 성립한다.
   - 완료 보고는 **텍스트로만** — 무엇을 왜 고쳤는지 + 커밋 메시지. 파일 설명은 쓰지 않는다.
9. **⚠️ 테스트는 영역만 돌리고, 배포 직전에만 전체를 돈다. (2026-08-21)**
   - `HTML=/경로/work.html ./run.sh <영역>` — `quick`/`money`/`goal`/`time`/`data`/`css`/`all`. 표는 `tests/README.md`.
   - **실측: 직렬 전체 114초 → 병렬 전체 44초 → 영역만 10~34초.** 한 업데이트에 서너 번 도니 검증만 8분이었다.
   - 🔒 **`all` 은 배포 직전에 예외 없이 한 번.** 영역 태그는 경험칙이지 보증이 아니다.
   - 🔒 **`index.html` 을 통째로 읽지 마라.** grep 으로 위치 잡고 그 구간만 본다.
10. **⚠️ 테스트에 상수를 박지 마라 — 경로도 날짜도. (0820·0821 연속으로 당함)**
   - 0820: `t_reg`/`t14`/`t18`/`t19`/`t20` 이 `/home/claude/work.html` 을 박아둬 **새 세션에서 통째로 안 돌았다.**
   - 0821: `t23` 이 `'2026-08-20'` 을 박아둬 **하루 지나자 2건이 빨간불.** 코드는 멀쩡했다.
   - 🔒 **HTML 은 `process.argv[2]`, 시드는 `path.join(__dirname,...)`, 날짜는 실행 시점에 계산한다.**
     `TODAY`/`DAYS(n)` 패턴은 `t23.js` 상단에 있다. 브라우저로 넘길 땐 **인자로 전달**해야 한다(Node 변수는 페이지에 없다).
   - 🔒 **테스트가 빨간불이면 코드부터 의심하되, 상수부터 확인한다.** 둘 다 안 하면 시간을 버린다.
11. **⚠️ 테스트는 '빈 상태'부터 먹인다. (2026-08-20 v1.1에서 확정)**
   - 회귀 테스트(`t_reg.js`)의 고정 STATE 가 **정상 데이터를 다 갖춘 상태**였다. 그래서 SEED 키 누락 7건 + `d.ui` 누락이
     9일 동안 전부 초록불이었다. **테스트가 통과했다는 건 '정상 상태에서 통과했다'는 뜻일 뿐이다.**
   - 🔒 새 기능·마이그레이션을 건드리면 **`t22.js` 를 반드시 돌린다** — 서버 행 없음 / `{}` / `schemaVersion`만 /
     구버전 유사 / `ui` 없음, 5종으로 15탭을 순회한다. 빈탭 0 · 에러 0 이어야 한다.
   - 🔒 **`DB.x.y` 형태로 새 필드를 읽기 시작하면, 그 자리에서 `migrateV6` (B)층에 존재 보장을 넣는다.** 나중에 하면 안 넣는다.
   - 마이그레이션 함수는 **둘**이다: `loadState` → `migrateDB`(health·saju·journal·posts·activity) → `migrateV6`((A)1회성 / (B)매 로드).
     헷갈리면 **`migrateV6` 의 (B)층**이 정답이다.
12. **저장 경로(`save`/`doSave`/`syncConflict`/`forceSave`)를 고칠 때의 불변식.** (v1.1)
   - **저장은 조용히 성공하고 시끄럽게 실패해야 한다.** 실패를 삼키는 빈 에러 콜백을 다시 넣지 않는다.
   - 문서 전체를 덮어쓰는 구조이므로 **`updated_at` 조건 없이 upsert 하는 코드를 되살리지 않는다** — 두 기기 동시 사용 시 통째 소실.
   - 단, **판단이 애매하면 저장을 성공시킨다**(0행인데 서버 버전이 내 것과 같으면 그냥 쓴다). 잠금 때문에 입력을 잃는 건 더 나쁘다.
13. **🚫 이 폴더에서 `git` 명령을 절대 실행하지 마라. (2026-08-20 사고)**
   - `device_bash` 는 마운트된 폴더에서 **파일을 지울 권한이 없다.** 그래서 `git status` 한 번만 돌려도
     git 이 만든 `.git/index.lock` 을 **git 자신이 해제하지 못하고 남긴다.**
   - 결과: 로한이 GitHub Desktop 에서 커밋하면 `A lock file already exists in the repository` 로 막힌다.
     **읽기 전용인 `git status` 조차 이 사고를 낸다.** `git log` 도 마찬가지로 의심하라.
   - 커밋 여부·변경 목록이 궁금하면 git 대신 **파일 타임스탬프(`ls -la`)와 sha256** 으로 판단한다.
   - 이미 걸렸다면: `mv .git/index.lock .git/index.lock.dead` (삭제는 안 되지만 이동은 된다). 그 뒤 로한에게 재시도 요청.
   - **커밋과 푸시는 로한이 GitHub Desktop 으로 한다. 내 일이 아니다.** 나는 파일을 놓고 커밋 메시지를 써주는 데까지다.
14. **작업 완료 시 항상 커밋 메시지를 써줄 것.** GitHub Desktop 기준 **Summary**(한 줄, `v버전 — 무엇을`) + **Description**(변경 항목 불릿 + 검증 결과 + 주의사항). 로한이 복붙만 하면 되게.

---

## 🧭 시스템 지도

> **DB 테이블은 둘이다.** `app_state`(현재 상태, 유저당 1행) / `app_state_snap`(스냅샷, v1.2 신설, RLS 동일). 둘 다 같은 프로젝트 안이다.

**앱**: "Rohan Book" (구 가계부). 단일 `index.html` SPA. 재정+건강+명리(사주)+일지 통합. 현재 **v1.0 (정식)**.
**배포**: GitHub Pages, 공개 repo `rohanchung/gagyebu`. (넷리파이에서 이관함 — 무료티어 배포 막힘 이슈)
- 코드 변경 → **commit + push** 필요 (GitHub Desktop).
- 데이터 변경 → Supabase 직접 쓰기, **커밋 불필요.**

**Supabase**: project_id `ytkbrdgbklnijbwkvino`. 단일 `app_state.data` jsonb 행에 전 상태 저장. `migrateDB()`로 스키마 진화.

**스킬 3개 (모든 방에서 발동)**:
- `load-my-finance-health` — 재정·건강 현황 스냅샷
- `evaluate-my-saju` — 사주 세운/월운/일운 평가 (고정 원국)
- `check-account-security` — 계정 로그인 이력 점검

---

## 👤 로한 프로필 (고정 사실)

- IT기획자 출신. 기획·사업·마케팅·창작 병행.
- 명리 앱은 **결정론적 만세력 엔진** 자체 구현(API 없음): Julian Day 일진 + Meeus 태양황경 근사 절기/월주 + 60갑자.
- **건강**: 키 **163cm** (BMI 역산 확정). 체중 하락 추세(약 78→68kg대). 8/5 기준 68.2kg. 피검사 21회 × 77지표 임포트됨.

---

## 💰 재정 로드맵 (2026-08 확정, 은행 실제숫자로 갱신 예정)

- **풍무점 이전 = 2027년 1월** (⚠️ 옛 handoff 의 '2026년 10월'은 틀렸다 — 로한 정정 8/24)**.** 월세 아끼려는 게 아니라 출근지 이동. → 보증금 **500~1000만** 필요, 그중 **현금 500 확보가 관건.**
- **부채 금리 우선순위**: 약관대출 **5.5%** > 정책자금(소상공인대환) **3.11%** > 세이프박스 ~2%.
  - → **약관대출 먼저 상환.** 정책자금은 일정대로만 납부.
- **로드맵**: 2026 = 보증금용 저축(현실적 상한 ~500만, 1천만은 비현실) · 2027 = 약관대출 상환 + 신용 구축 · 2028 = 전세 점핑(1억 오피스텔은 현금 1~2천 필요, 지금은 불가).
- 신용점수 900+ 안정. 약관·정책 대출은 신용 영향 미미.
- ✅ **앱 부채 계산 교정 완료 (8/14)**. 소상공인대환 실제 방식 = **원금 고정 416,000원**(1회차 448,000, 단수 조정) + **이자 = 잔액 × 3.11% × 경과일수 / 365**(일할). 납입일 매월 19일, 주말이면 익영업일. 총 48회 · **총이자 1,269,938 · 총상환 21,269,938 · 월평균 443,124**. 앱 `bankSchedule()`이 은행앱 6회차와 원 단위 일치.
- 약관대출 잔액 **1,990만 @5.5%** (SEED도 실제값으로 동기화).

---

## 🧾 날짜별 로그 (최신이 위)

### 2026-08-26 (v1.9)

- **↕ 데일리 정렬 토글.** ⚠️ **[내 판단 착오] v1.8 의 순서가 데일리에 '그룹째' 반영됐다.**
  로한: *"루틴에서 순서를 조정하면 여기에 반영이 안된다. 서로 연동되어야지 임마."*
  - **코드는 내가 의도한 대로 돌았다. 그 의도가 틀렸다.** 실측 확인: 로한이 `E.8a(7시 기상)` 을 order=1 로 올렸는데
    데일리가 목표별로 묶는 바람에 **E 그룹 전체**(E.8a·E3·E.8b)가 통째로 맨 위로 갔고,
    같은 그룹인 `E.8b(24시 전 취침)` 까지 아침 루틴들 사이에 끼었다.
  - 🔒 **로한이 순서를 매기는 이유는 '하루의 시간 흐름'이지 목표 묶음이 아니다.**
    7시 기상 → 유산균 → 스트레칭 → 학습. 그룹핑이 그 축을 이기면 순서를 정하는 의미가 없다.
  - → `[↕ 순서대로 | 🎯 목표별]` 토글(`DB.ui.dailyGroup`). **기본은 순서대로** — 설계 순서를 그대로 일렬로 복사.
    목표 연계는 각 줄의 코드·눈금 뱃지가 이미 하고 있어 그룹 머리글이 없어도 죽지 않는다.
  - 🔒 **v1.8 에서 내가 "그룹째 움직인다"를 '의도된 동작'이라며 테스트로 잠갔다.**
    잠근다고 옳아지지 않는다. 테스트가 설계 실수를 굳히는 데 쓰였다 — 같은 실수를 반복하지 마라.

- **🕯️ 일진 길/흉/평 + 말풍선** (로한: *"길, 흉, 평 이걸 표시 + 여기서도 말풍선이 보였으면 · 사주페이지랑 연동"*)
  - 🔒 **판정이 `renderSaju` 안에 인라인으로 박혀 있어 데일리가 못 썼다.** `dayVerdict()` / `dayVerdictBadge()` 로 분리.
    이제 두 화면이 **같은 함수**를 쓴다. `t30` J14 가 사주 페이지에 판정 코드가 남아 있지 않은지까지 확인한다.
  - 데일리 상단: `凶/吉/平` + 간지 + 걸린 것(충·형·합) + 💬. 근거 전문은 툴팁.
  - 💬 키는 사주 페이지와 **같은 `day:YYYY-MM-DD`** — 이미 저장된 `day:2026-09-03`(고대 진료),
    `day:2026-09-24`(추석 붕괴 경보) 등이 그 날 데일리에서 바로 뜬다.
  - ⚠️ **명리는 유파마다 판정이 다르다.** 지금 규칙은 *"충·형 계열이 있으면 凶 / 합 계열만 있으면 吉"* 이라는
    단순 규칙일 뿐 확정된 길흉이 아니다. 그래서 **근거를 항상 같이 노출**한다. 임의로 가중치 점수를 만들지 마라.
  - ⚠️ `⚔`/`⚜` 이모지는 폰트에 따라 두부(□)로 뜬다(컨테이너에서 실제로 그랬다). **한자만 쓰고 색으로 구분.**

- **⚠️ [테스트 결함] `t28` 이 `renderDay(todayStr())` 로 카드결제 행을 찾고 있었다.**
  v1.9(=v1.8 결함 수정)부터 카드결제가 오늘이 아니라 **실제 결제일**에 들어가므로 못 찾는다.
  거래의 `date` 를 읽어서 열도록 수정. 🔒 **또 날짜 가정이다** — 0820·0821·0825에 이어 네 번째.

### 2026-08-25 (v1.8)

- **↕ 루틴 순서 ▲▼** (로한: *"순서를 내가 조정할 수 있었으면 좋겠다. 조정 시 캘린더나 기타 다른 부분도 순서가 바뀌어야 된다."*)
  - ⚠️ **[결함] 세 화면이 서로 다르게 정렬하고 있었다.** 데일리는 `order`, 루틴 설계·전체 목록은 `code`.
    게다가 `order` 는 **추가할 때 한 번 박히기만** 하고 구 루틴엔 아예 없어서(전부 0) 데일리가 사실상 **입력순**이었다.
    → `rtCmp()` 하나로 합치고 마이그레이션에서 코드순 기준을 잡았다(`meta.rtOrdSeeded`).
  - `rtMove(id,±1)` → 이동 후 **1..N 로 재번호(`rtRenumber`)**. 구멍·중복이 생기면 다음 이동이 엉킨다.
  - 🔒 **데일리는 목표별 그룹**이라 한 칸 이동이 **그룹째** 움직인다. 의도된 동작이며 `t30` 이 두 불변식으로 잠갔다:
    ① 그룹 안 상대 순서 유지 ② 그룹 순서 = 그 그룹 첫 루틴의 설계 순서.
  - 새 루틴은 항상 맨 아래. `t30` 27건.

- **💳 [결함·중대] 카드결제 날짜가 무조건 오늘로 박혔다.** (로한 문의: *"1월 데이터 넣는데 처리 당일로 표현된다. 7월까지 이래도 되나?"*)
  - `doSettle()` 이 `date:todayStr()` 였다. **화면엔 `pd`(결제 예정일)를 이미 보여주고 있었는데 저장은 오늘로 했다** — 말과 기록이 달랐다.
  - 실측: `1월 결제분 113,987원` 이 **2026-08-25** 로 박혀 있었다(211일 어긋남). 7월까지 밀었으면
    **8월에 7개월치가 몰리고 1~7월 카드결제는 0으로 비었을 것.** 최종 잔액 총합만 맞고 월별은 전부 틀린다.
  - → 결제일 입력칸 신설. 기본값 = **예정일이 지났으면 그 날**(소급 입력) / **아직이면 오늘**(실제 선결제).
    지난 결제분이면 모달에 경고 문구.
  - 어긋난 기존 결제엔 `settleDateOff()` 로 **'날짜 ±N일'** 뱃지(임계 45일 — 로한의 평소 선결제 폭 20~35일은 안 걸린다).
    ✎ 로 **날짜만** 수정(`settleFixDate`). 🔒 금액·구성은 막았다 — 원장이 깨진다.
  - 실측 나머지 2건(8월분 4일 전, 9월분 35일 전 선결제)은 정상이라 경고 안 뜬다. **고칠 것은 1월분 1건뿐.**

- **💳 [결함·중대] 카드결제를 지우면 돈이 조용히 증발했다.**
  - `delTxn` 이 settle 을 그냥 지웠다 → 원 거래는 `card=false` 로 남고 결제 기록만 사라져
    **"낸 적도 없고 낼 것도 없는 돈"** 이 됐다. 로한이 "1건 지우고 다시 하면 되지" 할 때 바로 밟았을 지뢰다.
  - → `settleTxns(t)` 로 그 주기가 덮은 회차를 **역산**(저장하지 않는다), `unsettle()` 로 미결제 복원.
    할부는 회차 되감기(`instDone = k-1`). 삭제 확인창에 몇 건이 되돌아오는지 명시.
  - `t31` E섹션이 **지우고 → 다시 확정 왕복이 같은 금액·같은 날짜로 닫히는지** 잠갔다.

- **🔄 동기화 충돌에 설명을 붙였다.** (로한: *"무슨 데이터가 어떻게 충돌하는지 설명이 없어서 그냥 일괄 확인 누른다."*)
  - ⚠️ **v1.7까지 `syncConflict` 는 서버의 `updated_at`(시각)만 읽었다.** 비교할 재료 자체가 없었다 —
    설명을 안 한 게 아니라 **할 수가 없었다.**
  - → `select('data,updated_at')` 으로 서버 본문까지 받아 `syncDiffText()` 로 컬렉션별
    **내 쪽에만 / 저쪽에만 / 양쪽 다름** + 항목 라벨(날짜·계정과목·금액)을 보여준다.
    checks·timelog 은 날짜 키 단위. 내용이 같으면 "저장 시각만 어긋났다 — 덮어써도 잃는 것 없다"고 말한다.
  - 🔒 **덮어쓰기 직전 서버본을 스냅샷(`pre_overwrite`)으로 남긴다.** 잘못 눌러도 되돌릴 수 있다.
    ⚠️ `app_state_snap.day` 는 NOT NULL 이다 — insert 에서 빠뜨리면 통째로 실패한다(한 번 밟았다).
  - `t31` 38건(F섹션 포함).

- **⚠️ [테스트 환경] 16개를 한꺼번에 띄우니 크로미움이 못 떠서 `page.goto` 30초 타임아웃이 4개 났다.**
  코드가 아니라 **컨테이너 자원 문제**였다(따로 돌리면 전부 통과). `run.sh` 에 `JOBS=5` 동시 실행 상한 추가.
  🔒 실패를 보면 **코드부터 의심하기 전에 같은 테스트를 단독으로 한 번 돌려 봐라.**

- 📌 **로한이 지금 해야 할 것**: 새로고침 → 가계부 8/25 → `삼성 탭탭오 결제 · 1월 결제분` 의 ✎ →
  `예정일 2026-01-26 로` 버튼 → 저장. 그 뒤 2월분부터 이어서 입력.

### 2026-08-25

- **v1.7 — 🛡️ 보험 신설.** (로한 설계: *"로한ㅣ아버지ㅣ어머니 상단 탭 · 무슨 문제가 생겼을 때 로버트가 종합적으로 사고 할 수 있게"*)
  - **⚠️ 소유자 모호성을 물어보지 않고 데이터로 풀었다.** 로한은 *"내가 갖고 있는 보험 3가지"* 라 했는데
    준 md 두 건 모두 **피보험자 정복현** — 탭 구도와 충돌하는 것처럼 보였다.
    프로젝트에 붙어 있던 `0102_보험관리.xlsx` 를 열어 보니 헤더가 **복현 ㅣ 엄마 ㅣ 아빠** 로 갈라져 있었다.
    → **정복현 = 로한 본인.** 🔒 물어보기 전에 붙어 있는 자료부터 대조한다.
    같은 시트에서 **세 번째 계약이 우체국**이며 **보장 열이 통째로 비어 있다**는 것도 확인 —
    로한이 *"3가지 중 2가지"* 라 한 것과 정확히 일치한다.
  - **축을 둘로 나눴다.** 보험은 성격이 다른 두 축을 동시에 갖는다.
    - **비용 축** = 계약 · 특약별 월 보험료 → `policies[].premium` / `riders[].prem`
    - **보장 축** = 정규화 보장명 × 계약 매트릭스 → `covers[]`
    *"무슨 일이 나면 얼마"* 는 **보장 축에서만** 나온다. 그래서 `covers[].name` 은 사람·보험사가 달라도
    같은 문자열을 쓴다 — 그래야 가로로 합산된다.
  - 상단 **보장 한눈에** — 실손 · 암 · 뇌 · 심장 · 상해사망 · 질병사망 · 입원일당.
    탭을 옮기지 않아도 켜진 사람이 전부 한 표에 뜬다.
  - **보장 공백** 자동 표시. **'진짜 없는 것'과 '자료가 아직 안 들어온 것'을 구분해서 쓴다.**
  - **🔒 실손은 합산하지 않는다.** 여러 개 들어도 **비례보상**이라 겹쳐 못 받는다.
    매트릭스 합계에서 `insAgg()` 로 **최대치**를 잡는다. (아버지가 흥국·삼성 두 곳에 실손이 있어
    그냥 더하면 1억으로 뜬다 — 거짓말이 된다.)
  - **⚠️ [자료 결함] 로한이 준 원문 요약본이 스스로 안 맞는다.**
    교보 CI보험 특약 7개를 실제로 더하면 **45,200원**인데 요약본은 **43,200원**이라 적었다.
    차이 **2,000원 = 재해상해특약2형 보험료와 정확히 같은 액수**.
    → **맞춰서 고치지 않았다.** `insPremGap()` 파생으로 계산해 '보험료 불일치' 칩과 표 하단 경고로 드러낸다.
    🔒 **두 자료가 어긋날 때 한쪽을 조용히 맞추면 그 순간 진실이 사라진다.**
  - **세 탭 전부 활성 · 시드 9건 / 보장 171항목.** (로한 3 · 아버지 3 · 어머니 3)
    ⚠️ **한 번 방향이 뒤집혔다.** 작업 중 로한이 *"아직 아버지, 어머니는 정보 없으니까 그냥 탭만 만들고 활성화 꺼버려"* 라 해서
    껐다가(`insOn/insToggle/insImport` + `INS_XLSX` 분리), **xlsx 에 이미 자료가 있다고 알리자** 로한이
    *"엄마아빠 데이터가 있으면 그냥 탭 활성화 시켜라"* 로 정정했다. → 토글 기구를 전부 걷어내고 단일 시드로 되돌렸다.
    🔒 **로한이 '정보가 없다'고 말할 때, 정말 없는지 붙어 있는 자료로 먼저 확인한다.** 지시를 그대로 실행했으면
    자기 데이터를 자기가 못 보는 상태로 끝났을 것이다.
    ⚠️ 마이그레이션에 `insSeeded=1` 세대(로한 것만 심긴 판)가 잠깐 존재했다 → **2로 올리며 id 기준 멱등 병합**으로 채운다.
    `t29` H섹션이 이 승격 경로를 잠근다(중복 없음 · 로한이 고친 값 보존).
  - **DB생명(시트2, 담당자 최금주)은 넣지 않았다.** 로한 확인: **제안서**이지 가입한 계약이 아니다.
  - 합계 · 납입완료일 · 다음 갱신일 · 보험료 불일치는 **전부 파생.** 저장하지 않는다.
  - 🔒 계약 삭제 시 보장 동반 삭제 + 매 로드 **고아 보장 정리** (계약이 없는 `covers` 는 매트릭스에 유령을 그린다).
  - `t29.js` **52건** 신설(빈 상태 → 시드 → 멱등 → 꺼진 탭 → 불러오기 → 삭제 → 추가). 전체 14파일 통과 · 49초.
  - **⚠️ [테스트 결함 · 내 잘못] `t14` `t18` `t19` 가 `file:///home/claude/work.html` 을 박아 두고 있었다.**
    0820 에 5개를 고쳤다고 적었는데 이 셋이 남아 있었다 — **"고쳤다"고 쓴 뒤 전수 확인을 안 했다.**
    `process.argv[2]` + `__dirname` 으로 교정. 배포 직전 `./run.sh all` 이 없었으면 못 잡았다.
  - 📌 **로한의 보장에서 눈에 띄는 것** (판단은 로한 몫, 나는 숫자만 놓는다)
    - **급성심근경색 = 미보유.** 로한이 시트에 직접 그렇게 적어 놨다. 뇌(1,000만)는 있는데 심장만 비었다.
    - **암 진단비 1,000만.** xlsx 상 아버지 2,000만 / 어머니 4,000만 — **가족 중 로한이 가장 얇다.**
    - **실손은 살아 있다.** 흥국 2010년 가입 구실손(입원 5,000만 / 통원 25만, 본인부담 90%), **3년 갱신 · 다음 갱신 2028-07-30.**
    - **납입 완료 2030-07-30** (흥국). 교보는 청약일 미상이라 계산 불가 — **불확실**.
    - ⚠️ xlsx 에 값이 이상한 항목이 있다: `상해흉터복원수술비 20,070,000` / `특정전염병치료비 20,300,000`.
      입력 실수로 보이지만 **추측으로 고치지 않았다.** 원문 확인 필요.
  - 📌 **아직 안 준 것**: ① 우체국 계약(3번째) 자료 ② 교보 청약일 ③ 아버지·어머니 보험료·증권번호.
    (DB생명은 제안서로 확정 — 종결)

### 2026-08-25 (이어서)

- **v1.6 — ⚖️ 제지방량 · ⏳ 보조 태그 · 🌊 카드결제 분해.** (로한 수정요청 3건)
  - **⚠️ [죽은 탭] 체성분 '내장지방'은 실측 82건 중 0건이었다.** 체중계가 주지 않는 값이라
    입력칸(`WIN`)에도 없었고, `WTABS` 만 `e.visceral` 을 읽어 **영원히 빈칸**이었다.
    → **제지방량**(체중−체지방)과 **제지방률**로 교체. 실측 검증: 68.8−16.2=52.6 ≈ 근육 49.8+골무기질 2.7.
    🔒 **파생 가능한 값을 빈 입력칸으로 두지 마라.** v1.42 와 같은 계열이다.
  - ⏳ **보조 태그.** 로한: *"게임이나 유튜브는 낭비이긴 하지만 휴식도 된다. 두 개를 섞고 색도 섞였으면."*
    `tag2` 추가 · 색은 `linear-gradient` 로 섞고 · 라벨은 `낭비+휴식`.
    **⚠️ 두 태그에 각각 전부 세면 하루 합이 24시간을 넘는다.** v11.0 설계의 핵심이 **24시간 고정 총량**이라
    그게 깨지면 *"이만큼밖에 안 남았다"* 가 성립하지 않는다.
    🔒 **반반(0.5/0.5) 배분.** 총량은 그대로, 성격만 둘로 나뉜다. 모달에 배분 결과를 미리 보여준다.
  - 🌊 **카드결제는 지출 루트가 아니다.** 로한: *"온통 카드로 결제하는데 이걸 소비루트로 설정하면 안 된다."*
    Sankey 종착이 `카드결제 33만` 한 덩어리라 아무것도 안 보였다.
    → 선결제 확정 시 **계정과목 구성(`catMix`)을 박제**해 실제 사용처로 쪼갠다.
    🔒 **이건 파생 불가라서 저장한다** — 확정되면 원거래가 `card=false` 가 되어 어느 결제분이었는지 흔적이 사라진다.
    (v1.42 는 파생 가능한 걸 저장해서 틀렸고, 이번은 반대 경우다. 구분이 중요하다.)
    거래 행에도 세부 적요로 편다. `cfShort` 를 **만 → 천 단위**로(옆 공간이 남는다).
  - `t28.js` 29건 · `./run.sh all` **13개 47초 전부 통과** · 실데이터 SQL 대조 일치.
  - 배포 sha256 `855dcbb648b18bb673bfbca2c440343ac4e9b987487cd3360268b0179f7bb636`.

- **v1.61 — '카드결제(구)' 제거 · 현금흐름에 [현금|발생] 토글. (로한 지적 2건, 같은 뿌리)**
  - 로한: *"(구)카드결제가 뭔 뜻이야?"* — **화면에 나갈 말이 아니었다.** '구버전 데이터'라는 개발자 약어를 그대로 노출했다.
    ⚠️ **더 큰 문제는 그 라벨이 필요 없었다는 것.** v1.6 에서 나는 *"확정되면 `card=false` 가 되어 파생 불가"* 라고
    **단정하고 저장했는데 틀렸다.** card 플래그만 바뀔 뿐 **거래일과 카드명은 남으므로 결제분이 계산되고, 구성도 나온다.**
    실측 검증: 옛 settle 2건이 역산합과 **원 단위로 일치**(210,112 / 122,042).
    → `settleMix()` — **저장값 우선, 없으면 역산.** 🔒 **v1.42 원칙 그대로 — 파생 가능한 것을 저장으로 대신하지 않는다.**
    같은 결제분을 나눠 부분 선결제한 경우에만 역산이 못 가르므로 그때만 `카드결제 내역미상`.
  - 로한: *"현금흐름과 가계부 데이터가 안 맞아. 배달부터도 안 맞잖아."*
    **⚠️ 버그가 아니라 기준이 달랐다.** 실측 2026-08 배달:
    ```
    가계부(발생) 161,800 = 체크·현금 84,200 + 신용 77,600(선결제 52,700 + 미결제 24,900)
    현금흐름(현금) 136,900 = 84,200 + 52,700     차이 24,900 = 아직 안 나간 카드값
    ```
    **진짜 문제는 가계부엔 `[발생|카드결제]` 토글이 있는데 현금흐름엔 없다는 것이었다.**
    두 화면이 다른 기준으로 도는데 화면이 말을 안 해줬다.
    → 현금흐름에 **`[현금|발생]` 토글** + 차이의 정체를 한 줄로(`차이 24,900 = 아직 안 나간 카드값`).
    🔒 **발생 모드에서 가계부와 원 단위로 같은 숫자가 나온다** — 테스트로 못박았다.
  - `t28.js` 39건 · `./run.sh all` 13개 47초 전부 통과.
  - 배포 sha256 `bb27a6cce076f59d8ecb0eaf89325a1daca66a21c20b88051cf3c2b6ff1af98a`.

- **📋 `0901_기초잔액_정합계획.md` 신설.** 로한: *"실제 잔액과 로한북 잔액을 맞춰야 할 단계가 오고 있다."*
  - **⚠️ 계좌 14개 전부 `opening` 이 비어 있다.** 그래서 마지막 수기 스냅샷(7/28·8/12)이 출발점이라
    계좌마다 기준일이 달라 **맞을 수가 없는 구조**였다.
  - 핵심 판단: **대조 대상을 5개로 좁힌다.** 잔액이 40원·26원·1원·8,131원인 계좌 4개는 빼라 —
    오차 0을 만드는 데 방해만 된다.
  - **8월 데이터는 손대지 않는다.** 기초일 9/1 이면 8월 거래는 계산에서 자동으로 빠진다. **9/1 이 새 출발선이다.**
  - 🔒 **대조에서 차액이 만 원 넘으면 [차액반영]을 누르지 마라.** 오류를 이자로 위장하는 것이고,
    그러면 대조표가 **감사 기능**을 잃는다. 대조의 진짜 가치는 오차 0이 아니라 **빠진 거래를 잡는 것**이다.
  - 기초잔액이 들어가면 `A 보증금 확보` 목표 게이지가 **자동으로 살아난다**(현재값 출처 = 현금성 자산).

- **⚖️ 체중 경고 — 8/25 68.8kg.** 8/13 68.0 → 8/21 68.4 → **8/25 68.8**. 체지방 15.7 → 16.2.
  5~8월 67.6~68.2 횡보였는데 **8월 하순에 올라가는 중**이다. 배달 끊기 결심(8/24)의 근거가 데이터로 확인된다.

### 2026-08-24 (요약 — 상세 원문은 `handoff_0824.md`)
- **v1.51 🍽 음식칸 정정. ⚠️ 내가 요청 범위를 임의로 넓힌 것이 원인이었다.**
  로한: *"배달에서 뜬 뒤로 다른 지출 입력할 때도 안 사라진다."* **버그가 아니었다** — 재현 9개 전부 정상.
  요청은 **'배달' 하나**였는데 *"곧 식비도 온다"* 는 논리로 **묻지도 않고 4개**를 켰다.
  실측: 자주 쓰는 상위 5개 중 4개가 음식(카페205·간식137·식비75·배달58), 실제 기록은 **배달 3건뿐**.
  → 기본을 배달 하나로. **단 이미 기록한 카테고리는 안 건드린다.**
  🔒 **요청하지 않은 것을 기본으로 켜지 않는다. '나중에 필요할 것'은 근거가 아니다.**
- **🚨 2024귀속 종소세 과세예고 — 검산 완료, 세무사 위임 후 대기.**
  핸드오프는 Supabase `handoffs` 테이블 `slug='tax-2024-correction'` 에 있다(생활방 작성).
  통지 1,533,582 = 본세 1,270,890 + 과소신고 127,089 + 납부지연 135,603.
  **검산 결과 통지서와 원 단위 일치** — 과표 19,542,908 × 15% − 126만 = 1,671,436.
  과소신고가 **정확히 10%** = 세무서가 착오로 봤다는 뜻(부정행위 40% 아님).
  ⚠️ **세무사 추정 절감(55만)이 계산(120만)보다 낮다.** 성공보수 20% 구조에서 낮게 부를 이유가 없으므로
  **인건비 1,466만 소급을 어렵게 보고 있다는 신호.** 따지지는 말되 **준비 금액은 100만 근처로.**
  ⚠️ **더 급한 것: 2025~2026 미신고 리스크.** 2025 1기·2기 부가세, 2026 1기 부가세, 2025귀속 종소세 —
  **전부 기한이 지났다.** 2025년부터 일반과세라 무신고 시 20% 가산세. **본건보다 클 수 있다. 홈택스 확인 필요.**
- **💰 9월~2027년 1월 현금흐름 실측.** 평상시 월 흑자 **729,371**(2~8월 실측), 현재 현금성 **1,438,887**.
  9월 −558,787(근로장려금 130만이 세금·축의금을 메운다) → **버틴다.** 10월부터 +14만, 12월부터 +28.6만.
  ⚠️ **핸드오프의 "풍무점 이전 = 2026년 10월"은 틀렸다 → 2027년 1월** (로한 정정. 내가 검증 없이 인용했다).
  🔒 **보증금 금액이 최대 미확정 변수.** 300만이면 식비 감축만으로 닿고, 500만이면 자산 정리, 1,000만이면 다른 층의 결정.
  로한 결정: **이사 시 외환·ETF 전부 정리** · **배달 완전 중단**(다이어트 명분).
  ⚠️ 배달 순 절감은 16만이 아니라 **9~12만**(대체 식재료비). 카페(25건 5.3만)는 건드리지 마라 — 박탈감 대비 효과가 작다.

### 2026-08-21 (요약 — 상세 원문은 `handoff_0821.md`)
v1.4 → v1.5. **카드 청구주기가 들어오고 현금흐름 정합이 잡힌 날.**
- **v1.4 💳 청구주기·할부.** 카드에 `closeDay`/`payDay`(10/25) · 일시불/2/3개월 할부 · 미결제를 결제분별 분해 ·
  선결제를 결제분 단위로. ⚠️ **회차는 구매일이 아니라 결제일에 일시불과 합산돼 나간다.**
  ⚠️ **로한 정정: 할부 당월 회차는 선결제에 포함**(빼면 카드사 청구서와 안 맞는다).
  🔒 **결제일은 예정일 라벨이지 자동 처리 트리거가 아니다.** 가계부 `[발생 | 카드결제]` 토글.
  같은 날 🧾 재물 마감일 `1800` 결함(`new Date('1800')`=서기 1800년) · ⏳ 병원·가족 태그.
- **v1.41 🧹 정리.** 거래 행 2줄 재설계 · 선결제 확정 버튼 제거 · **전 페이지 설명 문구 삭제** · 유출 이중계상 수정.
- **v1.42 🔑 계좌는 저장이 아니라 파생이다.** ⚠️ **로한 지적 — 계열이 반복되니 기억할 것.**
  `acct` 를 스냅샷 저장했더니 같은 카드인데 45건 중 23건만 보유. '계좌 미지정'이라는
  **있어서는 안 될 상태**를 만들고 나는 그걸 경고+보정 버튼으로 정상 취급했다.
  → `txnAcct()` 파생. 🔒 **없어야 할 상태를 정상 취급하는 UI 를 만들지 마라.**
  부수 발견: **카드를 id 가 아니라 이름 문자열로 묶는다**(🟠 등록).
- **v1.5 🍽 음식 기록.** `isFood` 플래그 + 자동완성. → 8/24 에 범위 정정.
- **⚙️ `tests/run.sh` 영역별 병렬 러너.** ⚠️ 파일 볼륨이 병목이 아니었다 — 전체 회귀 4회 반복 ~8분.
  **직렬 114초 → 병렬 44초 → 영역만 10~34초.** 🚫 **`index.html` 분할 안 함. 트리거: 6,000줄**(현재 약 5,000줄).

### 2026-08-20 (요약 — 상세 원문은 `handoff_0820.md`)
v1.0(정식) → v1.4 직전까지. **저장·복구 층을 만든 날이다.**
- **v1.0** 9일 이터레이션 마감. 마감 점검에서 `logs2`·`activity`·`taxParams` 마이그레이션 구멍 3개.
  🔒 **테스트가 계속 같은 걸 지적하면 테스트가 틀린 게 아니라 코드가 틀린 것이다** (9일 무시했다가 터짐).
- **v1.1 🔐 동기화·백업.** ⚠️ 제일 위험한 코드는 렌더가 아니라 `save()` 였다 — 실패를 통째로 삼키는 빈 에러 콜백,
  조건 없는 upsert(폰·PC 동시 사용 시 통째 덮어쓰기), 복구 경로 0. → `updated_at` 낙관적 잠금 + 충돌 선택 +
  5회 재시도 + 저장 배지 + JSON 내려받기/복원. 🔒 **저장은 조용히 성공하고 시끄럽게 실패해야 한다.**
  [치명] SEED 최상위 키 **7개**가 마이그레이션 보장 밖 + `d.ui` 를 어느 마이그레이션도 안 만들었다.
  → 개별 나열을 그만두고 **SEED 기준 루프**로. `t22`(극단상태 5종) 신설.
  🔒 **새 기능 테스트는 '잘 된 상태'가 아니라 '빈 상태'부터 먹인다.**
- **v1.2 📀 스냅샷.** `app_state_snap` 신설. 접속 시 자동 1개(부분 유니크 인덱스로 DB가 강제) · 수동 지점 ·
  **되돌리기 직전 `pre_restore` 자동 보관**. 🔒 **복원에 '돌아올 자리'가 없으면 복구 도구가 아니라 흉기다.**
  ⚠️ **같은 프로젝트 안의 다른 테이블은 진짜 백업이 아니다** — 스냅샷=논리적 사고, JSON/GitHub=물리적 사고. 대체재가 아니다.
- **v1.3 🔁 루틴 이사 · 🎯 버킷 승격.** [결함] 루틴 소속을 바꾸면 `checks`(done/due/miss 전부 코드 문자열)가
  **조용히 고아**가 됐다 → `renameRoutineCode()`. **분모(due) 길이는 불변**(v6.0 과거 불변성 유지).
  「코드 고정」체크박스 — `lockCode` 는 v6.0부터 **읽기만 하고 켤 수 없었다**(입구 막힘 4회차).
  버킷 → 연간목표 승격(`promotedTo` ↔ `fromBucket`). ⚠️ `modal` 은 DOM 노드라 `_fromBk` 가 살아남는다 → `openModal` 에서 끊음.

### 2026-08-19 (요약 — 상세 원문은 `handoff_0819.md`)
하루에 v7.4 → v8.5, 12개 버전. 보상 체계가 완성된 날이다.
- **v8.0 🎁 보상 카드 모델 (로한 설계, 내 안보다 나았다).** `보상함(종류=덱) → 배치(예약) → 계단 달성(활성) → 소비하며 가계부 입력(소멸)`.
  **v7까지는 '수령 확정'이 곧 거래 생성이라, 받기로 해놓고 안 쓴 보상이 유령 거래로 남았다.**
  🔒 **상태는 저장하지 않고 파생**(`usedAt` / `tierOpen()`) — 문턱을 바꿔도 과거 판정이 안 어긋난다.
  🔒 **카드는 종류를 스냅샷** — 보상함 원본을 고쳐도 발급된 카드는 불변.
  거래 삭제 → 카드 복원 / 사용 취소 → **거래는 남기고 연결만 해제**(실제로 쓴 돈이므로). 유효기간 없음.
  계정과목 불변(배달=식비) — **보상 배지 없는 배달 건수가 곧 절제 실패 횟수**다.
- **v8.1** 목표 달성 카드에서 후보가 0개던 버그. 단계는 카드가 들고 있으므로 **보상함의 tier는 '기본 제안'일 뿐** → 다른 단계도 고를 수 있게 개방. 배치 모달에서 **버킷 제거**(로한: *"목표가 대가가 될 순 없다"*). 주간·월간은 **카드 1장 유지** — 계단은 루틴 축, 목표 달성은 목표 축.
- **v8.2** [버그] 감소형 목표가 `0/65kg 100%`. 미입력 판정이 `''·null`만 걸러 **숫자 0을 놓쳤다**(v6.1 재발). **루틴 기간 신설**(계속/이 달만/직접) — 종료일 필드가 없어 8월 눈금 루틴이 12월까지 떴다. 연동 미설정 '수동' 칩.
- **v8.3~8.4** 목표 탭 레이아웃. **병목은 '캘린더가 작다'가 아니라 '상단이 길다'.** 52주 트래커 접기, 캘린더 확대(칸에 비율·못한 루틴·사유·陰·🎁), **행 끝 '주' 칸으로 주간 계단이 캘린더 안에서 완결**. 전폭 게이지를 빈 공간으로 옮겨 **연간 10줄 → 5줄**.
- **v8.5 [UI/UX 감사]** `.rwpick` **base CSS 규칙이 통째로 없었다** — 클릭 목록 전체가 '그냥 글줄'로 렌더. **v7.1 `.tcard`와 같은 계열, 2회차.** 클래스↔CSS base 대조 스크립트로 발견. 모달 헤더 sticky, 목록형 모달 액션 상단화, 히트영역 11→26px, `:focus-visible` 신설.
- **v7.4~7.6** 버킷함 ⇄ 보상함(`place` 필드), 마이그레이션이 `schemaVersion>=6` 게이트에 막혀 통째로 스킵되던 치명 결함(→ (A)1회성 / (B)매 로드 멱등 2층 분리), 작성 중 모달이 바깥 클릭으로 닫히던 문제.

### 2026-08-11 ~ 08-18 (요약 — 상세는 `handoff_0818.md`)
- **v7.0~7.3** 🎁 보상 계단(일간 완전달성 → 주 4일 → 월 4회 → 연 9회), 소비/행동 2분류 + `budget` 축, 명리 음기일(3점=연 72일), 🪣 버킷함 신설.
- **v6.0~6.2** 목표·루틴 전면 재설계. **목적 함수: 12월 31일에 얼마나 달성했고 무엇이 부족했는지 반성할 수 있을 것.** 🔒 `checks[날짜]={done,due,miss}` **분모까지 스냅샷 — 과거 불변성**(v5.0에서 소급 변동 실측). 삭제 대신 은퇴. 미달 사유 태그.
- **v5.0.1** 현황판 빈 화면 회귀 → **`VIEW_RENDER` 맵.** 🔒 사이드바에 탭 추가하면 여기 반드시 등록.
- **v4.5** 부채 상환 엔진 — 원금 고정 416,000 + 잔액×3.11%×경과일/365. **은행앱 6회차와 원 단위 일치.**
- **v4.3~4.4** 일지 탭, 체성분 8지표. **v1~4.2** 프로젝트 시작, Supabase, 현금흐름, 🔮 사주 엔진.

---

## 🗺 목표·루틴 로드맵 (2026-08-14 결정)

**구글 캘린더 연동은 Phase 2로 미룸.** 판단 근거:
- 기술적으로는 가능. 브라우저 전용 정적 사이트에서 `gapi` + Google Identity Services로 백엔드 없이 호출 가능. 필요: Google Cloud 프로젝트 → OAuth 클라이언트 ID(웹) + API 키 → 승인된 JS 원본에 `https://rohanchung.github.io` 등록 → 동의화면.
- ⚠️ **불확실**: 브라우저 토큰은 1시간 만료에 refresh token이 없다. 조용한 재발급이 되는지 구글 문서에 명시가 없어, 최악의 경우 접속마다 구글 로그인 팝업.
- ❌ **로버트 경유 미러링 불가**: Google Calendar MCP 커넥터에 노출된 도구가 `search_events` 하나뿐. 기간 조회·이벤트 생성 없음. 배치로 긁어와 Supabase에 밀어넣는 우회로는 현재 도구로 막혀 있음.
- ❌ **양방향은 반대.** 루틴 체크 월 150건이 구글 캘린더를 도배하고, 충돌 해결 로직은 개인 프로젝트가 가장 많이 좌초하는 지점.
- → 붙인다면 **읽기 전용 오버레이**로만. 2주 써보고 실제로 아쉬울 때 판단.

**다음 단계 후보**
- 데이터 2~3개월 쌓이면 **사주 주의일 vs 평상일 루틴 달성률 상관 분석**. 지금은 표시만, 분석은 표본이 모인 뒤.
- 주간 리뷰 화면(주별 달성률 추이).

## ⏳ 미해결 / 대기

### 🚨 최우선 — 2024년 귀속 종합소득세 (2026-08-24 발생)
- [x] ~~핸드오프 확보~~ → **Supabase `handoffs` 테이블, `slug='tax-2024-correction'`** (생활방 작성, v2).
      🔒 **앞으로 방 사이 인수인계는 이 테이블을 쓴다.** `select body from handoffs where slug='...' order by version desc limit 1`
- [ ] **세무사 회신 대기.** 로한이 할 일은 끝났다.
      로한: *"9월 결혼식 축의금까지 합치면 거의 200만원이 나가게 생겼다. 비상이다."*
      ⚠️ **추측으로 손대지 마라.** 세금은 숫자 하나가 금액을 바꾼다. 고지서·신고서 원문을 보고 시작한다.
      참고 — DB의 `taxParams` 는 **2026 귀속용**이다(강사료 31,200,000 · 이월결손 1,909,506 · 노란우산 1,200,000 ·
      연금 1,682,640 · 원천징수 3.3%). 2024년 데이터는 앱에 없다.

### 🟢 9/1 기초잔액 정합 — `0901_기초잔액_정합계획.md` 참조
- [ ] 8/26~31 · 대조 제외 계좌 4개 정리 (토스 40원 · KB 26원 · K뱅크 1원 · ISA 8,131원)
- [ ] 8/26~31 · 8월 결제분 선결제 확정
- [ ] 8/26~31 · 계좌 미지정 2건 (8/08 Chatgpt 12,090 수단변경 · 8/25 월세 420,000 계좌지정)
- [ ] **8/31 밤 · 기초잔액 5건 입력** (기초일 `2026-09-01`, 은행앱 원 단위 그대로)
- [ ] 9월부터 · 현금·이체 지출에 출금계좌 지정 / 계좌 간 이동은 '이체'로
- [ ] 매주 일 · 대조 5분. **차액 만원 넘으면 반영 말고 원인부터**

### 🔴 로한이 해야만 하는 것 (로버트가 대신 못 함)
- [ ] **A 목표(풍무점 500만)** 을 수치형 `5,000,000원` + 현금성 연동으로 수정. 지금은 게이지가 안 쌓인다. ← **DB 검증 완료, 실제로 미완**
- [ ] **9/1 0시 계좌 기초잔액 입력** → 현금흐름 100% 정합 가동 (`cashflow-design.md`). 로한: "9/1에 할 것"
- [ ] **8/08 Chatgpt 12,090** — 결제수단 `카카오(사업자)` → `카카오-사업자체크카드` (마지막 고아 1건)
- [ ] **8/25 월세 420,000** — 출금계좌 지정 (마지막 미지정 1건). 미래 날짜라 급하진 않다.
- [ ] **카드 마감일·결제일 실물 대조** (v1.4) — 기본값 10/25 로 넣어뒀다. 삼성카드 앱 → 결제예정금액에서
      **9월분에 브리즘 안경 1회차 145,034 가 일시불과 합산돼 뜨는지** 확인. 다르면 카드 수정에서 고치고 로버트에게 알려라.
- [ ] **브리즘 안경 재물 데이터 수정** — 재물관리 → 브리즘 → 구매일 `2026-08-21`, 수명 `1800` 입력.
      (⚠ 마감일 형식 오류 뱃지가 떠 있다. 마감일 2031-07-26 이 자동 계산된다.)
- [x] ~~B 체중 목표 수정~~ → 8/20 완료
- [x] ~~루틴을 목표에 연결~~ → **애초에 다 돼 있었다.** 루틴 6개 전부 `goalId` 보유. 내가 검증 없이 옮긴 항목이었다.
- [ ] 월 1회 정도 🪶 로그 탭 → `⬇ JSON 내려받기` (물리적 사고 대비. 논리적 사고는 v1.2 스냅샷이 자동으로 막는다)

### 🟠 다음 창에서 바로 할 것

- [ ] **1~7월 카드 데이터 소급 입력 (로한 진행 중).** 하루 1개월 페이스. v1.8 부터 결제일이 자동으로
      실제 예정일로 잡히므로 그냥 진행하면 된다. ⚠️ **1월분 1건은 2026-08-25 로 박혀 있으니 ✎ 로 고쳐야 한다.**
- [ ] **9/1 기초잔액 정합 전에 settle 날짜 전수 확인.** `t->>'type'='settle'` 로 뽑아 '날짜 ±N일' 뱃지가
      뜨는 게 없는지 본다. 소급 입력이 끝나면 반드시 한 번.

- [ ] **보험 — 로한이 자료를 주면 채운다.** ① 우체국 계약(로한 3번째) ② 교보 청약일 ③ 아버지·어머니 보험료·증권번호.
      ✅ DB생명(시트2)은 **제안서**로 확인됨 — 반영하지 않는다. 다시 묻지 마라.

- [ ] **v1.5 — 현금흐름에 카드 결제 예정 꽂기.** `card-design.md` 5장에서 미룬 마지막 조각.
      주기별 미결제를 각 결제일에 예정 유출로 올린다. **주기 모델을 한 달 굴려 실제로 맞는지 본 뒤에** 한다.
- [ ] **카드 참조를 이름 → id 로 전환.** 거래가 `method` 문자열로 카드를 찾는다(`cardBy(name)`).
      **카드 이름을 바꾸면 옛 거래가 통째로 연결을 잃는다** — 실측 `카카오(사업자)` 3건이 이미 그 상태다.
      v1.42 는 고아 경고로 드러내기만 했다. 전환하려면 거래에 `cardId` 를 심고 마이그레이션으로 이름→id 매핑.
      ⚠️ 이름이 바뀐 뒤엔 매핑이 불가능하므로 **이름을 더 바꾸기 전에** 하는 게 좋다.
- [ ] **눈금 이월 UI** — 9월이 되면 `E.8a`(8월 눈금 소속) 같은 루틴을 매달 손대야 한다.
      지금은 **손으로 옮겨도 기록이 안 깨진다**(v1.3). 자동화는 9월에 실제로 번거로운지 겪고 판단한다. 미리 만들지 않는다.

### 🟡 로버트 대기 (데이터가 더 쌓여야 함)
- [ ] **회고 뷰** (goal-design 6장) — 기간 선택형. 목표별 성적표 · 월별 히트 · 루틴 기여도 · 미달 사유 집계. **데이터 3~6개월 뒤.** 그전까진 로버트가 SQL로 직접 분석해 대화한다.
- [ ] **주간 달성률 추이 그래프** — 12주 잔디 옆에 나란히 (dataviz 스킬 참조). ⚡ `goalTrend()` / `goalStreaks()` 는 **이미 구현돼 있고 아무도 호출하지 않는다.** 화면만 붙이면 된다.
- [ ] "About 로한" 공유용 다이제스트 — 로한이 "천천히"라 함, 보류.

### 🟢 다음 작업 후보 (우선순위 순)
1. **GitHub 자동 백업** — v1.2 스냅샷으로 **논리적 사고는 막혔다.** 남은 건 **물리적 사고**(Supabase 프로젝트·계정 소실)뿐이다.
   Edge Function 월 1회 `backups/` 커밋. 우선순위는 v1.1 때보다 **내려갔다** — 스냅샷이 일상 사고를 다 가져갔다.
2. **저장 페이로드 축소** — 아래 용량 항목 참조.
3. 죽은 코드 정리 — `rmk` `ensureRewardCat` `goalByCode` `goalLabel` `tlDel` `bankYearTotal` 은 호출자가 0이다. 전부 설계 변경으로 대체된 잔여물(각각 `bub()`, 사용자 지정 계정과목, `tlDelRange()` 로 대체). 지우는 건 언제든.

### 📏 용량 — **기존 메모가 틀렸다. 8/20 실측으로 교정.**
- 기존 handoff 의 "현재 48kB / 55kB" 는 **`pg_column_size`(TOAST 압축값)를 원본 크기로 잘못 읽은 것**이다.
- **실측(2026-08-20)**: `length(data::text)` = **227,758자 / 262,227 bytes (약 256KB)**. 거래 1,015건 기준.
- **"1MB 한계"도 틀렸다.** Postgres jsonb 필드 한계는 사실상 1GB다. 5~7년/3~4년이라는 시한은 근거가 없었다.
- **진짜 병목은 크기가 아니라 구조다**: `save()` 한 번이 **문서 전체(현재 256KB)를 업로드**한다. 400ms 디바운스이므로 입력이 몰리면 그만큼 반복된다.
  → 위험은 (a) 모바일 회선에서 저장 지연 · 실패 확률 상승, (b) 두 기기 동시 사용 시 통째 덮어쓰기. **(b)는 v1.1에서 막았다.**
  → (a)의 해법은 파티셔닝이 아니라 **분리 저장**(`transactions` / `timelog` 를 별도 테이블 또는 별도 행으로 빼고 변경분만 쓰기)이다. 시점은 **불확실** — 연간 증가량을 판단할 만한 사용 기간이 아직 없다(체크 2일, 타임로그 1일).
- [x] ~~은행 실제 상환액 받아서 앱 부채 계산 교정~~ → **8/14 완료.**
- [x] ~~JSON 내보내기 버튼~~ → **8/20 v1.1 완료.** (자동 백업은 위 1번으로 남음)
- [x] ~~아침 브리핑 스케줄 태스크~~ → **8/18 취소.** 로한 판단: 의미 없음.

---

## 2026-08-28 · v2.3 — 📋 로그 페이지 재편 + 핸드오프 게시판

### 무엇을 왜 고쳤나

로한: *"기타 페이지에 로그 부분이 난잡하게 정리되어있다. 탭을 만들든 해서 정리… 클로드(로버트)들이 각 방에서 핸드오프를 작성해 수파에 남긴다. 이걸 서버에만 남기지말고 서비스에도 남겨서 내가 볼 수 있었으면 좋겠다."*

**난잡했던 진짜 원인(실측):** `v-log` 한 페이지에 성격이 다른 다섯이 세로로 쌓여 있었다.

| 섹션 | 성격 | 분량 |
|---|---|---|
| 💾 백업 | 도구(누르는 것) | 짧음 |
| 📀 스냅샷 | 도구 | 짧음 |
| 📦 업데이트 기록 | 자동(읽는 것) | **v2.0 항목 하나가 1,200자** |
| 🕑 활동 로그 | 자동 | 40건 |
| 재정경제부 로그 | 수동(내가 쓴 것) | 가변 |

두 가지가 겹쳤다. ① 세 종류(도구·자동기록·내 기록)가 섞였다. ② 분량의 대부분을 체인지로그가 먹어서 아래쪽은 스크롤에 묻혀 사실상 안 보였다.

### 결정

- **4탭**: `📋 핸드오프 | 🕑 활동 | 🛟 백업·복원 | 📦 버전` (상태 `DB.ui.logTab`)
- **기본 탭 = 핸드오프**
- **버전은 최근 3건만 펼침**, 나머지는 `#verMore` 로 접기 (로한 선택)
- **`재정경제부 로그`(logs2)는 UI 에서 내렸다** — 로한 판단: *"안 쓴다, 핸드오프로 대체"*.
  ⚠️ **데이터는 지우지 않았다(2건).** `logModal`/`saveLog`/`delLog` 도 남겨뒀다 — 접근로를 끊지 않는다.
- 백업 + 스냅샷을 한 탭으로 합쳤다. 둘 다 "되돌리기 도구"다.
- 활동 로그 40건 → **200건 전체 + 날짜 그룹**

### 🔒 판단 (재검토 금지)

**① 핸드오프를 `app_state` 에 복사하지 않는다. `handoffs` 테이블을 직접 읽는다.**
복사하면 두 벌이 생기고, 방에서 갱신해도 앱은 옛 판을 들고 있게 된다. 원본은 `handoffs` 다. **파생은 저장하지 않는다** — 세무 진행연도·세전수입·체중 근사매칭과 같은 원칙.
캐시는 세션 메모리(`HANDOFFS`)에만 둔다.

**② 읽기 전용. CRUD 중 R 만.**
작성 주체는 각 방의 로버트다. 앱에서 고치면 방과 어긋난다. 쓰기 정책도 만들지 않았다.

**③ 마크다운은 자체 렌더 40줄. CDN 을 끌어오지 않는다.**
단일 파일 원칙. `#~####` / `**굵게**` / `` `코드` `` / `-` `1.` 리스트 / `---`.
⚠️ **기울임(`*단일*`)은 일부러 뺐다** — 본문의 곱하기·각주 `*` 를 잡아먹는다.

### ⚠️ 발견한 결함

**[결함·중대] `handoffs` 테이블은 RLS 가 켜져 있는데 정책이 0개였다.**
MCP(service_role)로는 읽히지만 **앱 키로는 한 줄도 못 읽었다.** 이걸 모르고 UI 만 만들었으면 "왜 비어 있지"로 한참 헤맸다.
→ `create policy read_handoffs on public.handoffs for select to authenticated using (true);` 적용 완료(마이그레이션 `handoffs_read_policy`).

**[결함·중대] 실패 시 무한루프.**
`hoLoad` 가 실패하면 `HANDOFFS` 는 null 로 남는다. 그런데 실패 화면을 그리려고 `renderLog()` 를 부르고, 그 렌더가 다시 `hoLoad()` 를 불러 **페이지가 멈췄다.**
→ `if(HO_STATE==='err'&&!force)return;` 추가. 재시도는 로한이 버튼을 누를 때만.
**t34-I 가 잡았다.** RLS 정책이 없는 상태가 정확히 이 경로였다 — 정책을 먼저 넣지 않았다면 실사용에서 터졌을 것이다.

**[테스트 결함] `#v-log button` 이 탭 버튼을 먼저 잡는다.**
G 블록을 이 셀렉터로 짰더니 "더보기"를 누른다는 게 탭을 눌러서 이후 단정 4개가 엉뚱한 화면을 봤다. → 코드에 `id="verMore"` 를 주고 셀렉터를 특정했다.

**[테스트 픽스처] t20·t23 이 깨졌다 — 코드가 아니라 설계 변경 때문이다.**
백업·스냅샷이 탭 안으로 들어가서 로그 페이지를 열어도 바로 안 보인다. → 두 테스트에 `setLogTab('bak')` 추가.

**[테스트 픽스처] t23 의 Supabase 스텁에 `handoffs` 가 없었다.**
`.select(...).order is not a function` 으로 페이지 전체가 죽었다. → `hoQ()` 추가. **스텁은 테이블별로 갈라야 실제와 같다.**

### 검증

- `tests/t34.js` 신설 — **62건** (탭 프레임 / 목록 / 방 필터 / 마크다운 / 🔒 주입방어 / 활동 / 버전 / 백업 / 실패처리 / 재요청 횟수)
- 주입 방어: `<img onerror>` · `<script>` 를 body 와 title 에 넣고 **실행되지 않음 + 텍스트로 이스케이프됨**을 단정
- 재요청: 탭을 오가도 서버를 다시 때리지 않음(`__hoHits===1`), 강제 새로고침만 재조회
- `./run.sh all` **19개 전부 통과 (150초)**
- 배포 `sha256 bae34393…` 양쪽 일치 확인

### 미해결 / 넘김

- 🟠 **`handoffs` 는 개정 이력이 남지 않는다.** `tax-2024-correction` 이 version 2 인데 row 는 하나다 — **v1 이 사라졌다.** slug 가 키고 version 은 그냥 숫자다. 앱에서 "v2"를 표시해도 v1 을 볼 방법이 없다. append-only 로 바꾸면 해결되지만 지금 급하진 않다.
- 🟠 `logs2` 2건(`정책자금 거치연장 요청` / `방 개설 & 전면 진단`)은 사실상 핸드오프성 기록이다. `handoffs` 로 옮기면 UI 에서 다시 보인다 — 로한 판단 대기.

---

## 2026-09-03 · v2.5 — 🍚 식사 기록 · 💊 복약 용량 · ⇔ 메뉴 접기 · 준비 태그

로한 요청 4건. 순서를 ④→③→①→② 로 잡았다 — 사이드바를 먼저 접어야 넓어진 캘린더에 맞춰 생활 모드를 설계할 수 있다.

### 🔒 판단 (재검토 금지)

**① 복약 용량 — 저장 구조를 바꾸지 않았다.**
`labMeds[i] = {am:[문자열], pm:[문자열]}` 그대로다. `"엔트레스토 100mg"` 처럼 쓰면 **이미 저장된다.**
21개 검사일 × 6~7개 약을 마이그레이션할 이유가 없다. **이름·용량은 읽을 때 파생**한다(`medParse`).
→ 검사일 사이 **증량 ▲ / 감량 ▼ / 신규 ＋** 를 표에 띄운다. 심부전은 용량 변화 자체가 치료 경과다.
⚠️ **단위가 같을 때만 비교한다.** `100mg` 과 `1정` 을 비교하면 거짓말이 된다.

**② 복약 여부에 새 저장소를 만들지 않았다.**
`labMeds` 는 *검사일 시점의 처방*이지 매일 복용 기록이 아니다. 그런데 **루틴·체크가 이미 날짜별로 쌓고 있다**
(로한은 이미 `매일 유산균&비타민C`, `매일 칼마디&오메가3` 를 루틴으로 찍고 있었다).
→ `☀ 아침약(E1)`, `🌙 저녁약(E2)` 루틴 2개를 넣었다. **달성률·캘린더·데일리에 전부 자동 연동된다.**

**③ 식사는 날짜키 맵이다.** `DB.meals[ds] = {b,l,d,s}` — `checks`·`timelog` 와 같은 모양이라 동기화 diff 가 이미 안다.
⚠️ **칼로리 칸은 넣지 않았다** — 매번 못 채워서 빈 칸만 남는다.
⚠️ **빈 값은 저장하지 않는다.** 30년이면 1만 일이고 `app_state` 는 한 덩어리로 매번 왕복한다. 빈 서랍이 그대로 용량이 된다.

**④ 캘린더를 두 모드로 갈랐다.** `💰 가계부`(기존) / `🩺 생활`(루틴·복약·체중·식사).
한 셀에 둘 다 넣으면 셀이 터지고 **둘 다 못 읽는다.**

### ⚠️ 발견한 결함

**[결함·중대] `checks` 를 id 로 읽었다.**
`calLifeBody` 를 처음에 `checks[ds][r.id]` 로 짰다. 실제 구조는 `{due:[코드], done:[코드], miss:{}}` 로 **루틴 코드 배열**이다.
그대로 나갔으면 **알약이 영영 안 켜졌다.** t36-E4 가 잡았다.
🔒 앱이 이미 쓰는 `checkGet()` 을 쓴다 — 판정이 둘이면 반드시 어긋난다.

**[결함] 사이드바를 감싸면서 모바일이 깨졌다.**
모바일 CSS 는 `.side` 가 `.app` 의 직계 자식이라 가정하고 `width:100%` 를 준다. `.sidewrap` 이 폭을 못 받아 탭바가 쪼그라들고,
`right:-11px` 인 토글이 **11px 넘쳐 페이지에 가로 스크롤이 생겼다.** t25·t26 이 잡았다.
→ 모바일에선 `.sidewrap{width:100%}` + 토글 숨김. **이미 가로 스크롤 탭바라 접기 자체가 의미 없다.**

**[결함] 미래 날짜에 회색 알약이 깔렸다.**
안 한 게 아니라 **아직 안 온 날**이다. 한 달 내내 회색 점이 뒤덮여서 오늘까지만 그리도록 고쳤다(스크린샷 보고 잡음).

**[테스트 픽스처] `cpill` 정규식이 `cpills` 래퍼까지 셌다.** 문자열 매칭 대신 요소 수를 센다.

### 구현 메모

- `medParse` 는 **뒤쪽 '숫자+단위'만** 용량으로 본다 — 앞 숫자는 약 이름의 일부일 수 있다(`자디앙 25`).
- 메뉴가 `📅 데일리` 형태라 **이모지가 그대로 아이콘**이다. ⚠️ 이모지는 서로게이트 쌍이라 `::first-letter` 로 못 자른다 — JS 로 한 번 감싼다(`sideWrapLabels`, 중복 감싸기 방지).
- 사이드바 접으면 캘린더가 **124px 넓어진다** — 생활 모드에 딱 필요한 만큼.

### 검증

- `tests/t36.js` 신설 (복약 파서 / 증감 표시 / 식사 / 생활 모드 / 사이드바 / 준비 태그 / 마이그레이션)
- t25·t28 태그 수 기대값 갱신(14→15), t25·t26 가로 넘침 회귀 확인
- `./run.sh all` **21개 전부 통과 (170초)**
- 배포 `sha256 983aaa33…` 양쪽 일치

### 미해결 / 넘김

- 🟠 **`매일 유산균&비타민C`·`매일 칼마디&오메가3` 의 days 가 평일(월~금)뿐이다.** 제목은 '매일'인데 주말엔 체크가 안 뜬다. 복약 루틴 2개는 일~토 전체로 넣었다 — 기존 2개도 맞출지 로한 판단.
- 🟠 식사 데이터가 쌓이면 **검사일 앞뒤 2주 식단 요약**을 피검사 표에 붙일 수 있다. 지금은 데이터가 없어 이르다.
- 🟢 태그가 15개가 됐다. **또 늘리면 코드에 박지 말고 CRUD 로 빼야 한다.**

---

## 2026-09-03 · v2.51 — 🍽 식단 제안 × 실제 · 💊 약 셀 줄바꿈 수정

### ① 한 끼가 두 축이 됐다

로한: *"생활방 로버트가 순환기 핸드오프와 예산을 보고 일주일치 식단을 짜서 조율한다. 왼쪽은 제안, 오른쪽은 실제."*

```
meals[ds][slot] = {p:'제안(생활방 로버트)', a:'실제(로한)'}
```

🔒 **둘을 한 칸에 합치지 않는다.** 합치면 '먹기로 한 것'과 '먹은 것'을 영영 못 가른다. **그 차이가 이 화면의 존재 이유다.**

- 데일리 식사 탭이 2열 그리드 — 왼쪽 제안(베이지), 오른쪽 실제. 일치하면 실제 칸이 연녹색.
- **⤵ 제안대로 먹었다** 버튼 — 매일 네 칸을 다시 타이핑하지 않는다. ⚠️ 이미 다르게 적은 칸은 덮지 않는다.
- 캘린더 생활 모드는 **실제를 먼저** 보여주고, 없으면 제안을 🍽 아이콘 + 흐리게(`.cmeal.plan`).
- 데일리 탭 뱃지: 실제 있으면 ●, 제안만 있으면 🍽.

⚠️ **제안은 생활방 로버트가 Supabase 에 직접 넣는다.** 앱에서도 고칠 수 있게 열어뒀다 — 조율 결과를 로한이 바로 반영할 수 있어야 한다.

**마이그레이션**: v2.5 의 문자열을 `{p:'',a:문자열}` 로 감싼다. ⚠️ **멱등이어야 한다** — 두 번 감싸면 `{a:{a:'…'}}` 가 된다. t36-I6 이 잠갔다.

### ② ⚠️ [결함] v2.5 약 셀이 세로로 터졌다

`medcellStyle` 이 `white-space:normal` + `max-width:120px` 였다. v2.4까지는 약 이름만이라 한 줄에 들어갔는데, v2.5에서 용량이 붙자 `자디앙듀오 12.5/850mg` 가 두 줄이 되고 **약 7개면 셀이 14줄**이 됐다. 로한 스크린샷으로 발견.

→ **한 줄로 못 박는다**(`nowrap`, `max-width` 제거). 표는 이미 가로 스크롤이라 **세로로 터지는 것보다 가로로 긴 게 낫다.**

**💊 용량 켬/끔 토글** 추가(피검사 헤더). 끄면 이름만 보이고 용량은 마우스 오버 시 툴팁.
🔒 **▲▼＋ 는 끄지 않는다** — 용량 변화를 보는 게 이 표의 목적이다. 숨기면 매번 짚어야 한다.

로한이 "좌우 확장이든 마우스 오버든" 둘 다 제시했는데 **둘 다 넣었다** — 기본은 확장(정보를 숨기지 않는다), 필요할 때 접는다.

### 검증

- t36 확장: 제안/실제 분리 저장, ⤵ 복사(덮어쓰기 금지 포함), 캘린더 제안 표시, 약 셀 nowrap·토글·툴팁, 마이그레이션 멱등
- ⚠️ **[테스트 픽스처] `curDate()` 는 `goalDate` 를 쓰고 미래를 오늘로 자른다.** 제안 검증일을 미래(09-04)로 잡았다가 데일리가 안 열렸다. 과거(09-01)로 옮겼다.
- `./run.sh all` **21개 전부 통과 (172초)**
- 배포 `sha256 99953b3c…` 양쪽 일치

### 넘김

- 🟠 생활방 로버트가 주간 식단을 넣는 **SQL 템플릿**을 정리해두면 매주 반복이 빨라진다.
- 🟠 식사 데이터가 2주 쌓이면 **검사일 앞뒤 식단 요약**을 피검사 표에 붙일 수 있다.

---

## 2026-09-03 · v2.6 — 🔗 가계부↔데일리 연동 · 입력순 · 기본값 자동화

로한 요청 6건. 그중 ⑥번이 **버그 신고였는데 실은 설계 잔재**였다.

### ⚠️ [결함·중대] 현황판이 거래를 안 따라왔다

로한: *"가계부 입력시 현황판 잔액이 실시간으로 변경되지 않는다. 일간 1번 업데이트인지, 버그인지."*

원인: `accVal(a) = latest(a.hist)` — **총자산·순자산·그룹합계·목표 현재값이 전부 hist(수기 스냅샷)만 봤다.**
거래를 아무리 넣어도 hist 가 안 바뀌니 숫자가 안 움직인다. 일간 갱신도 아니고 **영원히 안 바뀌는 구조**였다.

🔒 **버그가 아니라 기초잔액이 없던 시절의 잔재다.** 그때는 hist 가 유일한 진실이었다.
9/1 에 기초잔액을 박은 지금, 현금성 계좌는 `opening + 거래` 로 파생할 수 있다.

```js
function accIsDerived(a){return !!(isCashAcct(a)&&a.opening&&a.opening.date);}
function accVal(a){return accIsDerived(a)?acctComputed(a):latest(a.hist);}
```

⚠️ **투자·저축·외환은 그대로 hist 다** — 거래가 아니라 **평가액**이라 파생이 불가능하다.
→ 파생 계좌는 '현재 평가액' 칸을 **읽기전용**으로 막고, 저장해도 hist 를 찍지 않는다.
   읽히지도 않는 값이 쌓이면 나중에 '왜 다르지'가 된다.

**이게 8/31 에 로한이 겪은 "기초잔액만 넣으면 현황판이 안 바뀐다" 의 근본 원인이었다.** 그때는 두 칸을 다 채우게 해서 넘겼지만, 이제 한 칸만 채우면 된다.

### 나머지 5건

**① 🍽 끼니 슬롯** — 식비·배달·카페 거래에 아침/점심/저녁/간식 칩. 고르면 **데일리 식사 기록(실제)에 같이 들어간다.**
⚠️ 기본은 '해당없음' — **식재료·장보기는 끼니가 아니다**(로한 지적).
🔒 덮어쓰지 않고 **이어붙인다**(밥 + 음료). 같은 말은 두 번 안 넣는다.

**② 사업 → 카페 + 카카오 개인사업자 체크카드** 자동. **⑤ 수입 → 우리 SUPER** 자동.
⚠️ 둘 다 **수정 중에는 끼어들지 않는다**(`modal._editing`) — 이미 고른 값을 조용히 바꾸는 게 가장 나쁜 버그다.
⚠️ 계좌를 **이름으로 찾는다**(`mainInAcct`) — id 를 코드에 박으면 계좌를 지웠다 만들 때 끊긴다.

**③ 입력순 정렬** — 로한: *"토스 내역 보고 맞춰보기 쉽게."*
거래 목록이 **금액 내림차순**(`sort(b.amt-a.amt)`)이었고 캘린더 칸은 **수입→이체→지출 묶음**이었다. 통장 내역은 시간순이라 대조할 때마다 눈이 튀었다.
🔒 **정렬하지 않는 것이 곧 입력순이다.** 두 곳 다 sort 를 걷어냈다.

**④ 캘린더 칸 마우스오버 → 적요** (`txnTip`: 계정과목·금액·수단·음식·메모·미결제).
⚠️ 적요가 없으면 따옴표를 안 넣는다 — 빈 툴팁을 띄우지 않는다.

### 검증

- `tests/t37.js` 신설 — 파생 계좌 / 읽기전용 / 입력순(목록·캘린더) / 툴팁 / 사업·수입 기본값(수정 중 미개입 포함) / 끼니 슬롯(이어붙이기·중복방지·식재료 예외)
- `./run.sh all` **22개 전부 통과 (175초)**
- 배포 `sha256 7002fccb…`

### 넘김

- 🟠 `netSnapshots` 가 이제 파생 총자산을 기록한다 — 9월 말 첫 자동 스냅샷이 정확한지 확인할 것.
- 🟠 사업 기본값(카페·카카오)이 안 맞는 달이 오면 **카테고리별 기본 수단**으로 일반화해야 한다. 지금은 로한 패턴에 맞춘 하드코딩이다.

---

## 2026-09-04 · v2.7 / v2.8 — 🎓 학습 페이지 신설

### ⚠️ [실책] v2.6 끼니 연동이 실사용에서 안 보였다

로한: *"분명히 식비 카테고리랑 데일리 뭘 먹었나 연동시키라고 했는데 반영 안되어있다."*

**코드는 v2.6 에 들어가 있었다.** 안 보인 이유는 데이터였다:

```
배달        isFood: true   ✅
식비        isFood: null   ❌
카페        isFood: null   ❌
간식/편의점  isFood: null   ❌
```

끼니 칩은 `isFood` 가 켜진 계정과목에서만 뜬다. **테스트 픽스처에는 켜두고 실제 DB 는 확인하지 않았다.**
t37-G 가 통과한 건 픽스처가 켜져 있어서다.

🔒 **플래그로 켜지는 기능은 실데이터에서 그 플래그를 확인해야 한다.** 픽스처 통과는 증명이 아니다.
→ DB 에서 세 개를 켰다(SQL).

### 🎓 학습 페이지 — 3층 구조

로한: *"루틴이 데일리 관리·로그라면 학습은 학습만 집중한다. 그리고 특이점은 **테스트**다 — 로버트가 시험을 내고, 내가 풀고, 점수를 체크한다."*

**진단**: C 계열이 전부 `binary` 였다. "일 6뽀모 달성" 이 달성/미달 둘 중 하나라 **30일 중 며칠 했는지가 안 남았고**, JLPT N4 까지 뭘 얼마나 해야 하는지가 어디에도 없었다.

```
뽀모(투입)  →  진도(산출)  →  점수(검증)
"얼마나 했나"  "어디까지 갔나"   "늘었나"
```
셋이 같이 있어야 **"많이 했는데 왜 안 늘지"** 에 답할 수 있다.

### 🔒 판단 (재검토 금지)

**① 목표 시스템을 새로 만들지 않았다.** 연간(C)·월간 눈금(C.9)은 `goals` 가 하나의 진실이다. 학습 페이지는 **읽기만** 하고, 화면에도 "목표·루틴 탭에서 고친다" 고 박아뒀다.
⚠️ 주간·일간은 goals 에 넣지 않는다 — 그건 목표가 아니라 **계획**이고, 넣으면 1년에 52+365개가 쌓여 목표 탭이 터진다.

**② 교재 진도는 파생이다.** `시작값 + 매일 기록`. 저장하면 "언제 얼마나 나갔나"가 사라지고 두 숫자가 어긋난다.

**③ 루틴 자동 체크는 켜기만 하고 끄지 않는다.** 뽀모를 줄였다고 풀면 **손으로 켠 체크까지 날아간다.**

**④ 주간 플랜은 식단과 같은 구조다.** p(로버트 제안) × a(실제). 합치면 '하기로 한 것'과 '한 것'을 못 가른다.
⚠️ 주차 키는 **그 주 일요일 날짜**다 — 'W37' 같은 번호는 연말에 애매해지고, `weekStart()` 가 이미 일요일 시작이라 계산이 하나로 끝난다.

**⑤ 테스트는 결과만 남긴다.** 문제 본문은 대화방(핸드오프)에 있다. 앱에 시험지를 넣지 않는다.

### 구현 메모

- **예상 완료일**: 최근 14일 속도로 남은 분량을 나눈다. "이 속도면 시험 전에 못 끝낸다" 가 보인다. ⚠️ 기록이 없으면 계산하지 않는다(0 으로 나눈 무한대를 안 보여준다).
- **목표 뽀모**: 설정값 우선 → 없으면 월간 눈금 제목에서 `(\d+)뽀모` 추출. ⚠️ 추측이므로 화면에 `눈금 C.9` 라고 출처를 밝힌다.
- **⏰ 리듬 넛지**: 일요일 → 주간 플랜 / 월말 3일 → 다음 달 눈금. 이미 적어둔 주·달에는 안 뜬다. 강요하지 않는다.
- ⚠️ 날짜 산수를 직접 하지 않는다 — `lastDayOfYM`·`ymAdd` 가 이미 있고 연말 넘김까지 처리한다.
- ⚠️ [결함] 주간 표 헤더가 span 4개인데 격자가 3열이라 밀렸다 → `.swhdmid` 중첩 격자로 수정(스크린샷으로 발견).

### 검증

- `tests/t38.js` 신설 — 목표 읽기전용 / 목표뽀모 출처 / 루틴 자동체크(해제 안 함 포함) / 진도 파생 / 예상완료일 / 테스트 / 주간 플랜 / 넛지 / 마이그레이션
- `./run.sh all` **23개 전부 통과 (172초)**
- 배포 `sha256 7cb2877d…`

### 넘김

- 🟠 **학습방 로버트가 첫 주간 플랜을 넣어야 한다.** `study.plan[일요일날짜].d[요일].p`
- 🟠 교재는 로한이 직접 입력 (📚 탭 → + 교재)
- 🟠 다음 달(2026-10) 눈금 C.10 은 이미 있다 — 월말 넛지는 11월분부터 뜬다

---

## 2026-09-05 · v2.9 — 🎯 과락 게이지 · ❌ 오답노트

학습방 핸드오프 id=8(`study-page-spec` v3)을 받아서 알맹이 두 개만 먼저 넣었다.

### 스펙에서 잡아낸 것 (그대로 따르지 않았다)
- **"학습시간은 timelog 의 study 태그에서 파생한다"** → ⚠️ **파생할 원본이 0건이다.**
  timelog 에 12일치 기록이 있지만 study 태그는 하나도 없다. 지금 파생으로 걸면 **항상 0시간**이 나온다.
  → 시간은 당분간 표시하지 않는다. 뽀모(투입)로 갈음한다.
- **주차 키를 월요일로 하자** → 앱 전체(캘린더·보상 주간·weekStart())가 **일요일 기준**이다.
  두 기준을 섞으면 같은 주가 두 개가 된다. → **일요일로 통일.**
- **시험일** → 스펙에 날짜가 없다. **추측하지 않았다.** 비어 있으면 D-day 를 아예 안 그리고, 로한이 넣는다.

### 넣은 것
**🎯 과락 게이지** — 이게 핵심이다.
- 🔒 **총점 하나는 거짓말을 한다.** JLPT 는 과목별 과락이 있어 **총점 110 이어도 청해 15 면 불합격**이다.
  그래서 숫자 하나가 아니라 **과목별 막대 + 과락선**.
- 기준은 JLPT N4 공식값: 언어지식·독해 120(과락 38, 목표 60) / 청해 60(과락 19, 목표 40) / 합격 90.
- 🔒 **총점은 파생**이다 — 과목 점수만 저장한다. 같이 저장하면 두 숫자가 어긋날 자리가 생긴다.
- ⚠️ **안 친 과목을 0 으로 치지 않는다** — 그러면 **없는 과락**이 생긴다. `—` 로 두고 판정에서 뺀다.
- 테스트에 **유형**(주간 확인 / 모의고사 / 복습) 신설. 모의고사만 과목 점수를 받고 문항수·맞은수 축은 안 쓴다.
  점수 % 스파크에도 안 섞는다 — 180점 만점과 20문항 %를 한 그래프에 두면 거짓말이다.

**❌ 오답노트** — P4(마감) 국면의 유일한 과제.
- 🔒 **2회 연속 맞아야 회수**한다. 한 번 맞으면 지우는 방식은 **운으로 맞은 것까지** 지운다. X 하나면 hits 0.
- 분류 7개(가나·단어·한자·활용·문법·독해·청해), 분류별 미회수 카운트, 탭에 미회수 뱃지.
- 📄 **복습 뽑기** = 미회수만, **정답은 빼고** 텍스트로. 붙여넣고 바로 답이 보이면 시험이 아니다.
- 출제는 여전히 대화방이다. 여기 있는 건 **다시 볼 목록**이지 시험지가 아니다.

### 결함 (셋 다 스크린샷으로 잡았다 — 테스트는 전부 통과하고 있었다)
1. **과락선이 사라졌다.** 과락선을 빨강으로 뒀는데 **과락일 때 막대도 빨강**이라 선이 묻혔다.
   경고가 필요한 바로 그 순간에 안 보이는 선이다. → 선은 잉크색, 경고는 글자로.
2. **`87/90` 이 '87점 만점 90'처럼 읽힌다.** 90 은 만점이 아니라 **합격선**이다. → `87점 · 청해 과락`.
3. **모의고사 2회를 추이라고 그렸다.** 점 두 개는 추이가 아니다. flex 로 늘어나 회색 판이 됐다. → 3회부터.

### 검증
- t39 신설 50건. `./run.sh all` **24개 전부 통과**(172초).
- 배포 전 수동 스냅샷 `app_state_snap #49`.

### 🚫 하지 않은 것 — 카드 거래 `acct` 소급 보정
로한 요청: *"932건 중 839건에 acct 없음. method → 계좌 매핑 테이블을 두고 자동 부여, 기존 839건 소급 보정."*
→ **하지 않았다. 하면 v1.42 를 되돌리는 것이고, 그때 고친 결함을 되살린다.**

실제 조회 결과:
- 카드 5장의 `card.acct` 가 **로한이 요청한 매핑과 정확히 같다** — 우리(K패스)·네이버페이 → `xa1655eq`,
  카카오-사업자체크 → `xcnyr6sj`, 삼성 탭탭오·ID MOVE 는 credit 이라 settle 로만 차감.
- `txnAcct()` 가 **카드 거래의 계좌를 저장하지 않고 `card.acct` 에서 파생**한다. 잔액·Sankey·합계 전부 이 판정을 쓴다.
- 즉 **839건에 acct 가 없는 것이 정상이고 설계다.** 있으면 오히려 카드 연결계좌를 바꿨을 때 과거가 옛 계좌를 가리킨다.
- 진짜 미지정(현금·이체인데 사람이 안 고른 것) = **30건, 전부 기초잔액(2026-09-01) 이전** → 현재 잔액에 영향 0.

⚠️ **교훈:** SQL 로 `acct` 없는 건수만 세면 이 결론이 또 나온다. 이 필드는 **현금·이체 전용**이다.

### 넘김
- 🟠 주간 3단 플로우(①지난주 판정 → ②미이월 → ③이번주 설정). `carried≥2` 빨간 뱃지.
- 🟠 Phase P1~P4 배지 + goals 마일스톤 신설. 구 눈금 C.8/C.9/C.10 보관(⚠️ 루틴 `xahvyik8` 의 goalId 는 **옮기지 마라** — 소속 이동 시 과거 체크 유실 결함 미해결).
- 🟠 시험일 확정 필요 — 로한이 직접 넣어야 D-day 가 뜬다.

