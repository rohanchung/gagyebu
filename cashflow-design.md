# 현금흐름 100% 정합 시스템 — 설계문서 v0.1

> 목적: 2026년 9월부터 "앱이 계산한 잔액 = 실제 은행 잔액"이 항상 일치하게 만든다.
> 상태: **v2.0 구현 완료 (2026-08-12).** P1~P5 전부 코드 반영. 9/1 기초잔액 입력하면 즉시 가동.
> 남은 것: 로한이 9/1 0시 각 계좌 기초잔액 입력 → 이후 거래에 계좌/이체 붙여 입력.

---

## 1. 지금 왜 안 맞는가 (문제 정의)

현재 앱은 **두 개의 세계가 따로 논다.**

| | 현재 방식 | 문제 |
|---|---|---|
| 거래(가계부) | 수입·지출만 기록 | "어느 계좌에서" 나갔는지 없음 |
| 계좌 | 손으로 찍는 스냅샷(hist) | 거래와 연결 안 됨 |
| 결제수단 | 삼성카드/카카오 라벨 | 계좌 잔액과 무관한 문자열 |

핵심 결함 두 가지:

1. **거래에 계좌가 없다.** 그래서 지출을 아무리 정확히 적어도 "그래서 우리은행 잔액이 얼마 줄었나"를 앱이 모른다.
2. **이체 개념이 없다.** 로한의 자금운용은 절반이 지출이 아니라 **계좌 간 이동**이다:
   - 급여 → 우리은행 수취
   - 우리 → 카카오 이체
   - 카카오 → 세이프박스(데일리 이자)
   - 세이프박스 → 각 생활계좌 10만원씩 분배

   이 이동이 앱엔 존재하지 않으니, 옮길 때마다 어긋난다.

**결론: 구조를 안 바꾸면 100%는 원천적으로 불가능하다.**

---

## 2. 목표 (완성 상태 정의)

- 각 계좌마다: `앱 계산잔액`이 실제 은행 잔액과 **오차 0**.
- 모든 돈의 이동(수입·지출·이체)이 **출발/도착 계좌**를 갖는다.
- 데일리 이자 같은 자투리는 **일일이 안 적어도** 월말에 정확히 잡힌다.
- 순자산·현금성 자산이 실시간으로 신뢰 가능.

---

## 3. 핵심 개념 (딱 4개)

### 개념 1 — 기초잔액(Opening Balance)
각 계좌에 "출발점" 하나를 박는다. 예: `2026-09-01 우리은행 = 1,234,567원`.
이후 모든 계산은 여기서 시작. → `계좌잔액 = 기초 + Σ입금 − Σ출금`.

### 개념 2 — 거래에 계좌를 붙인다
- **지출**: 출금계좌 지정 (어디서 빠졌나)
- **수입**: 입금계좌 지정 (어디로 들어왔나)

### 개념 3 — 이체(Transfer) 타입
계좌 A → 계좌 B. **순자산 불변**, 계좌잔액만 이동.
로한의 자금분배(우리→카카오→세이프박스→생활계좌)가 전부 이 한 타입으로 처리된다.

### 개념 4 — 대조(Reconciliation)
주 1회 또는 월말, 계좌별로:

```
[ 앱 계산잔액 ] vs [ 실제잔액 입력 ] → [ 차액 ]
```

차액이 곧 "앱이 놓친 돈" = 대부분 **데일리 이자**.
버튼 하나로 → 차액을 그 계좌 **이자수입**으로 계상 → 즉시 오차 0.

---

## 4. 이자 처리 원칙 (중요)

> 데일리 이자 10원단위를 일일이 기록하지 않는다. 그건 노동이고 오류원이다.

- 이자는 **실시간 추적 대상이 아니라, 대조 시 자동 흡수 대상**이다.
- 월말 대조에서 각 계좌 차액을 "이자수입"으로 한 번에 계상.
- 은행 앱이 이미 월 이자합을 보여주므로 **검증도 된다** (차액 ≈ 은행 이자내역이면 정상).
- 차액이 이자보다 크면 → "적다 만 거래"가 있다는 신호. 대조가 곧 감사(audit) 기능.

---

## 5. 데이터 모델 변경안

### accounts (계좌)
```
+ opening: { date: '2026-09-01', amount: 1234567 }   // 기초잔액
```
→ 잔액은 더 이상 수기 스냅샷이 아니라 `opening + 거래 자동계산`.
(기존 hist 스냅샷은 과거 추세 조회용으로 유지 가능)

### transactions (거래)
```
+ acct:     출금/입금 계좌 id       // 지출·수입
+ type:     'income' | 'expense' | 'transfer'   ← transfer 신규
+ from:     출발계좌 id  (transfer 전용)
+ to:       도착계좌 id  (transfer 전용)
```

### 신용카드 지출 특례
- 체크카드·이체·현금 지출 → 즉시 해당 계좌 출금
- **신용카드 지출 → 현금 불변**, 나중에 **선결제 확정 시** 지정한 계좌에서 출금
- → 이미 만든 **선결제 체크리스트**에 "출금계좌" 지정만 추가하면 현금흐름과 완전 연결

---

## 6. 화면 흐름 변화

### 거래 추가 모달
- 지출: `유형 · 대분류 · 계정과목 · [출금계좌] · 금액 · 날짜`
- 수입: `유형 · 계정과목(소득구분) · [입금계좌] · 금액 · 날짜`
- **이체(신규 탭)**: `[출발계좌] → [도착계좌] · 금액 · 날짜` (계정과목 없음)

### 계좌·부채 탭
- 각 계좌에 `기초잔액(9/1)` 표시 + `앱 계산잔액` 표시

### 대조 (신규 **현금흐름 탭** 안, 실시간)
별도 주기 없음. 가계부가 실시간 반영이므로 이 표도 **항상 최신**. 실제잔액만 입력해두면 차액이 상시 보인다.
```
계좌         앱계산      실제입력      차액        조치
우리은행     882,300    [       ]    —          [이자로 반영]
카카오       120,450    [       ]    —          [이자로 반영]
세이프박스   1,153,610  [       ]    —          [이자로 반영]
```

---

## 7. 9월 시작 세팅 절차 (로한 실행)

1. **8/31 밤 또는 9/1 아침**, 각 계좌 실제 잔액을 은행 앱에서 확인.
2. 앱에 계좌별 `기초잔액(2026-09-01)` 입력.
3. 9월부터 모든 지출/수입/이체에 계좌를 붙여 기록.
4. **매주 일요일 5분** 대조 루틴 (또는 월말 1회).
5. 첫 달 지나면 오차 패턴이 보임 → 이자 자동흡수로 수렴.

---

## 8. 현금흐름 시각화 페이지 (신설) 🌊

> 이 시스템의 "출력부". 돈의 위치와 이동을 한눈에.

### 형태: Sankey(생키) 다이어그램
좌 → 우로 흐르는 강물 그림. 선의 굵기 = 금액.

```
[급여]───────┐
             ├─▶[우리은행]──┬─▶[카카오]──▶[세이프박스]──┬─▶[생활비]──▶ 식비/카페/배달…
[사업수입]───┘             │                          ├─▶[저축]──▶ 노랑우산
[이자]────────────────────┘                          └─▶[투자]──▶ ETF
```

### 노드(칸) 3종
1. **원천(수입)**: 급여 · 사업수입 · 이자 — 돈이 들어오는 입구
2. **계좌**: 우리 · 카카오 · 세이프박스 … — 돈이 머무는 곳(이체는 계좌↔계좌 선)
3. **종착(지출/저축)**: 소비는 **계정항목**으로(식비·카페·배달…), 저축·투자는 각 목적지로

### 규칙
- **소비** = 계좌 → 계정항목 노드로 흘러 나감(빠져나가는 강)
- **저축/투자** = 계좌 → 저축/투자 노드(순자산 유지, 위치만 이동)
- **이체** = 계좌 → 계좌(내부 순환)
- 선 굵기 = 금액, 색 = 성격(수입 초록 / 지출 빨강 / 이체 회색 / 저축 파랑)

### 기간 토글
`이번 달 · 올해 · 전체` — 같은 그림을 기간만 바꿔서. 월별로 보면 습관이 보이고, 전체로 보면 구조가 보인다.

### 기대 효과
- 로한: "내 돈이 실제로 어디로 새는지/쌓이는지" 직관적 파악 → 관리 포인트 자각
- Claude: 같은 그림을 데이터로 읽어 전략 제안 가능(예: "세이프박스 체류시간이 짧다 = 이자 못 받고 바로 소비로 빠진다")

### 구현 메모
- 데이터는 P1~P2(계좌·이체)가 있어야 그려진다. 그 전엔 그릴 소스가 없음.
- 라이브러리: D3-sankey 또는 경량 커스텀 SVG. (외부 CDN 최소화 원칙 고려해 커스텀 SVG 우선 검토)

---

## 9. 구현 로드맵 (Phase)

| Phase | 내용 | 산출 |
|---|---|---|
| **P1** | 계좌 기초잔액 필드 + 거래에 계좌 붙이기 + 잔액 자동계산 | 잔액이 거래로 자동 산출 |
| **P2** | 이체 타입 추가 (계좌 간 이동) | 자금분배 정확 반영 |
| **P3** | 대조 화면 + 차액→이자수입 자동계상 | 오차 0 달성 |
| **P4** | 선결제 확정에 출금계좌 연결 | 신용카드까지 현금흐름 통합 |
| **P5** | 🌊 현금흐름 시각화 페이지(Sankey, 월/연/전체) | 돈의 흐름 직관화 |

---

## 10. 결정 사항 — 전부 확정 ✅

1. **적용 시점**: 로한이 **9/1 0시까지** 계좌·가계부를 정리. **그 시점부터 새 방식.** 8월까지 데이터는 그대로 둔다.
2. **대조 주기**: 없음 = **실시간.** 가계부가 실시간 반영이므로 계산잔액이 항상 최신. 실제잔액만 넣어두면 차액 상시 표시.
3. **화면 구성**: **현금흐름 전용 탭 신설.** 대조 + Sankey를 한 탭에서 단독으로 본다. (좌측 네비에 `🌊 현금흐름` 추가)
4. **대상 계좌**: 우리 · 카카오 · 세이프박스 · 토스외환 등 **일상 현금만**. 투자·공제는 대조 제외(단 Sankey 종착 노드로는 표시).

---

## 11. 신설 탭 구성: `🌊 현금흐름`

한 탭 안에 위→아래로:

1. **기간 토글** — 이번 달 · 올해 · 전체
2. **Sankey 다이어그램** — 원천→계좌→종착(소비/저축/투자) 흐름
3. **실시간 대조표** — 계좌별 [앱계산 | 실제입력 | 차액 | 이자반영]
4. **요약** — 총 유입 / 총 유출 / 순증 / 계좌별 기말잔액

---

*문서 확정 완료. P1부터 착수 가능. 목표: 9/1 세팅 즉시 가동.*
