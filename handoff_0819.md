# 🗂️ HANDOFF — 로한 ↔ 로버트 인수인계 로그

> **이 파일이 규칙이다.** 새 방(대화)을 열면 로한이 "showmethemoney 폴더에서 **가장 최신 handoff_MMDD.md** 랑 index.html 읽어"라고 말한다.
> 로버트는 압축된 기억에 의존하지 말고 **이 파일을 진실의 원천(source of truth)** 으로 삼는다.
> **최종 갱신 2026-08-19 · 앱 v8.0** — 상세 로그는 8/18~8/19 2일치, 그 이전은 요약(원문은 `handoff_0818.md`).

---

## 📜 프로토콜 (로버트가 지킬 규칙)

1. **새 방 시작 시 읽을 것 (순서대로):**
   - `handoff_MMDD.md` ← **이 파일. 가장 최신 날짜 하나만.** 옛 날짜 파일은 안 읽어도 된다 — 유효한 상태는 전부 여기 있고, 옛 파일은 상세 로그 원문 보관용이다.
   - `goal-design.md` ← **목표·루틴 시스템 설계 기준.** v6.0 이후 이 영역을 건드릴 땐 반드시 먼저 읽는다.
   - `integration-design.md` ← **파트 간 연계 설계.** 보상↔가계부, 명리 권장일 등 파트를 가로지르는 작업 전 필독.
   - `index.html` ← 앱 전체 코드 (단일 파일 SPA)
   - 필요 시: `cashflow-design.md`(현금흐름 설계), `요구사항정의서.md`(초기 스펙)
2. **데이터가 필요하면** → Supabase MCP로 조회 (아래 접속정보). 앱 화면 추측 금지, 실제 조회.
3. **날짜가 바뀌면** → 이 파일을 `handoff_(새날짜).md`로 복사하고, 맨 위 로그에 새 항목만 얹는다. 옛 파일은 남겨둔다(히스토리).
   - **⚠️ 작업 시작 전에 오늘 날짜부터 확인한다.** 로한이 말해주기를 기다리지 않는다. (8/19 작업이 통째로 `handoff_0818.md`에 들어가 파일이 28KB로 불어난 사고가 있었다.)
   - **로그 상세는 최근 2일치만 유지.** 그 이전은 **한 줄 요약 + 옛 파일 포인터**로 압축한다. 압축해도 되는 이유 = 설계 근거는 `goal-design.md` / `integration-design.md` / `cashflow-design.md`에 남아 있고, handoff는 **지금 유효한 상태**를 전달하는 문서지 아카이브가 아니다.
   - 목표 크기 **35KB 이하.** 넘으면 압축을 안 한 것이다. (0819 = 34KB — 다음 날짜로 넘길 때 8/18 상세를 요약으로 내려라.)
4. **응답 원칙**: 로한 선호 = 결론→왜→다음. 아부·보험멘트 금지. 불확실하면 "불확실" 명시. 로버트는 재정 조언 시 "면허 있는 자문가 아님" 밝힘.
5. **로버트 = 나(Claude)의 이름.**
6. **결과물은 폴더에 넣는 것이 목적이다. 대화창에 파일을 띄우는 건 수단일 뿐.** (2026-08-19 로한 지시)
   - 로한은 받자마자 푸시해서 실사용한다. 파일 카드 자체는 아무 쓸모가 없다.
   - **작은 텍스트 파일(handoff·설계문서)** → `device_bash`로 디바이스에서 직접 작성/패치. 카드 안 뜬다.
   - **`index.html`처럼 큰 파일** → 클라우드에서 수정·검증 후 `SendUserFile` → `device_commit_files`.
     ⚠️ **이 경로는 대체 불가다.** 컨테이너와 디바이스는 파일시스템이 분리돼 있고,
     바이트를 옮기는 다리가 `file_uuid` 하나뿐이다. `device_bash`로 300KB를 넣으려면
     내용을 전부 내 출력으로 타이핑해야 해서 토큰이 수만 단위로 든다. **카드 1장이 훨씬 싸다.**
   - **반드시 양쪽 sha256을 대조한다.** 체크섬이 맞아야 "검증한 그 파일이 디스크에 놓였다"가 성립한다.
   - 완료 보고는 **텍스트로만** — 무엇을 왜 고쳤는지 + 커밋 메시지. 파일 설명은 쓰지 않는다.
7. **작업 완료 시 항상 커밋 메시지를 써줄 것.** GitHub Desktop 기준 **Summary**(한 줄, `v버전 — 무엇을`) + **Description**(변경 항목 불릿 + 검증 결과 + 주의사항). 로한이 복붙만 하면 되게.

---

## 🧭 시스템 지도

**앱**: "Rohan Book" (구 가계부). 단일 `index.html` SPA. 재정+건강+명리(사주)+일지 통합. 현재 **v8.0**.
**배포**: GitHub Pages, 공개 repo `rohanchung/gagyebu`. (넷리파이에서 이관함 — 무료티어 배포 막힘 이슈)
- 코드 변경 → **commit + push** 필요 (GitHub Desktop).
- 데이터 변경 → Supabase 직접 쓰기, **커밋 불필요.**

**Supabase**: project_id `ytkbrdgbklnijbwkvino`. 단일 `app_state.data` jsonb 행에 전 상태 저장. `migrateDB()`로 스키마 진화.

**스킬 3개 (모든 방에서 발동)**:
- `load-my-finance-health` — 재정·건강 현황 스냅샷
- `evaluate-my-saju` — 사주 세운/월운/일운 평가 (고정 원국)
- `check-account-security` — 계정 로그인 이력 점검

---

## 👤 로한 프로필 (고정 사실)

- IT기획자 출신. 기획·사업·마케팅·창작 병행.
- 명리 앱은 **결정론적 만세력 엔진** 자체 구현(API 없음): Julian Day 일진 + Meeus 태양황경 근사 절기/월주 + 60갑자.
- **건강**: 키 **163cm** (BMI 역산 확정). 체중 하락 추세(약 78→68kg대). 8/5 기준 68.2kg. 피검사 21회 × 77지표 임포트됨.

---

## 💰 재정 로드맵 (2026-08 확정, 은행 실제숫자로 갱신 예정)

- **풍무점 이전 = 2026년 10월, 확정·강제.** 월세 아끼려는 게 아니라 출근지 이동. → 보증금 **500~1000만** 필요, 그중 **현금 500 확보가 관건.**
- **부채 금리 우선순위**: 약관대출 **5.5%** > 정책자금(소상공인대환) **3.11%** > 세이프박스 ~2%.
  - → **약관대출 먼저 상환.** 정책자금은 일정대로만 납부.
- **로드맵**: 2026 = 보증금용 저축(현실적 상한 ~500만, 1천만은 비현실) · 2027 = 약관대출 상환 + 신용 구축 · 2028 = 전세 점핑(1억 오피스텔은 현금 1~2천 필요, 지금은 불가).
- 신용점수 900+ 안정. 약관·정책 대출은 신용 영향 미미.
- ✅ **앱 부채 계산 교정 완료 (8/14)**. 소상공인대환 실제 방식 = **원금 고정 416,000원**(1회차 448,000, 단수 조정) + **이자 = 잔액 × 3.11% × 경과일수 / 365**(일할). 납입일 매월 19일, 주말이면 익영업일. 총 48회 · **총이자 1,269,938 · 총상환 21,269,938 · 월평균 443,124**. 앱 `bankSchedule()`이 은행앱 6회차와 원 단위 일치.
- 약관대출 잔액 **1,990만 @5.5%** (SEED도 실제값으로 동기화).

---

## 🧾 날짜별 로그 (최신이 위)

### 2026-08-19
- **🎁 v8.0 — 보상 카드 모델. 로한이 설계했고 내 안보다 나았다.**
  - **로한의 지적**: *"보상에서 입력한 걸 가계부에 선 반영해버리면, 그 보상 카드를 안 썼을 때 가계부가 너무 더러워진다"*.
    맞다. v7까지는 **'수령 확정' = 가계부 거래 생성**이라 받기로 해놓고 안 쓴 보상이 **유령 거래**로 남았다.
    **거래는 실제 지출이 발생하는 순간에만 생겨야 하고, 카드는 거기에 '붙는' 것이다.**
    부수 효과: 수령 시점과 실제 소비 시점이 어긋날 여지가 사라진다 — 두 사건이 하나가 된다.
  - **카드 생명주기**: `보상함(종류=덱)` → **배치**(예약) → `placed` → **계단 달성** → `open` → **소비하며 가계부 입력** → `used`(소멸)
  - **🔒 상태는 저장하지 않고 파생한다.** 저장하는 사실은 **배치와 사용뿐.**
    `usedAt` 있으면 used, 없으면 `tierOpen()` 계산 → open/placed.
    문턱(`rewardCfg`)을 바꾸거나 과거 기간을 다시 봐도 판정이 어긋나지 않는다.
  - **🔒 카드는 종류를 스냅샷한다.** 보상함 원본(M4 '3만원 이하')을 나중에 고쳐도 **발급된 카드는 불변.**
    v6.0 '과거 달성률 소급변동'과 같은 실수를 여기서 반복하면 안 된다.
  - **배치 3경로**: 보상함에서 고르기 / **버킷에서 올리며 즉시 배치** / 새로 만들며 즉시 배치.
    로한 지적 *"저기서 선택하거나 생성이 가능해야 한다"* — v7엔 생성만 있었다.
  - **[버그] 목표 달성 보상에 목표 id가 안 넘어갔다.** `data-rwmanage="goal"`이 목표를 몰라서
    ① 모든 목표의 보상이 섞여 보이고 ② '연결할 연간 목표'가 `— 선택 —`으로만 떴다. 목표 tier 보상이 0개라 티가 안 났을 뿐.
  - **⚠️ 가계부를 타는 기준은 `kind`가 아니라 `budget`이다.** 처음에 `kind==='spend'`로 걸렀다가
    복합 보상(*일본 3박4일 = 행동 + 예산 80만*)이 가계부를 못 타는 걸 테스트에서 잡았다.
    **v7.2 두 축 설계('budget>0이면 가계부 연계 발동')를 내가 어긴 것.** `isSpendCard()`로 통일.
    budget=0 → 목표 탭에서 직접 사용 확정(행동 보상은 지출이 없으니 가계부를 못 탄다).
  - **계정과목을 바꾸지 않는다.** 배달은 식비로 남는다 — 안 그러면 식비 통계가 망가진다.
    `transaction.rewardKey ↔ card.txnId` 양방향으로 '보상으로 허용된 지출'이라는 사실만 남긴다.
    → **보상 배지 없는 배달 건수 = 절제 실패 횟수.** 이게 검증 지표다.
  - **되돌리기 비대칭**: 거래 삭제 → 카드 **복원**(usedAt null). 사용 취소 → **거래는 남기고 연결만 해제.**
    실제로 쓴 돈이기 때문이다. 이걸 대칭으로 만들면 가계부가 사실과 어긋난다.
  - **유효기간 없음.** 8월 카드를 9월에 써도 된다. 시한을 두면 급히 쓰게 되고 그건 절제에 정면으로 반한다.
    **안 쓴 카드가 쌓이는 것 자체가 데이터다** — 보상 없이도 굴러갔다는 증거.
  - **활성 카드가 없으면 거래 모달에 보상 영역이 아예 안 뜬다.** 평소 가계부 입력을 한 글자도 방해하지 않는다.
  - **⚠️ 이름 충돌 사고**: `cardModal`이 이미 **신용카드 추가/수정** 모달이었다. 내 보상 카드 모달이 덮어써서 즉사.
    → `rwCardModal` / `rwCards()`로 분리. **`DB.cards`(신용카드) vs `rwCards()`(보상 카드).**
    **교훈: 흔한 이름을 새로 정의하기 전에 `grep "function 이름("` 부터 한다.**
  - **캘린더 마커 개편** (로한 지적: *"점만 있으니 더러워지기만 하고 UX상 불편하다"*):
    의미 없는 점 2개 → **✅ 루틴 완전달성 · 💳 카드 미결제 · 🎁 보상 사용**, 툴팁 포함. 셀 높이 96→112px.
    **부분달성은 아이콘을 늘리지 않고 날짜 숫자 배경으로** 표현했다 — 아이콘이 3개 넘게 겹치면 그것도 점만큼 지저분하다.
  - `rewardLog` → `rewardCards` 승계 마이그레이션. 검증: 승계·멱등·보상함 10건 보존.
  - 검증(Playwright, 실제 DB 스냅샷): 배치→달성→행동 사용→소비 사용→상한 초과 경고→거래 삭제 시 카드 복원→사용 취소 시 거래 존치,
    목표 id 전달·자동선택·즉시 배치, 미달 카드는 가계부에 안 뜸, 계정과목 불변, 캘린더 마커, **원본과 14탭 대조 — 빈 탭·에러 수 동일(회귀 없음)**.
- **v7.6 — 모달·즉시반영 오작동 3건. (로한 실사용 지적)**
  - **① 작성 중인 모달이 바깥 클릭으로 닫혔다.** 오버레이 `mousedown` 핸들러가 전 화면 공통이라 폼도 예외 없이 닫혔고, 쓰던 내용이 통째로 날아갔다.
    → **모달 안에 `input/textarea/select`가 있으면 바깥 클릭으로 닫지 않는다.** ESC와 `×`는 그대로 닫힌다.
    **판정을 모달 내용으로 하므로 앞으로 추가되는 폼에도 자동 적용된다** — 모달마다 플래그를 다는 방식은 반드시 빠뜨린다.
    정보성 모달(입력 없음)은 기존대로 바깥 클릭으로 닫힌다. 규칙 한 줄: **"쓰고 있는 건 안 닫힌다."**
  - **② 보상함에 '+ 직접 추가'한 게 새로고침해야 보였다.** 로한이 정확히 짚은 증상.
    **원인은 렌더가 아니라 데이터였다.** `saveReward()`가 만드는 레코드에 **`place`가 없었다.**
    보상함 표는 `place==='reward'` 로 거르므로 새 보상이 통째로 빠졌고, 새로고침하면 마이그레이션 (B)가 `place`를 backfill 해서 그제서야 보였다.
    → 생성 시 `place:'reward'` · `status` · `addedAt/updatedAt` · `price`를 채운다.
    **⚠️ v7.4에서 `place`를 도입할 때 읽는 쪽(필터)만 고치고 쓰는 쪽(생성)을 안 고쳤다.** 필드를 추가하면 **생성·마이그레이션·필터 세 곳**을 같이 봐야 한다.
  - **③ `renderRW()` 신설.** 보상 데이터는 **목표 탭(칩)과 보상 탭(표) 양쪽**에 나온다. `saveReward`/`delReward`/`saveRwCfg`가 `renderGoal()`만 불러 반대쪽이 옛 화면으로 남았다.
    `doClaim`/`unclaimTier`의 "활성 탭 추측" 코드도 `renderRW()`로 통일. **두 곳에 나오는 데이터는 두 곳을 같이 그린다.**
  - **④ [부수] `toRewardModal`이 `modal._mvid`를 안 세워** 행동(act) 보상 승격 시 **'권장일(음기일)' 선택이 한 번도 안 떴다.** v7.2 기능이 조용히 죽어 있었다.
  - 검증: 폼 모달 바깥클릭 유지 + 입력 보존 + ESC 닫힘 / 정보성 모달은 닫힘 / 직접 추가 즉시 표시 / 승격 즉시 표시 / act 보상 권장일 노출 / **원본과 14탭 대조 — 빈 탭·에러 수 동일(회귀 없음)**.
- **🚨 v7.5 — 마이그레이션이 통째로 스킵되던 구조 결함. 버킷 7건이 화면에서 사라졌다.**
  - **증상**: 로한이 버킷함에 넣은 7건(K1~K7)이 v7.4 배포 후 빈 화면으로 떴다.
  - **데이터 손실 없음.** Supabase 조회 결과 `data->'bucket'` 에 7건 그대로 살아 있었다. 화면만 못 읽은 것.
  - **원인**: `migrateV6()` 첫 줄이 `if(d.schemaVersion>=6)return d;` 인데 로한 DB는 이미 `schemaVersion=6`.
    **v7.0~v7.4에서 추가한 보정 로직을 전부 이 함수 안쪽에 넣었기 때문에 한 번도 실행되지 않았다.**
    v7.4가 `bucketAll()`을 `DB.rewards` 기준으로 바꿨는데 `d.bucket→d.rewards` 흡수가 안 돌아서 빈 배열을 읽었다.
    → **버전 게이트가 있는 함수에 새 마이그레이션을 덧붙인 게 잘못.** 조용히 no-op 되는 게 이 패턴의 특성이다.
  - **🔒 구조 규칙 확정 — `migrateV6()`는 두 부분이다:**
    **(A) 버전 게이트 안** = 1회성 파괴적 전환(checks 재구성 등). `if((d.schemaVersion||0)<6){...}`
    **(B) 게이트 밖** = **매 로드마다 도는 멱등 정규화.** 전부 `if(!x)` 가드.
    **새 필드·새 배열 통합은 반드시 (B)에 넣는다.** 함수 상단 주석에 이 규칙과 사고 이력을 박아뒀다.
  - `SCHEMA_VERSION` 6 → **7**. `delete d.bucket`은 **빈 배열이어도** 실행(잔존 키가 다음 판단을 흐린다). 흡수 시 `id` 중복 체크 추가.
  - 검증: 실제 DB 스냅샷으로 마이그레이션 실행 → **K1~K7 전부 복원**(코드·기록일·예산 보존), `checks` 불변, **2회 실행 결과 동일(멱등)**,
    Playwright 14탭 회귀 통과, 보상 탭에 7건 렌더 확인, 저장 후 `schemaVersion=7` · `bucket` 키 제거 확인.
    **원본(v7.4)으로 같은 테스트를 돌려 버그를 재현**한 뒤 수정본과 대조했다.
  - **교훈**: 마이그레이션은 "코드를 넣었다"가 아니라 **"실제 사용자 데이터로 돌려봤다"** 로만 검증된다. v7.4는 신규 SEED에서만 통과했다.
- **📄 handoff 분리 — `handoff_0818.md` → `handoff_0819.md`.** (프로토콜 3번 뒤늦게 적용)
  - **사고 원인**: 로버트가 날짜 확인을 안 하고 8/19 작업(v7.1~v7.4)을 통째로 0818 파일에 적었다. 파일이 28KB로 불어남.
  - **조치**: 0819 신설(현재 유효 상태 + 8/18~8/19 상세 + 그 이전 요약), 0818은 8/18까지만 남겨 히스토리로 동결.
  - **프로토콜 3번 강화**: ① 작업 전 날짜 먼저 확인 ② 상세 로그는 **최근 2일치만**, 그 이전은 한 줄 요약 + 옛 파일 포인터 ③ 25KB 상한.
  - 압축해도 되는 근거 = 설계 근거는 `goal-design.md`·`integration-design.md`·`cashflow-design.md`에 있다. **handoff는 아카이브가 아니라 '지금 유효한 상태' 전달 문서다.**
- **v7.4 — 버킷함 ⇄ 보상함 연계.** (실사용 중 로한 지적)
  - **실사용이 문제를 드러냈다.** 로한이 담은 버킷 7개가 전부 거대했다 — *서울 오피스텔 3억 · 스페인/플로리다/일본/중국 1년 살기 · 일본 여친 · 모션데스크 40만*.
    **주간 보상으로 받을 수 있는 게 하나도 없다.** v7.3처럼 버킷 전체를 수령 선택지에 띄우면 "주간 4/4 달성 → 스페인 1년 살기 받기"가 뜬다. 무의미하다.
  - **필터 계층 신설**: `🪣 버킷함`(원하는 것 전부) **→ 승격 →** `🎁 보상함`(계단에서 받을 것) **→** 수령.
    **보상함에 있는 것만 수령 선택지에 뜬다.** 버킷은 무제한 담아도 부담이 없어진다.
  - **'고정 메뉴' → '보상함'** 개명 (로한 지적: 단어 선택이 이상하다).
  - **좌우 2열**: 좌 버킷함(큰 범위) · 우 보상함(실제 조작). 행의 `→` `←` 로 왕복 이동.
  - **⚠️ 역방향이 필수인 이유**: 되돌릴 수 없으면 승격을 망설인다. **승격이 가벼워야 실제로 쓴다.**
    보상함에 올렸는데 몇 달 안 고르면 내리고, tier가 안 맞으면 내렸다 재승격한다.
  - **모델 재통합**: `bucket` 배열을 `rewards`로 흡수하고 **`place:'bucket'|'reward'`** 로 구분.
    v7.3에서 tier 때문에 분리했는데, **이동이 빈번해지자 배열 분리가 오히려 id·코드·기록일을 깨뜨린다.**
    같은 객체가 위치만 바뀌는 구조가 맞다. 승격 시 `tier`/`goalId`/`timing` 부여, 되돌리면 해제.
  - 검증: 왕복 이동에서 **코드(K7)·기록일·예산 전부 보존**, 수령 모달에 버킷 제외, 14탭 회귀 통과.
- **v7.3 — 🪣 버킷함 + 🎁 보상 탭 신설 (사이드바 14번째 탭).**
  - **버킷 = 욕망의 원장.** 보상 후보 목록이 아니다. 로한 지적: *"'서울에 집 사기' 같은 버킷은 말그대로 버킷이지만 너무 이뤄내기 힘들어서 사실상 아주 큰 목표"*.
    **국밥부터 평생 못 이룰 수도 있는 것까지 같이 산다.** 그게 정직한 욕망의 모습이다.
  - **⚠️ 통합안 철회.** 최초엔 `rewards` 하나로 통합하고 `once` 플래그로 가르려 했으나, **tier를 뺄 수 없다는 점에서 성립하지 않았다.**
    `rewards`는 계단 층에 묶이는 게 본질(tier 필수), `bucket`은 어디에도 안 묶이는 게 본질. **분리했다.**
  - **티어는 담을 때가 아니라 고를 때 정해진다.** 수령 모달에서 버킷을 **예상비용 오름차순**으로 보여주면
    현실적인 것이 위에 오고 큰 것은 아래로 밀린다. **막지 않아도 정렬이 판단을 돕는다.**
  - 데이터: `bucket[{code:'K1', title, kind, price, addedAt, updatedAt, status:'want'|'got'|'dropped', gotAt, logKey, droppedAt, dropReason}]`
  - **표 UI** — 컬럼 클릭 정렬(코드/내용/분류/예상비용/기록일) + 상태 필터(대기·받음·포기·전체).
    가격순은 '지금 받을 수 있는 것', 기록일순은 '오래 묵은 욕망'을 드러낸다.
  - **90일 경과 `⚠` 표시** — 재촉이 아니라 질문이다. 큰 항목은 몇 년이고 남아도 된다.
  - **포기 사유 기록** — 목록 무한 증식 방지 + *"3개월 지나니 안 끌린다"* 자체가 자기 이해 데이터.
    **버킷의 진짜 산출물은 '안 산 것들'일 수 있다.**
  - 수령 시 `status:'got'` 소진 + 가계부 거래 생성. **수령 취소하면 버킷도 `want`로 복원**되고 거래도 삭제.
  - **좌절 지표에서 큰 항목 제외**: 10만원 이하 대기가 20개 넘을 때만 문턱 하향 신호. 5억짜리는 원래 못 받는 거라 세지 않는다.
  - 보상 관리를 목표·루틴 탭에서 분리 → 목표 탭은 칩만. **자주 쓰는 것(체크)과 가끔 쓰는 것(관리)의 분리.**
  - **`VIEW_RENDER`에 `reward` 등록 완료** (v5.0 dash 누락 회귀 재발 방지). 14탭 전수 통과.
  - 검증: 정렬·필터, 가격 오름차순, 소진→재선택 불가, 수령취소 복원, 포기 전이 전부 통과.
- **실측 근거 (배달)**: 7개월 46건 · 월 121,703원 · 주 1.5회.
  보상으로만 제한하면 월 4회 8만원 → **월 42,000 절감 · 연 50만 = A 보증금 목표의 10%**.
  ⚠️ 단, **평소엔 정말 안 시켜야** 원칙①(평소 안 하던 것)이 성립한다. 못 받은 주에 시켰는지는 가계부에 남는다.
- **v7.2 — 파트 연계 1차. 설계 문서 `integration-design.md` 신설.**
  - **보상 2분류 확정**: `spend`(새 지출 발생) / `act`(시간·행위). **'기타'는 만들지 않았다.**
    로한의 경계 사례 *"이미 산 재료로 소불고기 해먹기"* 는 `act + budget:0`으로 정확히 표현된다(넷플릭스 정주행과 같은 구조).
    '기타'를 남기면 분류 체계의 쓰레기통이 되고, 집계되지 않는 분류는 없는 편이 낫다.
  - **두 축 설계**: `kind`(성격) + `budget`(새 지출액). **budget>0이면 가계부 연계 발동.**
    복합 보상(일본 3박4일 = act + budget 80만)이 자연스럽게 표현된다.
  - **소비 보상 ↔ 가계부**: 수령 시 실제 지출 입력 → `보상` 카테고리로 거래 자동 생성.
    `rewardLog.txnId` ↔ `transaction.rewardId` **양방향** 연결(한쪽만 남기면 추적이 끊긴다).
    상한 초과는 **경고만 하고 막지 않는다**(기록이 목적). 수령 취소 시 연결 거래도 함께 삭제.
  - **⚠️ 의도된 긴장**: 보상 지출 → 현금 감소 → 현금 연동 재정 목표의 진행률 하락.
    이건 결함이 아니라 현실이라 숨기지 않고 표시한다 — `🎁 올해 보상 지출 42만 · 8.4% of A 보증금 500만 확보`.
    보상이 없으면 루틴이 죽고 과하면 목표가 죽는다. 균형점은 데이터로만 찾는다.
  - **명리 음기일**: 캘린더 `陰` 마커 + 행동 보상 `timing:'yin'` 권장.
    **⚠️ 과학적 근거 없음 — 불확실이 아니라 근거 없음이다.** 화면과 문서 양쪽에 명시했다.
    넣은 이유: 로한북 명리는 이미 '예언이 아니라 구조적 렌즈' 전제이고, "아무 때나"보다 규칙이 절제에 낫기 때문. 3~6개월 뒤 상관 분석으로 유용성 판정.
  - **설계를 검증으로 고침**: 최초 안 '2점 이상'은 365일 돌려보니 **정확히 50%(격일)**였다.
    원인은 60갑자 구조 — 甲子·乙丑처럼 **양간은 항상 양지, 음간은 항상 음지**와 짝이라 두 조건이 완전 중복.
    실질 변별은 오행뿐 → **3점(음간·음지 + 水金) = 연 72일 ≈ 주 1.4회**로 상향. 이틀에 한 번 오면 규칙이 아니다.
  - 검증: 음기일 연 72일, 옛 `cap`→`budget` 자동 추정, 거래 생성·양방향 id, 상한 초과 경고, 지출 비율 표시 전부 통과.
- **v7.1 — UI 원칙 교정 + 보상 계단 분산.** (로한 지적: "구성은 되어 있는데 연계성까진 고려 안 했다")
  - **⚠️ UI 기본 원칙 — 앞으로 이 앱의 모든 2열 배치에 적용한다.**
    **왼쪽 = 큰 범위 / 오른쪽 = 작은 범위 또는 사용자가 실제 조작하는 부분.**
    모바일에서 오른쪽이 아래로 내려가므로 그 순서가 곧 세로 흐름이 된다.
    → 좌: `2026년 8월`(과제 + 8월 현황 캘린더) / 우: `8월 19일`(그날 루틴 체크). 이전엔 반대였다.
  - **보상 계단의 별도 프레임 제거.** 보상은 자기가 속한 층에 붙어야 연계가 보인다:
    `연간 보상`→연간 목표 헤더 · `월간 보상`→8월 프레임 헤더 · `주간 보상`→일간 프레임 헤더 · `목표달성 보상`→각 목표 펼침 영역.
  - **보상 칩**: 항상 `🎁 주간 1/4` 처럼 진행률을 달고 있다. 열리면 초록 `받기`, 수령하면 `✓ 수령`.
    미달 상태에서 누르면 **조건 + 지금 몇/몇 + 등록된 보상 목록**이 열린다.
    → 로한이 겪은 "보상 등록했는데 반응이 없다"는 **버그가 아니라 체크 0건이라 계단이 안 열린 것**이었다.
      그게 화면에서 안 읽힌 게 UX 실패였고, 칩이 그걸 해결한다.
  - 52주 트래커를 연간 프레임 안으로 통합. `8월 체크` → `8월 현황`.
  - 검증: 좌우 배치, 4개 층 칩 위치, 칩 클릭 시 조건 안내, 루틴 3개 체크 → 완전달성 → 주간 1/4 상승 확인.

### 2026-08-18
- **v7.0 — 🎁 보상 계단. 로한북의 정체가 여기서 확정됐다.**
  - **대전제(로한의 신념)**: *연간/월간 = 목표. 일간/주간 = 루틴. **루틴이 쌓여서 목표를 달성한다.*** 
    재정·건강·명리는 이 액션플랜을 위한 **밑단 데이터**다. **목표관리가 본체다.**
  - **계단 구조 (병렬 아님)** — 아래 단계의 성공 횟수가 위 단계를 연다:
    `일간 완전달성(그날 due 전부)` → 주 4일 → `🎁 주간` → 그 달 4회 → `🎁 월간` → 연 9회 → `🎁 연간`
    별도 트랙: `G:코드` 연간 목표 달성 보상 (예: N4 합격 → 일본 3박4일).
  - 문턱은 `DB.rewardCfg{weekFullDays:4, monthWeeks:4, yearMonths:9}` — 익숙해지면 올린다.
  - **보상은 메뉴판**: 티어별로 여러 개 등록하고 성공 시 **하나만** 골라 수령. 내성 완화.
    `rewardLog` 키가 `tier@period`라 **기간당 1회 수령이 구조적으로 강제**된다. 수령 시 실적을 박제.
  - **12주 잔디 → 52주 주간 트래커로 교체.** 잔디와 월간 캘린더가 '언제 달성했나'로 목적이 겹쳐 중복이었다.
    칸 1개 = 1주, 색칠 = 그 주 성공. **칸을 세면 월간·연간 보상까지 남은 거리가 그대로 보인다.** 칸 클릭 → 그 주로 이동.
  - 주는 **일요일 시작**. 주가 달을 걸치면 **시작일이 속한 달**로 귀속.
  - 검증: 33주 시뮬레이션으로 계단 판정(주 4/4 열림, 월 3/4 미달, 연 3/9 미달), 월별 주성공 집계, 수령 후 재수령 차단, 트래커 52칸·클릭 이동 전부 통과.
  - **⚠️ 최종 목적지 = Recap.** 연말에 로버트가 "2026년 주간 성공 38/52 · 최장연속 11주 · 가장 무너진 루틴 · 미달사유 34%가 야근" 식으로 브리핑할 수 있어야 한다.
    이건 화면이 아니라 **데이터 요건**이다. 주간 성공·보상 수령을 매주 원장에 남겨야 연말에 그 문장이 나온다.
- **v6.2 — 목표 탭 2열 레이아웃 + 날짜 네비.** 1행 연간 / 2행 그날 루틴·월간 캘린더 / 3행 월간 과제·12주 기록.
  - **`◀ ▶ 📅` 로 지난 날 루틴 소급 체크 가능, 미래는 차단.** `DB.ui.goalDate`/`goalMonth` 신설, 체크·미달사유가 전부 `curDate()` 기준.
  - **'단독과제' → '월간 과제'로 통합** (연간 눈금 + 그 달 단발을 한 프레임에).
  - **월간 눈금은 부모 목표의 연동 소스·시작값을 승계한다.** 현재값은 하나뿐이라 눈금이 따로 들 이유가 없다. 눈금 출발점 = 직전 달 눈금값(없으면 부모 baseline).
- **v6.1 — 목표 현재값 자동연동 + 감소형 진행률 버그.**
  - **[버그] 감소형 목표가 100%로 표시.** 현재값 미입력 시 `(0-68.2)/(65-68.2)=2131%`→클램프. **비면 시작값으로 간주**하도록 수정.
  - **`source` 자동연동**: `weight`(체성분 최신) · `cash`(현금성 계좌 합) · `net` · `debt`. **근본 원인은 로한북에 이미 있는 데이터를 목표가 안 보고 있던 것.**
  - 월간 세부목표 직접 CRUD(binary 목표에도 수동 눈금 가능). 손대면 `manual:true`로 자동배분 제외.
- **🎯 목표·루틴 전면 재설계 (v6.0). 설계 원문 = `goal-design.md` — 이 영역 작업 전 필독.**
  - **목적 함수 확정**: *12월 31일에 얼마나 달성했고 무엇이 부족했는지 반성할 수 있을 것.* 모든 기능을 이 문장으로 검증한다. 매일의 체크는 목적이 아니라 회고를 가능하게 하는 **원장(ledger)**.
  - **계층**: 연간(대전제) → 월별 눈금 → 일간 루틴. **월간 2종 구분** — `A.8`(연간 A의 8월 눈금) vs `M8.1`(8월에 시작·종료하는 단독 과제). 섞으면 장기 진척이 안 보인다.
  - **코드 체계**: 연간 `A` · 눈금 `A.8` · 상시루틴 `A1` · 한시루틴 `A.8a` · 단독 `M8.1` · 미연결 `Z1`. **단조증가 카운터(`DB.meta.seq`) — 삭제해도 코드 재사용 안 함** (과거 기록이 다른 것을 가리키지 않도록).
  - **⚠️ v5.0의 치명적 결함 수정 — 과거 달성률 소급 변동.** 과거를 *현재 루틴 목록*으로 재계산해서 오늘 루틴을 추가하면 8/13이 3/3→3/4로 바뀌었다(실측). 기록으로서 실격.
    → **`checks[날짜]={done:[],due:[],miss:{}}` 로 분모까지 스냅샷.** 폴백은 루틴 `start`/`end` 재구성. 회귀 테스트로 고정. **이 불변성은 앞으로도 깨면 안 된다.**
  - **삭제 대신 은퇴**: `end=오늘`, 과거 보존, 내일부터 집계 제외. 물리 삭제는 체크 0건일 때만.
  - **미달 사유 태그**(야근·피로·날씨·약속·무기력·아픔·일정충돌·의지부족·기타) + 한 줄 메모. **'왜'의 유일한 원천** — 일지가 8개월 14건뿐이라 이게 없으면 원인 규명 불가.
  - 검증: 실데이터(거래 991·일지 14·피검사 21회) 마이그레이션 무손실 · 과거 불변성 · 13탭 회귀 · JS 에러 0.
  - **마이그레이션 직전 전체 백업**: `backups/rohanbook_2026-08-18_pre-v6.json` (306KB).
- **[버그수정 v5.0.1] 현황판 빈 화면 회귀.** 네비 핸들러 if 체인에 `dash`가 **처음부터 없었다.** v5.0 이전엔 첫 화면이 현황판이라 `enter()`가 그려줘서 안 드러났고, 첫 화면을 목표 탭으로 바꾸는 순간 `renderDash()` 진입점이 사라졌다.
  - 조치: if 체인 → **`VIEW_RENDER` 맵** 교체. 미등록 탭은 `console.warn`으로 드러난다.
  - **🔒 재발 방지 규칙: 사이드바에 탭을 추가하면 `VIEW_RENDER`에도 반드시 등록할 것.**
  - 교훈: **첫 화면을 바꾸는 변경은 기존 첫 화면의 렌더 경로를 같이 확인해야 한다.**

### 2026-08-11 ~ 08-14 (요약 — 상세는 `handoff_0818.md`)
- **v5.0 목표·루틴 탭 신설** (8/14). 로한북의 마지막 축 = 실행. 이후 v6.0에서 전면 재설계됨.
- **v4.5 부채 상환스케줄 엔진 교체** (8/14). `bankSchedule()` — 원금 고정 416,000 + 잔액×3.11%×경과일/365 일할, 납입일 19일·주말이면 익영업일. **은행앱 6회차와 원 단위 일치.** ⚠️ 공휴일 미반영.
- **v4.4 체중 → 체성분 페이지 전면 개편** (8/14). 8지표 탭 + 체중계 순서 8입력, 나머지 자동계산. 키 163 기준 검증 완료.
- 푸터 버전 `APP_VERSION` 동적 참조, SEED 부채값 실제 동기화(3.11% / 5.5% / 1,990만).
- **handoff 규칙 파일 신설** (8/14).
- **v4.3 일지(📖 성찰) 탭** (8/13) — 노션 미러링 15편 임포트. 대시보드 재구성 + "로버트의 정리" 보드. **로버트 이름 확정.**
- **현금흐름 시스템 P1~P5** (8/12) — 계좌 기초잔액·거래-계좌 연결·이체 타입·🌊 Sankey. 상세 `cashflow-design.md`.
- **🔮 사주 탭** (8/12) — 총운/대운/세운/월운/특이일 드릴다운, 자체 만세력 엔진.
- **프로젝트 시작** (8/11) — Supabase 스키마+시드, 피검사 21회×77지표 임포트. 초기 스펙 `요구사항정의서.md`.

---

## 🗺 목표·루틴 로드맵 (2026-08-14 결정)

**구글 캘린더 연동은 Phase 2로 미룸.** 판단 근거:
- 기술적으로는 가능. 브라우저 전용 정적 사이트에서 `gapi` + Google Identity Services로 백엔드 없이 호출 가능. 필요: Google Cloud 프로젝트 → OAuth 클라이언트 ID(웹) + API 키 → 승인된 JS 원본에 `https://rohanchung.github.io` 등록 → 동의화면.
- ⚠️ **불확실**: 브라우저 토큰은 1시간 만료에 refresh token이 없다. 조용한 재발급이 되는지 구글 문서에 명시가 없어, 최악의 경우 접속마다 구글 로그인 팝업.
- ❌ **로버트 경유 미러링 불가**: Google Calendar MCP 커넥터에 노출된 도구가 `search_events` 하나뿐. 기간 조회·이벤트 생성 없음. 배치로 긁어와 Supabase에 밀어넣는 우회로는 현재 도구로 막혀 있음.
- ❌ **양방향은 반대.** 루틴 체크 월 150건이 구글 캘린더를 도배하고, 충돌 해결 로직은 개인 프로젝트가 가장 많이 좌초하는 지점.
- → 붙인다면 **읽기 전용 오버레이**로만. 2주 써보고 실제로 아쉬울 때 판단.

**다음 단계 후보**
- 데이터 2~3개월 쌓이면 **사주 주의일 vs 평상일 루틴 달성률 상관 분석**. 지금은 표시만, 분석은 표본이 모인 뒤.
- 주간 리뷰 화면(주별 달성률 추이).

## ⏳ 미해결 / 대기

- [x] ~~은행 실제 상환액 받아서 앱 부채 계산 교정~~ → **8/14 완료.**
- [ ] "About 로한" 공유용 다이제스트 (타 AI/URL 공유로 로한 학습용) — 로한이 "천천히"라 함, 보류.
- [ ] **회고 뷰** (goal-design 6장) — 기간 선택형. 목표별 성적표 · 월별 히트 · 루틴 기여도 · 미달 사유 집계. **데이터 3~6개월 쌓인 뒤**. 그전까진 로버트가 SQL로 직접 분석해 대화한다.
- [ ] **주간 달성률 추이 그래프** — 12주 잔디 옆에 나란히 (dataviz 스킬 참조).
- [ ] **JSON 내보내기 버튼** + **GitHub 자동 백업**(Supabase Edge Function 월 1회 `backups/` 커밋). 20년 데이터가 단일 무료티어 계정에 매달려 있는 상태.
- [ ] **용량 한계 대비**: 현재 48kB, 연 ~150kB 증가. 단일 jsonb 행을 통째로 읽고 쓰는 구조라 **1MB(약 5~7년) 근접 시** 연도별 파티셔닝 필요.
- [x] ~~아침 브리핑 스케줄 태스크~~ → **8/18 취소.** 로한 판단: 의미 없음.
- [ ] 9/1 0시 계좌 기초잔액 입력 → 현금흐름 100% 정합 가동 (cashflow-design.md 참조).
