# GitHub에 올리지 않을 것 (개인 재무데이터·중복·문서)
import.sql
가계부.html
요구사항정의서.md
recalc.log
temp.xlsx

# 테스트 산출물 — run.sh 가 매번 새로 만든다 (2026-09-17, 로컬 실행 시작)
tests/.out/

# 혹시 레포 안에서 npm 을 돌렸을 때를 대비한 안전망.
# 🔒 원칙은 "레포에 node_modules 를 두지 않는다" — 설치는 레포 바깥 _testenv 에 한다(STATUS §21)
node_modules/
package-lock.json
