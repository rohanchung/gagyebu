# 🗂️ HANDOFF — 로한 ↔ 로버트 인수인계 로그

> **이 파일이 규칙이다.** 새 방(대화)을 열면 로한이 "showmethemoney 폴더에서 **가장 최신 handoff_MMDD.md** 랑 index.html 읽어"라고 말한다.
> 로버트는 압축된 기억에 의존하지 말고 **이 파일을 진실의 원천(source of truth)** 으로 삼는다.
> **최종 갱신 2026-08-20 · 앱 v1.3** — 상세 로그는 8/20 하루치, 그 이전은 요약(원문은 `handoff_0819.md`).

---

## 📜 프로토콜 (로버트가 지킬 규칙)

1. **새 방 시작 시 읽을 것 (순서대로):**
   - `handoff_MMDD.md` ← **이 파일. 가장 최신 날짜 하나만.** 옛 날짜 파일은 안 읽어도 된다 — 유효한 상태는 전부 여기 있고, 옛 파일은 상세 로그 원문 보관용이다.
   - `goal-design.md` ← **목표·루틴 시스템 설계 기준.** v6.0 이후 이 영역을 건드릴 땐 반드시 먼저 읽는다.
   - `integration-design.md` ← **파트 간 연계 설계.** 보상↔가계부, 명리 권장일 등 파트를 가로지르는 작업 전 필독.
   - `index.html` ← 앱 전체 코드 (단일 파일 SPA)
   - 필요 시: `cashflow-design.md`(현금흐름 설계), `요구사항정의서.md`(초기 스펙)
   - `tests/README.md` ← **검증 스크립트.** 코드를 고쳤으면 여기 있는 걸 클라우드로 복사해서 돌린다. 매번 새로 짜지 말 것.
2. **데이터가 필요하면** → Supabase MCP로 조회 (아래 접속정보). 앱 화면 추측 금지, 실제 조회.
3. **날짜가 바뀌면** → 이 파일을 `handoff_(새날짜).md`로 복사하고, 맨 위 로그에 새 항목만 얹는다. 옛 파일은 남겨둔다(히스토리).
   - **⚠️ 작업 시작 전에 오늘 날짜부터 확인한다.** 로한이 말해주기를 기다리지 않는다. (8/19 작업이 통째로 `handoff_0818.md`에 들어가 파일이 28KB로 불어난 사고가 있었다.)
   - **로그 상세는 최근 2일치만 유지.** 그 이전은 **한 줄 요약 + 옛 파일 포인터**로 압축한다. 압축해도 되는 이유 = 설계 근거는 `goal-design.md` / `integration-design.md` / `cashflow-design.md`에 남아 있고, handoff는 **지금 유효한 상태**를 전달하는 문서지 아카이브가 아니다.
   - 목표 크기 **30KB 이하.** 넘으면 압축을 안 한 것이다. (0820 = 약 20KB)
4. **⚠️ UI/UX 기본 원칙 — 구현 전에 이걸 먼저 통과시킨다.** (2026-08-19 로한 질책: *"구현은 잘하는데 디자인 기본원칙을 안 지킨다"*)
   - **클릭되는 것은 클릭처럼 보인다.** 경계 + `cursor:pointer` + **hover/active 반응** + 의도 라벨. 넷 중 하나라도 빠지면 사용자는 그게 버튼인 줄 모른다.
   - **버튼은 위, 내용은 아래.** 목록형 모달의 액션(추가·설정)은 헤더 바로 아래 액션 바에 두고 선으로 가른다. (폼 모달의 '저장'은 입력 흐름의 끝이라 하단이 맞다.)
   - **버튼 옆에 긴 설명을 인라인으로 붙이지 않는다.** 버튼이 문장 속에 묻힌다. 설명은 아래 줄로.
   - **손가락 크기.** 아이콘 액션의 히트 영역은 최소 26px. 11px 글자를 누르라고 하면 안 된다.
   - **스크롤되는 모달의 헤더는 sticky.** 닫기(×)를 잃으면 안 된다.
   - **포커스 링(`:focus-visible`)은 지운 적이 없어야 한다.**
   - **왼쪽 = 큰 범위, 오른쪽 = 조작.** 단 모바일에서는 '자주 쓰는 것 먼저'가 이긴다.
   - **진행바는 남는 가로를 쓰는 요소다.** 세로 한 줄을 따로 잡아먹게 두지 않는다.
   - **🔒 CSS base 규칙 누락 점검**: 클래스를 새로 쓰면 `.name{` base 규칙이 실제로 있는지 확인한다.
     `.name.modifier` 만 있고 base가 없으면 아무 스타일 없이 렌더된다 — v7.1 `.tcard`, v8.4 `.rwpick` 두 번 겪었다.
5. **응답 원칙**: 로한 선호 = 결론→왜→다음. 아부·보험멘트 금지. 불확실하면 "불확실" 명시. 로버트는 재정 조언 시 "면허 있는 자문가 아님" 밝힘.
6. **로버트 = 나(Claude)의 이름.**
7. **⚠️ 버전 체계 (2026-08-20 로한 확정).**
   - **`v1.0` = 정식.** 8/11~8/20 개발 이터레이션(v1.1~v12.2)은 여기서 마감했다.
   - **기능 추가 = 0.1 단위** (v1.0 → v1.1) · **세부 수정 = 0.01 단위** (v1.1 → v1.11 → v1.12).
   - `APP_VERSION` 과 `CHANGELOG` 를 **항상 같이** 올린다. 푸터는 `APP_VERSION` 을 동적 참조한다.
   - CHANGELOG 안에 `{v:'—'}` 구분 행이 있고 그 아래는 옛 이터레이션 로그다. 건드리지 않는다.
8. **결과물은 폴더에 넣는 것이 목적이다. 대화창에 파일을 띄우는 건 수단일 뿐.** (2026-08-19 로한 지시)
   - 로한은 받자마자 푸시해서 실사용한다. 파일 카드 자체는 아무 쓸모가 없다.
   - **작은 텍스트 파일(handoff·설계문서)** → `device_bash`로 디바이스에서 직접 작성/패치. 카드 안 뜬다.
   - **`index.html`처럼 큰 파일** → 클라우드에서 수정·검증 후 `SendUserFile` → `device_commit_files`.
     ⚠️ **이 경로는 대체 불가다.** 컨테이너와 디바이스는 파일시스템이 분리돼 있고,
     바이트를 옮기는 다리가 `file_uuid` 하나뿐이다. `device_bash`로 300KB를 넣으려면
     내용을 전부 내 출력으로 타이핑해야 해서 토큰이 수만 단위로 든다. **카드 1장이 훨씬 싸다.**
   - **반드시 양쪽 sha256을 대조한다.** 체크섬이 맞아야 "검증한 그 파일이 디스크에 놓였다"가 성립한다.
   - 완료 보고는 **텍스트로만** — 무엇을 왜 고쳤는지 + 커밋 메시지. 파일 설명은 쓰지 않는다.
9. **⚠️ 테스트는 '빈 상태'부터 먹인다. (2026-08-20 v1.1에서 확정)**
   - 회귀 테스트(`t_reg.js`)의 고정 STATE 가 **정상 데이터를 다 갖춘 상태**였다. 그래서 SEED 키 누락 7건 + `d.ui` 누락이
     9일 동안 전부 초록불이었다. **테스트가 통과했다는 건 '정상 상태에서 통과했다'는 뜻일 뿐이다.**
   - 🔒 새 기능·마이그레이션을 건드리면 **`t22.js` 를 반드시 돌린다** — 서버 행 없음 / `{}` / `schemaVersion`만 /
     구버전 유사 / `ui` 없음, 5종으로 15탭을 순회한다. 빈탭 0 · 에러 0 이어야 한다.
   - 🔒 **`DB.x.y` 형태로 새 필드를 읽기 시작하면, 그 자리에서 `migrateV6` (B)층에 존재 보장을 넣는다.** 나중에 하면 안 넣는다.
   - 마이그레이션 함수는 **둘**이다: `loadState` → `migrateDB`(health·saju·journal·posts·activity) → `migrateV6`((A)1회성 / (B)매 로드).
     헷갈리면 **`migrateV6` 의 (B)층**이 정답이다.
10. **저장 경로(`save`/`doSave`/`syncConflict`/`forceSave`)를 고칠 때의 불변식.** (v1.1)
   - **저장은 조용히 성공하고 시끄럽게 실패해야 한다.** 실패를 삼키는 빈 에러 콜백을 다시 넣지 않는다.
   - 문서 전체를 덮어쓰는 구조이므로 **`updated_at` 조건 없이 upsert 하는 코드를 되살리지 않는다** — 두 기기 동시 사용 시 통째 소실.
   - 단, **판단이 애매하면 저장을 성공시킨다**(0행인데 서버 버전이 내 것과 같으면 그냥 쓴다). 잠금 때문에 입력을 잃는 건 더 나쁘다.
11. **🚫 이 폴더에서 `git` 명령을 절대 실행하지 마라. (2026-08-20 사고)**
   - `device_bash` 는 마운트된 폴더에서 **파일을 지울 권한이 없다.** 그래서 `git status` 한 번만 돌려도
     git 이 만든 `.git/index.lock` 을 **git 자신이 해제하지 못하고 남긴다.**
   - 결과: 로한이 GitHub Desktop 에서 커밋하면 `A lock file already exists in the repository` 로 막힌다.
     **읽기 전용인 `git status` 조차 이 사고를 낸다.** `git log` 도 마찬가지로 의심하라.
   - 커밋 여부·변경 목록이 궁금하면 git 대신 **파일 타임스탬프(`ls -la`)와 sha256** 으로 판단한다.
   - 이미 걸렸다면: `mv .git/index.lock .git/index.lock.dead` (삭제는 안 되지만 이동은 된다). 그 뒤 로한에게 재시도 요청.
   - **커밋과 푸시는 로한이 GitHub Desktop 으로 한다. 내 일이 아니다.** 나는 파일을 놓고 커밋 메시지를 써주는 데까지다.
12. **작업 완료 시 항상 커밋 메시지를 써줄 것.** GitHub Desktop 기준 **Summary**(한 줄, `v버전 — 무엇을`) + **Description**(변경 항목 불릿 + 검증 결과 + 주의사항). 로한이 복붙만 하면 되게.

---

## 🧭 시스템 지도

> **DB 테이블은 둘이다.** `app_state`(현재 상태, 유저당 1행) / `app_state_snap`(스냅샷, v1.2 신설, RLS 동일). 둘 다 같은 프로젝트 안이다.

**앱**: "Rohan Book" (구 가계부). 단일 `index.html` SPA. 재정+건강+명리(사주)+일지 통합. 현재 **v1.0 (정식)**.
**배포**: GitHub Pages, 공개 repo `rohanchung/gagyebu`. (넷리파이에서 이관함 — 무료티어 배포 막힘 이슈)
- 코드 변경 → **commit + push** 필요 (GitHub Desktop).
- 데이터 변경 → Supabase 직접 쓰기, **커밋 불필요.**

**Supabase**: project_id `ytkbrdgbklnijbwkvino`. 단일 `app_state.data` jsonb 행에 전 상태 저장. `migrateDB()`로 스키마 진화.

**스킬 3개 (모든 방에서 발동)**:
- `load-my-finance-health` — 재정·건강 현황 스냅샷
- `evaluate-my-saju` — 사주 세운/월운/일운 평가 (고정 원국)
- `check-account-security` — 계정 로그인 이력 점검

---

## 👤 로한 프로필 (고정 사실)

- IT기획자 출신. 기획·사업·마케팅·창작 병행.
- 명리 앱은 **결정론적 만세력 엔진** 자체 구현(API 없음): Julian Day 일진 + Meeus 태양황경 근사 절기/월주 + 60갑자.
- **건강**: 키 **163cm** (BMI 역산 확정). 체중 하락 추세(약 78→68kg대). 8/5 기준 68.2kg. 피검사 21회 × 77지표 임포트됨.

---

## 💰 재정 로드맵 (2026-08 확정, 은행 실제숫자로 갱신 예정)

- **풍무점 이전 = 2026년 10월, 확정·강제.** 월세 아끼려는 게 아니라 출근지 이동. → 보증금 **500~1000만** 필요, 그중 **현금 500 확보가 관건.**
- **부채 금리 우선순위**: 약관대출 **5.5%** > 정책자금(소상공인대환) **3.11%** > 세이프박스 ~2%.
  - → **약관대출 먼저 상환.** 정책자금은 일정대로만 납부.
- **로드맵**: 2026 = 보증금용 저축(현실적 상한 ~500만, 1천만은 비현실) · 2027 = 약관대출 상환 + 신용 구축 · 2028 = 전세 점핑(1억 오피스텔은 현금 1~2천 필요, 지금은 불가).
- 신용점수 900+ 안정. 약관·정책 대출은 신용 영향 미미.
- ✅ **앱 부채 계산 교정 완료 (8/14)**. 소상공인대환 실제 방식 = **원금 고정 416,000원**(1회차 448,000, 단수 조정) + **이자 = 잔액 × 3.11% × 경과일수 / 365**(일할). 납입일 매월 19일, 주말이면 익영업일. 총 48회 · **총이자 1,269,938 · 총상환 21,269,938 · 월평균 443,124**. 앱 `bankSchedule()`이 은행앱 6회차와 원 단위 일치.
- 약관대출 잔액 **1,990만 @5.5%** (SEED도 실제값으로 동기화).

---

## 🧾 날짜별 로그 (최신이 위)

### 2026-08-20
- **v1.3 — 🔁 루틴 이사 · 🎯 버킷 승격. (미해결 🟠 2건 소진)**
  - **⚠️ (가)는 대기 항목이 아니라 살아있는 결함이었다.** 루틴 소속을 바꾸면 `saveRoutine()` 이 코드를 재발급하는데
    `checks` 는 `done[]` / `due[]` / `miss{}` **전부 코드 문자열 기반**이다. 경고도 없이 과거 기록이 고아가 됐다.
    → `renameRoutineCode(old,new)` 신설. 세 곳을 한꺼번에 옮긴다.
    🔒 **분모(`due`)의 길이는 바뀌지 않는다** — 라벨만 갈아끼우는 것이라 v6.0 '과거 불변성'과 충돌하지 않는다. 테스트로 못박았다.
  - **묻고 나서 옮긴다.** 옛→새 코드와 옮길 기록 일수를 보여주고 confirm. 취소하면 **저장 자체를 중단**한다
    (`Object.assign` 을 confirm 뒤로 내렸다 — 취소인데 객체가 이미 바뀌어 있으면 안 된다).
    **기록이 0건이면 묻지 않는다** — 잃을 게 없는데 묻는 건 마찰일 뿐이다.
  - `issueRoutineCode(goalId, peek)` — **peek 모드 신설.** 취소할 수 있는 confirm 에 새 코드를 보여주려면
    번호를 미리 태우면 안 된다. peek 은 `meta.seq` 를 소비하지 않고 '다음에 나올 코드'만 계산한다.
  - **☐ 코드 고정 체크박스.** `lockCode` 는 **v6.0부터 코드에 있었고 읽기만 했다 — 아무도 켤 수 없었다.**
    **전형적인 '입구 막힘' 4회차다**(v8.2 체성분, v10.0 요일 루틴, v12.1 타임로그에 이어).
    🔒 **필드를 읽는 코드가 있으면 그 필드를 쓰는 입구가 있는지 반드시 확인한다.**
  - **같은 구멍이 `delGoal()` 에도 있었다.** 목표를 지우면 산하 루틴이 미연결(Z)로 내려가며 코드가 바뀌는데
    거기도 이관이 없었다. 함께 막았다. **한 군데서 발견한 결함은 같은 패턴을 전부 훑는다.**
  - 🎯 **(나) 버킷 → 연간 목표 승격.** 버킷 ⇄ 보상함은 원래 양방향이었고 빠진 조각은 이것 하나였다.
    표 이동 칸에 `⬆`, 승격된 항목은 `🎯 코드` 뱃지 + `↩`. 최소 침습 — **`status`/`place` 를 건드리지 않고 링크만 양방향**
    (`bucket.promotedTo` ↔ `goal.fromBucket`). 폼은 안 만들고 **기존 `goalModal(null,'year')` 을 열어 프리필**만 한다.
  - **⚠️ `modal` 은 DOM 노드다.** `innerHTML` 을 갈아치워도 `modal._fromBk` 같은 JS 속성은 살아남는다.
    승격 모달을 닫고 그냥 `+ 목표` 를 열면 **엉뚱한 버킷에 링크가 걸린다.** → `openModal()` 에서 끊는다. 테스트 있음.
  - `migrateV6` **(B)층**에 끊어진 승격 링크 제거 + `lockCode:false` 정리. **만들지 않고 끊어진 것만 지운다.**
  - **⚠️ CSS: `.g2col > .pane` 이 `min-width:auto` 라 그리드 아이템이 못 줄어들었다.**
    그래서 `.tblwrap{overflow-x:auto}` 가 무력화되고 **표가 페이지 전체를 옆으로 밀고 있었다** —
    390px 에서 원래 14px 밀고 있었고(사전 존재), 이동 칸에 버튼이 하나 늘자 33px로 커져서 드러났다.
    `min-width:0` 으로 근본 수정. 이제 표가 자기 안에서 스크롤한다. `.mvbtn` 도 `flex:0 0 auto`
    (좁은 칸에서 28→23px 로 눌려 **히트 영역 26px 규칙을 어기고 있었다**).
    🔒 **교훈: `overflow:auto` 가 안 먹으면 그 조상 중에 `min-width:auto` 인 flex/grid 아이템이 있다.**
  - **⚠️ 테스트 5개가 경로를 하드코딩하고 있었다** (`/home/claude/work.html`, `/home/claude/state_rw.json`).
    폴더가 다른 새 세션에서 `t_reg`/`t14`/`t18`/`t19`/`t20` 이 **통째로 안 돌았다.**
    → `process.argv[2]` / `path.join(__dirname,...)` 으로 통일.
  - 신규 `t24.js` **48건 전부 통과** — 이관·분모불변·취소원복·peek(번호 안 태움)·코드고정·Z강등 이관·
    승격·해제·목표삭제 시 유령링크 정리·`_fromBk` 누수·마이그레이션 정합.
  - 검증: t_reg 15탭 0 · t22 극단상태 5종 빈탭0 에러0 · t23 스냅샷 · t14 6 브레이크포인트(**가로 overflow 전 폭 없음**) ·
    t18 · t19 · t20 · t21 전부 통과.
  - 배포 sha256 `d960c6553d68547799565c8506a41dc8734646fd331f5de7a89e7925d16fff91`.
- **v1.2 — 📀 스냅샷 백업. (로한 제안)**
  - 로한: *"JSON을 꼭 내 하드에 보관하기보단 Supabase 다른 한쪽에 백업으로 또 보관하게끔 구조를 짜는 게 더 낫지 않냐?"* — **맞다. 단 층이 다르다.**
    ・ **스냅샷 테이블 = 논리적 사고 방어** (코드 버그 / 다른 기기 덮어쓰기 / 실수 삭제). 자동이고 매일 쌓이고 앱 안에서 바로 되돌린다. **실제로 쓰이는 건 거의 항상 이쪽이다.**
    ・ **JSON 파일 · GitHub = 물리적 사고 방어** (프로젝트 정지·삭제, 계정 소실). 월 1회면 충분하다.
    ・ ⚠️ **같은 Supabase 프로젝트 안의 다른 테이블은 '진짜 백업'이 아니다.** 프로젝트가 죽으면 원본과 백업이 같이 죽는다. **둘은 대체재가 아니라 서로 다른 층이다.** 이 문장을 지우지 마라.
    ・ 로한이 옳게 짚은 핵심: **수동 백업은 사람이 안 누르면 없다.** 그래서 자동이 이긴다.
  - `public.app_state_snap` 신설 (RLS `user_id = auth.uid()`). `kind`: `auto` / `manual` / `pre_restore`.
    **`auto` 는 부분 유니크 인덱스**(`unique(user_id,day) where kind='auto'`)로 하루 1개를 DB가 강제한다 — 클라이언트 체크만 믿지 않는다.
  - 접속 시 자동 1개 · 📍 수동 지점(이름 붙일 수 있음) · **되돌리기 직전 `pre_restore` 자동 보관**.
    🔒 **복원 기능에 '돌아올 자리'가 없으면 그건 복구 도구가 아니라 흉기다.**
  - 보관 상한 자동 정리: auto 30 / manual 15 / pre_restore 10. 오래된 것부터 삭제. 실패해도 무시(정리는 부차적).
  - 용량: 스냅샷 1장 ≈ 압축 55KB. 상한 55장 ≈ 3MB. 무료티어 500MB 대비 문제 없다.
  - 신규 테스트 `t23.js` — 자동생성 / 같은날 중복방지 / 목록 렌더 / 되돌리기 시 `pre_restore` 선행 / 상한 정리.
    ⚠️ 기존 스텁 전부를 **테이블명 분기**로 보강해야 했다(`app_state` 와 `app_state_snap` 은 지원 메서드가 다르다).
  - 검증: 15탭 회귀 0 · 극단상태 5종 0 · 6 브레이크포인트 0 · t15/t17/t18/t19/t11 0 · t20/t21/t22/t23 전부 통과.
  - 배포 sha256 `56dbf98bd14da51fbdf9aa24770e74d0aff2bb12691f3e78be0f3b9aa9134422`.
- **🚫 내가 틀렸던 것 — 지우지 말 것.** 8/20 "로한이 해야 할 것"에 *"E.8a·E.8b·C.8a·B.8a 루틴을 연간 목표 직결로 옮겨라"* 를 넣었는데,
  **실제 DB를 보니 루틴 6개 전부 `goalId` 가 이미 붙어 있었다.** 옛 handoff 항목을 검증 없이 옮긴 것이다.
  🔒 **미해결 목록을 로한에게 넘기기 전에 DB로 한 번 검증한다.** 안 그러면 이미 끝난 일을 시키게 된다.
- **v1.1 — 🔐 동기화 · 💾 백업. (v1.0 기념 전면 점검에서 나온 것)**
  - **⚠️ 이 앱에서 제일 위험한 코드는 렌더가 아니라 `save()` 였다.** v1.0까지 세 가지가 동시에 깨져 있었다.
    1. `.then(function(){},function(){})` — **저장 실패를 통째로 삼켰다.** 네트워크가 끊겨도 화면엔 아무 표시가 없었다.
    2. **조건 없는 upsert** — 폰과 PC를 같이 켜두면 **나중에 저장한 쪽이 앞의 것을 통째로 지운다.** 조용히, 문서 전체를.
    3. **복구 경로 0** — 내보내기도 백업도 없었다. 사고가 나면 끝이었다.
  - → `updated_at` **낙관적 잠금**(조건부 update → 0행이면 충돌). 충돌 시 사용자에게 **덮어쓰기 / 새로고침**을 묻는다.
    실패는 **1.2s×n 백오프로 5회 재시도**, 그래도 안 되면 alert 로 백업을 유도. 우하단 **저장 배지**(대기/완료/실패).
    `beforeunload` 로 미저장 상태의 창 닫기를 잡는다. 🔒 **원칙: 저장은 조용히 성공하고 시끄럽게 실패해야 한다.**
  - 💾 **JSON 내려받기 / 파일에서 복원** + **접속 직후 로컬 복원지점 3일치**(localStorage). 복원은 2단 확인 + 형식 검증 + 양쪽 요약 비교.
    로컬 복원지점은 **저장할 때마다가 아니라 접속 직후 1회만** 쓴다 — 사고 뒤에 덮어쓰면 그건 복원지점이 아니라 사고 사본이다.
  - **[치명] SEED 최상위 키 7개가 마이그레이션 보장 밖이었다.** `transactions` `categories` `cards` `accounts` `debts` `items` `budgets` —
    하나라도 없으면 해당 재정 탭이 `.filter of undefined` 로 즉사한다. **v1.0에서 `logs2`/`activity`/`taxParams` 세 개만 개별로 막았는데 같은 결함이 7개 더 있었다.**
    → 개별 나열을 그만두고 **SEED 를 기준으로 도는 루프**로 바꿨다. 앞으로 SEED 에 키를 추가하면 자동 보장.
  - **[치명] `d.ui` 는 어느 마이그레이션도 만들지 않았다.** `DB.ui.xxx` 를 19군데에서 직접 읽는다. 복원 기능을 넣자마자 재현됐다.
  - **마이그레이션 함수가 둘이라는 사실**(`migrateDB` → `migrateV6`)을 코드 주석에 못 박았다. 다음 사람이 (B)층을 못 찾는 사고를 막기 위해.
  - **💡 이번 점검의 교훈: 회귀 테스트가 '정상 상태'만 먹여왔다.** t_reg 의 고정 STATE 에 `health`/`journal`/`posts` 가 들어 있어서
    9일 동안 이 결함들이 전부 초록불이었다. → **`t22.js` 신설: 극단 상태 5종**(행 없음 / `{}` / schemaVersion만 / 구버전 유사 / ui 없음)**으로 15탭을 순회한다.**
    🔒 **규칙: 새 기능 테스트는 '잘 된 상태'가 아니라 '빈 상태'부터 먹인다.**
  - 신규 테스트: `t20`(정상저장·충돌덮어쓰기·충돌새로고침·신규계정·백업UI) `t21`(일시실패 재시도·6회실패 경고) `t22`(극단상태·백업왕복·불량파일거부).
    기존 `t*.js` 34개의 supabase 스텁을 `update().eq().select()` 체인 지원으로 일괄 갱신. **`t16` 은 폐기**(v11에서 `#v-timelog` 탭이 데일리로 합쳐져 사라짐).
  - 검증: 15탭 회귀 에러 0 · 극단상태 5종 전부 빈탭 0 · 6개 브레이크포인트 통과 · t8/t10/t11/t15/t17/t18/t19 에러 0 · 백업 왕복(서버에 쓰인 값 직접 확인).
  - 배포 sha256 `a78dbbd0274173b75864c5392baceef2fafdfc890405629309be4f23c4e4f747`.
  - **점검했는데 문제 없던 것** (같은 걸 또 파지 말라는 뜻): CSS 클래스↔base 규칙 대조(`.medcell` `.dw1~6` 은 스타일이 없지만 무해),
    `.ghost` 중복 정의 의심 → `.ctxbar.ghost` 로 스코프돼 있어 문제 없음, 죽은 함수 8개는 전부 설계 변경 잔여물이지 '막힌 입구'가 아님.
- **🏁 v1.0 — 정식 버전. 9일간의 개발 이터레이션을 마감했다.**
  - 로한: *"지금까지의 버전을 1.0으로 하자. 기능 추가 시 0.1, 세부 수정 시 0.01."*
  - `APP_VERSION` v12.2 → **v1.0**. CHANGELOG 최상단에 v1.0 항목 + `{v:'—'}` 구분 행을 넣어
    **그 아래가 옛 이터레이션 번호임을 화면에서 갈랐다.** 이력을 다시 쓰지는 않았다 —
    **로그를 소급해 고치면 그건 기록이 아니다.**
  - **⚠️ 마감 점검에서 마이그레이션 구멍 3개를 잡았다.**
    `logs2` · `activity` · `taxParams` 가 **SEED 에만 있고 마이그레이션이 보장하지 않았다.**
    없는 계정은 **세무 탭과 로그 탭이 통째로 죽는다**(`DB.logs2.slice()`, `DB.taxParams.noorang`).
    로한 DB엔 다 있어서 안 터졌지만, **회귀 테스트가 두 탭을 계속 '빈 탭'으로 보고하고 있었다.**
    나는 그걸 **9일 내내 '스텁 데이터 부족'으로 넘겼다.** 실제 결함이었다.
    → (B)층에 보장 추가. **테스트가 계속 같은 걸 지적하면 테스트가 틀린 게 아니라 코드가 틀린 것이다.**
  - 검증: **15탭 전부 렌더 · 빈 탭 0 · JS 에러 0** (처음으로 완전 통과), 6개 브레이크포인트 정상.

#### v12.2 ~ v9.0 (요약 — v1.0 이전 이터레이션. **전문은 앱 안 `CHANGELOG` 에 그대로 있다**)
하루에 9개 버전. 실행 카테고리가 지금 모양이 된 날이다.
- **v12.2 / v12.1 — 타임로그 구간 수정.** 로한: *"한번 정한 구획을 지우고 다시 써야 한다 — **결론은 시간 조절이 안 된다는 뜻**"*.
  ⚠️ **원인은 데이터가 아니라 UI였다.** `tlPut()` 은 v11.0부터 겹침 자동 분할을 지원했는데
  `tlSlotClick` 이 기록된 칸을 '블록 전체 수정' 모달로 보내 범위 선택을 시작조차 못 하게 막고 있었다 — **기능은 있는데 입구를 막아둔 꼴**(입구 막힘 계열).
  → 규칙 통일: **항상 두 칸을 눌러 범위를 정하고 그 범위가 곧 편집 대상.** 앞뒤는 남는다. + 시작·끝 시각 select, '이 구간 비우기', ✎ 호버 빠른 수정.
- **v12.0 — 데일리 + 타임로그 통합.** 캘린더 한 칸에 계획(루틴)과 실제(24h 색띠)를 겹친다. 우측은 날짜 고정 + `[✓ 루틴 | ⏳ 로그]` 탭.
  ⚠️ **날짜 상태를 `curDate()` 하나로 통일** — 두 페이지가 각자 날짜를 들면 반드시 어긋난다. 탭 16 → 15개.
- **v11.1 — 데일리 캘린더 확대.** 칸에 `✗ 이름+사유` 3개까지, 한 것은 개수로 접는다. **핵심 정보는 '못 한 것의 이름과 이유'다.**
  ⚠️ CSS 3연타를 **전부 실측으로** 잡았다: `.g2col` 기본 규칙이 뒤에 있어 `.dcol`/`.gcalcol` 이 통째로 무시(특이도 동률→뒤가 승),
  `flex+baseline+overflow` 가 line box 를 줄당 20px로 부풀림, `.gcli` cross-stretch 미적용으로 이름이 `매…` 로만 보임(실측 폭 30px).
  🔒 **CSS 레이아웃 문제는 눈으로 보지 말고 재라.**
- **v11.0 — ⏳ 타임로그 신설.** 로한: *"하루를 어떻게 보냈는지 낱낱이 드러내고 싶다."*
  ⚠️ 지금까지 X의 이유는 미달 사유 태그뿐 — **그건 라벨이지 사실이 아니다.** `무기력` 대신 `21:00–23:30 유튜브` 가 남아야 반성이 된다.
  ⚠️ **가계부와 구조가 다르다**(로한이 짚음): 가계부=건·순서 무관·총량 무제한 / 타임로그=**구간·순서가 의미·24시간 고정 총량**.
  총량이 고정이라 *"이만큼밖에 안 남았다"* 가 보인다. 30분 슬롯 48칸 · 12태그(**`낭비` 가 드러내려는 대상**) · 블록에 루틴 코드 연결.
  🔒 **기록 안 된 시간은 채우지 않는다. 비어 있는 것 자체가 데이터다.**
- **v10.0 — 실행 카테고리 3분할** `📅 데일리 / 🎯 목표·루틴 / 🎁 보상`.
  ⚠️ 며칠간의 UI 싸움이 전부 같은 뿌리였다 — **사용 빈도가 다른 것을 한 페이지에 넣은 것**(설계 월 1~2회 vs 체크 매일).
  🔒 **레이아웃으로 풀 문제가 아니라 정보구조로 풀 문제였다.** ⚙ 요일 매트릭스 신설(데이터엔 v5.0부터 있었고 화면만 없었다 — 입구 막힘).
- **v9.0 — 모바일 전면 정리.** 14개 탭이 4줄로 상단 절반을 먹던 걸 **한 줄 가로 스크롤 sticky 탭바**로. 브레이크포인트 640→900px.
  가계부 헤더 2단 + 통계 고정 그리드, 모바일 칸은 일 합계만 축약(`shortWon`). **좁은 화면에서는 '자주 쓰는 것 먼저'가 좌우 위계를 이긴다.**

### 2026-08-19 (요약 — 상세 원문은 `handoff_0819.md`)
하루에 v7.4 → v8.5, 12개 버전. 보상 체계가 완성된 날이다.
- **v8.0 🎁 보상 카드 모델 (로한 설계, 내 안보다 나았다).** `보상함(종류=덱) → 배치(예약) → 계단 달성(활성) → 소비하며 가계부 입력(소멸)`.
  **v7까지는 '수령 확정'이 곧 거래 생성이라, 받기로 해놓고 안 쓴 보상이 유령 거래로 남았다.**
  🔒 **상태는 저장하지 않고 파생**(`usedAt` / `tierOpen()`) — 문턱을 바꿔도 과거 판정이 안 어긋난다.
  🔒 **카드는 종류를 스냅샷** — 보상함 원본을 고쳐도 발급된 카드는 불변.
  거래 삭제 → 카드 복원 / 사용 취소 → **거래는 남기고 연결만 해제**(실제로 쓴 돈이므로). 유효기간 없음.
  계정과목 불변(배달=식비) — **보상 배지 없는 배달 건수가 곧 절제 실패 횟수**다.
- **v8.1** 목표 달성 카드에서 후보가 0개던 버그. 단계는 카드가 들고 있으므로 **보상함의 tier는 '기본 제안'일 뿐** → 다른 단계도 고를 수 있게 개방. 배치 모달에서 **버킷 제거**(로한: *"목표가 대가가 될 순 없다"*). 주간·월간은 **카드 1장 유지** — 계단은 루틴 축, 목표 달성은 목표 축.
- **v8.2** [버그] 감소형 목표가 `0/65kg 100%`. 미입력 판정이 `''·null`만 걸러 **숫자 0을 놓쳤다**(v6.1 재발). **루틴 기간 신설**(계속/이 달만/직접) — 종료일 필드가 없어 8월 눈금 루틴이 12월까지 떴다. 연동 미설정 '수동' 칩.
- **v8.3~8.4** 목표 탭 레이아웃. **병목은 '캘린더가 작다'가 아니라 '상단이 길다'.** 52주 트래커 접기, 캘린더 확대(칸에 비율·못한 루틴·사유·陰·🎁), **행 끝 '주' 칸으로 주간 계단이 캘린더 안에서 완결**. 전폭 게이지를 빈 공간으로 옮겨 **연간 10줄 → 5줄**.
- **v8.5 [UI/UX 감사]** `.rwpick` **base CSS 규칙이 통째로 없었다** — 클릭 목록 전체가 '그냥 글줄'로 렌더. **v7.1 `.tcard`와 같은 계열, 2회차.** 클래스↔CSS base 대조 스크립트로 발견. 모달 헤더 sticky, 목록형 모달 액션 상단화, 히트영역 11→26px, `:focus-visible` 신설.
- **v7.4~7.6** 버킷함 ⇄ 보상함(`place` 필드), 마이그레이션이 `schemaVersion>=6` 게이트에 막혀 통째로 스킵되던 치명 결함(→ (A)1회성 / (B)매 로드 멱등 2층 분리), 작성 중 모달이 바깥 클릭으로 닫히던 문제.

### 2026-08-11 ~ 08-18 (요약 — 상세는 `handoff_0818.md`)
- **v7.0~7.3** 🎁 보상 계단(일간 완전달성 → 주 4일 → 월 4회 → 연 9회), 소비/행동 2분류 + `budget` 축, 명리 음기일(3점=연 72일), 🪣 버킷함 신설.
- **v6.0~6.2** 목표·루틴 전면 재설계. **목적 함수: 12월 31일에 얼마나 달성했고 무엇이 부족했는지 반성할 수 있을 것.** 🔒 `checks[날짜]={done,due,miss}` **분모까지 스냅샷 — 과거 불변성**(v5.0에서 소급 변동 실측). 삭제 대신 은퇴. 미달 사유 태그.
- **v5.0.1** 현황판 빈 화면 회귀 → **`VIEW_RENDER` 맵.** 🔒 사이드바에 탭 추가하면 여기 반드시 등록.
- **v4.5** 부채 상환 엔진 — 원금 고정 416,000 + 잔액×3.11%×경과일/365. **은행앱 6회차와 원 단위 일치.**
- **v4.3~4.4** 일지 탭, 체성분 8지표. **v1~4.2** 프로젝트 시작, Supabase, 현금흐름, 🔮 사주 엔진.

---

## 🗺 목표·루틴 로드맵 (2026-08-14 결정)

**구글 캘린더 연동은 Phase 2로 미룸.** 판단 근거:
- 기술적으로는 가능. 브라우저 전용 정적 사이트에서 `gapi` + Google Identity Services로 백엔드 없이 호출 가능. 필요: Google Cloud 프로젝트 → OAuth 클라이언트 ID(웹) + API 키 → 승인된 JS 원본에 `https://rohanchung.github.io` 등록 → 동의화면.
- ⚠️ **불확실**: 브라우저 토큰은 1시간 만료에 refresh token이 없다. 조용한 재발급이 되는지 구글 문서에 명시가 없어, 최악의 경우 접속마다 구글 로그인 팝업.
- ❌ **로버트 경유 미러링 불가**: Google Calendar MCP 커넥터에 노출된 도구가 `search_events` 하나뿐. 기간 조회·이벤트 생성 없음. 배치로 긁어와 Supabase에 밀어넣는 우회로는 현재 도구로 막혀 있음.
- ❌ **양방향은 반대.** 루틴 체크 월 150건이 구글 캘린더를 도배하고, 충돌 해결 로직은 개인 프로젝트가 가장 많이 좌초하는 지점.
- → 붙인다면 **읽기 전용 오버레이**로만. 2주 써보고 실제로 아쉬울 때 판단.

**다음 단계 후보**
- 데이터 2~3개월 쌓이면 **사주 주의일 vs 평상일 루틴 달성률 상관 분석**. 지금은 표시만, 분석은 표본이 모인 뒤.
- 주간 리뷰 화면(주별 달성률 추이).

## ⏳ 미해결 / 대기

### 🔴 로한이 해야만 하는 것 (로버트가 대신 못 함)
- [ ] **A 목표(풍무점 500만)** 을 수치형 `5,000,000원` + 현금성 연동으로 수정. 지금은 게이지가 안 쌓인다. ← **DB 검증 완료, 실제로 미완**
- [ ] **9/1 0시 계좌 기초잔액 입력** → 현금흐름 100% 정합 가동 (`cashflow-design.md`). 로한: "9/1에 할 것"
- [x] ~~B 체중 목표 수정~~ → 8/20 완료
- [x] ~~루틴을 목표에 연결~~ → **애초에 다 돼 있었다.** 루틴 6개 전부 `goalId` 보유. 내가 검증 없이 옮긴 항목이었다.
- [ ] 월 1회 정도 🪶 로그 탭 → `⬇ JSON 내려받기` (물리적 사고 대비. 논리적 사고는 v1.2 스냅샷이 자동으로 막는다)

### 🟠 다음 창에서 바로 할 것
- **비었다.** (가) 루틴 코드 이관 · (나) 버킷 승격 **둘 다 8/20 v1.3 에서 구현·검증 완료.**
- 남은 파생 1건: **눈금 이월 UI** — 9월이 되면 `E.8a`(8월 눈금 소속) 같은 루틴을 어디에 둘지 로한이 매달 손대야 한다.
  지금은 **손으로 옮겨도 기록이 안 깨진다**(v1.3). 자동화는 9월에 실제로 번거로운지 겪어보고 판단한다. 미리 만들지 않는다.

### 🟡 로버트 대기 (데이터가 더 쌓여야 함)
- [ ] **회고 뷰** (goal-design 6장) — 기간 선택형. 목표별 성적표 · 월별 히트 · 루틴 기여도 · 미달 사유 집계. **데이터 3~6개월 뒤.** 그전까진 로버트가 SQL로 직접 분석해 대화한다.
- [ ] **주간 달성률 추이 그래프** — 12주 잔디 옆에 나란히 (dataviz 스킬 참조). ⚡ `goalTrend()` / `goalStreaks()` 는 **이미 구현돼 있고 아무도 호출하지 않는다.** 화면만 붙이면 된다.
- [ ] "About 로한" 공유용 다이제스트 — 로한이 "천천히"라 함, 보류.

### 🟢 다음 작업 후보 (우선순위 순)
1. **GitHub 자동 백업** — v1.2 스냅샷으로 **논리적 사고는 막혔다.** 남은 건 **물리적 사고**(Supabase 프로젝트·계정 소실)뿐이다.
   Edge Function 월 1회 `backups/` 커밋. 우선순위는 v1.1 때보다 **내려갔다** — 스냅샷이 일상 사고를 다 가져갔다.
2. **저장 페이로드 축소** — 아래 용량 항목 참조.
3. 죽은 코드 정리 — `rmk` `ensureRewardCat` `goalByCode` `goalLabel` `tlDel` `bankYearTotal` 은 호출자가 0이다. 전부 설계 변경으로 대체된 잔여물(각각 `bub()`, 사용자 지정 계정과목, `tlDelRange()` 로 대체). 지우는 건 언제든.

### 📏 용량 — **기존 메모가 틀렸다. 8/20 실측으로 교정.**
- 기존 handoff 의 "현재 48kB / 55kB" 는 **`pg_column_size`(TOAST 압축값)를 원본 크기로 잘못 읽은 것**이다.
- **실측(2026-08-20)**: `length(data::text)` = **227,758자 / 262,227 bytes (약 256KB)**. 거래 1,015건 기준.
- **"1MB 한계"도 틀렸다.** Postgres jsonb 필드 한계는 사실상 1GB다. 5~7년/3~4년이라는 시한은 근거가 없었다.
- **진짜 병목은 크기가 아니라 구조다**: `save()` 한 번이 **문서 전체(현재 256KB)를 업로드**한다. 400ms 디바운스이므로 입력이 몰리면 그만큼 반복된다.
  → 위험은 (a) 모바일 회선에서 저장 지연 · 실패 확률 상승, (b) 두 기기 동시 사용 시 통째 덮어쓰기. **(b)는 v1.1에서 막았다.**
  → (a)의 해법은 파티셔닝이 아니라 **분리 저장**(`transactions` / `timelog` 를 별도 테이블 또는 별도 행으로 빼고 변경분만 쓰기)이다. 시점은 **불확실** — 연간 증가량을 판단할 만한 사용 기간이 아직 없다(체크 2일, 타임로그 1일).
- [x] ~~은행 실제 상환액 받아서 앱 부채 계산 교정~~ → **8/14 완료.**
- [x] ~~JSON 내보내기 버튼~~ → **8/20 v1.1 완료.** (자동 백업은 위 1번으로 남음)
- [x] ~~아침 브리핑 스케줄 태스크~~ → **8/18 취소.** 로한 판단: 의미 없음.
