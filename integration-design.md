# 🔗 로한북 파트 연계 설계

> 작성 2026-08-19 · 로한 ↔ 로버트
> 목표·루틴 내부 설계는 `goal-design.md`. **이 문서는 파트와 파트 사이를 다룬다.**

---

## 0. 왜 이 문서가 필요한가

로한의 정의: **목표관리가 본체이고, 재정·건강·명리는 그 액션플랜을 위한 밑단 데이터다.**

v7.1까지 완성된 것은 **보상 ↔ 목표·루틴** 연계다. 그러나 보상은 본질적으로
다른 파트를 건드린다. 돈을 쓰고(가계부), 몸을 쓰고(건강), 날을 고른다(명리).

**연계가 없으면 로한북은 한 앱 안에 든 다섯 개의 앱일 뿐이다.**

---

## 1. 보상의 2분류

| 분류 | 코드 | 정의 | 예 | 연계 |
|---|---|---|---|---|
| **소비 보상** | `spend` | **새 지출**이 발생하는 것 | 3만원 이하 먹고 싶던 것, 사고 싶던 옷 | **가계부** |
| **행동 보상** | `act` | 시간·행위로 받는 것 | 게임 3시간, JW, 여행, 늦잠 | 명리(권장일) |

### 두 축으로 모든 경계 케이스가 풀린다

`kind`(성격) + `budget`(새 지출액). 두 축이 독립적이라 복합도 자연스럽게 표현된다.

| 보상 | kind | budget | 성격 |
|---|---|---|---|
| 3만원 이하 먹고 싶던 것 | spend | 30,000 | 새 지출 |
| 사고 싶던 옷 | spend | 50,000 | 새 지출 |
| 일본 3박 4일 | act | 800,000 | **행동 + 새 지출** |
| 게임 3시간 | act | 0 | 시간만 |
| JW | act | 0 | 시간만 |
| **소불고기 해먹기** | **act** | **0** | **이미 산 재료** |
| 넷플릭스 정주행 | act | 0 | 이미 낸 구독료 |
| 하루 늦잠 | act | 0 | 시간만 |

**`budget > 0` 이면 가계부 연계가 발동한다.** kind는 집계 축, budget은 연계 트리거.

### '기타'를 만들지 않은 이유

로한이 초안에서 3분류(소비/행동/기타)를 제안했고, 경계 사례로 *"이미 산 식재료로 주말에
소불고기를 해먹는다"* 를 들었다. 검토 결과 **`act` + `budget:0`으로 정확히 표현된다.**
돈은 과거에 나갔고 수령 시점엔 추가 지출이 0, 행동은 있다. 넷플릭스 정주행과 같은 구조다.

**'기타'를 남기면 분류 체계의 쓰레기통이 된다.** 애매한 것이 전부 거기로 가고
1년 뒤 "기타 23건"은 아무것도 말해주지 않는다. **분류의 목적은 집계이고,
집계되지 않는 분류는 없는 편이 낫다.**

새 돈이 나가는가 — 이 판정은 애매할 일이 없다. 3번째 분류가 정말 필요해지면
**데이터가 근거를 준 뒤에** 쪼갠다. 지금 만들면 빈 서랍이 된다.

## 2. 소비 보상 ↔ 가계부

### 수령 플로우

```
주간 성공  →  🎁 받기  →  보상 선택
                          ├─ [행동] 메모만 남기고 끝
                          └─ [소비] 실제 얼마 썼나? → 가계부 거래 자동 생성
```

수령 모달에서 `budget > 0`이면 **실제 지출액**을 묻는다. 상한과 비교해 표시한다.

```
🎁 3만원 이하 먹고 싶던 것        상한 30,000원
실제 지출 [ 28,000 ]  ✓ 상한 이내
□ 가계부에 기록  (카테고리: 보상 · 2026-08-23)
```

상한을 넘기면 경고하되 **막지는 않는다.** 기록이 목적이지 통제가 목적이 아니다.

### 양방향 연결

```js
rewardLog['week@2026-08-16'] = { ..., spent: 28000, txnId: 'x8a2k' }
transactions[i]              = { ..., rewardId: 'r1', cat: '보상' }
```

양쪽에 서로의 id를 남긴다. 가계부에서 그 거래를 보면 "이건 보상 지출"이라고 알 수 있고,
보상 기록에서 실제 거래로 갈 수 있다. **한쪽만 남기면 나중에 추적이 끊긴다.**

### ⚠️ 의도된 긴장 — 이게 이 연계의 진짜 가치다

목표 `A 보증금 500만 확보`는 **현금성 자산에 연동**되어 있다.
보상으로 3만원을 쓰면 현금이 줄고 → **A의 진행률이 내려간다.**

**루틴을 지켜서 받은 보상이 재정 목표를 늦춘다.** 이 모순은 버그가 아니라 현실이다.
앱은 이걸 숨기지 말고 보여줘야 한다.

```
올해 보상 지출 420,000원  =  보증금 목표의 8.4%
```

이걸 보여주는 이유는 죄책감을 주려는 게 아니다. **의식적으로 쓰게 하려는 것이다.**
보상이 없으면 루틴이 죽고, 보상이 과하면 목표가 죽는다. 그 균형점은 데이터로만 찾을 수 있다.

---

## 3. 행동 보상 ↔ 명리 (권장일)

### 로한의 요청

*"JW는 웬만하면 사주에서 음기가 강한 날에 하고 싶다. 그게 어찌됐든 도움이 될 것 같아서?"*

### ⚠️ 먼저 정직하게

**생리적·행동적 활동과 명리 음양 사이에 과학적 근거는 없다. 이건 불확실이 아니라 근거 없음이다.**
로한 본인도 *"어찌됐든"* 이라고 반신반의하고 있고, 그 감각이 맞다.

그럼에도 기능을 넣는 이유는 두 가지다.

1. **로한북의 명리 파트는 이미 "예언이 아니라 구조적 렌즈"라는 전제로 운영 중이다.**
   길흉을 맞히는 도구가 아니라, 날에 의미를 부여해 행동을 배치하는 틀로 쓰고 있다.
2. **"아무 때나"보다 "이 날"이 절제에 유리하다.** 규칙이 있으면 자기 통제감이 생긴다.
   이 효과는 음양이 실재하는지와 무관하게 작동한다. 임의의 규칙이라도 규칙이면 된다.

**그리고 이것도 검증 대상으로 만든다.** 데이터가 쌓이면 음기일 수행 여부와
루틴 달성률·기분(일지)의 상관을 볼 수 있다. 상관이 없으면 그때 빼면 된다.

### 음기 점수 — 설계안을 검증으로 고쳤다

앱에 이미 `sjPillars`(일주), `SAJU_YIN`(천간 음양), `SAJU_OH`(오행)가 있다. 추가 데이터 불필요.

**최초 안은 "음간 +1, 음지 +1, 오행 水金 +1 → 2점 이상"이었다. 365일로 돌려보니 틀렸다.**

```
0점 110일(30%)   1점 72일(20%)   2점 111일(30%)   3점 72일(20%)
→ 2점 이상 = 183일 = 정확히 50%
```

**격일로 나온다.** 원인은 60갑자의 구조다 — 甲子·乙丑·丙寅처럼 **양간은 항상 양지, 음간은 항상 음지**와
짝을 이룬다. 즉 `음간`과 `음지`는 완전히 중복된 조건이고, 실질 변별력은 오행뿐이었다.

**"이 날"이 이틀에 한 번 오면 규칙으로서 의미가 없다.** 기준을 올린다.

| 등급 | 조건 | 빈도 | 용도 |
|---|---|---|---|
| **음기 강** | 음간·음지 + 오행 **水 또는 金** (3점) | 연 72일 · **주 1.4회** | `timing:'yin'` 권장일 |
| 음기 중 | 음간·음지 (2점) | 연 111일 | 표시만 |
| 양 | 그 외 | 연 182일 | — |

**주 1~2회**면 규칙으로 기능한다. 실제 앞으로의 음기 강일:

```
8/25 辛未(3)  ·  8/27 癸酉(3)  ·  9/6 辛巳(3)  ...
```

캘린더에 마커로 표시하고, 행동 보상에 `timing:'yin'`을 걸면 **권장일에 표시**한다.
강제하지 않는다 — 다른 날 수령해도 막지 않는다.

---

## 4. 파트 간 연계 지도

| 파트 | → 목표·루틴으로 (읽기) | ← 목표·루틴에서 (쓰기) | 상태 |
|---|---|---|---|
| **계좌·부채** | 잔액 → 목표 현재값 | — | ✅ v6.1 완료 |
| **체성분** | 체중 → 목표 현재값 | — | ✅ v6.1 완료 |
| **사주** | 충·형 주의일 → 캘린더 표시 | — | ✅ v5.0 완료 |
| **가계부** | 지출 → 재정 목표 진행률 | **소비 보상 → 거래 생성** | 🔨 이번 작업 |
| **사주** | **음기일 → 행동 보상 권장** | — | 🔨 이번 작업 |
| **예산** | 보상 예산 항목 | 보상 지출 집계 | ⏳ 다음 |
| **일지** | 감정 태그 → 미달 원인 분석 | 보상 수령 → 일지 자동 초안 | ⏳ 다음 |
| **피검사** | 건강 지표 → 목표 근거 | — | ⏳ 다음 |

**이번에 하는 것은 두 줄뿐이다.** 나머지는 필요가 확인된 뒤에 붙인다.
연계는 많을수록 좋은 게 아니라, **쓰이는 것만 있어야** 한다.

---

## 5. 데이터 모델 변경

```js
rewards: [{
  id, code, tier,
  kind: 'spend'|'act',           // 신규 — 2분류
  budget: 30000,                 // 신규 — 0보다 크면 가계부 연계 발동
  timing: 'any'|'yin'|'yang',    // 신규 — 행동 보상 권장일
  title, goalId, active
}]

rewardLog: {
  'week@2026-08-16': {
    rewardId, code, title,
    kind, claimedAt,
    spent: 28000,                // 신규 — 실제 지출
    txnId: 'x8a2k',              // 신규 — 가계부 거래 id
    memo: '',                    // 신규 — 행동 보상 메모
    metric: {cur, req, unit}
  }
}

transactions: [{ ..., rewardId: 'r1' }]   // 신규 필드 (역방향 태그)
categories: [{ id:'k_rw', name:'보상', type:'expense', deductible:false }]  // 신규 카테고리
```

기존 `rewards`에 `kind`가 없으면 마이그레이션 시 `budget>0 ? 'spend' : 'act'`로 추정한다.

---

## 6. 화면

**보상 등록 모달**
- 분류 선택 (소비 / 행동)
- 예산 입력 — "이 보상에 쓸 돈. 0이면 돈 안 드는 보상"
- 행동 보상이면 권장일 선택 (상관없음 / 음기일 / 양기일)

**수령 모달**
- `budget>0` → 실제 지출 입력 + 가계부 기록 체크박스 + 상한 비교
- `kind='act'` → 메모 입력 (선택)
- 권장일이 걸려 있으면 오늘이 그 날인지 표시

**연간 프레임**
- `올해 보상 42만 · 보증금 목표의 8.4%` 한 줄

**월간 캘린더**
- 음기일 작은 마커 (기존 사주 주의일 마커와 구분되는 색)

---

## 7. 버킷함 — 보상 탭 신설

### 왜 만드나

로한의 문제 제기: *"주말 배달 2만원 지출이 내 경제 상황에 안 맞다. 1끼 1만원 이하로 줄이고 싶다.
그러려면 미리 버킷에 내가 보상으로 뭘 원하는지 써놓고, 주간·월간·연간 보상과 연계해 고르게 한다."*

**가치가 셋이다.**

1. **충동 지출 → 조건부 허용.** 참는 게 아니라 미루고 조건을 붙인다.
   (24·48시간 규칙의 확장판. 널리 쓰이는 기법이나 **엄밀한 효과 크기 연구는 미확인**)
2. **"내가 뭘 원했나"의 기록.** 90일 지나도 안 끌리면 그건 욕구가 아니라 충동이었다는 증거다.
   **버킷의 진짜 산출물은 '안 산 것들'일 수 있다.**
3. **보상 내성이 구조적으로 해소된다.** 10장 리스크에 적어둔 *"같은 보상을 3개월 반복하면 효과가 준다"* 를
   버킷이 자동으로 푼다. 고정 메뉴 3개를 돌려쓰면 질리지만 버킷은 계속 갱신된다.

### 실측 근거 — 배달

```
배달   7개월 46건 · 월평균 121,703원 · 건당 18,520원 · 월 6.6회 (주 1.5회)
```

버킷+보상 구조로 바꾸면:

```
평소        1만원 이하로 해결 (배달 안 함)
주간 보상   달성한 주에만 배달 2만원 → 월 최대 4회 = 80,000원
──────────────────────────────────────────────────
월 42,000원 절감 · 연 504,000원  =  A 보증금 목표의 10%
```

**⚠️ 전제**: 배달을 보상으로 만들려면 **평소엔 정말 안 시켜야 한다.**
지금처럼 주 1.5회 시키면서 보상으로도 지정하면 원칙 ①(평소 안 하던 것)에 걸려 아무것도 안 바뀐다.
시스템은 강제할 수 없다. 다만 **못 받은 주에 배달을 시켰는지는 가계부에 남는다.** 그게 유일한 감시 장치다.

### ⚙️ 모델 — 통합안을 철회하고 분리한다

**최초 안은 `rewards` 하나로 통합하고 `once` 플래그로 가르는 것이었다. 로한의 지적으로 뒤집었다.**

> *"티어를 굳이 정할 필요 있나 싶다. '서울에 집 사기' 같은 버킷은 말그대로 버킷이지만
> 너무 이뤄내기 힘들어서 사실상 아주 큰 목표인거고, 담는 순간에 달성하고 싶은 욕망을
> 티어로 표현하기엔 단어로도 안 맞는다."*

**이 한 문장이 버킷의 정체를 바꿨다.**

| | 내가 잡았던 것 | 실제 |
|---|---|---|
| 버킷이란 | 보상 후보 목록 | **욕망의 원장 (ledger of wants)** |
| 담는 것 | 보상으로 받을 만한 것 | **원하는 것 전부** |
| 티어 | 담을 때 정함 | **정할 수 없다** |

"서울에 집 사기"에 `tier:'year'`를 붙이면 *연간 보상으로 집을 산다*는 말이 된다. 성립하지 않는다.
버킷에는 **주말에 먹고 싶은 국밥부터 평생 못 이룰 수도 있는 것까지** 같이 들어간다.
그게 정직한 욕망의 모습이다.

**티어는 담을 때가 아니라 고를 때 결정된다.** 수령 모달에서 버킷을 **가격 오름차순**으로 보여주면
현실적인 것이 위에 오고 큰 것은 아래로 밀린다. 분류를 강제하지 않아도 자연스럽게 정렬된다.

그러면 `rewards`와 `bucket`은 **tier 유무 + status 전이 유무**라는 두 가지가 달라진다.
`once` 하나로 가를 수 있는 관계가 아니다. **분리한다.**

```js
/* 고정 메뉴 — 반복 가능. 계단 층에 묶인다 */
rewards: [{
  id, code,                    // 'W1' 'M1' 'Y1' 'G:A'
  tier: 'week'|'month'|'year'|'goal',   // ★ 필수
  goalId, kind, budget, timing, title, active
}]

/* 버킷 — 욕망의 원장. 계단에 묶이지 않는다 */
bucket: [{
  id, code,                    // 'K1' 'K2'
  title,                       // 텍스트만
  kind: 'spend'|'act',
  price: 45000,                // 예상 비용. 0이면 돈 안 듦
  addedAt: '2026-05-19',       // ★ 기록일
  updatedAt: '2026-08-19',     // ★ 수정일
  status: 'want'|'got'|'dropped',
  gotAt, logKey,               // 수령 시 rewardLog 키
  droppedAt, dropReason,
  memo
}]

rewardLog['week@2026-08-16'] = { ..., bucketId: 'K1' }   // 역방향
```

`tier` 없음. `budget` → `price`(예상 비용)로 이름을 바꾼다 — 예산이 아니라 짐작이다.

### 화면 — 표(table)

로한 요청: *"UI는 그냥 표 형태로, 실행 카테고리에 단독 페이지"*

```
🎁 보상                                          [+ 담기] [+ 고정메뉴]

[주간 4/4 받기]  [월간 1/4]  [연간 0/9]        ← 계단 현황

▪ 버킷함   대기 12 · 받음 3 · 포기 2
┌──────┬────────────────────┬──────┬────────────┬──────────┬──────┬──────┐
│ 코드 │ 내용               │ 분류 │  예상비용  │  기록일  │ 경과 │ 상태 │
├──────┼────────────────────┼──────┼────────────┼──────────┼──────┼──────┤
│ K5   │ 편의점 신상 털기    │ 소비 │      8,000 │ 26.08.18 │  1일 │ 대기 │
│ K1   │ 스시 오마카세       │ 소비 │     45,000 │ 26.05.19 │ 92일⚠│ 대기 │
│ K3   │ 온천 1박            │ 행동 │    120,000 │ 26.08.16 │  3일 │ 대기 │
│ K9   │ 서울에 집 사기      │ 소비 │ 500,000,000│ 26.01.02 │229일 │ 대기 │
│ K2   │ 무선이어폰          │ 소비 │     89,000 │ 26.07.03 │      │ 받음 │
└──────┴────────────────────┴──────┴────────────┴──────────┴──────┴──────┘
     ↑ 컬럼 클릭으로 정렬 · 상태 필터

▪ 고정 메뉴   반복 가능
┌──────┬────────────────────┬──────┬──────────┬──────┬──────┐
│ 코드 │ 내용               │ 단계 │ 예상비용 │ 분류 │ 권장 │
├──────┼────────────────────┼──────┼──────────┼──────┼──────┤
│ W1   │ 배달 2만원          │ 주간 │   20,000 │ 소비 │      │
│ W2   │ 게임 3시간          │ 주간 │        0 │ 행동 │  陰  │
└──────┴────────────────────┴──────┴──────────┴──────┴──────┘

▪ 계단 문턱  주 4일 · 월 4회 · 연 9회   [조정]
▪ 수령 기록  5건                        [전체]
```

**정렬 가능한 표**가 핵심이다. 가격순으로 보면 "지금 받을 수 있는 것"이 위에 모이고,
기록일순으로 보면 "오래 묵은 욕망"이 드러난다. 티어로 미리 분류하는 것보다 유연하다.

### 수령 모달

```
🎁 주간 보상 받기            4/4 일 완전달성

─ 고정 메뉴 ─
  W1  배달 2만원                    소비  20,000
  W2  게임 3시간                    행동       0   陰 오늘 아님

─ 버킷에서 ─  (예상비용 낮은 순)
  K5  편의점 신상 털기               소비   8,000
  K1  스시 오마카세                  소비  45,000   92일째
  K3  온천 1박                      행동 120,000
  ⋯
```

큰 항목은 아래로 밀려 자연스럽게 선택지에서 빠진다. **막지 않아도 정렬이 판단을 돕는다.**

### 상태 전이

```
담기 → want ─┬─→ got      보상으로 수령 (logKey로 언제 어느 단계에서 받았는지 추적)
             ├─→ dropped  포기 (사유 기록 ← 자기 이해 데이터)
             └─→ (그대로) 못 받아도 남아있는 것. "서울에 집 사기"는 여기 산다
```

`90일 이상 want`면 `92일째 ⚠` 표시. **포기를 권하는 게 아니라 물어보는 것이다.**
큰 항목은 몇 년이고 남아 있어도 된다 — 그게 버킷이 원장인 이유다.

**승격 (다음 단계 후보)**: 버킷 항목이 커져서 보상으로는 도저히 안 되는 것이 명확해지면
**연간 목표로 전환**하는 경로를 열 수 있다. `K9 서울에 집 사기` → 연간 목표 `E`.
지금은 넣지 않는다. 실제로 그런 항목이 쌓인 뒤에 판단한다.

### 리스크

- **버킷이 커지면 관리 부담.** 90일 경과 표시 + 포기 기능 + 정렬/필터가 방어선이다.
- **담기만 하고 안 받으면 좌절 목록이 된다.** 문턱이 너무 높을 때 나오는 현상 →
  **현실적 가격대(주간 보상으로 받을 만한) 대기가 20개를 넘으면 문턱 하향 신호**로 본다.
  큰 항목은 이 계산에서 뺀다. 그건 원래 못 받는 것이라 좌절 지표가 아니다.

## 8. 리스크

- **보상 지출이 재정 목표를 잠식한다.** 이건 기능이지 결함이 아니다. 다만 **비율을 항상 보이게** 해서
  로한이 스스로 조절하게 한다. 앱이 막지 않는다.
- **명리 권장일은 근거가 없다.** 기능으로 제공하되 문서와 화면 모두에 그 사실을 남긴다.
  3~6개월 뒤 상관 분석으로 유용성을 판정한다.
- **연계가 늘수록 복잡해진다.** 이번엔 두 줄만 한다. 나머지는 필요가 증명된 뒤에.
- **가계부 자동 생성이 부담이 될 수 있다.** 체크박스로 끌 수 있게 하고, 기본값은 켬.

---

## 9. 구현 순서

**A. 모델**
1. `bucket` 배열 신설 (`code K*`, `price`, `addedAt`, `updatedAt`, `status`, `dropReason`)
2. `rewards`에서 `once`/버킷 관련 필드 제거 — 고정 메뉴 전용으로 유지
3. `K` 코드 발급기 (단조증가, 재사용 금지)

**B. 보상 탭 (단독 페이지)**
4. 사이드바 실행 섹션에 `🎁 보상` 신설 + **`VIEW_RENDER`에 등록** (v5.0 dash 누락 회귀 재발 방지)
5. 계단 현황 / 버킷 표 / 고정 메뉴 표 / 문턱 / 수령 기록
6. 표 정렬(컬럼 클릭) · 상태 필터 · 담기·수정·포기 모달

**C. 연결**
7. 수령 모달에 `고정 메뉴 + 버킷(가격 오름차순)` 통합 표시 → 버킷 선택 시 `got` 소진
8. 목표·루틴 탭에서 보상 관리 UI 제거 (칩만 남김)

**D. 검증**
9. 상태 전이(want→got/dropped), 90일 판정, 코드 발급, 정렬, 13→14탭 회귀, 소진 후 재선택 불가
