# 🔗 로한북 파트 연계 설계

> 작성 2026-08-19 · 로한 ↔ 로버트
> 목표·루틴 내부 설계는 `goal-design.md`. **이 문서는 파트와 파트 사이를 다룬다.**

---

## 0. 왜 이 문서가 필요한가

로한의 정의: **목표관리가 본체이고, 재정·건강·명리는 그 액션플랜을 위한 밑단 데이터다.**

v7.1까지 완성된 것은 **보상 ↔ 목표·루틴** 연계다. 그러나 보상은 본질적으로
다른 파트를 건드린다. 돈을 쓰고(가계부), 몸을 쓰고(건강), 날을 고른다(명리).

**연계가 없으면 로한북은 한 앱 안에 든 다섯 개의 앱일 뿐이다.**

---

## 1. 보상의 2분류

| 분류 | 코드 | 정의 | 예 | 연계 |
|---|---|---|---|---|
| **소비 보상** | `spend` | **새 지출**이 발생하는 것 | 3만원 이하 먹고 싶던 것, 사고 싶던 옷 | **가계부** |
| **행동 보상** | `act` | 시간·행위로 받는 것 | 게임 3시간, JW, 여행, 늦잠 | 명리(권장일) |

### 두 축으로 모든 경계 케이스가 풀린다

`kind`(성격) + `budget`(새 지출액). 두 축이 독립적이라 복합도 자연스럽게 표현된다.

| 보상 | kind | budget | 성격 |
|---|---|---|---|
| 3만원 이하 먹고 싶던 것 | spend | 30,000 | 새 지출 |
| 사고 싶던 옷 | spend | 50,000 | 새 지출 |
| 일본 3박 4일 | act | 800,000 | **행동 + 새 지출** |
| 게임 3시간 | act | 0 | 시간만 |
| JW | act | 0 | 시간만 |
| **소불고기 해먹기** | **act** | **0** | **이미 산 재료** |
| 넷플릭스 정주행 | act | 0 | 이미 낸 구독료 |
| 하루 늦잠 | act | 0 | 시간만 |

**`budget > 0` 이면 가계부 연계가 발동한다.** kind는 집계 축, budget은 연계 트리거.

### '기타'를 만들지 않은 이유

로한이 초안에서 3분류(소비/행동/기타)를 제안했고, 경계 사례로 *"이미 산 식재료로 주말에
소불고기를 해먹는다"* 를 들었다. 검토 결과 **`act` + `budget:0`으로 정확히 표현된다.**
돈은 과거에 나갔고 수령 시점엔 추가 지출이 0, 행동은 있다. 넷플릭스 정주행과 같은 구조다.

**'기타'를 남기면 분류 체계의 쓰레기통이 된다.** 애매한 것이 전부 거기로 가고
1년 뒤 "기타 23건"은 아무것도 말해주지 않는다. **분류의 목적은 집계이고,
집계되지 않는 분류는 없는 편이 낫다.**

새 돈이 나가는가 — 이 판정은 애매할 일이 없다. 3번째 분류가 정말 필요해지면
**데이터가 근거를 준 뒤에** 쪼갠다. 지금 만들면 빈 서랍이 된다.

## 2. 소비 보상 ↔ 가계부

### 수령 플로우

```
주간 성공  →  🎁 받기  →  보상 선택
                          ├─ [행동] 메모만 남기고 끝
                          └─ [소비] 실제 얼마 썼나? → 가계부 거래 자동 생성
```

수령 모달에서 `budget > 0`이면 **실제 지출액**을 묻는다. 상한과 비교해 표시한다.

```
🎁 3만원 이하 먹고 싶던 것        상한 30,000원
실제 지출 [ 28,000 ]  ✓ 상한 이내
□ 가계부에 기록  (카테고리: 보상 · 2026-08-23)
```

상한을 넘기면 경고하되 **막지는 않는다.** 기록이 목적이지 통제가 목적이 아니다.

### 양방향 연결

```js
rewardLog['week@2026-08-16'] = { ..., spent: 28000, txnId: 'x8a2k' }
transactions[i]              = { ..., rewardId: 'r1', cat: '보상' }
```

양쪽에 서로의 id를 남긴다. 가계부에서 그 거래를 보면 "이건 보상 지출"이라고 알 수 있고,
보상 기록에서 실제 거래로 갈 수 있다. **한쪽만 남기면 나중에 추적이 끊긴다.**

### ⚠️ 의도된 긴장 — 이게 이 연계의 진짜 가치다

목표 `A 보증금 500만 확보`는 **현금성 자산에 연동**되어 있다.
보상으로 3만원을 쓰면 현금이 줄고 → **A의 진행률이 내려간다.**

**루틴을 지켜서 받은 보상이 재정 목표를 늦춘다.** 이 모순은 버그가 아니라 현실이다.
앱은 이걸 숨기지 말고 보여줘야 한다.

```
올해 보상 지출 420,000원  =  보증금 목표의 8.4%
```

이걸 보여주는 이유는 죄책감을 주려는 게 아니다. **의식적으로 쓰게 하려는 것이다.**
보상이 없으면 루틴이 죽고, 보상이 과하면 목표가 죽는다. 그 균형점은 데이터로만 찾을 수 있다.

---

## 3. 행동 보상 ↔ 명리 (권장일)

### 로한의 요청

*"JW는 웬만하면 사주에서 음기가 강한 날에 하고 싶다. 그게 어찌됐든 도움이 될 것 같아서?"*

### ⚠️ 먼저 정직하게

**생리적·행동적 활동과 명리 음양 사이에 과학적 근거는 없다. 이건 불확실이 아니라 근거 없음이다.**
로한 본인도 *"어찌됐든"* 이라고 반신반의하고 있고, 그 감각이 맞다.

그럼에도 기능을 넣는 이유는 두 가지다.

1. **로한북의 명리 파트는 이미 "예언이 아니라 구조적 렌즈"라는 전제로 운영 중이다.**
   길흉을 맞히는 도구가 아니라, 날에 의미를 부여해 행동을 배치하는 틀로 쓰고 있다.
2. **"아무 때나"보다 "이 날"이 절제에 유리하다.** 규칙이 있으면 자기 통제감이 생긴다.
   이 효과는 음양이 실재하는지와 무관하게 작동한다. 임의의 규칙이라도 규칙이면 된다.

**그리고 이것도 검증 대상으로 만든다.** 데이터가 쌓이면 음기일 수행 여부와
루틴 달성률·기분(일지)의 상관을 볼 수 있다. 상관이 없으면 그때 빼면 된다.

### 음기 점수 — 설계안을 검증으로 고쳤다

앱에 이미 `sjPillars`(일주), `SAJU_YIN`(천간 음양), `SAJU_OH`(오행)가 있다. 추가 데이터 불필요.

**최초 안은 "음간 +1, 음지 +1, 오행 水金 +1 → 2점 이상"이었다. 365일로 돌려보니 틀렸다.**

```
0점 110일(30%)   1점 72일(20%)   2점 111일(30%)   3점 72일(20%)
→ 2점 이상 = 183일 = 정확히 50%
```

**격일로 나온다.** 원인은 60갑자의 구조다 — 甲子·乙丑·丙寅처럼 **양간은 항상 양지, 음간은 항상 음지**와
짝을 이룬다. 즉 `음간`과 `음지`는 완전히 중복된 조건이고, 실질 변별력은 오행뿐이었다.

**"이 날"이 이틀에 한 번 오면 규칙으로서 의미가 없다.** 기준을 올린다.

| 등급 | 조건 | 빈도 | 용도 |
|---|---|---|---|
| **음기 강** | 음간·음지 + 오행 **水 또는 金** (3점) | 연 72일 · **주 1.4회** | `timing:'yin'` 권장일 |
| 음기 중 | 음간·음지 (2점) | 연 111일 | 표시만 |
| 양 | 그 외 | 연 182일 | — |

**주 1~2회**면 규칙으로 기능한다. 실제 앞으로의 음기 강일:

```
8/25 辛未(3)  ·  8/27 癸酉(3)  ·  9/6 辛巳(3)  ...
```

캘린더에 마커로 표시하고, 행동 보상에 `timing:'yin'`을 걸면 **권장일에 표시**한다.
강제하지 않는다 — 다른 날 수령해도 막지 않는다.

---

## 4. 파트 간 연계 지도

| 파트 | → 목표·루틴으로 (읽기) | ← 목표·루틴에서 (쓰기) | 상태 |
|---|---|---|---|
| **계좌·부채** | 잔액 → 목표 현재값 | — | ✅ v6.1 완료 |
| **체성분** | 체중 → 목표 현재값 | — | ✅ v6.1 완료 |
| **사주** | 충·형 주의일 → 캘린더 표시 | — | ✅ v5.0 완료 |
| **가계부** | 지출 → 재정 목표 진행률 | **소비 보상 → 거래 생성** | 🔨 이번 작업 |
| **사주** | **음기일 → 행동 보상 권장** | — | 🔨 이번 작업 |
| **예산** | 보상 예산 항목 | 보상 지출 집계 | ⏳ 다음 |
| **일지** | 감정 태그 → 미달 원인 분석 | 보상 수령 → 일지 자동 초안 | ⏳ 다음 |
| **피검사** | 건강 지표 → 목표 근거 | — | ⏳ 다음 |

**이번에 하는 것은 두 줄뿐이다.** 나머지는 필요가 확인된 뒤에 붙인다.
연계는 많을수록 좋은 게 아니라, **쓰이는 것만 있어야** 한다.

---

## 5. 데이터 모델 변경

```js
rewards: [{
  id, code, tier,
  kind: 'spend'|'act',           // 신규 — 2분류
  budget: 30000,                 // 신규 — 0보다 크면 가계부 연계 발동
  timing: 'any'|'yin'|'yang',    // 신규 — 행동 보상 권장일
  title, goalId, active
}]

rewardLog: {
  'week@2026-08-16': {
    rewardId, code, title,
    kind, claimedAt,
    spent: 28000,                // 신규 — 실제 지출
    txnId: 'x8a2k',              // 신규 — 가계부 거래 id
    memo: '',                    // 신규 — 행동 보상 메모
    metric: {cur, req, unit}
  }
}

transactions: [{ ..., rewardId: 'r1' }]   // 신규 필드 (역방향 태그)
categories: [{ id:'k_rw', name:'보상', type:'expense', deductible:false }]  // 신규 카테고리
```

기존 `rewards`에 `kind`가 없으면 마이그레이션 시 `budget>0 ? 'spend' : 'act'`로 추정한다.

---

## 6. 화면

**보상 등록 모달**
- 분류 선택 (소비 / 행동)
- 예산 입력 — "이 보상에 쓸 돈. 0이면 돈 안 드는 보상"
- 행동 보상이면 권장일 선택 (상관없음 / 음기일 / 양기일)

**수령 모달**
- `budget>0` → 실제 지출 입력 + 가계부 기록 체크박스 + 상한 비교
- `kind='act'` → 메모 입력 (선택)
- 권장일이 걸려 있으면 오늘이 그 날인지 표시

**연간 프레임**
- `올해 보상 42만 · 보증금 목표의 8.4%` 한 줄

**월간 캘린더**
- 음기일 작은 마커 (기존 사주 주의일 마커와 구분되는 색)

---

## 7. 리스크

- **보상 지출이 재정 목표를 잠식한다.** 이건 기능이지 결함이 아니다. 다만 **비율을 항상 보이게** 해서
  로한이 스스로 조절하게 한다. 앱이 막지 않는다.
- **명리 권장일은 근거가 없다.** 기능으로 제공하되 문서와 화면 모두에 그 사실을 남긴다.
  3~6개월 뒤 상관 분석으로 유용성을 판정한다.
- **연계가 늘수록 복잡해진다.** 이번엔 두 줄만 한다. 나머지는 필요가 증명된 뒤에.
- **가계부 자동 생성이 부담이 될 수 있다.** 체크박스로 끌 수 있게 하고, 기본값은 켬.

---

## 8. 구현 순서

1. `kind`/`budget`/`timing` 필드 추가 + 마이그레이션 (기존 보상 추정 분류)
2. 보상 등록 모달에 3분류 · 예산 · 권장일
3. 수령 모달 분기 — 소비면 지출 입력 → 가계부 거래 생성 (양방향 id)
4. `보상` 카테고리 자동 생성
5. 음기 점수 함수 + 캘린더 마커 + 권장일 표시
6. 연간 프레임에 보상 지출 요약 (목표 대비 비율)
7. 검증 — 거래 생성·양방향 연결·상한 초과 경고·음기 판정
