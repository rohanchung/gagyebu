# 💳 카드 청구주기 · 할부 설계

> 작성 2026-08-21 · 로한 ↔ 로버트
> 가계부 내부 구조는 `요구사항정의서.md`, 현금흐름은 `cashflow-design.md`.
> **이 문서는 '신용카드를 쓴 시점'과 '돈이 실제로 나가는 시점'이 어긋나는 문제를 다룬다.**

---

## 0. 왜 다시 만드나

로한: *"신용카드 선택의 경우 일시불, 2개월 할부, 3개월 할부까지는 선택 항목을 추가해야겠다.
자연스럽게 카드 미결제와 연동되게. 내 카드들은 10일 기준, 11일부터 다음달 10일까지다.
**카드 처리 방식은 기획부터 수정해야 할 거다.**"*

맞다. 지금 구조로는 못 얹는다.

### 지금 있는 것 — 전부다

```js
transactions[i].card = true    // 미결제 플래그. 이게 전부다.
cardPendingByCard()            // card:true 인 것 단순 합계
doSettle()                     // 체크한 것 card=false + settle 거래 생성
```

**청구주기라는 개념이 아예 없다.** 그 증거가 선결제 모달 맨 아래 힌트에 남아 있다.

> *"(삼성카드 10일~익월9일 주기면 그 기간 건만 체크)"*

**계산을 사람에게 떠넘긴 것이다.** 앱이 날짜를 안다면 사람이 눈으로 고를 이유가 없다.

### 실측 (2026-08-21)

```
미결제 16건 · 490,754원 · 2026-08-02 ~ 2026-08-18
신용카드 2장: 삼성 탭탭오 · 삼성 ID MOVE
```

이 16건은 **사실 두 덩어리다.**

```
8/02~8/10 이용  →  8월 25일 결제분   (7/11~8/10 주기)
8/11~8/18 이용  →  9월 25일 결제분   (8/11~9/10 주기)
```

화면엔 `490,754` 한 줄로만 뜬다. **섞여 있는 것을 섞인 채로 보여주고 있다.**

---

## 1. ⚠️ 가장 중요한 사실 — 로한은 결제일을 기다리지 않는다

> 로한: *"매월 25일인데, **내가 항상 그전에 즉시 결제를 해서 지워버린다.**
> 보통 5일, 15일 정도 즉시결제로 25일 전에 처리한다."*

**이 한 문장이 설계를 바꾼다.**

- ❌ **"25일에 자동으로 결제 처리" 를 만들면 안 된다.** 그 일은 일어나지 않는다.
  자동 처리를 넣으면 이미 손으로 지운 건을 앱이 또 지우거나, 유령 결제가 생긴다.
- ✅ 로한에게 필요한 건 **"지금 내가 지울 수 있는 게 뭔지"** 를 주기별로 묶어서 보여주는 것이다.

`payDay(25일)` 는 **처리 트리거가 아니라 라벨**이다. 어느 덩어리인지 부르는 이름이고,
나중에 현금흐름에 '아직 안 지운 것'을 꽂을 때 쓰는 예정일이다. **그 이상의 권한을 주지 않는다.**

---

## 2. 청구주기

```js
cards[i] = {
  id, name, type:'credit'|'check', acct,
  closeDay: 10,   // 신규 — 이용기간 마감일. 11일~익월 10일
  payDay:   25    // 신규 — 결제일(라벨·예정일). 자동 처리 트리거가 아니다
}
```

```
cycleOf(date, closeDay) → 'YYYY-MM'      그 거래가 청구되는 '결제월'
    day <= closeDay  →  그 달
    day >  closeDay  →  다음 달

cycleRange('2026-09', 10) → '2026-08-11' ~ '2026-09-10'    이용기간
cyclePayDate('2026-09', 25) → '2026-09-25'                 주말이면 익영업일
```

- **주기 이름은 '결제월'로 통일한다.** `8/11~9/10 이용분` 보다 `9월 결제분` 이 짧고,
  로한이 카드사 앱에서 보는 이름과 같다. 이용기간은 괄호로 병기한다.
- 체크카드는 주기가 없다 — 즉시 출금이다. `closeDay`/`payDay` 는 신용카드에만 쓴다.

---

## 3. 할부

```js
transactions[i] = {
  ..., card: true,
  inst: 3,        // 신규 — 할부 개월수. 없거나 1 이면 일시불
  instDone: 0     // 신규 — 결제 완료된 회차 수
}
```

- **회차 금액**: `floor(amt/n)`, **첫 회차에 나머지**를 붙인다 (은행 관행).
  435,100 ÷ 3 → `145,034 / 145,033 / 145,033`
- **k회차 청구주기** = `cycleOf(거래일) + (k-1)개월`
  8/21 구매 3개월 → **9월 · 10월 · 11월** 결제분
- **미결제 잔액** = `amt − (1..instDone 회차 합)`
- 할부 수수료는 **넣지 않는다.** 2~3개월은 무이자가 기본이고,
  수수료가 붙는 경우를 만나기 전에 필드를 만들면 빈 서랍이 된다. 만나면 그때 넣는다.

### ⚠️ 거래를 회차별로 쪼개지 않는다

로한의 선택은 *"둘 다 보고 싶다"* 였다. **두 층으로 답한다 — 거래를 쪼개서가 아니라.**

| 층 | 기준 | 브리즘 안경 435,100 (3개월) |
|---|---|---|
| **가계부** | 발생 — 거래일 | **8월 지출 435,100 · 1건** |
| **미결제 / 현금흐름** | 현금 — 결제월 | 9월 145,034 · 10월 145,033 · 11월 145,033 |

거래를 3건으로 쪼개면 잃는 것:
- **"8월에 뭘 샀나"가 흐려진다.** 안경은 8월에 산 것이지 11월에 1/3 산 게 아니다.
- 카테고리·예산 통계가 1/3로 왜곡된다.
- 재물관리 연동(`재물로 등록`)이 어느 조각에 붙을지 모호해진다.
- 환불·취소 시 3건을 찾아 지워야 한다.

🔒 **거래는 사실의 기록이다. 회차는 그 사실에서 파생되는 일정이다. 파생을 원본에 섞지 않는다.**

---

## 4. 선결제 재설계 — 이번 작업의 본체

### 현황판

```
💳 카드 미결제                                        490,754

삼성 탭탭오                                           490,754
  ▸ 8월 결제분   7/11~8/10 이용 · 8/25 예정   312,400   [선결제 →]
  ▸ 9월 결제분   8/11~9/10 이용 · 9/25 예정   178,354   [선결제 →]
```

**주기별로 갈라 보여주는 것만으로 절반이 풀린다.** 지금 로한이 머리로 하던 계산이다.

### 선결제 모달

```
💳 삼성 탭탭오 · 8월 결제분          7/11~8/10 이용 · 8/25 예정
                                     [ 이 주기 전체 선택 ]
  ☑ 08-02  식비   배달                      18,500
  ☑ 08-05  교통   주유                      60,000
  ⋯ 일시불 9건                             312,400

─ 할부 (선결제 대상 아님) ─
  🔒 브리즘 맞춤 안경  1/3회차             145,033   9월 결제분
```

- **[이 주기 전체 선택]** 이 핵심 버튼이다. 로한이 5일·15일에 하는 행동이 정확히 이것이다.
- 주기 탭을 바꿔도 **선택은 그 주기 안에서만** 유지된다 — 섞이면 지금과 다를 게 없다.
- 🔒 **할부는 선결제 대상에서 제외한다.** 할부금은 회차대로만 나간다.
  *(잔여 일시상환은 카드사 앱에서 하는 별도 행위다. 앱에 넣지 않는다 — 실제로 할 일이 생기면 그때.)*
- 회차 결제 처리는 **주기가 지났을 때 그 주기 화면에서 `1/3 결제 완료` 로** 찍는다 → `instDone++`.

---

## 5. 하지 않는 것 (이번 범위 밖)

연계는 많을수록 좋은 게 아니라 **쓰이는 것만 있어야** 한다. v1.4 는 위 4장까지다.

| 항목 | 왜 미루나 | 시점 |
|---|---|---|
| 현금흐름에 결제 예정 꽂기 | 주기 모델이 실제로 맞는지 한 달 굴려본 뒤 | v1.5 |
| 가계부 월 지출 **발생/현금 토글** | 로한이 "둘 다"라 했지만, 미결제 층이 먼저 정확해야 의미가 있다 | v1.5 |
| 할부 수수료 | 무이자 아닌 할부를 아직 만난 적 없다 | 만나면 |
| 25일 자동 결제 처리 | **로한은 그 전에 손으로 지운다. 안 일어나는 일이다** | 안 함 |

---

## 6. 마이그레이션

```js
/* (B) 매 로드 멱등 */
(d.cards||[]).forEach(function(c){
  if(c.type==='credit'){
    if(c.closeDay==null)c.closeDay=10;
    if(c.payDay==null)  c.payDay=25;
  }
});
/* inst 는 없으면 일시불 — 필드를 만들지 않는다. inst||1 로 읽는다. */
```

기존 미결제 16건은 **손댈 필요가 없다.** `inst` 가 없으면 일시불이고,
거래일만 있으면 청구주기는 계산된다. **과거 데이터를 고치지 않고도 새 화면이 성립한다.**

---

## 7. 구현 순서

1. `cycleOf` / `cycleRange` / `cyclePayDate` + 카드 모달에 마감일·결제일 필드
2. 마이그레이션 (B) — 신용카드 기본값 10 / 25
3. 거래 모달: 신용카드 선택 시 `[일시불][2개월][3개월]` + 회차 미리보기
4. `cardPendingByCycle()` — 카드 × 주기 × (일시불 / 할부회차) 집계
5. 현황판 카드 미결제 주기별 분해
6. 선결제 모달: 주기 탭 + `이 주기 전체 선택` + 할부 잠금
7. 할부 회차 결제 처리 (`instDone++`)

## 8. 검증

- 경계일: `8/10` → 8월 결제분 · `8/11` → 9월 결제분 (closeDay=10)
- 할부 단수: 435,100 ÷ 3 = `145,034 + 145,033 + 145,033` = 435,100 **정확히**
- 주기 넘김: 12/21 구매 3개월 → **2027-01 / 02 / 03** (연도 넘김)
- 기존 16건이 `inst` 없이 두 주기로 정확히 갈리는지 (8/02~8/10 vs 8/11~8/18)
- 선결제 후 그 주기만 0 이 되고 다른 주기는 그대로인지
- 체크카드에는 주기 UI 가 안 뜨는지
- 15탭 회귀 · 극단상태 5종 · 6 브레이크포인트
