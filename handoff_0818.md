# 🗂️ HANDOFF — 로한 ↔ 로버트 인수인계 로그

> **이 파일이 규칙이다.** 새 방(대화)을 열면 로한이 "showmethemoney 폴더에서 **가장 최신 handoff_MMDD.md** 랑 index.html 읽어"라고 말한다.
> 로버트는 압축된 기억에 의존하지 말고 **이 파일을 진실의 원천(source of truth)** 으로 삼는다.

---

## 📜 프로토콜 (로버트가 지킬 규칙)

1. **새 방 시작 시 읽을 것 (순서대로):**
   - `handoff_MMDD.md` ← **이 파일. 가장 최신 날짜 하나만.** (누적식이라 옛 날짜 파일은 안 읽어도 됨)
   - `goal-design.md` ← **목표·루틴 시스템 설계 기준.** v6.0 이후 이 영역을 건드릴 땐 반드시 먼저 읽는다.
   - `integration-design.md` ← **파트 간 연계 설계.** 보상↔가계부, 명리 권장일 등 파트를 가로지르는 작업 전 필독.
   - `index.html` ← 앱 전체 코드 (단일 파일 SPA)
   - 필요 시: `cashflow-design.md`(현금흐름 설계), `요구사항정의서.md`(초기 스펙)
2. **데이터가 필요하면** → Supabase MCP로 조회 (아래 접속정보). 앱 화면 추측 금지, 실제 조회.
3. **날짜가 바뀌면** → 이 파일을 `handoff_(새날짜).md`로 복사하고, 맨 위 로그에 새 항목만 얹는다. 옛 파일은 남겨둔다(히스토리).
4. **응답 원칙**: 로한 선호 = 결론→왜→다음. 아부·보험멘트 금지. 불확실하면 "불확실" 명시. 로버트는 재정 조언 시 "면허 있는 자문가 아님" 밝힘.
5. **로버트 = 나(Claude)의 이름.**
6. **작업 완료 시 항상 커밋 메시지를 써줄 것.** GitHub Desktop 기준 **Summary**(한 줄, `v버전 — 무엇을`) + **Description**(변경 항목 불릿 + 검증 결과 + 주의사항). 로한이 복붙만 하면 되게.

---

## 🧭 시스템 지도

**앱**: "Rohan Book" (구 가계부). 단일 `index.html` SPA. 재정+건강+명리(사주)+일지 통합. 현재 **v7.3**.
**배포**: GitHub Pages, 공개 repo `rohanchung/gagyebu`. (넷리파이에서 이관함 — 무료티어 배포 막힘 이슈)
- 코드 변경 → **commit + push** 필요 (GitHub Desktop).
- 데이터 변경 → Supabase 직접 쓰기, **커밋 불필요.**

**Supabase**: project_id `ytkbrdgbklnijbwkvino`. 단일 `app_state.data` jsonb 행에 전 상태 저장. `migrateDB()`로 스키마 진화.

**스킬 3개 (모든 방에서 발동)**:
- `load-my-finance-health` — 재정·건강 현황 스냅샷
- `evaluate-my-saju` — 사주 세운/월운/일운 평가 (고정 원국)
- `check-account-security` — 계정 로그인 이력 점검

---

## 👤 로한 프로필 (고정 사실)

- IT기획자 출신. 기획·사업·마케팅·창작 병행.
- 명리 앱은 **결정론적 만세력 엔진** 자체 구현(API 없음): Julian Day 일진 + Meeus 태양황경 근사 절기/월주 + 60갑자.
- **건강**: 키 **163cm** (BMI 역산 확정). 체중 하락 추세(약 78→68kg대). 8/5 기준 68.2kg. 피검사 21회 × 77지표 임포트됨.

---

## 💰 재정 로드맵 (2026-08 확정, 은행 실제숫자로 갱신 예정)

- **풍무점 이전 = 2026년 10월, 확정·강제.** 월세 아끼려는 게 아니라 출근지 이동. → 보증금 **500~1000만** 필요, 그중 **현금 500 확보가 관건.**
- **부채 금리 우선순위**: 약관대출 **5.5%** > 정책자금(소상공인대환) **3.11%** > 세이프박스 ~2%.
  - → **약관대출 먼저 상환.** 정책자금은 일정대로만 납부.
- **로드맵**: 2026 = 보증금용 저축(현실적 상한 ~500만, 1천만은 비현실) · 2027 = 약관대출 상환 + 신용 구축 · 2028 = 전세 점핑(1억 오피스텔은 현금 1~2천 필요, 지금은 불가).
- 신용점수 900+ 안정. 약관·정책 대출은 신용 영향 미미.
- ✅ **앱 부채 계산 교정 완료 (8/14)**. 소상공인대환 실제 방식 = **원금 고정 416,000원**(1회차 448,000, 단수 조정) + **이자 = 잔액 × 3.11% × 경과일수 / 365**(일할). 납입일 매월 19일, 주말이면 익영업일. 총 48회 · **총이자 1,269,938 · 총상환 21,269,938 · 월평균 443,124**. 앱 `bankSchedule()`이 은행앱 6회차와 원 단위 일치.
- 약관대출 잔액 **1,990만 @5.5%** (SEED도 실제값으로 동기화).

---

## 🧾 날짜별 로그 (최신이 위)

### 2026-08-19
- **v7.3 — 🪣 버킷함 + 🎁 보상 탭 신설 (사이드바 14번째 탭).**
  - **버킷 = 욕망의 원장.** 보상 후보 목록이 아니다. 로한 지적: *"'서울에 집 사기' 같은 버킷은 말그대로 버킷이지만 너무 이뤄내기 힘들어서 사실상 아주 큰 목표"*.
    **국밥부터 평생 못 이룰 수도 있는 것까지 같이 산다.** 그게 정직한 욕망의 모습이다.
  - **⚠️ 통합안 철회.** 최초엔 `rewards` 하나로 통합하고 `once` 플래그로 가르려 했으나, **tier를 뺄 수 없다는 점에서 성립하지 않았다.**
    `rewards`는 계단 층에 묶이는 게 본질(tier 필수), `bucket`은 어디에도 안 묶이는 게 본질. **분리했다.**
  - **티어는 담을 때가 아니라 고를 때 정해진다.** 수령 모달에서 버킷을 **예상비용 오름차순**으로 보여주면
    현실적인 것이 위에 오고 큰 것은 아래로 밀린다. **막지 않아도 정렬이 판단을 돕는다.**
  - 데이터: `bucket[{code:'K1', title, kind, price, addedAt, updatedAt, status:'want'|'got'|'dropped', gotAt, logKey, droppedAt, dropReason}]`
  - **표 UI** — 컬럼 클릭 정렬(코드/내용/분류/예상비용/기록일) + 상태 필터(대기·받음·포기·전체).
    가격순은 '지금 받을 수 있는 것', 기록일순은 '오래 묵은 욕망'을 드러낸다.
  - **90일 경과 `⚠` 표시** — 재촉이 아니라 질문이다. 큰 항목은 몇 년이고 남아도 된다.
  - **포기 사유 기록** — 목록 무한 증식 방지 + *"3개월 지나니 안 끌린다"* 자체가 자기 이해 데이터.
    **버킷의 진짜 산출물은 '안 산 것들'일 수 있다.**
  - 수령 시 `status:'got'` 소진 + 가계부 거래 생성. **수령 취소하면 버킷도 `want`로 복원**되고 거래도 삭제.
  - **좌절 지표에서 큰 항목 제외**: 10만원 이하 대기가 20개 넘을 때만 문턱 하향 신호. 5억짜리는 원래 못 받는 거라 세지 않는다.
  - 보상 관리를 목표·루틴 탭에서 분리 → 목표 탭은 칩만. **자주 쓰는 것(체크)과 가끔 쓰는 것(관리)의 분리.**
  - **`VIEW_RENDER`에 `reward` 등록 완료** (v5.0 dash 누락 회귀 재발 방지). 14탭 전수 통과.
  - 검증: 정렬·필터, 가격 오름차순, 소진→재선택 불가, 수령취소 복원, 포기 전이 전부 통과.
- **실측 근거 (배달)**: 7개월 46건 · 월 121,703원 · 주 1.5회.
  보상으로만 제한하면 월 4회 8만원 → **월 42,000 절감 · 연 50만 = A 보증금 목표의 10%**.
  ⚠️ 단, **평소엔 정말 안 시켜야** 원칙①(평소 안 하던 것)이 성립한다. 못 받은 주에 시켰는지는 가계부에 남는다.
- **v7.2 — 파트 연계 1차. 설계 문서 `integration-design.md` 신설.**
  - **보상 2분류 확정**: `spend`(새 지출 발생) / `act`(시간·행위). **'기타'는 만들지 않았다.**
    로한의 경계 사례 *"이미 산 재료로 소불고기 해먹기"* 는 `act + budget:0`으로 정확히 표현된다(넷플릭스 정주행과 같은 구조).
    '기타'를 남기면 분류 체계의 쓰레기통이 되고, 집계되지 않는 분류는 없는 편이 낫다.
  - **두 축 설계**: `kind`(성격) + `budget`(새 지출액). **budget>0이면 가계부 연계 발동.**
    복합 보상(일본 3박4일 = act + budget 80만)이 자연스럽게 표현된다.
  - **소비 보상 ↔ 가계부**: 수령 시 실제 지출 입력 → `보상` 카테고리로 거래 자동 생성.
    `rewardLog.txnId` ↔ `transaction.rewardId` **양방향** 연결(한쪽만 남기면 추적이 끊긴다).
    상한 초과는 **경고만 하고 막지 않는다**(기록이 목적). 수령 취소 시 연결 거래도 함께 삭제.
  - **⚠️ 의도된 긴장**: 보상 지출 → 현금 감소 → 현금 연동 재정 목표의 진행률 하락.
    이건 결함이 아니라 현실이라 숨기지 않고 표시한다 — `🎁 올해 보상 지출 42만 · 8.4% of A 보증금 500만 확보`.
    보상이 없으면 루틴이 죽고 과하면 목표가 죽는다. 균형점은 데이터로만 찾는다.
  - **명리 음기일**: 캘린더 `陰` 마커 + 행동 보상 `timing:'yin'` 권장.
    **⚠️ 과학적 근거 없음 — 불확실이 아니라 근거 없음이다.** 화면과 문서 양쪽에 명시했다.
    넣은 이유: 로한북 명리는 이미 '예언이 아니라 구조적 렌즈' 전제이고, "아무 때나"보다 규칙이 절제에 낫기 때문. 3~6개월 뒤 상관 분석으로 유용성 판정.
  - **설계를 검증으로 고침**: 최초 안 '2점 이상'은 365일 돌려보니 **정확히 50%(격일)**였다.
    원인은 60갑자 구조 — 甲子·乙丑처럼 **양간은 항상 양지, 음간은 항상 음지**와 짝이라 두 조건이 완전 중복.
    실질 변별은 오행뿐 → **3점(음간·음지 + 水金) = 연 72일 ≈ 주 1.4회**로 상향. 이틀에 한 번 오면 규칙이 아니다.
  - 검증: 음기일 연 72일, 옛 `cap`→`budget` 자동 추정, 거래 생성·양방향 id, 상한 초과 경고, 지출 비율 표시 전부 통과.
- **v7.1 — UI 원칙 교정 + 보상 계단 분산.** (로한 지적: "구성은 되어 있는데 연계성까진 고려 안 했다")
  - **⚠️ UI 기본 원칙 — 앞으로 이 앱의 모든 2열 배치에 적용한다.**
    **왼쪽 = 큰 범위 / 오른쪽 = 작은 범위 또는 사용자가 실제 조작하는 부분.**
    모바일에서 오른쪽이 아래로 내려가므로 그 순서가 곧 세로 흐름이 된다.
    → 좌: `2026년 8월`(과제 + 8월 현황 캘린더) / 우: `8월 19일`(그날 루틴 체크). 이전엔 반대였다.
  - **보상 계단의 별도 프레임 제거.** 보상은 자기가 속한 층에 붙어야 연계가 보인다:
    `연간 보상`→연간 목표 헤더 · `월간 보상`→8월 프레임 헤더 · `주간 보상`→일간 프레임 헤더 · `목표달성 보상`→각 목표 펼침 영역.
  - **보상 칩**: 항상 `🎁 주간 1/4` 처럼 진행률을 달고 있다. 열리면 초록 `받기`, 수령하면 `✓ 수령`.
    미달 상태에서 누르면 **조건 + 지금 몇/몇 + 등록된 보상 목록**이 열린다.
    → 로한이 겪은 "보상 등록했는데 반응이 없다"는 **버그가 아니라 체크 0건이라 계단이 안 열린 것**이었다.
      그게 화면에서 안 읽힌 게 UX 실패였고, 칩이 그걸 해결한다.
  - 52주 트래커를 연간 프레임 안으로 통합. `8월 체크` → `8월 현황`.
  - 검증: 좌우 배치, 4개 층 칩 위치, 칩 클릭 시 조건 안내, 루틴 3개 체크 → 완전달성 → 주간 1/4 상승 확인.

### 2026-08-18
- **v7.0 — 🎁 보상 계단. 로한북의 정체가 여기서 확정됐다.**
  - **대전제(로한의 신념)**: *연간/월간 = 목표. 일간/주간 = 루틴. **루틴이 쌓여서 목표를 달성한다.*** 
    재정·건강·명리는 이 액션플랜을 위한 **밑단 데이터**다. **목표관리가 본체다.**
  - **계단 구조 (병렬 아님)** — 아래 단계의 성공 횟수가 위 단계를 연다:
    `일간 완전달성(그날 due 전부)` → 주 4일 → `🎁 주간` → 그 달 4회 → `🎁 월간` → 연 9회 → `🎁 연간`
    별도 트랙: `G:코드` 연간 목표 달성 보상 (예: N4 합격 → 일본 3박4일).
  - 문턱은 `DB.rewardCfg{weekFullDays:4, monthWeeks:4, yearMonths:9}` — 익숙해지면 올린다.
  - **보상은 메뉴판**: 티어별로 여러 개 등록하고 성공 시 **하나만** 골라 수령. 내성 완화.
    `rewardLog` 키가 `tier@period`라 **기간당 1회 수령이 구조적으로 강제**된다. 수령 시 실적을 박제.
  - **12주 잔디 → 52주 주간 트래커로 교체.** 잔디와 월간 캘린더가 '언제 달성했나'로 목적이 겹쳐 중복이었다.
    칸 1개 = 1주, 색칠 = 그 주 성공. **칸을 세면 월간·연간 보상까지 남은 거리가 그대로 보인다.** 칸 클릭 → 그 주로 이동.
  - 주는 **일요일 시작**. 주가 달을 걸치면 **시작일이 속한 달**로 귀속.
  - 검증: 33주 시뮬레이션으로 계단 판정(주 4/4 열림, 월 3/4 미달, 연 3/9 미달), 월별 주성공 집계, 수령 후 재수령 차단, 트래커 52칸·클릭 이동 전부 통과.
  - **⚠️ 최종 목적지 = Recap.** 연말에 로버트가 "2026년 주간 성공 38/52 · 최장연속 11주 · 가장 무너진 루틴 · 미달사유 34%가 야근" 식으로 브리핑할 수 있어야 한다.
    이건 화면이 아니라 **데이터 요건**이다. 주간 성공·보상 수령을 매주 원장에 남겨야 연말에 그 문장이 나온다.
- **v6.2 — 목표 탭 2열 레이아웃 재구성 (로한 제안).**
  - 1행 전폭: 연간 목표 / 2행: 그날 루틴 | 월간 체크 캘린더 / 3행: 월간 과제 | 12주 기록.
  - **날짜 네비 `◀ ▶ 📅`** — 지난 날 루틴을 소급 체크할 수 있다(그전엔 오늘만 가능). **미래는 차단**(▶ 비활성, 캘린더 미래칸 pointer-events:none).
  - **월간 체크 캘린더** — 날짜별 달성률 4단계 색칠, 칸에 `2/3` 표기, 미달사유 있으면 빨간점. 클릭하면 좌측이 그 날짜로 이동.
  - **'단독과제' → '월간 과제'로 통합.** 연간 목표의 그 달 눈금 + 그 달 단발 과제를 한 프레임에. 월 네비 `◀ ▶ 📅` 추가. 눈금은 `← A 목표명` 표시, 단발은 `단발` 배지.
  - `DB.ui.goalDate` / `DB.ui.goalMonth` 신설. 체크·미달사유 입력이 전부 `curDate()` 기준으로 동작.
  - **월간 눈금이 부모 목표의 연동 소스·시작값을 승계**한다. 현재값은 하나뿐이므로 눈금이 따로 들고 있을 이유가 없다.
    눈금의 출발점 = 직전 달 눈금값(없으면 부모 baseline). 검증: A.8 143.8만/100만 → 100%, B.8 68kg/67.6kg(출발 68.2) → 33%.
- **v6.1 — 월간 세부목표 CRUD · 앱 데이터 자동연동 · 진행률 버그.**
  - **[버그] 감소형 목표 진행률이 100%로 표시.** 체중 65kg(시작 68.2)에서 현재값 미입력 시 `(0-68.2)/(65-68.2)=2131%` → 100%로 잘림. 현재값이 비면 **시작값으로 간주**하도록 수정.
  - **목표 현재값 자동연동(`source`)**: `weight`(체성분 최신) · `cash`(현금성 계좌 합) · `net`(순자산) · `debt`(총부채). 수동 입력 없이 진행률이 따라간다. **로한북에 이미 있는 데이터를 목표가 안 보고 있던 게 근본 원인.**
  - **월간 세부목표 직접 추가/수정.** 연간 목표를 펼치면 월별 눈금 전체 목록 + `+ 세부목표`. **binary 목표(JLPT 등 자동배분 불가)에도 수동 눈금 생성 가능.** 손대면 `manual:true`로 자동배분 제외.
  - 연간 목표 헤더 `⚑ YYYY년 연간 목표` + 우측 버튼. ESC / 오버레이 바깥 클릭으로 모달 닫기.
- **🎯 목표·루틴 전면 재설계 (v6.0). 설계 문서 = `goal-design.md` — 이 영역 작업 전 필독.**
  - **목적 함수 확정**: *12월 31일에 얼마나 달성했고 무엇이 부족했는지 반성할 수 있을 것.* 모든 기능은 이 문장으로 검증한다. 매일의 체크는 목적이 아니라 회고를 가능하게 하는 **원장(ledger)**.
  - **계층**: 연간(대전제) → 월별 눈금 → 일간 루틴. 루틴은 **연간에 직결**(월이 바뀌어도 안 끊김) 또는 마일스톤/단독과제에 한시 연결.
  - **월간 목표 2종 구분**: `A.8`=연간 A의 8월 눈금(마일스톤) / `M8.1`=8월에 시작해 8월에 끝나는 단독 과제. 섞으면 장기 진척이 안 보인다.
  - **코드 체계**: 연간 `A B C` · 눈금 `A.8` · 상시루틴 `A1 A2` · 한시루틴 `A.8a` · 단독과제 `M8.1` · 그 산하 `M8.1a` · 미연결 `Z1`.
    단조증가 카운터(`DB.meta.seq`)로 발급 → **삭제해도 코드 재사용 안 함**(과거 기록이 다른 것을 가리키지 않도록).
  - **마일스톤은 due에서 자동 생성**. 수치형은 baseline→target 선형 배분(검증: 68.2→65, 5개월 = 67.6/66.9/66.3/65.6/65).
    손댄 눈금은 `manual:true`로 자동 재계산 제외. **지난 달 눈금은 절대 안 건드림.**
  - **⚠️ v5.0의 치명적 결함 수정 — 과거 달성률 소급 변동.** 과거를 *현재 루틴 목록*으로 재계산해서, 오늘 루틴을 추가하면 8/13이 3/3→3/4로 바뀌었다(실측). 기록으로서 실격.
    → `checks[날짜] = {done:[코드], due:[코드], miss:{코드:{tag,memo}}}` 로 **분모까지 스냅샷**. 폴백은 루틴의 `start`/`end` 기반 재구성. 회귀 테스트로 고정.
  - **삭제 대신 은퇴**: 루틴 은퇴 시 `end=오늘`, 과거 기록 보존, 내일부터 집계 제외. 물리 삭제는 **체크 기록 0건일 때만**.
  - **미달 사유**: 못 한 루틴에 태그(야근·피로·날씨·약속·무기력·아픔·일정충돌·의지부족·기타) + 한 줄 메모. **'왜'의 유일한 원천** — 일지가 8개월 14건뿐이라 이게 없으면 원인 규명이 불가능하다.
  - **화면**: 상단 컨텍스트바(연간, 접힘 → 펼치면 이달 눈금·투입일수) → 오늘 루틴(목표별 그룹) → 12주 잔디 → 이달 단독과제.
  - 검증: 실데이터(거래 991·일지 14·피검사 21회) 마이그레이션 리허설 무손실 · 과거 불변성 · 은퇴 시 기록 보존 · 13탭 회귀 · JS 에러 0.
  - **마이그레이션 직전 전체 백업**: `backups/rohanbook_2026-08-18_pre-v6.json` (306KB).
- **[버그수정 v5.0.1] 현황판이 빈 화면으로 뜨던 회귀.**
  - 원인: 네비게이션 핸들러의 `if(m.dataset.v==='...')render...()` 체인에 **처음부터 `dash`가 없었다.** v5.0 이전엔 첫 화면이 현황판이라 `enter()`의 `renderDash()`가 그려줬고, 탭을 눌러도 이미 그려져 있어서 문제가 드러나지 않았다. v5.0에서 첫 화면을 목표 탭으로 바꾸면서 **`renderDash()`를 호출하는 진입점이 완전히 사라졌다.**
  - 조치: if 체인을 `VIEW_RENDER` 맵으로 교체. 등록 안 된 탭은 `console.warn`으로 드러난다.
  - **재발 방지: 사이드바에 탭을 추가하면 `VIEW_RENDER`에도 반드시 등록할 것.**
  - 검증: Playwright로 사이드바 13개 탭 전수 클릭 → 활성화·DOM 렌더 확인. 전부 통과, JS 에러 0.
  - 교훈: 첫 화면을 바꾸는 변경은 **기존 첫 화면의 렌더 경로를 같이 확인**해야 한다. 이번엔 그걸 놓쳤다.

### 2026-08-14
- **🎯 목표·루틴 탭 신설 (v5.0). 로한북의 마지막 축 — 돈·건강·명리에 이어 실행.**
  - 데이터: `goals`(level: year|month, period, metric, progress, status) · `routines`(freq daily|weekly, days, goalId, active) · `checks`(**날짜키 맵** `{'2026-08-14':{routineId:1}}` — 스트릭·달성률 O(1) 조회용).
  - 화면: 오늘 루틴 **원터치 체크** / 🔥연속·⭐완전달성 스트릭 / 🌱12주 잔디 히트맵 / 월간 목표 진행률 / 연간 방향(표시등).
  - **연간 목표는 의도적으로 체크 대상에서 제외.** 월간이 실질 단위, 일간 루틴이 실제 엔진이라는 판단.
  - 월간 목표 측정 4종: 완료/미완 · **체크일수(자동집계)** · 횟수 · 금액. autodays는 그 달에 루틴을 하나라도 체크한 날 수를 자동으로 센다.
  - **사주 × 루틴 교차**: 히트맵 셀 우상단 빨간점 = 그날 충·형 등 '주의' 플래그 **2개 이상**. (1개 이상 기준은 365일 중 40%로 노이즈 → 2개 이상 27%로 상향)
  - 가계부 캘린더 날짜 옆에 루틴 달성 점(회색/부분/완전) 추가.
  - **첫 화면을 현황판 → 목표·루틴 탭으로 변경.** 매일 여는 화면이 체크리스트여야 지속된다는 판단. 되돌리려면 `enter()`의 `renderGoal()`을 `renderDash()`로, 사이드바 `class="m on"` 위치를 dash로.
  - 검증: Playwright로 렌더·클릭 토글·모달·전체 렌더러 12개 무예외 확인. 스트릭/요일필터/자동집계 단위 테스트 통과.
- **부채 상환스케줄 엔진 교체 (v4.5).** 기존 `computeMonthly()`의 원금균등 근사(월 416,667 고정)를 버리고 `bankSchedule()` 신설 — 은행 실제 방식 그대로 48회 생성.
  - 파라미터는 `debt.schedule.bank`에 저장: `{payDay:19, prevDate:'2026-08-19', firstYM:'2026-09', principal:416000, firstPrincipal:448000, basis:365}`.
  - `bankNextPay()` → 계좌·부채 탭에 다음 회차 실금액 표시. `bankSchedTable()` → 스케줄 모달에 48회 전체표(지난 회차는 흐리게).
  - **검증**: 2026-09-21 504,235 / 10-19 462,646 / 11-19 466,545 / 12-21 467,041 / 2027-01-19 461,228 / 02-19 463,248 — 은행앱 캡처와 **원 단위 전부 일치**. 원금합 = 정확히 2,000만, 최종잔액 0.
  - ⚠️ **공휴일 미반영** (주말만 익영업일 처리). 설·추석 등 낀 회차는 하루이틀 차이 가능.
- 푸터 버전 하드코딩 `v1.1` → `APP_VERSION` 동적 참조로 수정.
- SEED 부채값을 실제와 동기화(3.03→3.11%, 5.05→5.5%, 1,850만→1,990만). SEED는 폴백용이라 실사용 영향은 없었음.
- 사이드바 라벨 `🩹 체중·사고` → `🩹 체성분·사고`.
- **체중 페이지 → 체성분 페이지로 전면 개편 (v4.4).**
  - 그래프를 맨 위로, 8지표 탭(체중/BMI/체지방률/체지방량/근육량/골격근/내장지방/기초대사) + 중세풍 리디자인(양피지 배경·골드선).
  - 입력은 **8개만, 체중계 화면 순서대로**: 체중 → 체수분 → 체지방 → 골무기질 → 단백질량 → 근육량 → 골격근량 → 기초대사율.
  - 비율·BMI·제지방량·근육비율 등은 **자동 계산**(입력 안 함). 기록 리스트 각 행에 인라인으로 전 수치 표시("·계"=자동계산).
  - 데이터 모델: weight 항목에 flat 필드(kg, waterKg, fatKg, boneKg, protein, muscle, skMuscle, bmr, memo).
  - 계산 검증 완료: 키 163 → BMI 25.7, 체지방률 23.2, 근육비율 72.9 등 샤오미 화면값과 일치.
- **이 handoff 규칙 파일 신설** — 방 갈아탈 때 압축 토큰 낭비 방지용.

### 2026-08-13
- 대시보드 재구성: 총자산/총부채/순자산 한 테이블 + "로버트의 정리" 게시글 보드(클릭→모달). 로버트 이름 확정.
- 일지(📖 성찰) 탭 신설 — 노션 "한번씩 기록" 미러링. 15편 임포트. 마스머리 3단 카드(전체내용 표시). v4.3.

### 2026-08-12
- 현금흐름 시스템(P1~P5) 구현: 계좌 기초잔액, 거래-계좌 연결, 이체 타입, 실시간 대조, 🌊 현금흐름 탭(Sankey). 상세는 `cashflow-design.md`.
- 명리 🔮 사주 탭: 총운/대운/연도별세운/월별/특이일 드릴다운 + 저장 해석 버블 + 길흉평 배지.

### 2026-08-11
- 프로젝트 시작. Supabase 스키마 + 시드, 피검사 엑셀 임포트(21회×77지표), 네비 재정/건강/기타 구획화.
- 초기 스펙: `요구사항정의서.md`.

---

## 🗺 목표·루틴 로드맵 (2026-08-14 결정)

**구글 캘린더 연동은 Phase 2로 미룸.** 판단 근거:
- 기술적으로는 가능. 브라우저 전용 정적 사이트에서 `gapi` + Google Identity Services로 백엔드 없이 호출 가능. 필요: Google Cloud 프로젝트 → OAuth 클라이언트 ID(웹) + API 키 → 승인된 JS 원본에 `https://rohanchung.github.io` 등록 → 동의화면.
- ⚠️ **불확실**: 브라우저 토큰은 1시간 만료에 refresh token이 없다. 조용한 재발급이 되는지 구글 문서에 명시가 없어, 최악의 경우 접속마다 구글 로그인 팝업.
- ❌ **로버트 경유 미러링 불가**: Google Calendar MCP 커넥터에 노출된 도구가 `search_events` 하나뿐. 기간 조회·이벤트 생성 없음. 배치로 긁어와 Supabase에 밀어넣는 우회로는 현재 도구로 막혀 있음.
- ❌ **양방향은 반대.** 루틴 체크 월 150건이 구글 캘린더를 도배하고, 충돌 해결 로직은 개인 프로젝트가 가장 많이 좌초하는 지점.
- → 붙인다면 **읽기 전용 오버레이**로만. 2주 써보고 실제로 아쉬울 때 판단.

**다음 단계 후보**
- 데이터 2~3개월 쌓이면 **사주 주의일 vs 평상일 루틴 달성률 상관 분석**. 지금은 표시만, 분석은 표본이 모인 뒤.
- 주간 리뷰 화면(주별 달성률 추이).

## ⏳ 미해결 / 대기

- [x] ~~은행 실제 상환액 받아서 앱 부채 계산 교정~~ → **8/14 완료.**
- [ ] "About 로한" 공유용 다이제스트 (타 AI/URL 공유로 로한 학습용) — 로한이 "천천히"라 함, 보류.
- [ ] **회고 뷰** (goal-design 6장) — 기간 선택형. 목표별 성적표 · 월별 히트 · 루틴 기여도 · 미달 사유 집계. **데이터 3~6개월 쌓인 뒤**. 그전까진 로버트가 SQL로 직접 분석해 대화한다.
- [ ] **주간 달성률 추이 그래프** — 12주 잔디 옆에 나란히 (dataviz 스킬 참조).
- [ ] **JSON 내보내기 버튼** + **GitHub 자동 백업**(Supabase Edge Function 월 1회 `backups/` 커밋). 20년 데이터가 단일 무료티어 계정에 매달려 있는 상태.
- [ ] **용량 한계 대비**: 현재 48kB, 연 ~150kB 증가. 단일 jsonb 행을 통째로 읽고 쓰는 구조라 **1MB(약 5~7년) 근접 시** 연도별 파티셔닝 필요.
- [x] ~~아침 브리핑 스케줄 태스크~~ → **8/18 취소.** 로한 판단: 의미 없음.
- [ ] 9/1 0시 계좌 기초잔액 입력 → 현금흐름 100% 정합 가동 (cashflow-design.md 참조).
